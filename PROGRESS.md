# Прогресс работ — CRM «Свет в доме»

Обновлён 2026-09-12 (вечер). Полное описание фаз — в `PLAN_MODERNIZATION.md`.
Этот файл — точка восстановления: новая сессия читает его и продолжает.

## Текущее состояние

- Ветка: `main` = `5f4ac5d`, запушена. `refactor/phase4-arch` слита
  fast-forward 2026-09-12 вечером; **бэкенд-часть Фазы 4 задеплоена на прод**
  тем же вечером через `scripts/deploy.sh` (бэкап перед деплоем, миграций
  новых нет — 0 применено, FK-нарушений нет, оба приложения перезапущены,
  `pm2 save` сделан, health-check [7/7] зелёный).
- Прод = `main` = `5f4ac5d`, версия 2.2.0. Маркеры после деплоя Фазы 4:
  `/api/version` = 2.2.0, страницы-сироты 404, `/nakladnye` 401, хэши
  ассетов совпадают, `crm-error.log` пуст. Счётчики боевой БД по эталону:
  cards 925, transactions 357, nakladnye 14, clients 17, users 6, tasks 1.
- Ранее тем же днём: деплой Фаз 0–3 (`prod-20260912`, версия 2.2.0),
  миграция `0005` применена (14/14 записей, FK-нарушений нет).
- **Фаза 3 закрыта полностью.** Открытый хвост — `nakladnye-bot-error.log`
  (638 КБ ошибок за ~13 ч на старом коде): отдельное расследование, не
  блокирует Фазу 4.
- **Фаза 2 закрыта полностью: все 14 дефектов починены** (чек-лист ниже)
- Тесты: `.venv/bin/python -m pytest -q` → **259 passed** (на 2026-09-12,
  включая забытый тест `test_notifications_utc.py`, влитый в `a290e65`)
  Второй профиль: `CRM_TEST_SCHEMA=models .venv/bin/python -m pytest -q`
  → **253 passed, 4 skipped**
- Линтер: конфиг закреплён в `ruff.toml` (до Фазы 3 его не было ни на одном
  уровне, правила определялись дефолтами версии ruff). Проверка: `.venv/bin/ruff check .`
- Окружение: `.venv` на Python 3.12.14, ставить через
  `.venv/bin/pip install -r requirements-dev.txt`
- Инспекция read-only 2026-09-12 **опровергла** прежнее утверждение этого
  файла: pm2 на хостинге **ЕСТЬ** (v7.0.3, пользовательский), оба процесса
  (`crm`, `nakladnye-bot`) под `PM2 God Daemon`. Опасный
  `scripts/restart_crm.sh` удалён в Фазе 3. **Ключевой факт для рестартов:**
  токены бота (`TELEGRAM_BOT_TOKEN`, `OPENAI_API_KEY`) есть только в env
  процесса — бот рестартится БЕЗ `--update-env`, `crm` — с ним; после
  изменений состава процессов обязателен `pm2 save`.
- Миграция `0005_unique_nakladnye_doc_key` **применена на проде**
  2026-09-12 в ходе деплоя: колонки `doc_series_norm`/`doc_number_norm`
  заполнены у 14/14 записей, частичный уникальный индекс создан,
  нарушений не найдено.

## Жёсткие правила

1. На прод — только чтение (`rsync`, `sqlite3 .backup`, `SELECT`, `curl /health`).
   Никаких перезапусков сервиса и правок файлов на сервере.
2. Миграции только аддитивные. Перед применением — прогон на копии боевой БД
   и `PRAGMA foreign_key_check`.
3. Тесты идут на фактическом DDL прода (`tests/fixtures/prod_schema.sql`).
   Профиль переключается `CRM_TEST_SCHEMA=prod|models`; прогонять нужно оба.
   Поверх DDL `tests/conftest.py` накатывает `migrations/` — иначе тесты
   проверяли бы схему, которой на проде после Фазы 3 не будет, и уникальность
   накладных (миграция 0005) осталась бы непроверенной.
4. Каждый дефект — отдельный коммит. При починке снять `@pytest.mark.known_bug`
   и `@pytest.mark.xfail` с соответствующего теста: он `strict=True`, поэтому
   после починки прогон сам напомнит. На 2026-09-12 xfail'ов в тестах не осталось.
5. Контрольные счётчики боевой БД (эталон, не должны меняться):
   cards 922, transactions 357, nakladnye 14, clients 17, users 6, tasks 1.
   Бэкап: `~/backups/manual/phase0_pre_refactor_20260911_222437.db` на сервере.

## Чек-лист 14 дефектов Фазы 2 — ЗАКРЫТ

| # | Дефект | Статус |
|---|---|---|
| 1 | Удаление накладной сносило постороннюю запись-остаток (потеря данных) | ✅ `b228f66` |
| 2 | 500 при удалении накладной со сделки в «Сборке» (autoflush, двойная вставка остатка) | ✅ `9bc3532` |
| 3 | 500 при удалении задачи с подзадачами (backref без passive_deletes) | ✅ `d398b59` |
| 4 | 500 при удалении кастомной записи со ссылкой value_relation | ✅ `d398b59` |
| 5 | 500 при удалении шага воркфлоу с дочерними шагами | ✅ `d398b59` |
| 6 | Один номер накладной на одной сделке выписывается многократно | ✅ `c60281e` |
| 7 | TOCTOU в приёме накладных: check и create двумя запросами, уникальности в БД нет | ✅ `0db7ae8` + миграция `0005` |
| 8 | Переименование поставщика не обновляет снимки supplier_name / company_name | ✅ `1278193` |
| 9 | Смена суммы сделки не пересчитывает writeoff_groups.total_amount | ✅ `fc45d84` |
| 10 | `CardUpdateStatus` без валидатора — мусорный статус сохраняется | ✅ `ca2a7f7` |
| 11 | Проверки `amount >= vat_amount` на сервере нет | ✅ `0e4afeb` |
| 12 | `NAKLADNYE_STATUSES` / `NAKLADNYE_DOC_TYPES` объявлены, но не применяются | ✅ `e50b508` |
| 13 | `Task.priority`, `Transaction.date`/`company_name`/`is_secondary_check`, `Card.owner_id` нельзя изменить | ✅ `674d882` |
| 14 | `nakladnye.amount_no_vat` есть в боевом DDL, но нет в `models.py` | ✅ `7ec012d` |

`KNOWN_DEFECTS` и `KNOWN_DDL_DRIFT` в `tests/test_schema_parity.py` очищены.
Оба списка оставлены пустыми переменными, а не удалены: метатесты сверяют с ними
фактическое множество, поэтому новый незамеченный пробел схемы или новый дрейф
DDL уронят прогон.

## Ограничения, которые сознательно НЕ менялись

Заблокированных дефектов нет — все 14 закрыты. Но три решения приняты
как «зафиксировать, а не переделать», потому что переделка требует продуктового
решения владельца. Каждое закреплено тестом, чтобы его нельзя было потерять:

1. **`Transaction.company_name` перезаписывается при переименовании сделки.**
   `card_details_router.update_card` при смене `title` синхронизирует
   `company_name` во ВСЕХ транзакциях карточки. Поэтому ручная правка подписи
   одной записи (теперь возможная — дефект 13) держится только до следующего
   переименования сделки. Денормализация задумана намеренно
   (`PLAN_MODERNIZATION.md`, Фаза 2 п.1: суммы не трогаем, синхронизацию подписи
   оставляем). Тест: `test_transaction_company_name_is_still_overwritten_by_card_title`.
   **Вопрос к владельцу:** нужно ли разрешать подписи расходиться с названием сделки.

2. **Дефект 6 закрыт в коде, а не индексом БД.** Уникального индекса по
   `(card_id, invoice_number)` нет: правило сравнения «только цифры»
   средствами SQLite в выражение индекса не оформляется, а индекс по сырой
   колонке пропустил бы «ТТН 4881030» против «ТТН4881030» — ровно тот дубль,
   который встречается в жизни. Гонка двух одновременных выписок одной накладной
   теоретически возможна, но это действие менеджера из модалки, а не приём
   документа из бота. Если понадобится гарантия БД — нужна колонка
   с нормализованным номером, как сделано для `nakladnye` в миграции 0005.

3. **`uq_nakladnye_doc_key` защищает только записи, созданные через ORM.**
   Производные колонки `doc_series_norm`/`doc_number_norm` считает слушатель
   `models._nakladnaya_sync_doc_key`; вставка сырым SQL или пакетный
   `query.update()` его не вызывают, и такая строка под частичный индекс
   не попадёт. Сейчас все записи `nakladnye` создаются через ORM
   (`create_nakladnaya`, `bot_create_nakladnaya`), других мест нет.

## Про дефекты 6 и 7 — штатное не сломано (проверено)

Владелец подтвердил, и данные боевой БД это показывают: сценарии
**«2 карточки — 1 накладная»** и **«1 карточка — 2 накладных»** штатные.

- один `invoice_number` на нескольких карточках: `0002314`, `ТН0002236` (3 карточки),
  `ТТН 4881030`, `ТТН4881017`, `ТТН4881032`; суммы разные и равны итогам своих карточек
- несколько накладных на одной карточке: карточки 493, 525, 751
- дублей `(card_id, invoice_number)` — **ноль**
- в `nakladnye` (14 записей) дублей по `(doc_series, doc_number)` — **ноль**

Как это учтено в починке (инварианты остались зелёными):

- **дефект 6** — проверка только в пределах ОДНОЙ карточки
  (`payments_router._find_duplicate_invoice`), сравнение по цифрам. Индекс по
  одному `invoice_number` не ставился: он сломал бы пять рабочих накладных.
  Почему индекса нет вовсе — см. «Ограничения» выше, п.2.
- **дефект 7** — индекс по **нормализованному** ключу (серия strip+upper,
  номер только цифры), потому что в данных соседствуют «ТТН 4881030» и
  «ТТН4881030». Индекс **частичный**: записи без номера не ограничены, иначе
  fallback-ветка бота «Не распознано» сработала бы ровно один раз.
- миграция 0005 идемпотентна и **сначала** проверяет нарушения: при нахождении
  дублей печатает пары построчно и падает, ничего не удаляя. Проверено на
  локальной копии боевой схемы обоими способами (чистая база и база с дублем).
- инварианты закреплены тестами в `tests/test_nakladnye_dedup.py`
  (`test_one_invoice_across_two_cards_is_allowed`,
  `test_one_card_with_two_invoices_is_allowed`) — оба зелёные.

## Отложено по решению владельца

- **«Сохранённые представления»**: `saved_views_router` не подключён в `main.py`,
  `js/saved_views.js` не грузится ни одной страницей. Фича мертва с обоих концов.
  Решение отложено до Фазы 4. Задокументировано в `KNOWN_UNREGISTERED`
  в `tests/test_schema_parity.py`.
- **8 мёртвых Pydantic-схем** — в `KNOWN_DEAD_SCHEMAS` там же.
- **Деньги остаются на `float`** — переход на Decimal отклонён владельцем.
- **Дизайн** — не полный редизайн, а переработка ради снижения шума и качества
  решений; адаптивность под диапазон 1280–1920 CSS-px (владелец на масштабе 75%,
  коллеги на 100%). Детали в Фазе 5 плана.

## Фаза 3 — ЗАКРЫТА (деплой 2026-09-12, прод = main = 2.2.0)

### Сделано

| Что | Коммит |
|---|---|
| `tools/stamp_assets.py` (sha1[:10], `--check`, коды 0/1/2) + 20 тестов + гейт на реальном репозитории; **26 из 35 хэшей в HTML были протухшими** | `a80e054` |
| Письма только-в-HTML молча не импортировались: параметр `html` затенял stdlib-модуль, `html.unescape()` падал с AttributeError, исключение глотал `except Exception: continue` | `70e30ae` |
| Локальный re-import затенял `get_tenant_db` в `create_tenant_admin` | `e814b8b` |
| `/manifest.json` и `/static/logo.svg` отдавали 404 (StaticFiles был смонтирован только на `/css` и `/js`) | `c65c0ab` |
| Запись фото до 25 МБ блокировала event loop в async-хендлерах; убран мёртвый код в 6 файлах | `c4d9327` |
| `date.today()` вместо UTC ломал ключ дня просрочек и IMAP-фильтр `UNSEEN SINCE` | `3c276a9` |
| `ruff.toml`: конфиг закреплён, 1603 → 166 замечаний; находки безопасности разобраны | `b426f0c` |

### Проверенные факты о проде (инспекция read-only, 2026-09-12)

- **pm2 есть**: v7.0.3, пользовательский. Два приложения делят один каталог кода —
  id 7 `crm` (`server.py`, 2 воркера) и id 16 `nakladnye-bot`. Деплой обязан
  перезапускать **оба**, а `nakladnye-bot` **не объявлен в `ecosystem.config.js`**,
  поэтому фолбэк watchdog'а `pm2 start ecosystem.config.js` бота не поднимет.
- **Автозапуска pm2 на уровне ОС нет**: после ребута его возвращает только cron
  `*/5` → `scripts/watchdog_crm.sh` → `pm2 resurrect` (сработало в бою 07.09 03:50).
  `~/.pm2/dump.pm2` устарел относительно текущих процессов ⇒ после изменений нужен `pm2 save`.
- **`scripts/restart_crm.sh` опасен и неработоспособен**: его посылка «pm2 на
  хостинге нет» ложна; `pkill` убивает pm2-процесс, pm2 поднимает свой, а скрипт
  параллельно запускает вторую nohup-копию ⇒ гонка за порт 20008. К тому же он
  опирается на `ss`, которого на сервере нет, и пишет в `$APP/logs/crm.log`,
  который мёртв с 2026-09-04. Подлежит удалению/замене.
- **`scripts/watchdog_crm.sh` — ещё и планировщик почты**: каждые 5 минут дёргает
  `POST /email-parser/sync-all`. Переписывать его без переноса этой функции нельзя.
- **На сервере нет `ss`, `netstat`, `lsof`** ⇒ проверки порта только через
  `/proc/net/tcp` или `curl`. `python3` системный — **3.9**, приложение работает на
  `.venv` — **3.13**: миграции и анализ кода запускать только через `.venv/bin/python`.
- **На проде нет `.git`** — голая копия файлов. Git-шаги старого `deploy.sh` фиктивны,
  откат возможен только из `~/backups/` (ежедневно 03:00, шифрованные `.enc`,
  с integrity-check и WAL-checkpoint).
- **`rsync --delete` уничтожит боевое состояние** (проверено dry-run: 58 удалений):
  `.pm2.env` (без него воркеры теряют `CRM_DATA_DIR` и открывают пустую базу,
  теряется `CRM_SECRET_KEY` ⇒ инвалидируются JWT), `tenants/*.db`, `.secret_key`,
  `routers/.secret_key`, `.cron_token` (сломаются оба cron-вызова), `.venv/`, `logs/`.
- **Локальный rsync — openrsync protocol 29, серверный — 3.2.5 protocol 31**:
  скрипт писать под наименьший общий знаменатель, без `--info=`/`--filter`/`-A`/`-X`.
- **Расхождение кода**: различаются 12 Python-файлов, и во всех локальная версия
  новее (Фаза 2 не выкачена). Прод-хотфиксов, отсутствующих в git, не найдено.
  Фронтенд (`js/*`, `css/*`) идентичен. Только на проде — 3 осиротевших файла
  В КОРНЕ (`style.css`, `nakladnye.js`, `nakladnye_router.py`): ни один не
  загружается, корень как статика не смонтирован.
- **`migrate_v2.py` на проде нет** ⇒ шаг 4 старого `deploy.sh` молча ничего не делает.
- **`version.py` не бампается** (`2.1.0` / `20260716`) ⇒ `/api/version` пока
  непригоден как маркер успешности деплоя.
- **На проде 23 из 35 кэш-хэшей протухли** (9 соответствуют sha1, 3 — следы старого
  md5-штамповщика, 23 — ничему; `js/nakladnye.js?v=6` — мусор). В рабочем дереве
  баг закрыт, на прод не выкачен.
- **Маркер успеха деплоя**: `/custom_objects.html`, `/workflows.html`, `/settings.html`
  сейчас отдают **500** (прод-`main.py` регистрирует роуты на отсутствующие файлы,
  `crm-error.log` непрерывно пишется). Локальный `main.py` роуты удалил ⇒ после
  деплоя обязаны отдавать **404**.
- `nakladnye-bot-error.log` — **638 КБ за ~13 часов**: бот непрерывно error'ит,
  разбирается отдельно.

### Осталось в Фазе 3 — ВСЁ ЗАКРЫТО (деплой 2026-09-12 прошёл)

1. ✅ `scripts/deploy.sh` переписан (`0b4937c`) и **исполнен на проде**:
   preflight → бэкап → штамповка + гейт → rsync без `--delete` с
   protect-исключениями → pip install (requirements.txt реально изменился —
   зависимости поставлены) → миграции СТРОГО до рестарта + `foreign_key_check`
   → `pm2 restart crm --update-env`, `pm2 restart nakladnye-bot` (**без**
   `--update-env` — токенов бота нет в `.pm2.env`) + `pm2 save` →
   health-check. Все проверки шага [7/7] зелёные.
2. ✅ `nakladnye-bot` добавлен в `ecosystem.config.js` (`0b4937c`), дамп
   обновлён `pm2 save` — resurrect после ребута поднимет оба приложения.
3. ✅ `scripts/restart_crm.sh` удалён, `HANDOFF.md` приведён к pm2 (`0b4937c`).
4. ✅ `version.py` → `2.2.0` / `20260912`; прод отвечает `/api/version` =
   2.2.0 — маркер сработал.
5. ✅ Смоук на проде выполнен самим deploy.sh: хэши ассетов совпадают,
   страницы-сироты 404, `/nakladnye` 401, `crm-error.log` пуст.
   Дополнительно сверены счётчики БД (см. «Текущее состояние»).

**Новый факт инспекции 2026-09-12 (важен для всех будущих рестартов):**
`.pm2.env` содержит ТОЛЬКО `CRM_DATA_DIR`, `CRM_SECRET_KEY`, `CRM_UPLOADS_DIR`,
`PORT`. Токены `nakladnye-bot` (`TELEGRAM_BOT_TOKEN`, `OPENAI_API_KEY`)
живут исключительно в env процесса, которым его стартовали (pm2 хранит их
в дампе). Поэтому: рестарт бота — только `pm2 restart nakladnye-bot` без
`--update-env`; `pm2 save` после любых изменений состава процессов.



## Фаза 4 — в работе (бэкенд-часть ЗАКРЫТА и задеплоена 2026-09-12 вечером)

### Сделано

| Что | Коммит |
|---|---|
| `scripts/check.sh` — 6 CI-гейтов: `import main` (143 роута) → pytest×2 профиля (259 / 255+4) → `stamp_assets --check` → ruff (теперь **0 ошибок**) → чистый venv + проверка bcrypt 4.0.1. Первый красный гейт останавливает прогон. Полный прогон: exit 0 | `ad5cac1` |
| 118 находок ruff занулены без изменения поведения: B904 везде `from e`, S110 → `contextlib.suppress`, TRY300 → `else`, TRY401 без `{e}` в `logging.exception`, TRY301 → helper `_search_unseen` (ответ клиенту прежний), TRY004 → `TypeError` (grep: никто не ловил `RuntimeError`), PERF/SIM/B007 — механика | `ad5cac1` |
| `constants.py`: `MONEY_EPSILON = 0.01` вместо 20 хардкодов в 4 роутерах (деньги на float — решение владельца, допуск теперь в одном месте) | `45bb322` |
| Мёртвый тенант-код удалён (−2 125 строк, 18 файлов): псевдонимы `tdb`, guard-finally, ветки `tenant_id is None`, циклы по tenant-базам; `db_utils` упрощены до «всегда основная сессия» с warning. **Найдено вопреки HANDOFF: эндпоинт `/auth/create-tenant` был жив** — удалён (единственный способ получить tenant_id ≠ NULL). Полный гейт exit 0 | `2fbfa07` |
| Service-слой, срез 1: пакет `services/` (invoices / writeoffs / nakladnye), 12 бизнес-правил вынесено из роутеров посимвольно теми же телами; роутеры −274 строки. card_details перенацелен с роутеров на services | `dc92e39` |
| Service-слой, срез 2: `services/payments.py` — tx_dict, delete_transaction, add_invoice, issue_invoice; хендлеры — HTTP-обвязка. Инвентаризация коммитов: ровно 4 двухкоммитных эндпоинта | `721e45f` |
| Инвариант «один запрос = один commit» (4 эндпоинта через flush, notify после commit — неподвижно) + метатест `tests/test_atomicity.py` (пустой ALLOWED; новый много-коммитный хендлер роняет прогон). 260/256 тестов | `029e5d5` |
| GitHub Actions: `.github/workflows/check.yml` — гейт `check.sh` на push/PR. **Запуск не верифицирован**: PAT в gh не видит репозиторий (git — по SSH), владельцу проверить вкладку Actions | `8e716cc` |
| Pre-push гейт: `.git/hooks/pre-push` → `bash scripts/check.sh`, **проверен в бою** на коммите `029e5d5` (полный гейт отработал, push прошёл). Образец — `scripts/pre-push.sample` | `8e3ae31` |
| Скриншот-регрессия в гейте: `tools/visual-check/regress.mjs` + `seed_db.py` (детерминированная сид-БД, замороженные часы, TZ=Europe/Minsk), 44 скрина — 8 разделов × 4 ширины + тёмная серия + модалки + экран входа, pixelmatch (по факту 0px). Эталоны `baselines/` в git, `shots-diff/` в .gitignore. `check.sh` — 8 шагов: шаг 8/8 поднимает сид-сервер на 8799 (trap гасит), **в CI пропускается** (эталоны macOS-рендеры; локально закрыт pre-push). Влито в main | `88736f3` + merge `8dd95ee` |

### Найдено аудитом 5.1 и исправлено сразу

| Что | Коммит |
|---|---|
| **P0-баг**: `localDateISO is not defined` — форма выписки накладной из карточки сделки не рендерилась (ReferenceError в `card_modal.js:1831` и `:2088`; определение не приземлилось при синхронизации с продом `73a345f`, на проде сломано с начала сентября). Хелпер добавлен (YYYY-MM-DD из локальных компонент, не UTC-срез). Проверено на копии боевой БД: черновик рендерится, pageerror нет. Групповой путь (`group-inv-date`) на живых данных не проверен — в копии БД нет writeoff-групп; проверить при первой группе | `36158ae` + merge `531db18` |

### Осталось в Фазе 4

1. Проверить в браузере, что GitHub Actions реально гоняет гейт (workflow-файл
   в репозитории, PAT сессии репозиторий не видит — API 404, владельцу
   проверить вкладку Actions; push `5f4ac5d` 2026-09-12 вечером должен был
   запустить workflow). Если Actions недоступна — уже есть проверенный
   в бою локальный pre-push (`8e3ae31`).
2. ~~Frontend strangler fig: Vite-пилот на `nakladnye.js`~~ — **сделано
   2026-09-12** (ветка `feat/vite-pilot-nakladnye`, коммиты `b94977a` +
   `fd2c8fd`): скаффолдинг (package.json + vite ^6 + `vite.config.js`,
   `emptyOutDir: false`), исходник переехал в `frontend/nakladnye.js`,
   бандл `js/nakladnye.bundle.js` коммитится (node на сервере нет),
   `index.html` грузит его как `<script type="module">`, контракт
   `window.NakladnyeUI`/`window.loadNakladnyeTable` сохранён. Гейт
   `check.sh` стал 7-шаговым: шаг 5/7 «frontend-бандл свеж»
   (npm ci → vite build → git diff бандла). Проверка в бою:
   `tools/visual-check/nakladnye-check.mjs` на копии боевой БД — 7/7 OK
   (таблица 14 строк, итоги, модалка + inline-закрытие, поиск, экспорт
   CSV, ноль ошибок консоли после логина). **На прод НЕ выкачен** —
   деплой отдельным решением владельца.
3. ~~Скриншот-регрессия `tools/visual-check` на 4 ширинах + тёмная тема в
   гейте~~ — **сделано 2026-09-12** (ветка `feat/visual-regression`):
   `regress.mjs` — 44 скрина (8 разделов × 4 ширины светлой темы, тёмная
   серия на 1440, модалки сделки/накладной, таб накладных, экран входа) на
   детерминированной сид-БД (`seed_db.py`, фиксированные id и даты) и
   замороженных часах (Clock API, TZ=Europe/Minsk); сравнение pixelmatch,
   допуск 0.1% пикселей, по факту 0px на всех 44. Эталоны `baselines/`
   коммитятся, `shots-diff/` в .gitignore. `check.sh` стал 8-шаговым:
   шаг 8/8 поднимает сид-сервер на 8799 (trap гасит его при любом выходе)
   и гоняет regress.mjs; **в CI шаг пропускается** — эталоны отрендерены
   на macOS, шрифты/антиалиасинг Ubuntu-раннера дали бы ложные
   расхождения, локально гейт закрывается pre-push хуком. Полный прогон
   check.sh: exit 0.
4. ~~Деплой накопленного Фазы 4 на прод~~ — **сделано 2026-09-12 вечером**
   (main = `5f4ac5d`, см. «Текущее состояние»).

## Дальше по плану

1. **Фазы 0–4 закрыты и влиты в `main`**. Открытый хвост вне
   фаз: `nakladnye-bot-error.log` (638 КБ ошибок за ~13 ч на старом коде) —
   расследовать отдельно.
2. **Фаза 5 — в работе** (ветка `feat/phase5-ux`): шаг 5.1 (аудит) сделан,
   P0-дефекты D2 и D3 исправлены (см. ниже), дальше P1 по разделам
   5.2–5.6 плана — см. блок «Следующая сессия» ниже.

## Фаза 5 — в работе (старт 2026-09-12)

### Сделано: 5.2–5.3 — P0 закрыты, P1/P2 срез 10 дефектов (ночь 12→13.09)

Все коммиты на `feat/phase5-ux`, каждый через полный гейт `check.sh` (8/8),
эталоны скриншот-регрессии обновлены осознанно с попиксельной проверкой
диффов (шумовые откатлялись). Разведка — 4 параллельных субагента, правки —
в основном контексте с функциональными проверками на стендах 8799/8801.

- **D13+D14** (`40b291f`): суммы накладных не рвутся по разрядам, пилюли
  фильтров не обрезаются (тулбар переносится). Бонус: оживлён индикатор
  тени горизонтального скролла (ResizeObserver) — он был мёртв у всех таблиц.
- **D7** (`8e49c4b`): итоги футера при поиске пересчитываются во всех трёх
  таблицах (платежи, документы, накладные) — общий хелпер
  `getVisibleTableRows`, тот же предикат видимости, что у экспорта CSV.
- **D12** (`1957a1f`): payments/documents входят в 1440 без скролла
  (Оплата 150→124, Печать 190→160, Сумма/№ТН документов 132→120,
  min-width 1240/1120→1150/1060, мёртвые правила удалены, перенос
  возвращён настоящему «Примечанию» payments — 10-я колонка, не 9-я).
  На 1280 — честный скролл со sticky «Название» (11 колонок в 993px
  физически не помещаются).
- **D11+D17** (`de4813b`): группы списаний кэшируются (дедупликация
  in-flight + инвалидация во всех точках мутаций, включая writeoffs.js);
  убран стартовый безусловный груз доски списаний; мёртвый операнд
  `btn-delete-attachment` удалён; двойной кэш поставщиков слит в CRM_STORE.
- **D10** (`d9074ac`): черновики (заметка, поля чек-листа, формы накладных,
  правка комментария) получили защиту от молчаливой потери — делегированный
  перехват на #card-modal + флаг dirty в трекере + двухкнопочный диалог;
  оплата и PATCH чек-листа идут через трекер (guard дожидается, ошибку
  можно повторить).
- **D15+D16** (`11d5b17`): кнопки плиток списаний и иконки задач видны
  всегда, но тихо (0.6/0.55); пустые колонки задач схлопываются в
  вертикальную полоску 36px — drop-зона жива, ширину забирают непустые.
- **D4** (`e2d8ec3`): чипы фильтра оплаты «Все/Не оплачены/Частично/
  Оплачены/Отсрочка» над канбаном, действуют на доске и в «Списке»,
  счётчики «N из M» пересчитываются сами.

Открытыми остались D5, D6, D8, D9, D17-хвост (readonly-поле) — всё, что
требует согласования макетов (5.7: макеты — до вёрстки).

### Сделано: 5.2 — P0-дефекты D2 и D3 (2026-09-12, вечер)

- **D2 — подтверждение на каскадные чекбоксы накладных** (коммит `712875a`).
  Каскадный PATCH («оплачена» ⇒ до 4 полей) теперь требует `confirmDialog`
  с перечислением последствий; «Отмена» возвращает чекбокс и не шлёт запрос.
  Простые изменения без каскада — по-прежнему один клик.
- **D3 — вкладка «Контроль» подключена** (коммит `9997fd7`). `js/control.js`
  (дебиторка + очередь на списание) жил без единой точки входа: вкладка
  «Контроль» первой во вкладках Финансов, bento-сводка из 3 плиток
  (Дебиторка / Долг 30+ дней / Очередь) + таблицы; лоадеры в `features.js`
  и `page-freshness.js`; CSS на токенах. Заодно в ветку влит `main`
  (фикс `localDateISO` — D1), pageerror при открытии карточки из «Контроля»
  исчез.
- **Проверки:** `tools/visual-check/control-check.mjs` (новый, 14/14 OK,
  0 ошибок консоли — открывает вкладку, проверяет плитки/таблицы/карточку
  из долга и поведение D2); `bash scripts/check.sh` — все 8 гейтов зелёные;
  скриншот-регрессия: эталоны обновлены осознанно для 6 finance-скринов
  (новая вкладка в шапке), шумовые (модалка/календарь/дашборд/поставщики)
  не трогали; два прогона подряд — 44/44, 0px.
- **CI:** вкладка GitHub Actions проверена (`gh run list`): workflow `check`
  гоняется на каждый push, последние прогоны зелёные, ~2,5 мин.
- **На прод НЕ выкачано** — деплой отдельным решением владельца (вместе
  с Vite-пилотом nakladnye).

### Сделано: 5.1 — аудит на доказательствах

Deliverable — **`UX_AUDIT.md`** (корень репозитория): 17 дефектов с замерами,
скриншотами и приоритетами — **P0: 3 (один уже исправлен — `localDateISO`,
см. таблицу выше), P1: 10, P2: 4**. Базовая линия для проверки «стало ли
лучше» (план 5.7) зафиксирована в разделе «Базовая линия» UX_AUDIT.md.

Инструменты аудита (закоммичены, воспроизводимы): `tools/visual-check/`
`audit-shots.mjs` (матрица 44 экрана на копии боевой БД), `ux-noise-metrics.mjs`
(замер шума: distinct computed-значения), `task-cost.mjs` (8 сценариев,
клики/поля), `verify-s1-fix.mjs` (регрессия S1). Артефакты прогонов —
`tmp/ux-audit/` (вне git). Стенд: сервер на 8801 с
`CRM_DATA_DIR=tmp/prod-copy` (копия боевой БД 2026-09-12), креды программно
из `tools/visual-check/.env` (как walk.mjs).

Ключевые числа базовой линии: самый шумный экран — модалка сделки
(61 distinct-значение, 54 % текста <13px, 41 действие, 5–9 fetch на
открытие), далее канбан (50, 57 %) и финансы (48, 752 интерактивных);
`style.css` 487,5 КБ / 13 962 строки / 1 156 `!important` / 22 `@media`
(брейкпоинтов 1280–1920 нет) / 68 % объявлений font-size ≤13px.
Что НЕ проблема: горизонтального скролла нет на 44 экранах, font-family
всего 2, ошибок консоли нет.

## Следующая сессия — с чего начать

Контекст: `PLAN_MODERNIZATION.md` (Фаза 5, строки 544–756), `UX_AUDIT.md`
(таблица D1–D17 с актуальными статусами). Ветка `feat/phase5-ux`
(последний коммит ночного среза — `e2d8ec3`).

1. **Решения владельца**:
   - деплой на прод накопленного (Vite-пилот + localDateISO + D2, D3 + ночной
     срез D4–D17) — ждёт решения;
   - подтверждение направления дизайна 5.2 (таблицы «Берём/Отвергаем») и
     макетов ключевых экранов — перед D5/D6/D8/D9.
2. **Остаток аудита — только согласуемое**: D5+D17-хвост (карточка сделки),
   D6+D8+D9 (канбан-шум, монолит CSS, брейкпоинты 1280–1920). Порядок по
   плану 5.6.5: карточка сделки → канбан.
3. **Гейт**: `bash scripts/check.sh` на каждую правку вёрстки; эталоны
   обновлять частично — `--update-baselines` пишет все 44, шумовые
   (календарь, поставщики, изредка login/finance/модалка — maxΔ<16 из 255)
   откатывать `git checkout --`, реальные — коммитить с разбором диффа
   (полосная карта отличий python+PIL показала себя хорошо).
4. **Стенды**: сид строго под `.venv/bin/python` (системный python3 падает
   без jwt и успевает снести БД сида); 8799 — сид-БД, 8801 — копия боевой.
5. **Не забыть**: групповой путь накладной (`card_modal.js` ~2088, второй
   `localDateISO`) не проверен на живых данных — при первой writeoff-группе;
   хвост `nakladnye-bot-error.log` на проде; у накладных на 1280/1024
   осознанный горизонтальный скролл с тенью (трейд-офф D13) — если владелец
   против, обсуждать ужимание колонок под конкретную ширину.
