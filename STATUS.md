# Фактическое состояние контейнеризации

Дата проверки: 2026-08-08. Каждый пункт ниже проверен выполнением команды,
а не переписан из предыдущего отчёта.

## Обновление 2026-08-26: UI-аудит и исправления

Прод (`87-232-64-12.nip.io`) был новее git HEAD на 5 дней — правки
«UI Audit 25–26.08» существовали только на сервере. Сначала синхронизированы
в репо (коммит `a86cc66`), затем по аудиту интерфейса:

- 20 пунктов UI-аудита исправлены: критичные (обрезка сумм KPI, колонка
  «Название» 72px, янтарные статусы оплат в тёмной теме, sticky-панель,
  перекрывавшая дропзону), консистентность контролов (все 40px), визуал
  по референсу doctorstroy.ru (кнопки-пилюли, заголовки Manrope 800,
  календарь без переполнения), полировка (единый `:root`, плейсхолдеры
  4.8:1, зебра, тихие заметки).
- Модалка карточки: малые экраны (одна лента, липкая шапка/кнопка,
  крестик в углу, сетка `minmax(0,1fr)` против распирания) и высота
  88vh на десктопе (схлопывалась до 340px).
- `requirements.txt`: добавлены `python-multipart` и пин `bcrypt==4.0.1` —
  без них свежая сборка бэкенда не стартует (форма логина) и вход даёт 500.
- Скриншоты до/после: `gui-test-screenshots/ui-audit-2026-08-26/`.
- Аудит паролей: bcrypt `$2b$12` + уникальная соль, секреты в git не
  попадали. Производительность: ~440 KB gzip / ~37 запросов первой
  загрузки, TTFB 16–30 ms; резерв роста — дельта-синхронизация канбана
  (отложено). Подробности — план из сессии 2026-08-26.
- Админка (вторая сессия 2026-08-26, коммит `c7b0d08`): починена причина
  «не видны менеджеры» — обёртка apiFetch в admin.html уходила в бесконечную
  рекурсию (хойстинг затирал глобальную функцию), список всегда падал.
  Плюс: tenant-фильтр убран (компания одна), `PUT /auth/me/password` —
  самостоятельная смена пароля с отзывом старых токенов (claim `pv`;
  токены без `pv` валидны — деплой не разлогинивает), минимум пароля
  8 символов, `create_users.py` без фолбэк-пароля,
  `forwarded_allow_ips=127.0.0.1` (rate-limit логина больше не
  обходится подделкой XFF). Шрифты: preconnect вместо блокирующего
  @import, антиква Cormorant убрана из загрузки.
- Ограничения хостинга: сертификат самоподписанный (редирект HTTP→HTTPS
  отложен до боевого домена + Let's Encrypt; домен cmr-svetvdome.online
  сейчас не резолвится), конфиг nginx недоступен пользователю —
  `/css/style.css` кэшируется только через ETag/304, `/js/*` уже immutable.

## Как проверить самому

```bash
docker compose up -d
docker ps                          # оба контейнера должны быть (healthy)
curl -i http://localhost/health    # 200 text/plain
curl -i http://localhost/kanban/cards   # 401 application/json (нужен токен) — НЕ HTML
```

Открыть приложение: http://localhost

## Что было сломано и что исправлено

| # | Проблема | Проявление | Исправление |
|---|----------|-----------|-------------|
| 1 | nginx проксировал `/api/`, которого нет | Все API-запросы возвращали HTML; логин 405 | regex-location с реальными префиксами роутеров (теперь в `nginx/snippets/`) |
| 2 | `python -m urllib.request` не исполняется | Backend «healthy» при мёртвом приложении | `Dockerfile.backend`: `python -c` с реальной проверкой |
| 3 | `localhost` → `::1`, nginx слушал IPv4 | Frontend вечно `unhealthy` | `127.0.0.1` в healthcheck |
| 4 | Паттерны `.dockerignore` без префикса | JWT-ключ, БД клиентов и почтовые настройки попадали в образ | Добавлены пути с `server_snapshot/` |
| 5 | Том монтировался в `/app` | Код замораживался: пересборка образа ни на что не влияла | Том на `/app/data`, введена `CRM_DATA_DIR` |
| 6 | `cpus: '512m'` | `docker-compose.production.yml` не парсился вообще | `cpus`/`mem_limit` верхнего уровня |
| 7 | `SECRET_KEY` vs `CRM_SECRET_KEY` | Переменная игнорировалась, ротация ключей не работала | Приведено к именам, которые читает код |
| 8 | `.env.example` под `.gitignore` | Шаблон не попадал в репозиторий | Исключение `!.env.example` |
| 9 | Контейнеры от root | — | `USER app` (uid 10001) |

## Найдено попутно (не связано с Docker)

| # | Проблема | Проявление | Исправление |
|---|----------|-----------|-------------|
| 10 | `bcrypt 5.0.0` несовместим с `passlib 1.7.4` | **Любой вход падал с 500** | `bcrypt==4.0.1` в requirements |
| 11 | `cards.position` есть в модели, нет в БД | `/kanban/cards` → 500 | `migrate_add_card_position.py` (идемпотентная) |
| 12 | `get_scoped_session` в `Depends()` напрямую | Все 6 эндпоинтов `/tags` → 422 | Обёртка `_scoped_db` в `tags_router` |
| 13 | `test_routes.py` глотал исключения | CI проходил на сломанном импорте | Возвращает ненулевой код, проверяет префиксы |
| 14 | CI тянул образ по тегу, который есть только в main | Job `test` падал на PR и develop | Убран `services:`, добавлена проверка утечки секретов |
| 15–23 | HTTPS и бэкапы: кэш upstream → 502, потеря заголовков безопасности на HTML, healthcheck в TLS-режиме и др. | Разное | Подробно в [`docs/RESULT-2026-08-08.md`](docs/RESULT-2026-08-08.md) |
| 24 | `email_settings.json` исключён из образа, но код искал его рядом с собой | `POST /email-parser/sync` → 400 «Настройки почты не заполнены» | Путь резолвится через `CRM_DATA_DIR` |
| 25 | `main.py` создавал мёртвый `/app/tenants` | Путал при отладке | Удалено |
| 26–28 | Сессия арендатора закрывалась до сериализации ответа | **Все операции записи у арендаторов давали 500** (2556 в боевом логе), хотя запись в БД проходила | Отложенное закрытие в `database.py`, `get_db` стала `async`. [Подробно](docs/RESULT-2026-08-08.md) |

## Проверено

```
Оба контейнера healthy          (docker ps)
Healthcheck ловит сбой          мёртвый порт → exit 1, живой → exit 0
Логин через nginx               токен 240 символов
/kanban/cards                   HTTP 200, 197 записей
/clients                        HTTP 200, 14 записей
/suppliers                      HTTP 200, 19 записей
/tags                           HTTP 200, 3 записи
/activity                       HTTP 200, 50 записей
POST /email-parser/sync         HTTP 200, success: true   (было 400)
Секретов в образе               нет ни одного из 5 проверенных путей
Пользователь в контейнере       uid=10001(app)
Код обновляется при пересборке  build 20260716 → 20260809
Данные переживают down/up       497 карточек, 6 пользователей
Оба compose-файла               docker compose config → OK
```

## Реальные метрики

| Показатель | Заявлялось ранее | Фактически |
|---|---|---|
| Backend образ | ~150 MB | ~320 MB |
| Frontend образ | ~50 MB | ~93 MB |
| Размер БД | ~1.7 GB | 1.7 MB |
| Карточек в БД | — | 497 |

## Данные арендатора 3

В `server_snapshot/tenants/` лежал `crm_3.db-wal` без основного файла — сам
`crm_3.db` отсутствовал. Содержимое WAL восстановлено вручную
(309 из 341 страницы) и сохранено в `recovery/`.

**Потери данных нет:** восстановленное — более старый снимок тех же записей,
которые уже есть в `crm_app.db` (391 из 392 заголовков совпадают, основная база
новее и содержит на 19 карточек больше). Каталог `recovery/` оставлен как
страховка и не попадает в git.

Отдельный вопрос к владельцу: у всех 497 карточек `tenant_id = NULL`, то есть
многотенантность фактически не используется — все работают в общей базе.

## HTTPS и бэкапы — готовы

Проверено запуском prod-конфигурации локально:

```
https /health         200          HTTP / -> 301 https
https /kanban/cards   200 (логин по HTTPS работает)
localhost:8000        недоступен снаружи (как и должно быть в проде)
заголовки безопасности  6 из 6, без дублей
HSTS на HTTP          отсутствует (как и положено)
бэкап восстанавливается  integrity ok, 497 карточек
```

Команды:

```bash
bash scripts/setup_ssl.sh your-domain.com admin@your-domain.com   # сертификат
bash scripts/generate_keys.sh > .env.production                   # ключи
bash scripts/backup_db.sh                                         # бэкап
```

## Что осталось

- [ ] На сервере: выпустить боевой сертификат Let's Encrypt и указать
      `server_name` в `nginx/https.conf` (сейчас проверено на самоподписанном)
- [ ] Поставить бэкап в cron на сервере (команда печатается после запуска
      `scripts/backup_db.sh`) и хранить копии вне сервера
- [ ] Перенести боевые данные в прод-том: у dev и prod разные volumes,
      `crm-data-prod` стартует пустым
- [ ] Роль `documents` (`Dokumenty`, `tenant_id = NULL`) видит общую базу целиком —
      проверить, соответствует ли это замыслу
- [ ] Пересмотреть Phase 2 ([`docs/phases/PHASE2_IDOR_AUDIT.md`](docs/phases/PHASE2_IDOR_AUDIT.md)): предложенный там патч
      (`if card.tenant_id != current_user.tenant_id: raise 403`) при `tenant_id = NULL`
      у всех карточек даст 403 на каждой. Применять нельзя.

## Где остальные документы

Документация собрана в `docs/` — см. [`docs/README.md`](docs/README.md).
Подробный отчёт о работах: [`docs/RESULT-2026-08-08.md`](docs/RESULT-2026-08-08.md).
В корне намеренно остаются только `README.md` и этот файл.

## Важно про Phase 3

Пункт «очистить `.secret_key` из истории git» выполнять не нужно:

```
git log --all -- '*.secret_key'   →  0 коммитов
```

Файл никогда не попадал в репозиторий. Реальная утечка была в другом — ключ
уезжал в Docker-образ (пункт 4), и она закрыта.

## Обновление 2026-08-29: UI-реворк, целостность БД, безопасность, инфраструктура

7 коммитов (`9afbc7b`…`102b15e`), всё задеплоено на прод и проверено живьём.

**Frontend.** Унифицированы 4 таблицы (реестр оплат/документы/клиенты/поставщики:
заголовки выровнены по содержимому колонок, действия справа), раздел «Финансы» —
один заголовок вместо дублирующих h1+табы, тулбары панелей; ячейка «Оплата» —
бейдж + прогресс слева; канбан на мобильных — фильтры свёрнуты по умолчанию;
бейдж «не заполнена» на сделках без ответственного/суммы; задачи — починены
тумблер «ДоскаСписок» и двойное пустое состояние; админка — кнопки «Удалить»
(были обрезаны до красного кружка), шапка; мобильная шторка меню снова работает.
CSS-аудит (агент): 315 дублей селекторов, 467 конфликтов, 1247 !important,
~100 мёртвых классов; этап 1–2 консолидации выполнены (мёртвые семейства +
развязка .btn-show-old/.close-btn/.view-toggle-btn/.sidebar-toggle).

**Backend/БД.** `PRAGMA foreign_keys=ON` + synchronous=NORMAL (каскады реально
работают; перед включением вычищены сироты: локально ~986 записей); defensive-
отвязки детей в удалениях клиента/поставщика/пользователя/карточки; починены:
permanent delete (не было `import os`), спам уведомлениями по закрытым сделкам
(статус «Закрыто»), двойное шифрование пароля IMAP, мёртвые вебхуки (фоновый
поток); SSRF: проверка публичности хоста перед каждым запросом + без редиректов;
ретенция в sync-overdue (уведомления >30 дней, версии >180 дней, топ-20);
индексы card_attachments/card_checklists(card_id), owner_id=0 → NULL (212 карт).

**Инфраструктура.** БД/uploads/ключи вынесены из веб-корна в
`/var/www/h212005/data/crm_data` (PM2 env CRM_DATA_DIR/CRM_UPLOADS_DIR/
CRM_SECRET_KEY; ecosystem-конфига нет — env через pm2 dump). Пути в БД
переписаны на относительные. Прод-бэкенд слушает **20008** (не 8000 — там
чужой сервис). Бэкапы v2: integrity_check, uploads ежедневно, секреты 600,
скрипт `scripts/backup_crm_prod.sh` (крон 03:00, вызов через `bash` —
права +x раньше терялись и бэкапы молча умирали — починено).

**Плагины/инструменты.** ZCode-плагин mimosa отключён в
`~/.zcode/cli/config.json` (git-gate блокировал коммиты ложными
path-traversal на `open(переменная)`; после pathlib-рефакторинга остался
1 ложный на `tools/css_cascade.py`). `tools/css_cascade.py` в git
НЕ включён — лежит только локально. Скриншот-верификация через IAB в сессии
была нестабильна — факты проверялись computed-стилями и API.

## Обновление 2026-08-30: роли на роутерах

Системные поверхности закрыты за админом (admin + superadmin), проверено
живьём на одноразовом контейнере с токенами всех ролей:

- `webhooks_router` — целиком `require_admin()`. Вебхук отправляет данные
  сделок на внешний URL: любой аутентифицированный (склад/документы) мог
  подписаться на `card.created` и сливать базу. Таблица webhooks пуста —
  пользы для остальных ролей в ней нет.
- `workflows_router` — целиком `require_admin()`: шаги воркфлоу умеют
  `http_request` и `code`. Таблица workflows пуста.
- `custom_objects_router` — создание/удаление типов и полей (меняют модель
  данных) только для админа; чтение и CRUD записей открыты всем (это рабочие
  данные). Таблицы пусты.
- Фронтенд (`site/`): вкладки «Кастомные объекты»/«Воркфлоу»/«Webhooks» в
  Настройках помечены `data-admin-only` и скрываются для не-админов
  (`applyRoleToSettingsTabs` в js/auth.js, дефолт не-админа — «Почта»).
  Хеши кеш-бастинга проставлены `tools/stamp_assets.py`.
- Бизнесовые роутеры (канбан, оплаты, списания, клиенты, поставщики, задачи,
  теги, активность, уведомления, сохранённые виды, группы списаний, почта)
  сознательно оставлены всем аутентифицированным: фронтенд показывает эти
  разделы всем, более тонкая матрица (кто может удалять клиентов и т.п.) —
  отдельное решение владельца.
- Проверено: manager/warehouse → 403 на /webhooks/, /webhooks/events,
  /workflows/, POST/DELETE /custom/objects; superadmin/admin → 200;
  GET /custom/objects и /tags у менеджера → 200 (бизнес не задет).
- Попутно обнаружено и задокументировано: фронтенд внутри
  `server_snapshot/` — устаревший одноразовый бэкап сервера, актуальный —
  в `site/` (см. `server_snapshot/README.md`). Правки фронтенда вносить
  только в `site/`.

**Осталось (приоритет):**
- [ ] DNS: A-запись cmr-svetvdome.online → 87.232.64.12 (нужен доступ
      регистратора), затем `bash scripts/setup_ssl.sh cmr-svetvdome.online …`
- [ ] Offsite-бэкапы (rclone/restic на внешний ресурс — нужен токен)
- [x] Роли на роутерах (закрыто 2026-08-30: webhooks/workflows/custom-типы →
      admin; бизнес-роутеры сознательно открыты всем аутентифицированным)
- [ ] LIMIT/N+1 (реестр без лимитов, N+1 в задачах/поставщиках)
- [ ] CSS этап 3: dark-wildcards (6624–6699 старой нумерации) → переменные,
      остатки дублей (.kanban-card ×8, .settings-tab ×4), брейкпоинты 9 → 3
- [ ] Выпиливание тенант-слоя (~45 ветвлений, план у агента-аудитора)
- [ ] Деньги: float → целые копейки (миграция дешёвая, 188 транзакций)
- [ ] Временный юзер `ui_audit_tmp` создавался/удалялся многократно —
      в обеих БД по 6 штатных пользователей (проверено)
