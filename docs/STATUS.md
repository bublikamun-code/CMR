# Фактическое состояние

Дата актуализации: **2026-09-18**. Раздел 18.09 проверен локально и на проде;
исторические записи ниже описывают проверки на указанные в них даты.

## 18.09: серверные бэкапы — причина обрывов найдена, архивы восстановлены, скрипт доработан и выложен

Гитом `main` синхронизирован с `server/main` (fast-forward, SHA совпадают).
Продовые данные при всех операциях не тронуты: отпечаток (30 таблиц + uploads)
до и после идентичен, health `ok`, PM2 без рестарта.

- **Диагноз uploads-бэкапов.** На аккаунте квота диска: свободно было ~600 МБ
  (dd-проба). С 15.09 nightly-шифрование uploads упиралось в квоту: `.enc`
  обрезался, открытый `.tar.gz` оставался рядом, а секция uploads не писала
  FAIL — сбой был виден только как строка `error writing output file`.
  Валидным был только архив за 14.09.
- **Восстановление.** Шесть открытых копий (13.09 ×2, 15–18.09) проверены
  `gzip -t`, перешифрованы серверным ключом, каждый результат проверен
  обратной распаковкой, битые `.enc` заменены, открытые копии удалены;
  безвозвратно битый `uploads_20260913_114821.tar.gz.enc` (источника нет)
  удалён. Все 7 uploads-архивов теперь проверяются `OK`. Место: 4.2G → 2.4G,
  запас ~2.6 ГБ.
- **`server_snapshot/scripts/backup_crm.sh`** (encrypted-версия, логика шагов
  сохранена): staging-каталог внутри DST, шифрование + обратная проверка
  каждой части, публикация только готовых `.enc` (600), честные FAIL по
  каждой секции и ненулевой код выхода при сбое, старые архивы при отказе не
  удаляются, ротация KEEP=14 по строгому формату имён, APP/DATA/DST/KEEP/BK_DIR
  переопределяются окружением. Тесты `test_backup_crm_encrypted.py` (6,
  настоящие SQLite/WAL/openssl). Гейт: 114 passed.
- **Выкладка и ручной прогон на проде 18.09 10:42**: db OK (integrity ok),
  uploads OK (341M — первый полный с 14.09), code OK, secrets OK, EXIT=0.
  Скрипт заменён rsync без рестарта PM2 (bash не требует перезагрузки).
- **Мусор инцидента 06.08**: в webroot/tenants заархивированы и удалены битый
  симлинк `crm_3.db` и его `-shm`/`-wal`; архив в `~/backups/manual/`.
  Живой `managerd.db` не тронут.
- **Две несвязанные линии истории.** `origin/main` (GitHub) — отдельный
  репозиторий с другим корнем и раскладкой (vite-сборка фронта, css/ в корне):
  продовой `css/style.css` байт-в-байт равен версии `origin/main` (деплой
  13.09, D5/D6/D9/D8-срез 1). Данный репозиторий — бэкенд/ops-линия. Слияние
  сознательно не выполнялось: разные раскладки и право решения за владельцем.
  Пуш серверной линии на GitHub веткой `server-line` не прошёл: 403 у
  сохранённой учётки (нет прав на запись).
- **Открытый риск (решение владельца):** при росте uploads KEEP=14 хранит
  ~5 ГБ архивов против квоты ~7 ГБ — либо поднимать квоту, либо снижать KEEP
  для uploads; теперь такой сценарий завершится громким FAIL, а не битым
  архивом.

## 16.09: критические исправления инструментов — локально, без деплоя

Рабочий проект: `crm-svetvdome`, ветка `fix/local-gate-backups`.
Прототип в другой директории и UI не менялись. Деплой отложен до ребилда UI.

- `tools/deploy.sh`: ранее исправлены запрет реального рестарта при
  `--dry-run restart` и остановка до PM2 при ошибке каталога/загрузки `.pm2.env`.
  Проверяется успешная загрузка файла, не полнота его переменных. Шесть тестов
  используют заглушки SSH/PM2; реальный рестарт в этой работе не выполнялся.
- `scripts/backup_db.sh`: уникальные временные каталоги, очистка только своего
  запуска, обязательная исходная БД, проверка SQLite-снимков, сжатие до ротации,
  `KEEP` отдельно для каждой tenant-БД, права 600. Исправлена ротация под
  Bash 3.2 с `set -u`; пути с пробелами проверены.
- `scripts/backup_crm_prod.sh`: источник открывается read-only и не создаётся
  при отсутствии; приватный WAL-снимок проверяется с возможностью создать
  служебные файлы SQLite. Ошибки gzip/tar останавливают запуск до ротации.
  Архивы создаются с `umask 077`, используются уникальный staging и валидация KEEP.
  Это локальная копия скрипта, не подтверждение установленного server cron.
  `server_snapshot/scripts/backup_crm.sh` (отдельная encrypted-версия) не менялся.
- `scripts/check.sh`: единая локальная проверка без установки зависимостей,
  SSH/PM2, Docker или запуска сервера. Изолированы cwd, ключи и данные pytest.
- `tools/check_handlers.js`: устранены ложные срабатывания для методов объектов
  и длинных объектов; неизвестный обработчик даёт ненулевой код. Это ограниченный
  статический анализ, не браузерная проверка поведения UI.

**Итоговый локальный гейт:** `bash scripts/check.sh` — exit 0;
**108 passed, 2 warnings** (устаревающие API Starlette/AnyIO и passlib/crypt).
Shell-синтаксис 11 скриптов и JS-синтаксис в порядке; обработчики 49/49;
штампы ресурсов — 37 ссылок, 0 расхождений. В наборе — 27 backup-тестов,
15 тестов анализатора и 11 тестов самого гейта.

**Реальный Docker round-trip:** отдельный контейнер без сети и host volumes;
основная БД и две tenant-БД с непустым WAL → архив → распаковка →
`integrity_check = ok` и точное совпадение трёх записей в каждой БД.
Проверены KEEP=2 для каждой БД, права 600 и очистка staging. Контейнер удалён;
рабочие контейнеры и данные CRM не затрагивались.

Полный гейт требует macOS JavaScriptCore, Node.js и подготовленный Python venv;
существующий почтовый тест пытается подключиться к 127.0.0.1. Проверки не
подтверждают текущую работоспособность production или возможность восстановления
всего production-окружения. Инструкция восстановления обновлена: нельзя копировать
снимок поверх работающей SQLite и нельзя использовать `down -v`.


## Фидбек 07.09 (ночь): почта — найдена и устранена причина падения синка

Проверка почты после перешифровки пароля выявила: синк падал с «неверным
паролем». Расследование (временная отладка в живом приложении) показало
цепочку:

1. В серверном venv **не был установлен пакет `cryptography`** (в коде это
   допускалось try/except — пароль хранился открытым текстом и расшифровка
   не была нужна) → `_fernet = None`.
2. Вчерашняя перешифровка пароля (№4) сделала хранение зашифрованным →
   приложение не смогло расшифровать и подставляло Fernet-токен как пароль
   → IMAP отвечал «incorrect password or account name», карточки из писем
   не создавались.

**Исправление**: `cryptography==50.0.1` установлен в серверный venv
(`pip` в нём был сломан после переезда — старый shebang; запуск через
`python -m pip`) и добавлен в requirements.txt; в коде — лог ошибки при
недоступной cryptography на старте (раньше молчало). Проверено: sync-all
`success: true, tenants_failed: 0`, почтовый логин OK, пароль расшифровывается
(15 симв.), свежих писем к импорту нет — очередь чистая. Конвейер писем
работал и раньше: 560 карточек создано из почты, последняя — 06.09 18:50.

Попутно (из того же разбора): watchdog дёргает IMAP-синк каждые 5 минут —
частые логины могут триггерить троттлинг на почтовом сервере; если ошибки
вернутся — разнести синк и watchdog по разным cron-частотам.

## Фидбек 07.09 (вечер): «Запомнить меня» — перестало выкидывать из аккаунта

Причина вылетов: срок жизни токена/куки — ровно 24 часа, после чего первый
же запрос получал 401, и пользователь оказывался на форме входа.

Сделано:
- На форме входа чекбокс «Запомнить меня на этом устройстве (30 дней)»
  (включён по умолчанию): `POST /auth/login` принимает `remember=1` →
  токен и httpOnly-кука на 30 дней (claim `rm`).
- Скользящее продление в `get_current_user`: когда токен прожил больше
  половины срока, активному пользователю выдаётся свежая кука на полный
  срок — при регулярной работе вылетов больше нет.
- Смена пароля по-прежнему инвалидирует все сессии (claim `pv`).
- Тесты: кука 30 дней при remember, 24ч без него, продление на /auth/me
  (23/23 зелёные). Деплой front + auth.py/auth_router.py, PM2 restart.

Примечание: пока прод на HTTP, 30-дневная кука — тот же открытый текст,
что и раньше, просто дольше живёт; Secure включится автоматически с TLS.

## Безопасность хранения данных (фидбек 07.09, пункты 2–5)

Живые проверки: чувствительные файлы веб-корна (база/ключи/бэкапы/env) по
HTTP не отдаются (404), ключи 600, данные вне веб-корна. Закрыто:

- **№2 — преддеплойный бэкап вне веб-корна**: перенесён в
  `/var/www/h212005/data/backups_predeploy` (каталог 700, файл 600,
  integrity ok), веб-корн больше бэкапов не содержит. CI-шаг «Backup
  database on server» переписан: снимок через sqlite .backup из
  `crm_data/crm_app.db` (раньше cp несуществующего файла из веб-корна —
  шаг был сломан, но спал), права 600, ротация 5 копий.
- **№3 — бэкапы шифруются**: `scripts/backup_crm.sh` шифрует ВСЕ артефакты
  (база/тенанты/uploads/код/секреты) AES-256-CBC + PBKDF2. Ключ:
  `/var/www/h212005/data/keys/.backup_key` (600, авто-генерация),
  **копия на Mac — `~/.crm_backup_key`** (перенести в парольный менеджер:
  без ключа зашифрованные бэкапы не восстановить). Проверено: ручной прогон,
  round-trip расшифровки — integrity ok, 828 карточек; старые
  НЕзашифрованные артефакты из ~/backups удалены. Восстановление — по
  инструкции в шапке скрипта.
- **№4 — почтовый ключ**: фактический ключ Фернета для пароля почты —
  `CRM_SECRET_KEY` из .pm2.env (600, env приоритетнее файлов), файлы
  `.email_secret_key`/`routers/.secret_key` — неиспользуемые fallback'и.
  Legacy `routers/.secret_key` был 644 в веб-корне → права 600. Обнаружено
  и исправлено попутно: пароль ящика хранился в email_settings.json
  ОТКРЫТЫМ ТЕКСТОМ (записан старой версией) — перешифрован через текущий
  save_settings, расшифровка ключом проверена.
- **№5 — права файлов базы**: crm_app.db/-wal/-shm → 600 (читает только
  процесс приложения).

Осталось осознанно: база на диске не шифруется (SQLite без SQLCipher —
защита уровня «утёк один файл» недостижима без большой переделки);
офсайт-копия на Mac тоже содержит зашифрованные архивы + ключ рядом
(перенесите ключ в менеджер паролей); TLS — ждёт DNS.

## Фидбек 2026-09-07: стилизация переключателя «Liquid Glass»

Переключатель в «Настройки → Профиль → Внешний вид» был системным
чекбоксом 16×16: правило `.settings-card input[type="checkbox"]`
(16×16 `!important`, accent-color) перебивало пилюлю из liquid-glass.css.

Сделано: полноценный тумблер-пилюля 44×24, ручка 18px с translate 20px,
трек `var(--switch-track, #ccd3de)` + отдельный цвет для тёмной темы,
включённое состояние — градиент #0ea5e9→#6366f1, focus-visible ring.
Из правила настроек чекбокс исключён через `:not(#glass-mode-toggle)`.
Проверено живьём: размеры 44×24, вкл/выкл переключает data-glass,
фокус-рамка на месте. Деплой `tools/deploy.sh front` (style.css
`b0de2835e7`, liquid-glass.css `434f20b928`).

## Пункты 2–3 из плана 06.09 (владельческие решения + косметические спринты)

**У1/Н20 — страницы-сироты удалены.** settings.html, workflows.html,
custom_objects.html (и их роуты в main.py + мёртвый JS: custom_objects.js,
settings_page.js, workflows_page.js) — функционал на 100% покрывают вкладки
index.html, входящих ссылок на страницы не было ни одной. Страница воркфлоу
вдобавок описывала несуществующий движок. CRUD воркфлоу в API и во вкладке
«Настройки» сохранён; САМ движок исполнения (триггеры/шаги) — отдельный
продуктовый спринт, пока не построён. На сервере файлы удалены вручную
(rsync без --delete), разделы отдают 404.

**Н4 — решено**: правило «склад сделки единый» (смена склада сделки
синхронно обновляет склад всех её транзакций, включая накладные) оставлено
как сознательное, задокументировано в card_details_router.

**Н8 — глобальный обработчик IntegrityError**: нарушение уникальности/связей
теперь отдаёт 409 с читаемым текстом вместо безликого 500; детали в лог.
Тест на возврат 409.

**Ф9 — частично**: Chart.js (205КБ) больше не грузится при каждом входе —
dashboard.js подключает его лениво при первом открытии Дашборда; проверено
(на канбане вендора нет, на Дашборде график рендерится). Полноценный
бандлер (Vite/esbuild) — отдельно, с gzip выигрыш умеренный.

**Ф3 — частично**: в шапке style.css закреплена карта целевых брейкпоинтов
(640/768/900/1100). Массовый перенос исторических медиазапросов (их ~11
уникальных ширин, 1181 !important) сознательно не выполнялся без визуальной
выверки на каждой ширине — высокий риск регрессий; отдельный спринт.

Деплой: front + main.py (PM2 restart чисто). На проде: index 200,
страницы-сироты 404, API без токена 401.

## Фидбек 2026-09-06 (ночь, 2): «На списании» дата окончания не просрочка

Фидбек: если карточка ушла в «Списание», дата окончания сделки считается
закрытой — сделка фактически выписана, «висят» только вопросы оплат
(они отслеживаются отдельно по payment_due_date).

Изменено везде, где решается «просрочена ли сделка по due_date»:
- kanban.js: сводка просрочек над доской, подсветка карточек (card-overdue/
  card-due-soon), фильтр «Просроченные», строки режима «Список» — статус
  «На списание» теперь в одном ряду с «Закрыто» (константа
  DEAL_DEADLINE_DONE);
- card_modal.js — красная пометка просрочки в модалке;
- calendar.js — «Просроченные сделки» в календаре;
- сервер `notifications_router.sync-overdue` (cron ночью):
  `DEFAULT_OPEN_STATUSES = («На списание», «Закрыто»)` — уведомления
  «Просрочена сделка» больше не создаются по таким карточкам. Просрочки
  ОПЛАТ (payment_due_date) считаются отдельно и не затронуты.

Тест: сделка с просроченной датой в «В работе» получает card_overdue,
в «На списании» — нет (19/19 зелёные). Проверено вживую на копии базы:
карточка с вчерашней датой в «В работе» красная, после перевода в
«Списание» подсветка уходит. Деплой: фронт + notifications_router,
PM2 перезапущен чисто (штампы b430787d9b/c74ab1b11f/bc10a3a5f6 на проде).

## Фидбек 2026-09-06 (ночь): интерактивные кнопки на плитках «Списания»

По образцу панели переноса на канбане: кнопки действий плитки «Списания»
(«Выписать накладную», «Списать», «⧉ Прикрепить сделку» — и у групповых
плиток тоже) появляются при наведении мыши или фокусе с клавиатуры
(`.writeoff-tile-actions`, opacity + :hover/:focus-within). Место под
кнопки зарезервировано (opacity, а не display) — высота плиток не прыгает;
на тач-устройствах (`hover: none`) кнопки видны всегда. Клик проверен:
«Выписать накладную» открывает карточку сделки. Локальная проверка на
копии боевой базы; деплой `tools/deploy.sh front` (writeoffs.js
`8f17d33d03`, style.css `870e172b98`).

## Фикс 2026-09-06 (поздний вечер): «Списание» — стабильная полная ширина

Фидбек с двумя скриншотами: при заходе «Списание» заполняет всю ширину
(2 склада по ~800px, плитки ~370px) — так и надо; но после F5 доска
сжималась до узких панелей ~574px (плитки 255px) — это был баг.

**Причина сжатия.** Раунд 04.09 привязал ширину плиток к фактической ширине
канбан-карточки (`--kanban-card-w` пишет kanban.js после рендера доски).
После F5 приложение открывалось сразу в «Списании», канбан не рендерился,
гвардия нулевой ширины не позволяла записать переменную — оставался
фолбэк 255px, и доска сжималась.

**Фикс** (`bb2853f678` на проде): привязка к канбан-переменной убрана,
складские панели растягиваются равномерно на всю ширину средствами CSS —
`.writeoffs-container { grid-template-columns: repeat(auto-fit,
minmax(574px, 1fr)) }`. Результат не зависит от того, рендерился ли канбан:
SPA-вход и F5 дают идентичную раскладку.

**Проверено** на локальной копии боевой базы (828 карточек, 1920px):
SPA-вход и F5 — блоки 795+795 (заполняют 1610 контейнера), плитки 365px,
идентично; на 1440px склады встают друг под другом на всю ширину
(адаптивно, горизонтального переполнения нет). Деплой — `tools/deploy.sh
front` (штамп `bb2853f678`), PM2 не перезапускался.

Промежуточная версия этого фикса (зафиксировать 574px, коммит `2d9e79a`)
оказалась понята наоборот и заменена текущей — владелец хочет широкую
раскладку из первого скриншота.

## Обновление 2026-09-06 (ночь, деплой): все спринты аудита 06.09 на проде

Порядок деплоя (данные — приоритет): бэкап → миграции → бекенд → фронт.

1. **Бэкап до изменений**: `crm_app.db.20260906_223231` в `backups/` на сервере
   (SQLite backup API, 828 карточек, 6 пользователей, integrity ok).
2. **Миграции** (`migrate.py`, CRM_DATA_DIR из .pm2.env): 0001–0004 применены.
   0004 не нашла ни одного дубля остатков (гонка ни разу не срабатывала) —
   данные не менялись, создан только unique-индекс. После миграций:
   cards=828, transactions=313, integrity ok, journal=4.
3. **Бекенд**: 23 файла через `tools/deploy.sh back` (main, db_utils, schemas,
   migrate.py, migrations/, 12 роутеров), PM2 перезапущен с боевым
   окружением — оба воркера startup complete, ошибок в err-логе нет.
4. **Фронт**: `tools/deploy.sh front` (check_js + штампы прошли, rsync ok).
5. **Проверки прода**: `/health` ok v2.1.0; index 200 с `Content-Encoding:
   gzip` и CSP `script-src 'self'` (unsafe-inline ушёл); `/kanban/cards` без
   токена 401; `/docs` 404; отдаются boot.js/handlers.js с новыми штампами.
   Живой прогон логин-экрана прода в браузере: 0 нарушений CSP, 0 ошибок JS.

Откат при необходимости: бэкап из п.1 + `git revert` + повторный
`tools/deploy.sh` (front/back). Нюанс: после миграции 0003 база хранит даты
в aware-формате — откат ТОЛЬКО кода (без бэкапа) вернёт смешение форматов
дат (исходная проблема Н7); корректный откат — код + бэкап базы вместе.
Индекс 0004 старому коду не мешает (он лишь запрещает второй остаток).

## Обновление 2026-09-06 (ночь): Н13, Н9, У2, У3 — «данные не пропали»

- **Н13 — унификация сессий**: удалены ВСЕ 44 копии бойлерплейта
  `if role == "superadmin" and tenant_id is None: session = db` в 6 роутерах
  (card_details 11, payments 13, writeoff_groups 7, clients 5, suppliers 5,
  writeoffs 3). Доказательная база: `resolve_tenant_db` возвращает внедрённую
  `db` ровно при `tenant_id is None`, а ветка при истинности условия лишь
  переприсваивала `db` — то есть была no-op при любом раскладе ролей.
  Единственный оставшийся условный переход — activity_router (`src_is_main`,
  там ветка делает реальную фильтрацию тенанта). Дифф — 109 удалённых строк,
  0 добавленных. Регрессионный тест: менеджер (tenant_id NULL) читает все
  бизнес-списки через `_db`-путь (200).
- **Н9 — гонка остатка закрыта физически**: миграция 0004 находит карточки с
  >1 «пустым» остатком, вычисляет авторский остаток (сумма сделки минус
  выписанное), оставляет одну запись (приоритет — совпадающая по сумме, иначе
  пересчёт свежей), удаляет ТОЛЬКО строки-заглушки (накладные/документы/
  списания неприкосновенны) и создаёт частичный unique-индекс
  `uq_remainder_per_card`. `trigger_from_card` ловит IntegrityError и
  возвращает запись победителя гонки. Тест на синтетической базе: дубли
  схлопываются до одного остатка с верной суммой, накладная с документом
  выживают, второй остаток через индекс запрещается; идемпотентность.
- **У2 — подписи полей**: переобследовано (часть из «24» уже обёрнута в
  label и имеет доступное имя); реальные пробелы — 2 поля с
  placeholder-подписями (`sst-config`, `task-checklist-input`) получили
  aria-label. hidden-инпут из подсчёта исключён.
- **У3 — клавиатурная альтернатива drag&drop**: в меню карточки (⋯, видно и
  по :focus-within — кнопки в tab-порядке) добавлены «→ статус» для всех
  колонок, кроме текущей. Бизнес-правила те же, что у перетаскивания: без
  суммы дальше «Нового запроса» нельзя; уход из «Сборки» назад убирает
  запись из реестра. Проверено живьём: перенос кнопкой меняет колонку,
  фокус раскрывает действия.
- Тесты: 18 (было 12) — добавлены регрессия Н13, две валидации Н19,
  уникальность остатка и два теста миграции 0004. Штампы синхронны,
  py_compile/node --check чисты, живой прогон в браузере (меню, перенос,
  фокус) без ошибок.

## Обновление 2026-09-06 (вечер): остаток бэклога аудита 06.09

- **С4/подготовка TLS — проверена, код не потребовался**: nginx уже передаёт
  `X-Forwarded-Proto` (snippets/app-locations.conf:65), прод-энтрипоинт
  `server.py` уже запускается с `proxy_headers=True, forwarded_allow_ips=
  "127.0.0.1"` — после включения TLS `request.url.scheme` станет https и кука
  автоматически получит `Secure`. В CORS-дефолты добавлен
  `https://cmr-svetvdome.online`.
- **Н22 — gzip**: `GZipMiddleware` в приложении (API и HTML сжимаются при
  любой схеме проксирования; проверено: index 73 КБ → 12 КБ) + директивы gzip
  в обоих repo-конфигах nginx (http/https). Для голого nginx хостера, который
  обслуживает статику мимо приложения, — попросить хостера включить gzip
  (единственный недоступный нам уровень).
- **Н17 — мёртвые файлы удалены**: pm2_restart.js, restart_trigger.sh,
  restart_crm.sh (битый, в корне snapshot), routers/.secret_key,
  tenants/managerd.db, scripts/{backup,deploy,rollback}.sh со старыми путями.
  Оставлены: scripts/backup_crm.sh (упомянут в DEPLOYMENT.md),
  scripts/restart_crm.sh (рабочий, ручной рестарт), scripts/watchdog_crm.sh
  (живой cron) — и в нём оказалась уже реализованная ротация лога, т.е. **Н24
  закрыта** без правок.
- **Н5 — мини-раннер миграций**: `server_snapshot/migrate.py` + `migrations/`
  (NNNN_имя.py с идемпотентной `up(cur)`, журнал в schema_migrations).
  Залиты 0001 (cards.position) и 0002 (платёжные колонки) — порты прежних
  разовых скриптов, которые жили только на сервере. Запуск:
  `CRM_DATA_DIR=... python3 migrate.py` (или `status`). Проверено на свежей
  базе: применяется один раз, повтор — 0.
- **Н11 — предохранители списков**: kanban `/cards`, `/trash`, `/clients`,
  `/suppliers`, `/tags`, `/writeoffs/pending` обрезаются `db_utils.cap_list`
  на 5000 записей с заголовками X-Total-Count/X-Truncated (контракт реестра
  оплат). Это пробки, не пагинация — канбану нужны все карточки сразу.
- **Н12 — N+1**: `/clients/{id}/cards` и `/writeoffs/pending` получили
  selectinload (как канбан); `custom_objects list_records` выбирает значения
  всех записей одним IN-запросом вместо запроса в цикле.
- **Н7 — единый формат дат**: `utcnow()` в tasks/notifications заменён на
  aware `datetime.now(timezone.utc)` (единый формат с дефолтами моделей);
  миграция 0003 добавляет `+00:00` старым naive-строкам в
  tasks/notifications (идемпотентно). Без неё смесь форматов ломала
  строковые сравнения дат в SQLite (просрочки, read_at).
- **Н19 — валидации**: `CardUpdateStatus.status` — только валидные статусы
  (опечатка раньше уходила в БД, карточка пропадала с доски);
  `TransactionUpdate.amount` — строго > 0.
- **Ф6**: мёртвое чтение токена из cookie в api.js удалено (кука httpOnly,
  ветка всегда возвращала null). **У6**: пустая доска канбана получила
  баннер «Сделок пока нет» с кнопкой «Создать первую сделку» (переиспользует
  модалку создания); баннер создаётся после ветки колонок — та делает
  `board.innerHTML=''` (поймано живым прогоном).
- **Проверено**: py_compile, node --check, pytest 12/12, штампы синхронны;
  живой прогон в браузере: списки 200, gzip на больших ответах, пустой
  канбан с работающим CTA (модалка открывается).
- **Осталось вне этой сессии** (требует владельца/отдельного спринта):
  Н13 (унификация superadmin-бойлерплейта — 45 мест, нужен отдельный
  рефакторинг с тестами на каждый роутер), Н9 (unique-индекс на остаток —
  нужна ревизия прод-данных на дубли), Ф3 (CSS-долг), У2/У3 (a11y форм и
  клавиатурный drag&drop), Ф9 (бандлинг), Н20/У1/Н4 (решения владельца:
  воркфлоу, страницы-сироты, склад в накладных), TLS — после A-записи.

## Обновление 2026-09-06 (день): план исправлений по аудиту 06.09

Коммит «Аудит 06.09» (утренние фиксы) + этот. Пункты бэклога отчёта:

- **P0-1, Ф2**: `liquid-glass.css` и все правки закоммичены; **Ф8**: `.bak`-файлы
  (5×css, 1×js) удалены из `site/` (фикстура `_fx4.html` в веб-корне уже
  отсутствовала).
- **P1-3, Н2 — реальные тесты**: `server_snapshot/tests/` (pytest + TestClient,
  12 сценариев): логин/кука httpOnly, 401 без токена, роли (403 на вебхуки
  менеджеру), идемпотентность trigger_from_card, частичная накладная + **фикс Н1**
  (правка суммы сделки не трогает выписанные накладные, остаток пересчитывается),
  отклонение накладной сверх остатка, rate limit логина и смены пароля,
  закрытие /docs (С6), заголовки безопасности + script-src без unsafe-inline (С5),
  обобщённая ошибка почтового синка (С7). CI «Run tests» заменён с smoke-импорта
  `test_routes.py` (удалён) на pytest; локальный прогон — `server_snapshot/.venv`
  (python3.12, добавлен в .gitignore).
- **P2-7, С3 — rate limits**: `PUT /auth/me/password` 5/мин, `POST
  /email-parser/sync` 5/мин, оба upload-эндпоинта 30/мин. Внимание: за nginx
  нужен `proxy-headers` (см. P0-HTTPS), иначе лимит по IP прокси — общий на всех.
- **P2-8, С6**: `/docs`, `/redoc`, `/openapi.json` выключены (`docs_url=None`…).
- **Низкий, С7**: тексты исключений почтового сервера больше не уходят клиенту —
  только обобщённые формулировки, детали в логе.
- **P2-8, С5 — CSP без `unsafe-inline`**: конвертированы ВСЕ inline-обработчики
  (не только 26 в index.html из оценки аудита — CSP блокирует и остальные):
  73 onclick + 5 onchange в 5 страницах и 29 onclick в шаблонах JS-модулей
  (calendar, settings, tasks, custom_objects, clients, email_parser, suppliers),
  плюс 13 инлайн-скриптов. Новые файлы: `js/boot.js` (ранние темы/стекло, без
  defer до стилей — без вспышки темы), `js/handlers.js` (делегирование
  `data-handler`/`data-arg`/`data-args`/`data-change` + data-tab + биндинги
  канбана/фильтров + общие toggleSidebar/восстановление сайдбара/браузерные
  уведомления), `js/settings_page.js` / `js/workflows_page.js` / `js/admin_page.js`
  (бывшие большие инлайн-скрипты страниц). `script-src 'self'` — в main.py
  (nginx CSP не добавляет, сниппет трогать не потребовалось). Штампы переставлены,
  node --check чист.
- **Найдено и починено попутно (латентные баги)**: (1) в settings.html,
  workflows.html и custom_objects.html был ВТОРОЙ обработчик клика на
  переключателе темы поверх features.js — двойное переключение; дубли удалены;
  (2) `SettingsUI.switchTab` подсвечивал вкладку через
  `.settings-tab:nth-child(N)` и попадал в чипы статусов задач (тот же класс,
  раньше в DOM) — подсветка вкладки настроек не работала; теперь активный класс
  ставится по `data-tab`; (3) экранирование кавычек в именах пользователей в
  inline-onclick админки (`replace(/'/g…)`) заменено на замыкания — имена с
  кавычками больше не ломают кнопки «Изменить/Удалить».
- **Проверено живьём**: приложение поднято локально (uvicorn + временная база),
  вход, канбан (сайдбар, тема, Доска/Список, фильтры, модалка «Новая сделка»),
  вкладки настроек (SPA и settings.html), workflows/custom_objects/admin — на
  всех страницах 0 нарушений CSP (`securitypolicyviolation`) и 0 ошибок JS.
  pytest 12/12.

## Аудит 2026-09-06: полный (бекенд + фронтенд) — 3 глобальных фикса

Полный отчёт — `docs/audits/AUDIT-2026-09-06.md`. Кратко:

- **Исправлена порча финансовых данных**: правка суммы сделки в карточке
  перезаписывала суммы ВСЕХ её транзакций, включая уже выписанные накладные
  (реестр оплат расходился с документами). Теперь пересчитывается только
  запись-остаток: сумма сделки минус выписанное; накладные не трогаются
  (`card_details_router.py`, PATCH /cards/{id}).
- **Закрыт последний IDOR**: `GET /files/{filename}` отдавал любой файл из
  uploads любому авторизованному без проверки владельца; фронт им не
  пользуется с 31.08. Эндпоинт удалён, из `card_modal.js` удалена его
  мёртвая обёртка `downloadFile` (скачивание — по id, как и было).
- **Почтовый синк не зависает**: таймаут 15 с на IMAP-connect (раньше
  settimeout ставился после конструктора) + `logout` в finally (раньше
  соединение текло при ошибке посреди выборки) — `email_parser_router.py`.
- Проверки: py_compile и node --check чисты, штампы статики переставлены
  (`card_modal.js?v=ef542b41e1`).
- Главные открытые риски (см. отчёт, §0 и §6): прод без TLS (ждёт DNS);
  **`site/css/liquid-glass.css` и правки 10 файлов не закоммичены** —
  закоммитить в первую очередь; тестов нет (CI «Run tests» — smoke-импорт).

## Фиксы 2026-09-05 (вечер): оплата, скролл «Списания», ховер меню, модалка

- **«Списание»: карточки больше не сплющены** (фидбек 2026-09-06: «карточки
  опять скрывают часть информации»). Причина: `.writeoff-cards` — грид с
  определённой флексной высотой, и Chromium делил авторяды поровну —
  карточки сжимались до ~60px, `overflow:hidden` прятал заголовок, сумму
  и кнопки. Лечится `grid-auto-rows: min-content` — ряды снова по
  содержимому. Проверено на стенде: карточка 148px целиком (заголовок,
  сумма, «Оплата», обе кнопки), колонка листается отдельно, месячные
  группы «Списано» не сломаны. Воспроизведение и проверка — live-стендом
  с реальными style.css/liquid-glass.css.
- **Канбан и «Списание»: каждая колонка листается отдельно** (фидбек
  2026-09-06). Колонки снова скроллятся сами, но высота — не магический
  `calc(100vh - Npx)` (неверный N давал недостижимый низ и «обрезанные»
  карточки — первопричина обеих жалоб), а честный флекс-остаток страницы:
  доска/контейнер магазинов растягиваются до низа вьюпорта, списки карточек
  `flex:1 + overflow-y:auto`, низ колонки и скроллбар всегда доступны.
  Маски-затухания нижнего края убраны, у канбана постоянный тонкий
  скроллбар (как в «Списании»). Проверено в браузере: низ доски 810px при
  вьюпорте 830px, последняя карточка после прокрутки видна целиком.
- **Оплаченная карточка — зелёная полоса.** Раньше красный индикатор фасада
  держался до перезахода: `isOverdue` в `kanban.js`/`card_modal.js` не смотрел
  на статус оплаты. Теперь `payment_status === 'Оплачен'` снимает просрочку
  и «скоро дедлайн», карточка получает класс `card-paid` (шалфей #5aa98c);
  оплаченная сделка также не «гниёт» (deal-rotting/-danger подавляются).
- **Единая шкала полос** в обоих режимах: просрочка — приглушённая роза
  #c45a68 (как высокий приоритет/тосты-ошибки), «скоро дедлайн» — тан #d4a373
  (как «Ждет оплаты»). Glass-полоски в liquid-glass.css переведены с ярких
  `--success/--danger/--warning` на те же значения — цвет «Сборки» больше не
  прыгает при включении стекла. Плашка «Просрочено»/сводка — #b04a58
  (одна красная семья, контраст белого 5.3:1).
- **Ховер пунктов меню (glass)**: плоская заливка заменена на «мыльное»
  стекло — диагональный блик, светлая кромка сверху, тонкое кольцо, мягкая
  тень. Проверено живым :hover в браузере (вычисленный стиль).
- **Модалка сделки (glass) — одно стекло**: `.modal-split-left/right` выведены
  из списка стеклянных поверхностей (собственные фон+граница+тень давали
  серую шапку и шов между половинами), альфа поверхности 0.80 → 0.93
  (единый тон при любом фоне за модалкой),
  разделитель панелей и футер действий — полупрозрачные. Фолбэк-списки
  (`@supports not backdrop-filter`, `prefers-reduced-transparency`) обновлены.
- Штампы проставлены `tools/stamp_assets.py`; проверено скриншотами
  (свет/тьма, glass on) через fixture с реальными style.css + liquid-glass.css.

## Обновление 2026-09-05: режим оформления Liquid Glass

- Новый опциональный режим «стекло» поверх обеих тем: `site/css/liquid-glass.css`
  включается атрибутом `data-glass="on"` на `<html>`, хранится в
  `localStorage.crm_glass`. Тумблер-капля `#glass-toggle` в `.sidebar-controls`
  всех 5 страниц, обработчик `initGlass` в `features.js`; ранняя инициализация
  в `<head>` (без вспышки старого вида). Вечером 2026-09-05 (фидбек: «в
  инкогнито старое») режим включён ПО УМОЛЧАНИЮ — проверка
  `crm_glass !== 'off'`; явное выключение каплей сохраняется и уважается.
- Состав слоя: mesh-фон body (цветная подложка, без него блюр серый), сайдбар,
  модалки и оверлей, тосты, дропдауны, панель уведомлений — настоящий
  `backdrop-filter`; карточки канбана и поля ввода — только полупрозрачность
  (сотни элементов, blur уронил бы FPS скролла доски). Статусные тинты
  («Сборка», old-card) переобъявлены полупрозрачными; deal-rotting/-danger
  не тронуты (красятся через background-image).
- Фолбэки: `@supports not (backdrop-filter)` → плотные поверхности;
  `prefers-reduced-transparency: reduce` → стекло off (a11y).
- Специфичность селекторов `html[data-glass="on"] …` (0,3,1) перекрывает
  `!important`-фоны тёмной темы из style.css (0,2,1) — правки style.css не нужны.
- Попутно починен баг раннего скрипта всех страниц: `document.body.classList`
  в `<head>` падал (body ещё не существует) — «плотный режим» не переживал
  перезагрузку. Теперь класс навешивается на DOMContentLoaded.
- Проверено вживую в dev (временный пользователь `ui_glass_tmp`, удалён):
  вход со стеклом, канбан свет/тьма вкл-выкл тумблером, модалка «Новая сделка»
  (блюр фона + блик), мобильная ширина 390px без поломок, возврат к исходному
  виду при выключении. Деплой на прод — `tools/deploy.sh front`.
- Правка тёмной темы (фидбек владельца, 2026-09-05): «менее ярко и градиентно» —
  mesh-альфы 0.26/0.13/0.14/0.11 → 0.09/0.05/0.05/0.04, база почти плоская;
  saturate тёмных стеклянных поверхностей 160–180% → 115–130%; активный пункт
  меню без цветного свечения. Светлая тема не тронута.
- Правки по фидбеку владельца (вечер 2026-09-05):
  - **Чекбоксы в светлой теме выглядели непроставленными**: стеклянный фон
    input перекрывал кастомную отрисовку `:checked` (белая галочка на белом).
    Чинится исключением `input[type=checkbox/radio]` из стеклянного правила;
    проверено: unchecked `rgb(255,255,255)`, checked `rgb(79,70,229)`.
  - **Карточки канбана** плотнее (свет 0.93 / тьма 0.94) — на просвет читались
    «пустой обводкой»; зелёный фон «Сборки» убран (не сочетался с янтарными
    бейджами), статус — зелёная полоса слева; цвета статусных полос
    (сборка/просрочено/скоро) переобъявлены — стеклянный border-color их глушил.
  - **Одна кнопка темы в сайдбаре**: кнопка-капля убрана (путалась со
    стеклом), переключатель «Liquid Glass» — чекбокс в Настройки → Профиль;
    стеклянный режим включён по умолчанию, выключение сохраняется.
- Задеплоено на прод и проверено (`tools/deploy.sh front`, 9 файлов):
  http://87-232-64-12.nip.io отдаёт index с хешем liquid-glass.css
  `eb8a68a237`, новую альфу 0.09, тумблер `glass-toggle` и `initGlass`.
  Голый IP 87.232.64.12 ведёт на парковку хостера (start.hoster.by) —
  сайт отвечает только по hostname (пре-существующее поведение).
- Нюанс dev-окружения: frontend-контейнер копирует `site/` в образ при
  СБОРКЕ — после правок фронтенда пересобирать
  (`docker compose up -d --build frontend`), простого рестарта мало.
  Свёрнутый сайдбар — сохранённое состояние браузера
  (`crm_sidebar_collapsed`, в т.ч. legacy-значение `'true'`), не баг.

## Обновление 2026-09-04: «Списание», отказоустойчивость, аудит

- Вкладка «Списание» переработана: компактные карточки в стиле канбана
  (сумма + «к списанию» в строку, дата + № ТН в строку, янтарная кнопка
  вместо бейджа «не выписана»), месячные группы «Списано» с «Показать ещё»,
  итоги складов в заголовках. Ширина плиток синхронизирована с канбаном
  через `--kanban-card-w` (kanban.js пишет фактическую ширину).
- Шапка «Финансов» в одну строку (заголовок + табы + тулбар вкладки),
  колокольчик в строке заголовка.
- Скроллбары убраны со всех досок — листание колесом; продолжение подсказывает
  затухание внизу колонок (`updateScrollHints`). Высота колонок списания
  `calc(100vh - 230px)` — на 100% колонки влезают целиком.
- Чистка сервера: веб-корень без бэкапов/`.bak`/миграций (~296 МБ);
  архив — `/var/www/h212005/data/archive/cmr-webroot-cleanup-20260904`.
  `backup.sh` (перед деплоем) пишет в `~/data/crm_backups_predeploy`.
- Аудит (безопасность/надёжность/UI) — `docs/audits/AUDIT-2026-09-04.md`,
  план исправлений — `docs/audits/FIX-PLAN-2026-09-04.md`. Исправлено сразу:
  лимит логина 10/мин; watchdog поднимает PM2 после ребута сервера
  (`pm2 resurrect`, коммит `72b4068`); pm2-logrotate (10 МБ, retain 7);
  офсайт-копии бэкапов на Mac — `tools/pull_backups.sh` (тест: integrity ok).
- Открытый риск №1: сайт по HTTP без TLS (домен ждёт A-запись; Let's Encrypt
  готов скриптом `scripts/setup_ssl.sh`).
- Реструктуризация репо: фронт только в `site/`, бэкенд только в
  `server_snapshot/` (дубли фронта удалены, −17.9k строк), дневники в `docs/`,
  карта структуры в README.

## Обновление 2026-08-26: UI-аудит и исправления

Прод (`87-232-64-12.nip.io`) был новее git HEAD на 5 дней — правки
«UI Audit 25–26.08» существовали только на сервере. Сначала синхронизированы
в репо (коммит `a86cc66`), затем по аудиту интерфейса:

- 20 пунктов UI-аудита исправлены: критичные (обрезка сумм KPI, колонка
  «Название» 72px, янтарные статусы оплат в тёмной теме, sticky-панель,
  перекрывавшая дропзону), консистентность контролов (все 40px), визуал
  по референсу doctorstroy.ru (кнопки-пилюли, заголовки Manrope 800,
  календарь без переполнения), полировка (единый `:root`, плейсхолдеры
  4.8:1, зебра, тихие заметки).
- Модалка карточки: малые экраны (одна лента, липкая шапка/кнопка,
  крестик в углу, сетка `minmax(0,1fr)` против распирания) и высота
  88vh на десктопе (схлопывалась до 340px).
- `requirements.txt`: добавлены `python-multipart` и пин `bcrypt==4.0.1` —
  без них свежая сборка бэкенда не стартует (форма логина) и вход даёт 500.
- Скриншоты до/после: `gui-test-screenshots/ui-audit-2026-08-26/`.
- Аудит паролей: bcrypt `$2b$12` + уникальная соль, секреты в git не
  попадали. Производительность: ~440 KB gzip / ~37 запросов первой
  загрузки, TTFB 16–30 ms; резерв роста — дельта-синхронизация канбана
  (отложено). Подробности — план из сессии 2026-08-26.
- Админка (вторая сессия 2026-08-26, коммит `c7b0d08`): починена причина
  «не видны менеджеры» — обёртка apiFetch в admin.html уходила в бесконечную
  рекурсию (хойстинг затирал глобальную функцию), список всегда падал.
  Плюс: tenant-фильтр убран (компания одна), `PUT /auth/me/password` —
  самостоятельная смена пароля с отзывом старых токенов (claim `pv`;
  токены без `pv` валидны — деплой не разлогинивает), минимум пароля
  8 символов, `create_users.py` без фолбэк-пароля,
  `forwarded_allow_ips=127.0.0.1` (rate-limit логина больше не
  обходится подделкой XFF). Шрифты: preconnect вместо блокирующего
  @import, антиква Cormorant убрана из загрузки.
- Ограничения хостинга: сертификат самоподписанный (редирект HTTP→HTTPS
  отложен до боевого домена + Let's Encrypt; домен cmr-svetvdome.online
  сейчас не резолвится), конфиг nginx недоступен пользователю —
  `/css/style.css` кэшируется только через ETag/304, `/js/*` уже immutable.

## Как проверить самому

```bash
docker compose up -d
docker ps                          # оба контейнера должны быть (healthy)
curl -i http://localhost/health    # 200 text/plain
curl -i http://localhost/kanban/cards   # 401 application/json (нужен токен) — НЕ HTML
```

Открыть приложение: http://localhost

## Что было сломано и что исправлено

| # | Проблема | Проявление | Исправление |
|---|----------|-----------|-------------|
| 1 | nginx проксировал `/api/`, которого нет | Все API-запросы возвращали HTML; логин 405 | regex-location с реальными префиксами роутеров (теперь в `nginx/snippets/`) |
| 2 | `python -m urllib.request` не исполняется | Backend «healthy» при мёртвом приложении | `Dockerfile.backend`: `python -c` с реальной проверкой |
| 3 | `localhost` → `::1`, nginx слушал IPv4 | Frontend вечно `unhealthy` | `127.0.0.1` в healthcheck |
| 4 | Паттерны `.dockerignore` без префикса | JWT-ключ, БД клиентов и почтовые настройки попадали в образ | Добавлены пути с `server_snapshot/` |
| 5 | Том монтировался в `/app` | Код замораживался: пересборка образа ни на что не влияла | Том на `/app/data`, введена `CRM_DATA_DIR` |
| 6 | `cpus: '512m'` | `docker-compose.production.yml` не парсился вообще | `cpus`/`mem_limit` верхнего уровня |
| 7 | `SECRET_KEY` vs `CRM_SECRET_KEY` | Переменная игнорировалась, ротация ключей не работала | Приведено к именам, которые читает код |
| 8 | `.env.example` под `.gitignore` | Шаблон не попадал в репозиторий | Исключение `!.env.example` |
| 9 | Контейнеры от root | — | `USER app` (uid 10001) |

## Найдено попутно (не связано с Docker)

| # | Проблема | Проявление | Исправление |
|---|----------|-----------|-------------|
| 10 | `bcrypt 5.0.0` несовместим с `passlib 1.7.4` | **Любой вход падал с 500** | `bcrypt==4.0.1` в requirements |
| 11 | `cards.position` есть в модели, нет в БД | `/kanban/cards` → 500 | `migrate_add_card_position.py` (идемпотентная) |
| 12 | `get_scoped_session` в `Depends()` напрямую | Все 6 эндпоинтов `/tags` → 422 | Обёртка `_scoped_db` в `tags_router` |
| 13 | `test_routes.py` глотал исключения | CI проходил на сломанном импорте | Возвращает ненулевой код, проверяет префиксы |
| 14 | CI тянул образ по тегу, который есть только в main | Job `test` падал на PR и develop | Убран `services:`, добавлена проверка утечки секретов |
| 15–23 | HTTPS и бэкапы: кэш upstream → 502, потеря заголовков безопасности на HTML, healthcheck в TLS-режиме и др. | Разное | Подробно в [`docs/RESULT-2026-08-08.md`](docs/RESULT-2026-08-08.md) |
| 24 | `email_settings.json` исключён из образа, но код искал его рядом с собой | `POST /email-parser/sync` → 400 «Настройки почты не заполнены» | Путь резолвится через `CRM_DATA_DIR` |
| 25 | `main.py` создавал мёртвый `/app/tenants` | Путал при отладке | Удалено |
| 26–28 | Сессия арендатора закрывалась до сериализации ответа | **Все операции записи у арендаторов давали 500** (2556 в боевом логе), хотя запись в БД проходила | Отложенное закрытие в `database.py`, `get_db` стала `async`. [Подробно](docs/RESULT-2026-08-08.md) |

## Проверено

```
Оба контейнера healthy          (docker ps)
Healthcheck ловит сбой          мёртвый порт → exit 1, живой → exit 0
Логин через nginx               токен 240 символов
/kanban/cards                   HTTP 200, 197 записей
/clients                        HTTP 200, 14 записей
/suppliers                      HTTP 200, 19 записей
/tags                           HTTP 200, 3 записи
/activity                       HTTP 200, 50 записей
POST /email-parser/sync         HTTP 200, success: true   (было 400)
Секретов в образе               нет ни одного из 5 проверенных путей
Пользователь в контейнере       uid=10001(app)
Код обновляется при пересборке  build 20260716 → 20260809
Данные переживают down/up       497 карточек, 6 пользователей
Оба compose-файла               docker compose config → OK
```

## Реальные метрики

| Показатель | Заявлялось ранее | Фактически |
|---|---|---|
| Backend образ | ~150 MB | ~320 MB |
| Frontend образ | ~50 MB | ~93 MB |
| Размер БД | ~1.7 GB | 1.7 MB |
| Карточек в БД | — | 497 |

## Данные арендатора 3

В `server_snapshot/tenants/` лежал `crm_3.db-wal` без основного файла — сам
`crm_3.db` отсутствовал. Содержимое WAL восстановлено вручную
(309 из 341 страницы) и сохранено в `recovery/`.

**Потери данных нет:** восстановленное — более старый снимок тех же записей,
которые уже есть в `crm_app.db` (391 из 392 заголовков совпадают, основная база
новее и содержит на 19 карточек больше). Каталог `recovery/` оставлен как
страховка и не попадает в git.

Отдельный вопрос к владельцу: у всех 497 карточек `tenant_id = NULL`, то есть
многотенантность фактически не используется — все работают в общей базе.

## HTTPS и бэкапы — готовы

Проверено запуском prod-конфигурации локально:

```
https /health         200          HTTP / -> 301 https
https /kanban/cards   200 (логин по HTTPS работает)
localhost:8000        недоступен снаружи (как и должно быть в проде)
заголовки безопасности  6 из 6, без дублей
HSTS на HTTP          отсутствует (как и положено)
бэкап восстанавливается  integrity ok, 497 карточек
```

Команды:

```bash
bash scripts/setup_ssl.sh your-domain.com admin@your-domain.com   # сертификат
bash scripts/generate_keys.sh > .env.production                   # ключи
bash scripts/backup_db.sh                                         # бэкап
```

## Что осталось

- [ ] На сервере: выпустить боевой сертификат Let's Encrypt и указать
      `server_name` в `nginx/https.conf` (сейчас проверено на самоподписанном)
- [ ] Поставить бэкап в cron на сервере (команда печатается после запуска
      `scripts/backup_db.sh`) и хранить копии вне сервера
- [ ] Перенести боевые данные в прод-том: у dev и prod разные volumes,
      `crm-data-prod` стартует пустым
- [ ] Роль `documents` (`Dokumenty`, `tenant_id = NULL`) видит общую базу целиком —
      проверить, соответствует ли это замыслу
- [ ] Пересмотреть Phase 2 ([`docs/phases/PHASE2_IDOR_AUDIT.md`](docs/phases/PHASE2_IDOR_AUDIT.md)): предложенный там патч
      (`if card.tenant_id != current_user.tenant_id: raise 403`) при `tenant_id = NULL`
      у всех карточек даст 403 на каждой. Применять нельзя.

## Где остальные документы

Документация собрана в `docs/` — см. [`docs/README.md`](docs/README.md).
Подробный отчёт о работах: [`docs/RESULT-2026-08-08.md`](docs/RESULT-2026-08-08.md).
В корне намеренно остаются только `README.md` и этот файл.

## Важно про Phase 3

Пункт «очистить `.secret_key` из истории git» выполнять не нужно:

```
git log --all -- '*.secret_key'   →  0 коммитов
```

Файл никогда не попадал в репозиторий. Реальная утечка была в другом — ключ
уезжал в Docker-образ (пункт 4), и она закрыта.

## Обновление 2026-08-29: UI-реворк, целостность БД, безопасность, инфраструктура

7 коммитов (`9afbc7b`…`102b15e`), всё задеплоено на прод и проверено живьём.

**Frontend.** Унифицированы 4 таблицы (реестр оплат/документы/клиенты/поставщики:
заголовки выровнены по содержимому колонок, действия справа), раздел «Финансы» —
один заголовок вместо дублирующих h1+табы, тулбары панелей; ячейка «Оплата» —
бейдж + прогресс слева; канбан на мобильных — фильтры свёрнуты по умолчанию;
бейдж «не заполнена» на сделках без ответственного/суммы; задачи — починены
тумблер «ДоскаСписок» и двойное пустое состояние; админка — кнопки «Удалить»
(были обрезаны до красного кружка), шапка; мобильная шторка меню снова работает.
CSS-аудит (агент): 315 дублей селекторов, 467 конфликтов, 1247 !important,
~100 мёртвых классов; этап 1–2 консолидации выполнены (мёртвые семейства +
развязка .btn-show-old/.close-btn/.view-toggle-btn/.sidebar-toggle).

**Backend/БД.** `PRAGMA foreign_keys=ON` + synchronous=NORMAL (каскады реально
работают; перед включением вычищены сироты: локально ~986 записей); defensive-
отвязки детей в удалениях клиента/поставщика/пользователя/карточки; починены:
permanent delete (не было `import os`), спам уведомлениями по закрытым сделкам
(статус «Закрыто»), двойное шифрование пароля IMAP, мёртвые вебхуки (фоновый
поток); SSRF: проверка публичности хоста перед каждым запросом + без редиректов;
ретенция в sync-overdue (уведомления >30 дней, версии >180 дней, топ-20);
индексы card_attachments/card_checklists(card_id), owner_id=0 → NULL (212 карт).

**Инфраструктура.** БД/uploads/ключи вынесены из веб-корна в
`/var/www/h212005/data/crm_data` (PM2 env CRM_DATA_DIR/CRM_UPLOADS_DIR/
CRM_SECRET_KEY; ecosystem-конфига нет — env через pm2 dump). Пути в БД
переписаны на относительные. Прод-бэкенд слушает **20008** (не 8000 — там
чужой сервис). Бэкапы v2: integrity_check, uploads ежедневно, секреты 600,
скрипт `scripts/backup_crm_prod.sh` (крон 03:00, вызов через `bash` —
права +x раньше терялись и бэкапы молча умирали — починено).

**Плагины/инструменты.** ZCode-плагин mimosa отключён в
`~/.zcode/cli/config.json` (git-gate блокировал коммиты ложными
path-traversal на `open(переменная)`; после pathlib-рефакторинга остался
1 ложный на `tools/css_cascade.py`). `tools/css_cascade.py` в git
НЕ включён — лежит только локально. Скриншот-верификация через IAB в сессии
была нестабильна — факты проверялись computed-стилями и API.

## Обновление 2026-08-30: роли на роутерах, LIMIT/N+1, миграция платёжных колонок

Коммиты `61ec556`, `3b7040d`. Всё задеплоено на прод (tools/deploy.sh) и
проверено живьём.

**Роли.** Системные поверхности закрыты за админом (admin + superadmin):
- `webhooks_router` — целиком `require_admin()`. Вебхук отправляет данные
  сделок на внешний URL: любой аутентифицированный (склад/документы) мог
  подписаться на `card.created` и сливать базу. Таблица webhooks пуста —
  пользы для остальных ролей в ней нет.
- `workflows_router` — целиком `require_admin()`: шаги воркфлоу умеют
  `http_request` и `code`. Таблица workflows пуста.
- `custom_objects_router` — создание/удаление типов и полей (меняют модель
  данных) только для админа; чтение и CRUD записей открыты всем (это рабочие
  данные). Таблицы пусты.
- Фронтенд (`site/`): вкладки «Кастомные объекты»/«Воркфлоу»/«Webhooks» в
  Настройках помечены `data-admin-only` и скрываются для не-админов
  (`applyRoleToSettingsTabs` в js/auth.js, дефолт не-админа — «Почта»).
  Хеши кеш-бастинга проставлены `tools/stamp_assets.py`.
- Бизнесовые роутеры (канбан, оплаты, списания, клиенты, поставщики, задачи,
  теги, активность, уведомления, сохранённые виды, группы списаний, почта)
  сознательно оставлены всем аутентифицированным: фронтенд показывает эти
  разделы всем, более тонкая матрица (кто может удалять клиентов и т.п.) —
  отдельное решение владельца.
- Проверено на одноразовом контейнере токенами всех ролей: manager/warehouse
  → 403 на /webhooks/, /webhooks/events, /workflows/, POST/DELETE
  /custom/objects; superadmin/admin → 200; GET /custom/objects и /tags у
  менеджера → 200 (бизнес не задет).

**LIMIT/N+1.**
- Задачи: `selectinload(checklist_items)` + словарь имён пользователей одним
  запросом (было до 500 ленивых SELECT на список).
- Поставщик/закупки: `selectinload(card)` (было по SELECT на пункт чек-листа).
- Реестр оплат и документы: `REGISTRY_HARD_LIMIT=5000` — не пагинация, а
  предохранитель от аномального роста (сейчас 188 транзакций), заголовки
  X-Total-Count/X-Truncated; группировка по сделкам не задета (98 групп).

**Миграция платёжных колонок (найдено при проверке).** В dev-томе tenant-база
crm_3.db не имела колонок `paid_amount/payment_status/payment_due_date` —
kanban для tenant-пользователей падал 500 (на проде колонки есть, добавлялись
там вручную без миграции). Новая идемпотентная
`migrate_add_payment_columns.py` починила dev-том; на проде отчиталась
«already present» для обеих баз. Запуск на сервере: с
`CRM_DATA_DIR=/var/www/h212005/data/crm_data`.

**Важно про каталоги.** Фронтенд внутри `server_snapshot/` — устаревший
одноразовый бэкап сервера, актуальный — в `site/` (см.
`server_snapshot/README.md`). Правки фронтенда вносить только в `site/`.

**Прод-верификация 2026-08-30:** health 200; index.html/auth.js отдаются с
новыми хешами; API без токена 401; логи PM2 без ошибок после рестарта.

**Осталось (приоритет):**
- [ ] DNS: A-запись cmr-svetvdome.online → 87.232.64.12 (нужен доступ
      регистратора), затем `bash scripts/setup_ssl.sh cmr-svetvdome.online …`
- [ ] Offsite-бэкапы (rclone/restic на внешний ресурс — нужен токен)
- [x] Роли на роутерах (закрыто 2026-08-30: webhooks/workflows/custom-типы →
      admin; бизнес-роутеры сознательно открыты всем аутентифицированным)
- [x] LIMIT/N+1 (закрыто 2026-08-30: selectinload в задачах и закупках
      поставщика, предохранитель LIMIT=5000 в реестре/документах)
- [ ] CSS этап 3: dark-wildcards (6624–6699 старой нумерации) → переменные,
      остатки дублей (.kanban-card ×8, .settings-tab ×4), брейкпоинты 9 → 3
- [ ] Выпиливание тенант-слоя (~45 ветвлений, план у агента-аудитора)
- [ ] Деньги: float → целые копейки (миграция дешёвая, 188 транзакций)
- [ ] Временный юзер `ui_audit_tmp` создавался/удалялся многократно —
      в обеих БД по 6 штатных пользователей (проверено)

## Обновление 2026-08-30 (вечер): спринт 0 плана доработок — сохранение и валидация

План доработок по аудиту скопирован в [`docs/PLAN_CRM_SPRINTS.md`](docs/PLAN_CRM_SPRINTS.md).
К этой сессии в спринте 0 уже были закрыты предыдущими работами: 0.3 (разделение
«оплата клиента» / «закупка у поставщиков» — блок оплаты, статусы, чек-лист),
0.4 (матрица «статус → кнопки этапов», `applyStageButtons`), 0.5 (подтверждения
удалений через `confirmDialog` — вложения, чек-лист, накладные, комментарии,
клиенты, поставщики). 0.6 (HTTPS) заблокирован на DNS-запись. В этой сессии
закрыты 0.1 и 0.2, всё в `site/` (фронт), проверено вживую в dev-окружении.

**0.1 Индикатор сохранения + защита закрытия** (`site/js/card_modal.js`):
- `CardSaveTracker` и индикатор `#card-save-indicator` в шапке модалки:
  «Сохранение… / Сохранено ЧЧ:ММ / Ошибка сохранения — Повторить». Через него
  проходят название, сумма, дата окончания, магазин, клиент — раньше PATCH
  уходил молча, а у магазина вообще не было обработки ошибок (unhandled
  rejection). Хелпер `saveCardField(card, patch, onDone)`.
- При сбое сети в индикаторе остаётся кнопка «Повторить». Закрытие карточки
  (крестик, подложка, Esc, внутренние `closeModal`) при неперезаписанной
  ошибке перехватывается capture-слушателем, объявленным раньше ui-polish
  (card_modal.js грузится раньше — порядок в index.html важен), и показывает
  диалог «Есть несохранённые изменения»: Сохранить (повтор, при неудаче
  остаёмся) / Не сохранять / Отмена. Пока PATCH летит — закрытие ждёт до 3 с.
- Esc во включённом коде не закрывал модалку вовсе; теперь закрывает через ту
  же защиту, а Esc в поле суммы/чек-листа — только откат правки поля
  (`stopPropagation`, иначе глобальный Esc-обработчик ui-polish закрывал
  модалку поверх отката).
- Fix: `.card-save-indicator[hidden]{display:none!important}` — класс display
  перебивал атрибут hidden, индикатор не исчезал по таймеру.

**0.2 Валидация суммы сделки**:
- Поле суммы: `type=number` → `text inputmode=decimal` с разделителями разрядов
  (в фокусе — сырое число, как у сумм чек-листа), фильтр ввода, Enter —
  сохранить, Esc — отменить. «abc» больше не превращается молча в 0:
  значение откатывается, тост «Введите сумму числом», красная подсветка.
- Нулевая сумма блокирует переход дальше «Новый запрос» (критерий плана):
  в модалке «Передать в сборку»/«В списание» — тост + подсветка + фокус в поле;
  на доске drag&drop откатывается в исходную колонку, карточка не уезжает.
  `refreshCardOnBoard` теперь ещё и обновляет кэш `_allCards` (проверка суммы
  при drop читает его).
- Тултипы чекбоксов чек-листа уточнены по плану 0.3: «Оплачен поставщику» /
  «Пришло на склад».

**Проверено живьём** (dev docker, временный пользователь `ui_test_tmp`, удалён
после теста): сохранение суммы с индикатором во всех трёх состояниях и скрытие
по таймеру; откат «abc»; запись на сервере (5176.45 → 6000 → 6001 по API);
сбой сети патчем fetch → индикатор ошибки → диалог закрытия → «Отмена» →
восстановление сети → «Сохранить» → карточка закрылась, данные на сервере;
Esc в поле/вне поля; drag нулевой карточки заблокирован, с ненулевой —
работает в обе стороны; смена магазина через дропдаун; ошибок консоли нет.
Скриншоты через IAB в этой сессии снова нестабильны — верификация по DOM,
computed-стилям и API. В dev-базе у карточки 15 остались тестовые сумма 6001
и магазин «Матусевича» — вреда нет, данные тестовые.

## Обновление 2026-08-30 (ночь): спринт 1 — история изменений, заголовок, дедлайны

Продолжение плана ([`docs/PLAN_CRM_SPRINTS.md`](docs/PLAN_CRM_SPRINTS.md)).
К моменту старта в спринте 1 уже были закрыты: индикация просрочки на доске
(бар «Просрочено» + классы card-overdue/card-due-soon), фильтр «Просроченные»,
скрытие невалидных кнопок этапов (работа 0.4). В этой сессии закрыты 1.2,
1.4 и остаток 1.7. Пункт 1.3 (сравнение суммы чек-листа с суммой сделки)
реализовывать не нужно: решением от 06.08.2026 чек-лист объявлен закупкой у
поставщиков, суммы сознательно не сравниваются со суммой сделки (см. комментарий
в renderChecklistSummary).

**1.2 История изменений** — бэкенд + фронт:
- `card_details_router`: PATCH карточки теперь пишет в ленту одну запись
  «Изменение» со всеми поменявшимися полями (название, сумма, дата окончания,
  магазин, клиент — «Сумма: 6 001,00 → 7 000,00 BYN», клиент по имени);
  платёжный PATCH пишет «Оплата: Не оплачен → Оплачен; Оплачено: …», включая
  автостатус. `kanban_router.update_card_status` пишет «Статус: A → B».
  Раньше логировались только создание, импорт почты, накладные и комментарии —
  изменения полей терялись бесследно.
- `/activity` возвращает `user_name` (схема + один запрос имён); фронт рисует
  имя и инициалы аватара из реального имени (раньше инициалы собирались из
  текста действия — фикция).
- Лента в карточке: заголовок «История», фильтр «Всё / Комментарии / События»
  со счётчиками (выбор помнится в localStorage), limit 10 → 50, пустые
  состояния («Комментариев пока нет»). Комментарии по-прежнему редактируемые,
  события — нет (право правки на сервере уже было ограничено action='Комментарий').

**1.4 Заголовок сделки** — режим просмотра по умолчанию (текст + карандаш),
правка по клику: Enter/blur — сохранить (через трекер), Esc — отмена,
stopPropagation не даёт глобальному Esc закрыть модалку поверх правки.
Случайное переименование курсором исключено.

**1.7 Дедлайны (остаток)** — дата окончания обязательна при выходе из
«Нового запроса»: кнопки этапов в модалке и drag&drop на доске требуют
заполненную дату (аналогично сумме из 0.2).

**Деплой.** Фронт + бэкенд (4 файла, PM2 рестарт) выкачаны на прод и проверены:
health 200, бэкенд отдаёт новый card_modal.js, в err-логе только startup-строки.

**Проверено живьём в dev** (пользователь `ui_test_tmp`, удалён): события
«Изменение/Статус/Оплата» в API и в ленте с именем автора; счётчики фильтра
(«Всё (7) Комментарии (1) События (6)»); переключение фильтров и пустые
состояния; комментарий виден в «Комментариях» с кнопками правки; карандаш
заголовка — Esc-отмена, Enter-сохранение, обновление доски и ленты; drag без
даты заблокирован («Укажите дату окончания»), с датой — работает; ошибок
консоли нет. В dev-базе у карточки 15 остались тестовые данные (сумма 7000,
статус «В работе», дата 15.09.2026, переименование, один комментарий).

## Обновление 2026-08-30 (мелочи): план 4.3, 4.1, 4.5 — короткое

- Enter отправляет заметку в карточке, Shift+Enter — перенос строки (4.3);
  поле заметки теперь пустое при открытии и очищается после отправки —
  работает как сообщение, вся история видна в ленте «История» (поле раньше
  дублировало последнее описание сделки).
- Заголовок воронки отражает режим: «Воронка продаж — Доска / — Список» (4.1).
- «Выполнены» → «Выполненные» в колонке задач (4.5).

Проверено в dev (заголовок в обоих режимах, комментарий по Enter очищает поле
и появляется в ленте), фронт выкачан на прод.
