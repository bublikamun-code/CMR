# Бриф сессии — 2026-09-23 (после волны; заменяет бриф начала дня)

Промт главной сессии кампании v2. Самодостаточен: поднимается чтением журнала,
контекста прошлых сессий не требует. Всё состояние — на диске.

---

Ты — главная сессия кампании v2 CRM «Свет в доме» (`/Users/yaroslav/Documents/crm-svetvdome`,
ветка `main`). Контекста прошлых сессий у тебя нет; всё состояние — на диске.
Работай по `AGENTS.md` и памяти проекта.

## ШАГ 0 — подними состояние, не читая ничего сверх списка

1. `docs/audits/V2-WORKPLAN-2026-09-22.md` — журнал и реестр дефектов, источник истины. Читай целиком.
2. `git log --oneline -n 12 && git status --short`. Ожидание: HEAD `faffd34`, дерево чистое кроме
   untracked `docs/SESSION-BRIEF-2026-09-22.md` (служебный, не коммитится).
3. `docs/audits/V2-ITEM12-CLIENT-SPEC-2026-09-23.md` — только когда дойдёшь до пункта 12.

## Закрыто и проверено — НЕ перепроверять

Блок 0, пункты 1–11, 14, 17; пункт 15 частично; 16 — серверная половина; 18 частично;
дефекты доски 1–9. `scripts/check.sh` exit 0 (pytest 878); перепись на стенде 8127 —
0 pageerror / 0 аномалий (8 консоль-записей — известные артефакты, удвоенные флагом `--dark`);
функциональный аудит 25/26 (единственный провал средовой — пустые настройки ящика в копии БД);
`v2_drag_perf.py` — все критерии пункта 4; скриншоты поиска в обеих темах лежат в
`gui-test-screenshots/` (каталог под `.gitignore:54`, коммитить кадры не нужно).

## ЗАДАЧА — очередь 13 → 12 → 16 → S

Строго в этом порядке: он снимает конфликт, при котором «Архив» из пункта 13 строится на том же
массиве карточек, который пункт 16 переводит на страницы.

**Пункт 13 — «Паритет-пакет 1: виды и фильтры доски»** (формулировка и критерий — в журнале;
референс паритета — замороженный legacy `site/js/kanban.js`, только чтение):

- режимы «Список» и «Архив», фильтры по приоритету и типу оплаты, кнопка «Сбросить»;
- сохранённые виды в localStorage по спецификации из журнала (серверных роутов для них нет):
  ключ `v2.savedViews.board.<userId>` (`userId` из `V2Api.userId()`), состав
  `{id,name,q,store,statuses[],assignee,mode}`; применение — выставить состояние и записать адрес
  в формате, который доска уже парсит (`#board?q=…&store=…`); событие `kb:saved-view-applied`;
  тихая потеря несуществующих фильтров по `kb:dictionaries-changed`; `try/catch` на недоступное
  хранилище (`file://`);
- «Архив» = закрытые сделки; помни, что сервер НЕ возвращает карточки из «Закрыто» при отмене
  списания (`services/payments.py:60-141`) — сверь семантику с реестром дефектов.

## ПРАВИЛА ИСПОЛНЕНИЯ (выжимка; полное — в AGENTS.md)

- фронт v2 правится ТОЛЬКО в `server_snapshot/tools/mockups/` и шаблонах `v2-{api,boot}-template.js`;
  `site-v2/` — артефакт, руками не править; пересборка ОДНА и одним человеком в конце пакета:
  `cd server_snapshot && python3 tools/build_site_v2.py` (она же пересчитывает штампы);
- правки одного файла — один держатель; `board.js` и `prototype.html` — хабы, двум исполнителям
  одновременно не давать;
- CSP: инлайн-атрибуты событий и `eval` запрещены; комментарии и тексты UI — по-русски;
- не коммить чужое, не деплой, не пуш без слова владельца; миграции к живым БД не применять
  (0013 заблокирована до клиентской половины пункта 12; 0014 занята; ротация журналов получит 0015);
- исполнение делегируй builder'ам из `.qwen/agents/` с самодостаточным брифом, разведку — scout,
  гейты — verifier (все на `qwen3.8-flash`); в основной сессии держи только решения, не вывод прогонов.

## ВЕРИФИКАЦИЯ после каждого пакета

- `node --check` на правленых mockups; fin-скрипт извлекать буквальным якорем
  `build_site_v2.py:100` и проверять `node --check` (якорь нельзя ломать — упадёт `assert` сборщика);
- после пересборки: `bash scripts/check.sh` (ожидание exit 0, pytest 878+);
- свой стенд, порт 8127. Рецепт: `cp /tmp/v2-audit-prod/crm_app.db` (+ `-wal`) и `uploads/` в свой
  каталог в `/tmp`; накатить ТОЛЬКО 0014 вручную (importlib-загрузка миграции, `up(cur)`, строка в
  `schema_migrations`; `migrate.py apply` НЕЛЬЗЯ — накатит заблокированную 0013); создать суперадмина
  `v2_audit` через `auth.pwd_context.hash`; запуск `CRM_DATA_DIR=… CRM_UPLOADS_DIR=…
  ./.venv/bin/python -m uvicorn main:app --port 8127`; учётки копии БД: `audit_admin` /
  `audit_manager`, пароль `Audit-2026!`;
- перепись: `CRM_USER=… CRM_PASS=… python3 tools/v2_error_census.py --base
  http://127.0.0.1:8127/v2/ --dark` (цель 0 pageerror / 0 аномалий; 8 консоль-записей — норма);
- ⚠ `tools/v2_functional_audit.py` НЕ разбирает аргументы: запуск с `--help` уходит на ПРОД.
  Запускать только с явным `V2_BASE=http://127.0.0.1:8127/v2/ V2_USER=v2_audit V2_PASS=V2Audit2026!`;
- один браузер за раз (ноут 16 ГБ): census / аудит / drag-перф параллельно не гонять.

## ЗАБЛОКИРОВАНО НА ВЛАДЕЛЬЦЕ — предлагать, не делать

Деплой бэка (прод до сих пор уязвим к P0: отмена групповой ТН удаляет постороннюю транзакцию,
фикс в main не выкатан); деплой v2 (прод на штампе `7a8311f788`); пуш (main впереди origin примерно
на 60 коммитов); применение 0013/0015; проверка на проде `crontab -l` (строка `watchdog_crm.sh`)
и наличие `.cron_token`. Порядок деплоя бэка: `CRM_DATA_DIR=… python3 migrate.py` ДО рестарта PM2,
иначе каждый запрос падает в 500 (`no such column: users.is_active`).

## ДОЛГ ДЕРЖАТЕЛЯМ (полный список — в журнале; не потерять)

Общий `window.KBGoto` вместо трёх копий приёма «клик по рейке + фокус в rAF»; ответственный в
карточке «Клиент 360»; капсула поиска в финансах (дефект D2 там не закрыт); заявка к бэку
`GET /kanban/cards?debt=true&sort=debt_desc`; мёртвый `procurementFields()` в `board.js`; отсутствие
отката значения поля в `mutate('card')`; отключённого пользователя можно назначить ответственным;
форма вариантов списка хранит «метка = значение».

## ФОРМАТ

Каждый логичный шаг — отдельный коммит с развёрнутым русским сообщением (что и зачем, что
проверено); закрытие пунктов и новые дефекты фиксировать в журнале `V2-WORKPLAN` по мере
готовности; контекст не раздувать — прогоны и чтение выносить в субагентов.

Начни с пункта 13: прочитай его формулировку и критерий в журнале и legacy-референс, затем
декомпозируй и запусти исполнителей.
