# Бриф сессии — 2026-09-23

Точка входа для новой сессии. Всё ниже проверено командами 22–23.09, не по памяти.
Первым делом прочитать: этот файл → `docs/audits/V2-WORKPLAN-2026-09-22.md` (план и
журнал) → свой пункт плана. Пересказывать контекст не нужно: он весь здесь и в плане.

## 0. Координаты и порядок работы

- Рабочее репо: `/Users/yaroslav/Documents/crm-svetvdome`, ветка `main` @ `1f4e0bf`,
  **ahead `origin/main` на 17 коммитов — пуш не выполнен** (решение владельца).
  **Деплой блока 0 выполнен 23.09**: `tools/deploy.sh v2` (только статика `site-v2`,
  без рестарта); прод на штампе `4d896ae8e3`, маркеры фиксов проверены curl'ом
  (`balanceLoading`, `kb-new-card`, `KBData.reload`). Бэкенд в блоке 0 не менялся,
  поэтому `deploy.sh back` не требовался.
- Порядок: **один пункт плана = одна сессия**. Закрыл пункт → прогнал его критерий
  готовности → атомарный коммит → строка в журнале плана. Упёрся в решение
  владельца — не гадать, остановить пункт и зафиксировать в журнале.
- **Пункт 1 закрыт 22.09**: `b361fd9` (CSP `blob:` в `img-src` + тест
  `tests/test_security_headers.py`) и `1f4e0bf` (отметка и журнал плана).
  Прод всё ещё на старом CSP — деплой бэка (`tools/deploy.sh back main.py`
  + рестарт PM2) ждёт слова владельца.
- **Первая задача новой сессии: пункт 2 плана** (производительность
  взаимодействий доски). Учтите: коммит `75a8682` (поле поставщика без
  нативного datalist) уже изменил `mockups/shell-v2-board.js` и пересобрал
  `site-v2` — читать актуальное дерево, а не строки из плана.
- Параллельные сессии: worktree `/private/tmp/crm-item4`, ветка `v2-item4-drag` —
  ведёт **пункт 4** (drag&drop). Пункт 4 не трогать. Конфликты в `site-v2` при
  мердже решать пересборкой, а не ручным слиянием. Пересборку `site-v2` в main
  делает один человек; если пункт требует пересборки, а чужая сессия активна —
  согласовать через владельца или дождаться.

## 1. Стенд (локальная копия прод-данных)

- Поднят: `http://127.0.0.1:8126/v2/` (uvicorn main:app, `CRM_DATA_DIR=/tmp/v2-audit-prod`,
  `CRM_UPLOADS_DIR=/tmp/v2-audit-prod/uploads`). Вход: `audit_admin` / `Audit-2026!`
  (роль admin) и `audit_manager` / тот же пароль (роль manager).
  Перезапущен 22.09 23:08 на коде с CSP `blob:` (пункт 1); лог этого прогона —
  `/tmp/v2-audit-prod/stand-item1.log`.
- Если упал, поднять заново:
  `cd server_snapshot && CRM_DATA_DIR=/tmp/v2-audit-prod CRM_UPLOADS_DIR=/tmp/v2-audit-prod/uploads ./.venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8126`
- **Порт 8125 не использовать и не убивать**: там чужой старый стенд на устаревших
  данных (PID меняется, владелец о нём знает).
- Данные: снимок прод-БД от 22.09 (1060 карточек / 383 активных). В копии живут
  тестовые сущности аудитов (карточки 1063 и 1065 «AUDIT v2 тест», клиент 30) —
  это не прод-данные, не «находить» в них дефекты.
- Прод — только чтение (`curl`, SELECT, `sqlite3 .backup`): `h212005@87.232.64.12`,
  ключ `~/.ssh/crm_svetvdome_deploy`, БД `/var/www/h212005/data/crm_data/crm_app.db`.

## 2. Закрыто в блоке 0 (22.09) — не перепроверять без причины

- **0.1** Петля `MutationObserver` в дровере клиента (`shell-v2-insights.js`) —
  `5eff3ef`. Подтверждено на стенде: клик по строке клиента → страница отвечает,
  ровно 2 запроса `/clients/{id}/balance`, баланс отрисован (ранее — вечная
  заморозка и ~30 запросов/с, в логе стенда было 20 544 одинаковых GET).
- **0.2** Перезагрузка данных без F5 (`applyInPlace`, `KBData.reload`, `kb:reloaded`),
  создание сделки с доски (`#kb-new-card`, форма-диалог, ветка `card-create`),
  идемпотентная загрузка модулей — `81a23f2`; инструмент аудита: шаг 6k +
  автоподтверждение `#kb-confirm` — `fca52d1`. Шаги 6a и 6k функционального
  аудита PASS.
- **0.3** Комбобокс поставщика в «Закупке»: на сборке `3c9d008` путь чист
  (ввод текста + клик по подсказке → страница отвечает, 1 `PATCH /checklists`,
  значение сохранено). A/B расследования доказало: зависание в Safari/Yandex/Chrome
  давала петля дровера клиента из 0.1 — старая сборка зависает детерминированно
  3/3 со шквалом `GET /clients/{id}/balance` (1961–7294 запросов за 10–27 с),
  новая не зажглась ни разу за 12+ прогонов; код комбобокса не дефектен, шквала
  записей в БД нет. **Правок не требуется; деплой выполнен 23.09** (`tools/deploy.sh
  v2`, прод на штампе `4d896ae8e3`).
  Логи и сценарии: `/tmp/supplier-freeze/` (repro4–repro12, result*.json,
  `insights-OLD.js` для подмены). Если после деплоя жалоба вернётся — начинать оттуда.
- **0.4–0.6** Пересборка `site-v2` закоммичена (`3c9d008`); верификация на стенде:
  `stamp_assets.py --check` — 0 расхождений; `node --check` по собранным js — OK;
  перепись ошибок — 0 DOM/дата-аномалий, 14 ошибок JS = только CSP-blob (уйдут
  пунктом 1); функциональный аудит — 26 проверок, 2 провала-артефакта стенда
  (пустые настройки почты; те же CSP-blob); восстановление из корзины — плитка и
  `KBData` обновляются без F5.

## 3. Решения владельца, которые ждут (не делать без слова)

1. Пуш 11 коммитов и деплой на прод (`tools/deploy.sh v2` + `back` при необходимости).
2. Применение миграции заполнения `deal_statuses` на проде (пункт 12) — после `.backup`.
3. Матрица ролей для V11 (пункт 17).
4. Отзыв GitHub PAT, регистрация домена и HTTPS, офсайт-копия бэкапов.

## 4. Артефакты и готовые инструменты

- `/tmp/v2-audit-prod/` — снимок БД и uploads (37 изображений); скрипты обходов
  `scripts/s1…s10`; скриншоты `shots/` (102 PNG) и `toolout/design/` (74 кадра
  матрицы ширина×тема); выводы `toolout/*.txt|json`; Performance-трейсы
  `traces/trace-{idle-15s,active-10s}.zip`; лог стенда `stand.log`; состояния
  логинов `state-admin.json`, `state-manager.json`.
- Пункт 1 (CSP `blob:`): перепись после фикса — `toolout/census-item1/`
  (census.json + скриншоты), проверка миниатюр — `scripts/s12_csp_blob_thumbs.py`
  (сводка `toolout/census-item1/csp-blob-thumbs.json`, кадры
  `incoming-thumbs-{light,dark}.png`). Тест заголовка —
  `server_snapshot/tests/test_security_headers.py`.
- `/tmp/verify-0-block.py` — быстрая регрессия блока 0 (клиенты + комбобокс
  поставщика): запускать после любой пересборки.
- Штатные проверки: `tools/v2_error_census.py --base http://127.0.0.1:8126/v2/`,
  `tools/v2_functional_audit.py` (переменные `V2_BASE`, `V2_USER`, `V2_PASS`),
  `tools/v2_design_matrix.py`; бэкенд: `cd server_snapshot && ./.venv/bin/python -m
  pytest -q` и то же с `CRM_TEST_SCHEMA=models`.
- Документация: `docs/audits/V2-PLAN-RECON-2026-09-22.md` (сверка плана с git),
  `docs/audits/V2-UI-AUDIT-2026-09-22.md` (визуал/юзабилити/производительность,
  дефекты F1–F10), `docs/audits/V2-WORKPLAN-2026-09-22.md` (план + журнал),
  `docs/audits/INDEX.md`.
- `docs/SESSION-BRIEF-2026-09-22.md` — служебный файл прошлой сессии, untracked,
  **не коммитить**.

## 5. Правила правок (выжимка AGENTS.md)

- Фронт v2 правится только в `server_snapshot/tools/mockups/` и шаблонах
  `server_snapshot/tools/v2-{api,boot}-template.js`; `site-v2/` руками не править;
  пересборка: `cd server_snapshot && python3 tools/build_site_v2.py &&
  python3 tools/stamp_assets.py --check`.
- CSP прода `script-src 'self' 'unsafe-inline'`: никаких `onclick`/eval, только
  `data-*` + `addEventListener`.
- Комментарии и тексты UI — по-русски, идентификаторы — по-английски.
- Коммиты — по-русски, развёрнуто: что, зачем, что проверено.
- Не трогать: архивный клон `/Users/yaroslav/cmr-svetvdome` (истории не связаны),
  порт 8125, прод-БД, `pm2` на проде.

## 6. Чек-лист старта пункта

1. Прочитать пункт плана и его критерий готовности; сверить, не занят ли файл
   параллельной сессией (`git branch -vv`, `git status`).
2. Проверить стенд 8126 (или поднять).
3. Правки → пересборка → критерий пункта → регрессия `/tmp/verify-0-block.py`
   → `v2_error_census` (новых ошибок нет) → pytest, если тронут бэкенд.
4. Коммит + строка в журнале плана.
5. Короткий отчёт владельцу: что закрыто, чем проверено, что осталось.
