# Рабочий план v2 по пунктам (по одному пункту на сессию) — 22.09.2026

Порядок работы, согласованный с владельцем 22.09: **каждый пункт ниже выполняется
в отдельной новой сессии**; сессия начинает с чтения этого файла и
`docs/SESSION-BRIEF-<дата>.md`, делает ровно свой пункт, прогоняет его критерий
готовности, коммитит атомарно и помечает пункт `[x]` здесь. В конце — сведение
(пункт S): один прогон всех проверок и обновление документации.

Общие правила для любой сессии (из AGENTS.md, не повторять в пунктах):
- правки фронта v2 — только `server_snapshot/tools/mockups/` и шаблоны
  `server_snapshot/tools/v2-{api,boot}-template.js`; `site-v2/` руками не править;
  пересборка `cd server_snapshot && python3 tools/build_site_v2.py` +
  `python3 tools/stamp_assets.py --check` — одним человеком в конце пункта;
- CSP: без `onclick`/eval, только `data-*` + `addEventListener`;
- комментарии и тексты UI по-русски; идентификаторы по-английски;
- прод — только чтение; пуш и деплой — по явному слову владельца;
- стенд: копия прод-БД в `/tmp/v2-audit-prod/`, порт 8126, вход
  `audit_admin` / `Audit-2026!` (как поднять — `docs/audits/V2-PLAN-RECON-2026-09-22.md`, §12);
- не трогать: архивный клон `/Users/yaroslav/cmr-svetvdome`, порт 8125 (чужой стенд),
  `docs/SESSION-BRIEF-*` (служебные, не коммитятся).

Источники фактов: `docs/audits/V2-PLAN-RECON-2026-09-22.md` (сверка плана с git),
`docs/audits/V2-UI-AUDIT-2026-09-22.md` (визуал/юзабилити/производительность),
расследование зависания комбобокса — `/tmp/supplier-freeze/` (отчёт приложен к брифу).

---

## Блок 0 — завершается в текущей сессии (не пункт для новых)

- [x] 0.1 Фикс бесконечной петли `MutationObserver` в дровере клиента
      (`shell-v2-insights.js`, метка «в работе» до вставки блока). Коммит `5eff3ef`.
      Подтверждено переписью: 1 запрос `/clients/{id}/balance` на весь обход
      (по F1 было 20 544), 0 `pageerror`.
- [x] 0.2 Перезагрузка данных без F5 (`applyInPlace`, `KBData.reload`, `kb:reloaded`),
      создание сделки с доски (`#kb-new-card` + форма-диалог + ветка `card-create`),
      идемпотентная загрузка модулей (`inject` с реестром, `showLogin` без дублей).
      Коммит `81a23f2` (+ `fca52d1` — шаг 6k аудита и автоподтверждение
      модального `#kb-confirm`, без него аудит вставал на 6e).
- [ ] 0.3 Фикс зависания на комбобоксе поставщика в «Закупке» (`.kb-sup-combo`,
      `initCombo`) — по отчёту расследования `/tmp/supplier-freeze/`.
- [ ] 0.4 Одна пересборка `site-v2` со всеми правками блока 0 + `stamp_assets --check`.
      Промежуточно: сборка со всеми правками 0.1–0.2 сделана (stamp: 0 расхождений),
      финальная — после 0.3; `site-v2` в git пока не закоммичен.
- [ ] 0.5 Верификация на стенде 8126: `v2_error_census` (цель 0 ошибок JS, кроме
      известных CSP-blob до пункта 1), `v2_functional_audit` (шаги 6a и 6k PASS),
      сценарии: клиент не виснет; создать сделку без F5; восстановить из корзины
      без F5; комбобокс поставщика не виснет (WebKit + Chromium).
      Промежуточно (после 0.1–0.2, до фикса 0.3): census — 0 `pageerror`,
      1 запрос `/balance`, 0 аномалий DOM, 0 дыр в KBData
      (`/tmp/v2-audit-prod/toolout/census-block0/`); functional_audit — 26 проверок,
      **6a и 6k PASS**, провалы только 7 (на стенде не заполнены настройки почты)
      и 10 (14 CSP-blob миниатюр). Осталось: сценарии корзины и комбобокса.
- [ ] 0.6 Атомарные коммиты: фикс петли; пакет reload+создание+boot; фикс комбобокса;
      пересборка site-v2. Сделаны первые два + инструментарий аудита
      (`5eff3ef`, `81a23f2`, `fca52d1`).
- [ ] 0.7 Бриф следующей сессии `docs/SESSION-BRIEF-<дата>.md` + коммит этого плана.
      Сам план закоммичен в `da97b25`.

## Пункты для новых сессий

### 1. CSP: разрешить `blob:` для изображений
Правка: `server_snapshot/main.py:194` — `img-src 'self' data: https:` → добавить `blob:`.
Зачем: 14 миниатюр фото входящих накладных битые с загрузки страницы
(`V2-UI-AUDIT` F2; census: 14 ошибок CSP + 14 провалов `transport: csp`).
Критерий: `v2_error_census.py --base http://127.0.0.1:8126/v2/` → «Ошибки JS: 0»;
скриншот «Входящие накладные → НДС и файлы» с видимыми миниатюрами (на стенде фото
есть: 37 изображений скачаны в `/tmp/v2-audit-prod/uploads`).
Тест: добавить проверку заголовка CSP в `server_snapshot/tests/` (сейчас теста CSP нет).
Уточнение критерия (замер 22.09 на стенде, `/tmp/v2-audit-prod/toolout/census-block0/`):
после фикса `blob:` в переписи останутся 4 console-сообщения, к CSP не относящиеся —
1× `400 POST /email-parser/sync` («Настройки почты не заполнены» — на стенде почта
не настроена, не баг v2) и 3× «An invalid form control with name='' is not
focusable» в админке (скрытое required-поле в форме; отдельная мелочь, не пункт 1).
`pageerror` сейчас 0 — вот это и есть рабочая планка «Ошибки JS: 0».
Деплой — отдельное решение владельца.

### 2. Производительность взаимодействий доски
Правки в `tools/mockups/shell-v2-board.js`: убрать полные `refresh()` из обработчиков
диалога (галочки закупки ~:1937, поля ~:1916, «ТН у нас» ~:1843) — точечно обновлять
плитку + `updateColumnHead()` + `renderQueue()`; рассылать `kb:documents`/`kb:groups`
только при реальном изменении документов/групп. В инлайн-скрипте прототипа
(`shell-v2-prototype.html:1258-1292`) собирать строки одним HTML вместо
`insertAdjacentHTML` в цикле.
Критерий: замер скриптом (образец `/tmp/v2-audit-prod/scripts/s3_perf.py`): одна
галочка → мутации DOM < 300 (сейчас 2092), long task < 30 мс (сейчас 60–69),
1 PATCH; перерисовка финансов не вызывается от галочки.

### 3. Стоимость отрисовки: ленивые финансы + дешёвые плитки
Правки: не отрисовывать скрытый `#view-fin` до первого входа в раздел (сейчас
14 839 из 20 843 узлов DOM создаются сразу); `content-visibility:auto` +
`contain-intrinsic-size` на `.kb-card` (`shell-v2-board.css:70-77`); SVG-счётчики
плитки (`shell-v2-board.js:146-178`) → один `<symbol>` + `<use>`.
Критерий: DOM после входа < 8 000 узлов; SVG в доске < 100; матрица
`v2_design_matrix.py` — 0 горизонтальных скроллов и 0 наложений на всех ширинах/темах.

### 4. Drag&drop: троттлинг и обратная связь
Правки в `shell-v2-board.js`: `placeDrop()` не чаще одного раза на кадр (флаг в rAF),
кэш `getBoundingClientRect()` плиток на время перетаскивания, убрать дублирующий вызов
из `scrollDrag()` (`:246-312`); после успешного переноса — тост (сейчас молча, F8).
Критерий: трейс перетаскивания без пачек layout-пересчётов; тост виден; статус
сохранён (`PATCH /kanban/cards/{id}/status` один).

### 5. Поле почты на первой вкладке карточки (запрос владельца 22.09)
Добавить в «Обзор» дровера (`shell-v2-board.js`, секция полей сделки) поле
`sender_email`: показ текущего значения + правка с сохранением через существующий
`PATCH /cards/{id}` (поле уже в модели и схемах; в БД 741 карточка с `sender_email`).
Связать с вкладкой «История → Письма отправителя» (пункт 9): после сохранения почты
письма подтягиваются по новому значению.
Критерий: значение видно и редактируется; после сохранения уходит один PATCH;
скриншот до/после; census без новых ошибок.

### 6. Списание в финансах НЕ создавать (решение владельца 22.09)
Не добавлять вкладку «Списание» в финансы и не переносить legacy-экран: списание
живёт очередями «На списание» / «Списано» на доске (они работают). Вычеркнуть
«Финансы: вкладка Списание» из списка паритета (`V2-UI-AUDIT` §6 п.5 — помечено
как не нужное). Проверить, что в финансах нет мёртвых кнопок/пунктов списания и что
очереди на доске покрывают сценарии legacy (выписка, отмена, групповая ТН).
Критерий: в финансах нет UI создания списания; сценарий «на списание → выписать ТН →
отменить» проходит на стенде без обращения к финансам; правка списка паритета
в `V2-UI-AUDIT` закоммичена.

### 7. Поиск на доске: один инпут, один крестик (запрос владельца 22.09)
Сейчас при открытии поиска появляется вложенное «внутреннее окошко» с синим
выделением и два крестика (сброс внутри + закрытие снаружи). Сделать один инпут
с одним крестиком-сбросом и одним способом закрыть (Esc или тот же крестик);
пустой результат — подсказка «ничего не найдено» + кнопка «Сбросить фильтры»
(сейчас четыре плашки «Пусто» без подсказки, `V2-UI-AUDIT` D2).
Критерий: скриншот открытого поиска до/после; в DOM один `input` поиска и один
крестик; пустой поиск показывает подсказку и сброс; поиск по-прежнему фильтрует
(190 → N плиток).

### 8. История карточки: письма отправителя
Вызывать `loadRelatedEmails()` при открытии вкладки «История» (сейчас вызывается
только из `linkEmailToCard()`, плейсхолдер «Загрузка…» вечный, запросов 0 — F3);
различать состояния «писем нет» и «ошибка загрузки» с кнопкой повтора.
Критерий: при открытии вкладки уходит `GET /email-parser/related/{card}`; через 2 с
плейсхолдера нет; скриншот обоих состояний (карточка с `sender_email` и без).

### 9. Ролевая модель в UI
Под ролью manager сейчас виден весь раздел «Управление» с кнопками правки
справочников и пользователей (F5). Скрыть админ-карточки/кнопки по роли из
`/auth/me`; сверить с бэком: мутации под manager должны давать 403 (проверить и,
если бэк пропускает, — отдельным пунктом закрыть на сервере).
Критерий: под `audit_manager` нет кнопок «+ Новый пользователь/магазин/статус»,
«Изменить», «Синхронизировать»; попытка прямого POST/PATCH из консоли → 403;
под `audit_admin` всё на месте.

### 10. Уведомления: без авто-прочтения, со ссылками
Убрать авто-`POST /notifications/read {ids:[]}` при открытии; добавить кнопку
«Прочитать все», ссылки/переход к объекту уведомления и удаление одного
(F9; legacy `site/js/notifications.js:84-104,145`).
Критерий: открытие колокольчика не меняет статусы; клик по уведомлению открывает
сделку/задачу; «Прочитать все» помечает; скриншоты.

### 11. Мелочи дровера и текстов
Висячий пустой label «Условия оплаты» (F6); декодирование percent-имён вложений
+ tooltip на полное имя (F7); убрать «Демо-» формулировки на живых данных (F10).
Критерий: скриншоты «Обзор», «Закупка/вложения», «Накладные» без брака; census без
новых ошибок.

### 12. Справочник статусов сделок на проде
Аддитивная миграция заполнения `deal_statuses` из фактических статусов карточек
(по образцу 0011 для магазинов): сейчас `/dictionaries/statuses` = `[]`, доска живёт
на клиентском хардкоде `STATUS_MAP` (`V2-PLAN-RECON` N1). Убрать зависимость доски
от хардкода после заполнения. Применение на проде — решение владельца (после `.backup`).
Критерий: на копии БД `/dictionaries/statuses` возвращает 6 статусов; админка v2
показывает их и даёт переименовать; доска рисует те же колонки.

### 13. Паритет-пакет 1: виды и фильтры доски
Режимы «Список» и «Архив» (закрытые сделки сейчас не видны нигде, кроме финансов),
фильтры по приоритету и типу оплаты («Безнал (БН)»), кнопка «Сбросить»
(`V2-UI-AUDIT` §6 п.1–3; legacy `site/js/kanban.js`, `site/index.html`).
Критерий: сравнение скриншотов с legacy (`/tmp/v2-audit-prod/shots/80-legacy-board.png`);
режим «Архив» показывает закрытые; фильтры сужают набор; сброс возвращает всё.

### 14. Паритет-пакет 2: финансы и задачи
Вкладка «Контроль»/дебиторка в финансах; экспорт CSV документов (экспорт реестра
оплат уже есть и работает); отдельный раздел «Календарь»; фильтры задач
«Все / Просроченные»; карточка настроек «Внешний вид» (`V2-UI-AUDIT` §6 п.4,7–9,11).
Критерий: скриншоты новых экранов в обеих темах; экспорт скачивает файл; census чисто.

### 15. Паритет-пакет 3: настройки и админка
Workflows (11 роутов), кастомные объекты (9), сохранённые виды, вебхуки (6),
удаление/деактивация пользователя, авто-синк почты по таймеру
(`V2-PLAN-RECON` §4; legacy `site/js/settings.js`, `site/js/admin_page.js`).
Критерий: каждый экран открывается, CRUD проходит на стенде, census чисто;
права — только админ (связка с пунктом 9).

### 16. Сервер: пагинация и гигиена журналов
Серверные страницы для `/kanban/cards` и `/payments/transactions` (дефект B11:
474 КБ и 132 КБ одним куском) + точечная дозагрузка в клиенте; ротация
`record_versions`/`activity_log` (очистки нет вовсе; 1456/1638 строк и растут).
Критерий: первый экран доски грузит < 100 КБ; дозагрузка по скроллу работает;
миграция ротации аддитивна и идемпотентна.

### 17. Роли на бизнес-операциях и «Оригинал ТН возвращён»
V11: `require_role` на `DELETE /payments/transactions/{id}` и аналогичных
(решение владельца по матрице ролей); V7: либо колонка `originals_returned`
(миграция + схемы + API), либо легализовать маппинг на `is_invoice_doc`, устранив
конфликт с «ТН у нас» в финансах (`V2-PLAN-RECON` §6).
Критерий: тесты доступа в `server_snapshot/tests/` покрывают матрицу; конфликт
семантик устранён и задокументирован.

### 18. Инфраструктура v2
Внести боевой nginx-конфиг в репо с location'ами `/v2` и `/legacy` (сейчас в репо их
нет вовсе); CI: сборка и выкатка v2 (сейчас `deploy-production` зовёт только
`tools/deploy.sh front`); гейт на свежесть `site-v2` в `scripts/check.sh`;
тест кэш-политики `/v2` (`main.py:214-236`); чистка устаревшего CSP
(`fonts.googleapis/gstatic`); usage-строка `deploy.sh:203`; мусор
`server_snapshot/js/.mimosa/` (`V2-PLAN-RECON` §8, N5–N7).
Критерий: `docker compose`-путь отдаёт `/v2/` корректно; CI-джоба v2 зелёная;
`scripts/check.sh` падает на рассинхроне mockups↔site-v2.

### S. Сведение (последняя сессия)
Прогнать всё разом: pytest оба профиля, `stamp_assets --check` (оба), `node --check`,
`v2_error_census` (0 ошибок), `v2_functional_audit` (все шаги PASS), матрица
(0 скроллов/наложений), сквозной обход ролей admin/manager, адаптив 768–1920,
обе темы. Обновить: чек-лист `V2-FIX-PLAN-2026-09-19.md`, `V2-UI-AUDIT`,
`docs/STATUS.md`, `docs/audits/INDEX.md`. Сверить с владельцем: деплой, пуш,
отзыв PAT, домен/HTTPS, офсайт-бэкап. Итоговый отчёт владельцу одним документом.

---

## Журнал выполнения (заполняется по мере закрытия пунктов)

| Дата | Пункт | Сессия/коммит | Статус |
| --- | --- | --- | --- |
| 22.09 | 0.1 | `5eff3ef` | закрыт, подтверждено census (1 запрос `/balance`, 0 `pageerror`) |
| 22.09 | 0.2 | `81a23f2` + `fca52d1` (аудит: шаг 6k, автоподтверждение `#kb-confirm`) | закрыт, 6a и 6k PASS на стенде 8126 |
| 22.09 | 0.3 | проверка на сборке `3c9d008`: карточка 1058 → «Закупка» → комбобокс поставщика (ввод текста + клик по подсказке) — страница отвечает, 1 `PATCH /checklists`, шквала `/balance` нет; A/B расследования (`/tmp/supplier-freeze/`, repro10–12): старая сборка зависает детерминированно 3/3 со шквалом `GET /clients/{id}/balance` (1961–7294 запросов), новая — 0 зажиганий за 12+ прогонов; механизм = петля дровера клиента из 0.1, код комбобокса не дефектен, записей-шквала в БД нет; правок не требуется, нужен деплой собранного | закрыт |
| 22.09 | 0.4–0.6 | `3c9d008` — пересборка `site-v2` закоммичена в main; верификация на стенде 8126: stamp 0 расхождений, census без новых ошибок, функциональный аудит 26 проверок / 2 провала-артефакта стенда, шаги 6a и 6k PASS, восстановление из корзины без F5 | закрыт |
