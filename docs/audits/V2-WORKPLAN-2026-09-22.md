# Рабочий план v2 по пунктам (по одному пункту на сессию) — 22.09.2026

Порядок работы, согласованный с владельцем 22.09: **каждый пункт ниже выполняется
в отдельной новой сессии**; сессия начинает с чтения этого файла и
`docs/SESSION-BRIEF-<дата>.md`, делает ровно свой пункт, прогоняет его критерий
готовности, коммитит атомарно и помечает пункт `[x]` здесь. В конце — сведение
(пункт S): один прогон всех проверок и обновление документации.

Общие правила для любой сессии (из AGENTS.md, не повторять в пунктах):
- правки фронта v2 — только `server_snapshot/tools/mockups/` и шаблоны
  `server_snapshot/tools/v2-{api,boot}-template.js`; `site-v2/` руками не править;
  пересборка `cd server_snapshot && python3 tools/build_site_v2.py` +
  `python3 tools/stamp_assets.py --check` — одним человеком в конце пункта;
- CSP: без `onclick`/eval, только `data-*` + `addEventListener`;
- комментарии и тексты UI по-русски; идентификаторы по-английски;
- прод — только чтение; пуш и деплой — по явному слову владельца;
- стенд: копия прод-БД в `/tmp/v2-audit-prod/`, порт 8126, вход
  `audit_admin` / `Audit-2026!` (как поднять — `docs/audits/V2-PLAN-RECON-2026-09-22.md`, §12);
- не трогать: архивный клон `/Users/yaroslav/cmr-svetvdome`, порт 8125 (чужой стенд),
  `docs/SESSION-BRIEF-*` (служебные, не коммитятся).

Источники фактов: `docs/audits/V2-PLAN-RECON-2026-09-22.md` (сверка плана с git),
`docs/audits/V2-UI-AUDIT-2026-09-22.md` (визуал/юзабилити/производительность),
расследование зависания комбобокса — `/tmp/supplier-freeze/` (отчёт приложен к брифу).

---

## Блок 0 — завершается в текущей сессии (не пункт для новых)

- [x] 0.1 Фикс бесконечной петли `MutationObserver` в дровере клиента
      (`shell-v2-insights.js`, метка «в работе» до вставки блока). Коммит `5eff3ef`.
      Подтверждено переписью: 1 запрос `/clients/{id}/balance` на весь обход
      (по F1 было 20 544), 0 `pageerror`.
- [x] 0.2 Перезагрузка данных без F5 (`applyInPlace`, `KBData.reload`, `kb:reloaded`),
      создание сделки с доски (`#kb-new-card` + форма-диалог + ветка `card-create`),
      идемпотентная загрузка модулей (`inject` с реестром, `showLogin` без дублей).
      Коммит `81a23f2` (+ `fca52d1` — шаг 6k аудита и автоподтверждение
      модального `#kb-confirm`, без него аудит вставал на 6e).
- [ ] 0.3 Фикс зависания на комбобоксе поставщика в «Закупке» (`.kb-sup-combo`,
      `initCombo`) — по отчёту расследования `/tmp/supplier-freeze/`.
- [ ] 0.4 Одна пересборка `site-v2` со всеми правками блока 0 + `stamp_assets --check`.
      Промежуточно: сборка со всеми правками 0.1–0.2 сделана (stamp: 0 расхождений),
      финальная — после 0.3; `site-v2` в git пока не закоммичен.
- [ ] 0.5 Верификация на стенде 8126: `v2_error_census` (цель 0 ошибок JS, кроме
      известных CSP-blob до пункта 1), `v2_functional_audit` (шаги 6a и 6k PASS),
      сценарии: клиент не виснет; создать сделку без F5; восстановить из корзины
      без F5; комбобокс поставщика не виснет (WebKit + Chromium).
      Промежуточно (после 0.1–0.2, до фикса 0.3): census — 0 `pageerror`,
      1 запрос `/balance`, 0 аномалий DOM, 0 дыр в KBData
      (`/tmp/v2-audit-prod/toolout/census-block0/`); functional_audit — 26 проверок,
      **6a и 6k PASS**, провалы только 7 (на стенде не заполнены настройки почты)
      и 10 (14 CSP-blob миниатюр). Осталось: сценарии корзины и комбобокса.
- [ ] 0.6 Атомарные коммиты: фикс петли; пакет reload+создание+boot; фикс комбобокса;
      пересборка site-v2. Сделаны первые два + инструментарий аудита
      (`5eff3ef`, `81a23f2`, `fca52d1`).
- [ ] 0.7 Бриф следующей сессии `docs/SESSION-BRIEF-<дата>.md` + коммит этого плана.
      Сам план закоммичен в `da97b25`.

## Пункты для новых сессий

### 1. [x] CSP: разрешить `blob:` для изображений
Правка: `server_snapshot/main.py:194` — `img-src 'self' data: https:` → добавить `blob:`.
Зачем: 14 миниатюр фото входящих накладных битые с загрузки страницы
(`V2-UI-AUDIT` F2; census: 14 ошибок CSP + 14 провалов `transport: csp`).
Критерий: `v2_error_census.py --base http://127.0.0.1:8126/v2/` → «Ошибки JS: 0»;
скриншот «Входящие накладные → НДС и файлы» с видимыми миниатюрами (на стенде фото
есть: 37 изображений скачаны в `/tmp/v2-audit-prod/uploads`).
Тест: добавить проверку заголовка CSP в `server_snapshot/tests/` (сейчас теста CSP нет).
Уточнение критерия (замер 22.09 на стенде, `/tmp/v2-audit-prod/toolout/census-block0/`):
после фикса `blob:` в переписи останутся 4 console-сообщения, к CSP не относящиеся —
1× `400 POST /email-parser/sync` («Настройки почты не заполнены» — на стенде почта
не настроена, не баг v2) и 3× «An invalid form control with name='' is not
focusable» в админке (скрытое required-поле в форме; отдельная мелочь, не пункт 1).
`pageerror` сейчас 0 — вот это и есть рабочая планка «Ошибки JS: 0».
Деплой — отдельное решение владельца.

### 2. [x] Производительность взаимодействий доски
Правки в `tools/mockups/shell-v2-board.js`: убрать полные `refresh()` из обработчиков
диалога (галочки закупки ~:1937, поля ~:1916, «ТН у нас» ~:1843) — точечно обновлять
плитку + `updateColumnHead()` + `renderQueue()`; рассылать `kb:documents`/`kb:groups`
только при реальном изменении документов/групп. В инлайн-скрипте прототипа
(`shell-v2-prototype.html:1258-1292`) собирать строки одним HTML вместо
`insertAdjacentHTML` в цикле.
Критерий: замер скриптом (образец `/tmp/v2-audit-prod/scripts/s3_perf.py`): одна
галочка → мутации DOM < 300 (сейчас 2092), long task < 30 мс (сейчас 60–69),
1 PATCH; перерисовка финансов не вызывается от галочки.

### 3. [x] Стоимость отрисовки: ленивые финансы + дешёвые плитки
Правки: не отрисовывать скрытый `#view-fin` до первого входа в раздел (сейчас
14 839 из 20 843 узлов DOM создаются сразу); `content-visibility:auto` +
`contain-intrinsic-size` на `.kb-card` (`shell-v2-board.css:70-77`); SVG-счётчики
плитки (`shell-v2-board.js:146-178`) → один `<symbol>` + `<use>`.
Критерий: DOM после входа < 8 000 узлов; SVG в доске < 100; матрица
`v2_design_matrix.py` — 0 горизонтальных скроллов и 0 наложений на всех ширинах/темах.
Итог 23.09: закрыт, но SVG-иконки плитки переведены не на `<symbol>`+`<use>`, а на
CSS-маски — `<use>` оставляет по элементу `<svg>` на экземпляр и критерий «< 100»
не закрывает в принципе. Подробности в журнале.

### 4. Drag&drop: троттлинг и обратная связь
Правки в `shell-v2-board.js`: `placeDrop()` не чаще одного раза на кадр (флаг в rAF),
кэш `getBoundingClientRect()` плиток на время перетаскивания, убрать дублирующий вызов
из `scrollDrag()` (`:246-312`); после успешного переноса — тост (сейчас молча, F8).
Критерий: трейс перетаскивания без пачек layout-пересчётов; тост виден; статус
сохранён (`PATCH /kanban/cards/{id}/status` один).

### 5. Поле почты на первой вкладке карточки (запрос владельца 22.09)
Добавить в «Обзор» дровера (`shell-v2-board.js`, секция полей сделки) поле
`sender_email`: показ текущего значения + правка с сохранением через существующий
`PATCH /cards/{id}` (поле уже в модели и схемах; в БД 741 карточка с `sender_email`).
Связать с вкладкой «История → Письма отправителя» (пункт 9): после сохранения почты
письма подтягиваются по новому значению.
Критерий: значение видно и редактируется; после сохранения уходит один PATCH;
скриншот до/после; census без новых ошибок.

### 6. Списание в финансах НЕ создавать (решение владельца 22.09)
Не добавлять вкладку «Списание» в финансы и не переносить legacy-экран: списание
живёт очередями «На списание» / «Списано» на доске (они работают). Вычеркнуть
«Финансы: вкладка Списание» из списка паритета (`V2-UI-AUDIT` §6 п.5 — помечено
как не нужное). Проверить, что в финансах нет мёртвых кнопок/пунктов списания и что
очереди на доске покрывают сценарии legacy (выписка, отмена, групповая ТН).
Критерий: в финансах нет UI создания списания; сценарий «на списание → выписать ТН →
отменить» проходит на стенде без обращения к финансам; правка списка паритета
в `V2-UI-AUDIT` закоммичена.

### 7. Поиск на доске: один инпут, один крестик (запрос владельца 22.09)
Сейчас при открытии поиска появляется вложенное «внутреннее окошко» с синим
выделением и два крестика (сброс внутри + закрытие снаружи). Сделать один инпут
с одним крестиком-сбросом и одним способом закрыть (Esc или тот же крестик);
пустой результат — подсказка «ничего не найдено» + кнопка «Сбросить фильтры»
(сейчас четыре плашки «Пусто» без подсказки, `V2-UI-AUDIT` D2).
Критерий: скриншот открытого поиска до/после; в DOM один `input` поиска и один
крестик; пустой поиск показывает подсказку и сброс; поиск по-прежнему фильтрует
(190 → N плиток).

### 8. История карточки: письма отправителя
Вызывать `loadRelatedEmails()` при открытии вкладки «История» (сейчас вызывается
только из `linkEmailToCard()`, плейсхолдер «Загрузка…» вечный, запросов 0 — F3);
различать состояния «писем нет» и «ошибка загрузки» с кнопкой повтора.
Критерий: при открытии вкладки уходит `GET /email-parser/related/{card}`; через 2 с
плейсхолдера нет; скриншот обоих состояний (карточка с `sender_email` и без).

### 9. Ролевая модель в UI
Под ролью manager сейчас виден весь раздел «Управление» с кнопками правки
справочников и пользователей (F5). Скрыть админ-карточки/кнопки по роли из
`/auth/me`; сверить с бэком: мутации под manager должны давать 403 (проверить и,
если бэк пропускает, — отдельным пунктом закрыть на сервере).
Критерий: под `audit_manager` нет кнопок «+ Новый пользователь/магазин/статус»,
«Изменить», «Синхронизировать»; попытка прямого POST/PATCH из консоли → 403;
под `audit_admin` всё на месте.

### 10. [x] Уведомления: без авто-прочтения, со ссылками
Убрать авто-`POST /notifications/read {ids:[]}` при открытии; добавить кнопку
«Прочитать все», ссылки/переход к объекту уведомления и удаление одного
(F9; legacy `site/js/notifications.js:84-104,145`).
Критерий: открытие колокольчика не меняет статусы; клик по уведомлению открывает
сделку/задачу; «Прочитать все» помечает; скриншоты.

### 11. Мелочи дровера и текстов
Висячий пустой label «Условия оплаты» (F6); декодирование percent-имён вложений
+ tooltip на полное имя (F7); убрать «Демо-» формулировки на живых данных (F10).
Критерий: скриншоты «Обзор», «Закупка/вложения», «Накладные» без брака; census без
новых ошибок.

### 12. Справочник статусов сделок на проде
Аддитивная миграция заполнения `deal_statuses` из фактических статусов карточек
(по образцу 0011 для магазинов): сейчас `/dictionaries/statuses` = `[]`, доска живёт
на клиентском хардкоде `STATUS_MAP` (`V2-PLAN-RECON` N1). Убрать зависимость доски
от хардкода после заполнения. Применение на проде — решение владельца (после `.backup`).
Критерий: на копии БД `/dictionaries/statuses` возвращает 6 статусов; админка v2
показывает их и даёт переименовать; доска рисует те же колонки.

**Статус 23.09: серверная половина закрыта** (`e77b86b` — миграция
`migrations/0013_fill_deal_statuses.py` + `tests/test_migration_0013_deal_statuses.py`).
Критерий по API на копии БД выполнен: 6 статусов в порядке `position` с цветами,
`PATCH /dictionaries/statuses/{id}` переименовывает (200). Клиентская половина
НЕ сделана, и **до неё миграцию нигде не применять — ни к проду, ни к общему
стенду 8126**: непустой `/dictionaries/statuses` переключает v2 с хардкода на
ветку словаря (`tools/v2-boot-template.js:124`), где «Закрыто» получает
`role='board'` — на доске появляется пятая колонка со 179 закрытыми сделками, а
её id `done` сталкивается с хардкодом «Списано» в списке этапов
(`tools/mockups/shell-v2-board.js:1289`) и с очередью списания, где
`stage === 'done'` означает «Списано» (`shell-v2-board.js:569`). Клиентской
половине заодно решить: переименование статуса не переписывает `cards.status`,
поэтому переименованная колонка оставит старые карточки без колонки.

Спецификация клиентской половины — `docs/audits/V2-ITEM12-CLIENT-SPEC-2026-09-23.md`:
принятые решения Р1–Р6 (роль «Закрыто» = `archive`, разведение id `closed` и
`done`, запрет переименования канона в UI, `canAssign` в drop и клавиатуре,
`is_active` в админке, отмена фолбэка «неизвестный статус → Новый запрос»),
работы по файлам A–E с держателями, критерий готовности 1–9 и таблица As-built.

### 13. Паритет-пакет 1: виды и фильтры доски
Режимы «Список» и «Архив» (закрытые сделки сейчас не видны нигде, кроме финансов),
фильтры по приоритету и типу оплаты («Безнал (БН)»), кнопка «Сбросить»
(`V2-UI-AUDIT` §6 п.1–3; legacy `site/js/kanban.js`, `site/index.html`).
Критерий: сравнение скриншотов с legacy (`/tmp/v2-audit-prod/shots/80-legacy-board.png`);
режим «Архив» показывает закрытые; фильтры сужают набор; сброс возвращает всё.

### 14. Паритет-пакет 2: финансы и задачи
Вкладка «Контроль»/дебиторка в финансах; экспорт CSV документов (экспорт реестра
оплат уже есть и работает); отдельный раздел «Календарь»; фильтры задач
«Все / Просроченные»; карточка настроек «Внешний вид» (`V2-UI-AUDIT` §6 п.4,7–9,11).
Критерий: скриншоты новых экранов в обеих темах; экспорт скачивает файл; census чисто.

### 15. Паритет-пакет 3: настройки и админка
Workflows (11 роутов), кастомные объекты (9), сохранённые виды, вебхуки (6),
удаление/деактивация пользователя, авто-синк почты по таймеру
(`V2-PLAN-RECON` §4; legacy `site/js/settings.js`, `site/js/admin_page.js`).
Критерий: каждый экран открывается, CRUD проходит на стенде, census чисто;
права — только админ (связка с пунктом 9).

### 16. Сервер: пагинация и гигиена журналов
Серверные страницы для `/kanban/cards` и `/payments/transactions` (дефект B11:
474 КБ и 132 КБ одним куском) + точечная дозагрузка в клиенте; ротация
`record_versions`/`activity_log` (очистки нет вовсе; 1456/1638 строк и растут).
Критерий: первый экран доски грузит < 100 КБ; дозагрузка по скроллу работает;
миграция ротации аддитивна и идемпотентна.

### 17. Роли на бизнес-операциях и «Оригинал ТН возвращён»
V11: `require_role` на `DELETE /payments/transactions/{id}` и аналогичных
(решение владельца по матрице ролей); V7: либо колонка `originals_returned`
(миграция + схемы + API), либо легализовать маппинг на `is_invoice_doc`, устранив
конфликт с «ТН у нас» в финансах (`V2-PLAN-RECON` §6).
Критерий: тесты доступа в `server_snapshot/tests/` покрывают матрицу; конфликт
семантик устранён и задокументирован.

### 18. Инфраструктура v2
Внести боевой nginx-конфиг в репо с location'ами `/v2` и `/legacy` (сейчас в репо их
нет вовсе); CI: сборка и выкатка v2 (сейчас `deploy-production` зовёт только
`tools/deploy.sh front`); гейт на свежесть `site-v2` в `scripts/check.sh`;
тест кэш-политики `/v2` (`main.py:214-236`); чистка устаревшего CSP
(`fonts.googleapis/gstatic`); usage-строка `deploy.sh:203`; мусор
`server_snapshot/js/.mimosa/` (`V2-PLAN-RECON` §8, N5–N7).
Критерий: `docker compose`-путь отдаёт `/v2/` корректно; CI-джоба v2 зелёная;
`scripts/check.sh` падает на рассинхроне mockups↔site-v2.

### S. Сведение (последняя сессия)
Прогнать всё разом: pytest оба профиля, `stamp_assets --check` (оба), `node --check`,
`v2_error_census` (0 ошибок), `v2_functional_audit` (все шаги PASS), матрица
(0 скроллов/наложений), сквозной обход ролей admin/manager, адаптив 768–1920,
обе темы. Обновить: чек-лист `V2-FIX-PLAN-2026-09-19.md`, `V2-UI-AUDIT`,
`docs/STATUS.md`, `docs/audits/INDEX.md`. Сверить с владельцем: деплой, пуш,
отзыв PAT, домен/HTTPS, офсайт-бэкап. Итоговый отчёт владельцу одним документом.

---

## Журнал выполнения (заполняется по мере закрытия пунктов)

| Дата | Пункт | Сессия/коммит | Статус |
| --- | --- | --- | --- |
| 22.09 | 0.1 | `5eff3ef` | закрыт, подтверждено census (1 запрос `/balance`, 0 `pageerror`) |
| 22.09 | 0.2 | `81a23f2` + `fca52d1` (аудит: шаг 6k, автоподтверждение `#kb-confirm`) | закрыт, 6a и 6k PASS на стенде 8126 |
| 22.09 | 0.3 | проверка на сборке `3c9d008`: карточка 1058 → «Закупка» → комбобокс поставщика (ввод текста + клик по подсказке) — страница отвечает, 1 `PATCH /checklists`, шквала `/balance` нет; A/B расследования (`/tmp/supplier-freeze/`, repro10–12): старая сборка зависает детерминированно 3/3 со шквалом `GET /clients/{id}/balance` (1961–7294 запросов), новая — 0 зажиганий за 12+ прогонов; механизм = петля дровера клиента из 0.1, код комбобокса не дефектен, записей-шквала в БД нет; правок не требуется, нужен деплой собранного | закрыт |
| 22.09 | 0.4–0.6 | `3c9d008` — пересборка `site-v2` закоммичена в main; верификация на стенде 8126: stamp 0 расхождений, census без новых ошибок, функциональный аудит 26 проверок / 2 провала-артефакта стенда, шаги 6a и 6k PASS, восстановление из корзины без F5 | закрыт |
| 23.09 | деплой блока 0 | `tools/deploy.sh v2` (статика, без рестарта); прод на штампе `4d896ae8e3` = локальному, маркеры фиксов на проде проверены curl'ом; `/` → 302 `/v2/`, `/legacy` → 200, кэш `immutable` | выполнен |
| 22.09 | 1 | `b361fd9` (main.py + tests/test_security_headers.py) | закрыт: `img-src 'self' data: blob: https:` (main.py:194), blob: только в img-src; перепись `census-item1`: js_errors 18→4 (остались 4 артефакта стенда — 400 `email-parser/sync` и 3 «invalid form control» в админке), net_failures 15→1 (14 провалов `transport: csp` ушли), 0 pageerror, 0 DOM/дата-аномалий; `s12_csp_blob_thumbs`: 14/14 blob-миниатюр загружены в обеих темах, 0 отказов CSP, скриншоты `incoming-thumbs-{light,dark}.png`; тест CSP 5 passed, негативная проверка (без blob:) падает; pytest 414 / 410+4skip; регрессия блока 0 OK. Деплой — решение владельца; 22.09 владелец выбрал «только v2»: back НЕ задеплоен, CSP прода всё ещё без `blob:` |
| 22.09 | деплой v2: фикс двойного меню комбобокса поставщика | `75a8682` + `tools/deploy.sh v2` (статика, без рестарта) | выполнен: прод на штампе `7a8311f788` = локальному, 0 вхождений `kb-suppliers-datalist` в bundle прода; до деплоя — перепись на стенде 8125: 0 pageerror, 0 аномалий, скриншоты обеих тем |
| 23.09 | 6 | воркер главной сессии: анализ + штамп в V2-UI-AUDIT §6 п.5 (`3fc2c80`) | закрыт чтением кода: UI создания списания в финансах v2 нет, мёртвых пунктов нет; таблица паритета 9 сценариев очередей в отчёте и штампе; стенд-сценарий отложен до свободного браузерного окна (8126, карточка-жертва для P0); найдено 5 дефектов доски — реестр разделом ниже, P0 во главе очереди board.js |
| 23.09 | 17 | воркер главной сессии `5828f2d` + гейт email-parser `16646a3` | закрыт: V11 — гейты admin на 5 деструктивных ручках + матрица 48 тестов, операционные действия менеджера/склада не тронуты (owner_id-скоупинг — отдельное решение владельца); V7 — без миграции, is_invoice_doc каноничен, PATCH 400 на не-документах; находка вне пункта: POST /email-parser/settings закрыт админом. pytest: 1 предсуществующее падение (timezone 00:00–03:00, реестр дефектов п.6). Отложено держателям: унификация подписи и скрытие удаления для не-админов (п.3), часть A P0 (сессия 3), части B/C + tnHere групп (воркер P0). **Поведение прода изменится при деплое бэка — нужно подтверждение владельца** |
| 23.09 | P0 часть B | воркер главной сессии `cafc5a9` | выполнен: POST /writeoffs/groups/{id}/annul (гейт admin, unwind зеркально одиночной отмене, идемпотентно 400), invoice_transaction_id в ответе issue-invoice, docTxId/tnHere/billHere групп в boot-шаблоне (проявится в пересборке пункта 3); 15 тестов + регрессия 72 passed, node --check OK; site-v2 не пересобирал. Часть C и клиентская ветка флагов — после слияния worktree'ов 3 и 4 |
| 23.09 | 6 стенд-сценарий | главная сессия, Playwright на 8126 (скриншоты `gui-test-screenshots/2026-09-23-*`) | частично: «выписать → отменить» проходит (POST/DELETE 200 за 0.0 с, диалог 0.2 с, жизненный цикл ТН чистый, 0 pageerror); «ТН у нас» переживает перезагрузку, обе темы сняты — замерный пробел пункта 2 по этому сценарию закрыт; FAIL: после отмены карточка падает в историю вместо «На списание» — дефект 7 реестра (инвалидация remaining_kop без пересчёта). Пункт 6 остаётся «закрыт чтением кода», сценарий перегнать после фикса дефекта 7 |
| 23.09 | 2 | код `54c184f`; верификация воркером `verifier` на HEAD `028afa4`, штамп сборки `V2_ASSET_VER=27a20e16db` | закрыт: повторный замер критерия на текущем main, два прогона дали идентичные числа — мутации `#kb-board` на одну галочку **4** (порог <300), longest task **0 мс** (порог <30), ровно **1** `PATCH /checklists/454` на взаимодействие, перерисовка финансов от галочки **0** (0 мутаций `#view-fin`, 0 событий `kb:documents`); итоговый флаг `"pass": true` в `/tmp/v2-audit-prod/perf-item2-recheck-{1,2}.json`. Провязка к коммиту есть: `site-v2` и `mockups` чистые относительно HEAD до и после прогонов, HEAD за время замеров не сдвинулся. Регрессия блока 0 (`/tmp/verify-0-block.py`) OK — clients-петля и комбобокс поставщика. Перепись `toolout/census-item2-recheck/`: 0 pageerror, js_errors 4 = известные артефакты стенда (400 `email-parser/sync` + 3 «invalid form control» в админке), 0 DOM/дата-аномалий, net_failures 1. **Не доказано замером:** сценарий «ТН у нас» (в карточке-жертве 1058 нет документов, `data-originals` отсутствует) и тёмная тема; порог «<300» считает только поддерево `#kb-board` (всплытие в диалоге, `counts.dialog.mut`=99, в вердикт не входит). Прежние артефакты `perf-item2-{before,after}.json` сняты за ~6 мин до `54c184f` и без штампа сборки — как доказательство не использовались. pytest не гоняли: бэкенд пунктом не тронут, в дереве были чужие незакоммиченные правки |
| 23.09 | 12 (серверная половина) | `e77b86b` — миграция `migrations/0013_fill_deal_statuses.py` + `tests/test_migration_0013_deal_statuses.py`; реализация воркером `builder`, ревью и контрольные прогоны в главной сессии | частично: `deal_statuses` наполняется 6 фактическими статусами карточек — `position` 0..5 и `color` именами существующих CSS-токенов (`faint/accent/warn/ok/danger/faint`), статус вне канона получает `position ≥ 50` и `color NULL`; аддитивно и идемпотентно, `cards` не меняются, созданные руками строки не перезаписываются, `tenant_id` не заполняется. Роутер не правили — GET/POST/PATCH `/dictionaries/statuses` уже были (`routers/dictionaries_router.py:55-86`). Проверено: `pytest -q` 488 passed (conftest накатывает все миграции в каждой сессии, 0013 в том числе); целевые 63 passed; на копии прод-БД `/tmp/item12` (`sqlite3 .backup` из `/tmp/v2-audit-prod`) — ровно 6 строк, повторный `migrate.py` «применено 0», прямой повторный `up()` дублей не дал; живой API на временном uvicorn:8127 — `GET /dictionaries/statuses` 6 объектов в порядке `position` с цветами, `PATCH /dictionaries/statuses/6` 200 + новое имя (имя откатили), стенд убит после проверки. Общий стенд 8126 и прод НЕ тронуты: в `/tmp/v2-audit-prod/crm_app.db` по-прежнему 0 строк и 11 миграций. **Миграцию не применять до клиентской половины** — см. пометку в пункте 12: непустой справочник переключает v2 на ветку словаря (`v2-boot-template.js:124`), добавляет пятую колонку «Закрыто» (179 сделок) и даёт коллизию id `done` с хардкодом «Списано» (`shell-v2-board.js:1289`, очередь `:569`). Не закрыто: админка v2 и доска (клиентская половина, файлы держат сессии пунктов 3/4/10), а также поведение при переименовании статуса — `cards.status` остаётся строкой |
| 23.09 | 3 | код `a7e504c`/`e5e7727` (ленивые финансы + `content-visibility`) и `9d4668d` (SVG-маски); исполнители — воркеры `general-purpose` и `builder` в worktree `/private/tmp/crm-item3`, `crm-integration` | закрыт: `#view-fin` не строится до первого входа — элементов в DOM после входа **6334** вместо 21 097 (критерий <8000), в скрытом разделе 111 узлов вместо 14 912; `kb:documents`/`kb:groups` до первого открытия копятся в `pending*` и проигрываются при сборке, итоговый `innerHTML #view-fin` побайтово равен прежнему (jsdom-паритет HEAD↔правка, 6 краевых сценариев PASS); `.kb-card` получил `content-visibility:auto` + `contain-intrinsic-size` 105/131px по замеру (средние высоты 123.4/135.4 минус паддинги и рамка 18/26px: свойство задаёт content-box, прежние оценочные 160/180 давали плейсхолдер 178/206px) — сжатие `scrollHeight` при первой прокрутке колонки из 168 плиток **−28.01% → −0.70%**, прыжки по скроллбару 2 из 4 (53px) → 0 из 4; SVG в доске **591 → 0**: иконки плиток переведены на CSS-маски (data-URL в `shell-v2-board.css`, `background-color: currentColor`), потому что `<symbol>`+`<use>` оставляет по `<svg>` на экземпляр и критерий не закрывает, а новые `.svg`-файлы сборщик не копирует; пиксельное сравнение 24 пар скриншотов (плитка/hover/чипы/доски 1280·1440·1920 × 2 плотности × 2 темы, scale 3) — 0 пикселей с расхождением >30/255, то же в WebKit; высоты плиток не изменились. Матрица дизайна 60/60 (0 скроллов, 0 наложений), перепись 0 pageerror. Побочно: миниатюры входящих накладных больше не грузятся до первого входа в финансы |
| 23.09 | P0 части A, C, D | `88d31a2` в ветке `v2-item3-render-cost`, в main через `a7e504c`; исполнитель `general-purpose` по спекам `P0-HOTFIX-GROUP-ANNUL-SPEC-2026-09-23.md` и `BOARD-PACKAGE-SPEC-2026-09-23.md` (разделы A, B) | закрыт клиент: A — `serverTxId` теперь `invoice_transaction_id` (был id группы — причина удаления посторонней транзакции), туда же `docTxId/tnHere/billHere`, чтобы флаги группы жили в сессии выписки; C — основной путь отмены `POST /writeoffs/groups/{id}/annul` (200 → `reloadData()` и тост с числом карточек ИЗ ОТВЕТА СЕРВЕРА; 403 → «доступна только администратору», данные не трогаем; 400/404 → сообщение + перечитывание; сеть/401 → сообщение без изменений), `annul-tx` оставлен фолбэком только для документа группы без `written_off`, `doCancelGroupLocal` — только для групп, которых нет на сервере; D — строка групповой ТН в «Документах» финансов собирается с `docTxId/tnHere/billHere` из `KBData.groups`, галочка «ТН у нас» уходит `PATCH /payments/transactions/{docTxId}` и переживает F5. Стенд 8127 (копия прод-БД): отмена БЕЗ F5 и ПОСЛЕ F5 — ровно 1 POST/200 и **0** `DELETE /payments/transactions/*`, документы 197→198→197 (удалён ровно id 426), оплаты 210→212 (остатки вернулись), карточки вышли из «Закрыто»; под `audit_manager` — 403 и ни одной мутации; часть D — PATCH 200 и галочка сохранена после F5. Деплой не выполнялся (решение владельца): до выкатки обеих половин запрет нажимать «Отменить» у групповых ТН в силе |
| 23.09 | дефект 7 реестра | `cbea561`; исполнитель воркер `builder` (`.qwen/agents`, `qwen3.8-flash`) | закрыт: `recalcCardMoney(card)` вместо инвалидации `remaining_kop`/`issued_total` в `doCancelLocal` и `doCancelGroupLocal`; расхождение со спекой осознанное — `card.amount` в board.js уже в копейках, поэтому `amount*100` завысило бы остаток в сто раз; при неизвестном `amount` остаток `null`, семантика `hasRemaining(null)` сохранена. `tools/v2_queue_scenario.py` на стенде 8127 (спека велит 8126, но там крутится код main без правок — отступление зафиксировано): ДО «FAIL после отмены карточка снова в очереди «На списание» (дефект 7) — history», `remaining_kop=None`; ПОСЛЕ «PASS … — pending», `remaining_kop=1822186` — совпало с сервером до копейки (карточка 553: total 57046.38, issued 38824.52, remaining 18221.86). Локальный путь отмены группы проверен синтетически (в копии БД групп нет). Побочно: тост об отмене печатает реальный остаток вместо «—». Не закрыто: симметричная инвалидация в `issueGroup` (путь выписки группы) |
| 23.09 | 10 | `1aed6a0` в ветке `v2-item10-notifications`, в main через `34558f9`; исполнитель `general-purpose` | закрыт: авто-`POST /notifications/read {ids:[]}` при открытии убран (3 открытия — 3 GET и 0 POST, бейдж и непрочитанные не меняются); «Прочитать все» рисуется только при непрочитанных и шлёт один POST; клик по уведомлению открывает объект синхронным кликом по рейке — дровер остаётся открытым и через 1.5 с (прежний путь через `location.hash` закрывал его отложенным `hashchange`), задача скроллится и получает фокус, клиент открывается дровером; удаление одного через существующий `DELETE /notifications/{id}` (`notifications_router.py:88`) с передачей фокуса на соседнюю корзину или колокольчик; уведомление без объекта — некликабельный `div` вне tab-порядка; Escape закрывает панель и возвращает фокус, `aria-expanded` переключается. Бэкенд не менялся; +6 тестов контрактов панели. Артефакты: `/tmp/v2-verify-int/toolout/b3*`. Находка вне пункта — дефект 8 реестра (фокус после `kb:reloaded`) |
| 23.09 | 18 | `c681eae` в ветке `v2-item18-infra`, в main через `eeeff45` + снятие автодеплоя в `e5e7727`; исполнитель `general-purpose` | частично: гейт свежести `site-v2` — `server_snapshot/tools/check_site_v2_fresh.py` (эталон собирается во временный каталог, сверка по sha1, коды 0/1/2, `--json`), подключён этапом в `scripts/check.sh` перед `stamp_assets --check` и в job `test` CI; при рассинхроне падает с русской подсказкой (обе ветки продемонстрированы) — он же доказал рассинхрон main после `cafc5a9` (boot-шаблон изменён без пересборки: расходятся `index.html` и `js/v2/boot.js`, `exit=1`), устранённый пересборкой `e5e7727`. Тесты кэш-политики `/v2` (`tests/test_v2_cache_headers.py`, 6): `no-cache` на HTML, `public, max-age=31536000, immutable` на css/js/fonts, контракт `V2_IMMUTABLE_PREFIXES`, непопадание политики на legacy, `/` → 302 `/v2/`. usage `deploy.sh` перечисляет все действия (`front/v2/back/restart/status/logs`). `nginx/snippets/v2-legacy-locations.conf` — реконструкция location'ов `/v2` и `/legacy`, помечена «требует сверки с боевым»; закрывает пробел, из-за которого `/v2/` в docker-пути молча отдаёт legacy `index.html` (`Dockerfile.frontend` копирует только `site/`), и дополняет `gzip_types` для `text/javascript` (в `nginx/http.conf:18` и `https.conf:55` его нет, а приложение отдаёт `.js` именно так). **Не сделано по решению владельца 23.09:** чистка CSP от `fonts.googleapis/gstatic` — сломала бы Manrope на замороженном legacy (локальных `@font-face` там нет, `site/index.html:10` тянет CSS с Google, CSP из `main.py` ставится и на `/legacy`, тесты это не ловят) — и автодеплой `deploy.sh v2` в `deploy-production`: выкатка основного фронта остаётся ручной. **Не проверено:** docker-путь (контейнеры не поднимали) и `nginx -t` (нет nginx на машине разработки), боевой конфиг не сверялся (SSH запрещён). Мусор `.mimosa` не удаляли — рекомендация: игнор скрытых путей в `collect_hashes()` `server_snapshot/tools/stamp_assets.py` (по образцу `IGNORED_PARTS` в новом гейте), на код выхода шум не влияет |
| 23.09 | 16 (серверная половина) | `d6bfd29` в ветке `v2-item16-server`, в main через `c1301c3` (слияние и ревизия в главной сессии); исполнитель `general-purpose` | частично: серверные страницы для `/kanban/cards` и `/payments/transactions` (дефект B11: 474 КБ и 132 КБ одним куском) — аддитивно и обратно совместимо: без `limit`/`offset` ответ прежний (плоский массив), с ними `{items,total,limit,offset}` + `X-Total-Count`; пороги взяты с единственного прецедента в репо (`GET /activity`: дефолт 50, потолок 200), валидация `Query(ge=/le=)` → 422, `total` одним `count(*)` по тому же фильтру, сортировка прежняя и тотальная (страницы не перекрываются), фильтры работают вместе со страницей; помощники в `db_utils.py`. +39 тестов (`tests/test_server_pagination.py`), pytest 544 passed; слияние с main без конфликтов (`payments_router.py` сошёлся с ролевыми гейтами пункта 17 и annul из `cafc5a9`). **Не сделано:** клиентская дозагрузка по скроллу (`board.js`/`api.js`) — без неё критерий «первый экран < 100 КБ» не закрывается; ротация `record_versions`/`activity_log` — по решению владельца 23.09 отложена до `.backup` боевой БД и отдельного окна (миграция безвозвратно удаляет строки журналов), лежит в ветке `v2-item16-server`: `git checkout v2-item16-server -- server_snapshot/migrations/0012_rotate_history_tables.py server_snapshot/tests/test_history_rotation.py`, перед применением **перенумеровать в 0014** (в main уже есть `0013_fill_deal_statuses.py`, номер 0012 пропущен) |
| 23.09 | вне пунктов: дровер и высоты плиток | `ba0f65b` | закрыт дефект, найденный верификацией пункта 10 и подтверждённый чтением кода: `KBBoard.open` писал `location.hash = '#board'`, отложенный `hashchange` → `render(true)` → `navigation.js:31-35` закрывал только что открытый `#kb-dialog`, из-за чего «Открыть сделку» с Пульта дня (`#day`) и из «Клиент 360» (`#clients`) захлопывало карточку сразу после открытия. Раздел переключается синхронным кликом по рейке, `queueMode`/`openCard` — после него, при отсутствующем пункте рейки оставлен фолбэк с открытием ПОСЛЕ `hashchange`; если уже на доске — клик не делается, поэтому путь уведомления не даёт двойного переключения (`railClicks=1`, `push=1`, back/forward адекватны). Попутно фокус возвращается в дровер после rAF оболочки (`render(true)` утаскивал его на `#shell-content`, который под модалкой inert). 37 браузерных проверок — 36 PASS, единственный FAIL — предсуществующий дефект 8. Там же: `contain-intrinsic-size` приведён к замеру и добавлен детерминированный тест коллизии id для P0 (`test_annul_does_not_touch_transaction_with_same_id`; мутационная проверка — 3 failed при откате реализации к поиску по `Transaction.id == group_id`) |
| 23.09 | сведение веток 3/10/18/16 | fast-forward main `d706234` → `9d4668d` (ветки `v2-item3-render-cost`, `v2-item10-notifications`, `v2-item18-infra`, `v2-item16-server`; worktree в `/private/tmp/crm-*`) | выполнено: потоки собраны в одну ветку, пересборка `site-v2` одна (`e5e7727`, правило одной пересборки), конфликтов при слиянии не было. Гейты в основном чекауте после fast-forward: `scripts/check.sh` **exit 0** (JS-синтаксис, data-handler 49/0, свежесть site-v2, штампы 37 ссылок/0 расхождений, pytest **544 passed**), профиль `CRM_TEST_SCHEMA=models` — 501 passed + 4 skipped; перепись ошибок на стенде 8127 — 0 pageerror и ровно 4 известных артефакта стенда; функциональный аудит 26 проверок / 1 провал-артефакт (пустые настройки почты); матрица дизайна 60/60. Дублирующий вариант серверного P0 (ветка `v2-p0-group-cancel`, `POST …/cancel-invoice`) в main НЕ вливался — вытеснен `cafc5a9`, оттуда взят только тест коллизии id. Деплой не выполнялся: прод на прежней сборке |

---

## Параллелизация пунктов (добавлено 22.09)

Рабочий каталог один на все сессии → **один держатель файла одновременно**.
Держатели на момент добавления: `shell-v2-board.js` и `shell-v2-prototype.html` — пункт 2.
Пункт 2 закрыт 23.09 — его держательство снято. Фактически `board.js` и
`prototype.html` на 01:20 23.09 правят сессии пунктов 3 (worktree
`/private/tmp/crm-item3`, грязные `mockups/shell-v2-board.css` +
`shell-v2-prototype.html`) и 4 (`/private/tmp/crm-item4`, грязный
`mockups/shell-v2-board.js`) — перед любой правкой доски сверять
`git worktree list` и `git status` в чужих worktree.

| Пункт | Основные файлы | Параллельно с 2 |
| --- | --- | --- |
| 3 | board.js, board.css, prototype.html | нет (board.js + html) |
| 4 | board.js | нет |
| 5 | board.js | нет |
| 6 | V2-UI-AUDIT (список паритета), fin только чтение | да; сценарии на стенде — в окно, когда 2 не верифицируется |
| 7 | board.js, css поиска | нет |
| 8 | board.js | нет |
| 9 | management.js, prototype.html, routers/ | нет (html; management.js делит с 10 и 15) |
| 10 | management.js, select.css | да (пока не стартовали 9/15) |
| 11 | board.js | нет |
| 12 | migrations/, routers/, management.js, board.js | нет |
| 13 | board.js, board.css | нет |
| 14 | fin.js, prototype.html (новые экраны) | нет (html) |
| 15 | management.js, prototype.html, routers/ | нет (html) |
| 16 | routers/, board.js, api.js | нет (board.js) |
| 17 | routers/, migrations/ или маппинг, tests/, fin.js | да; верификация pytest; если V7 = колонка-миграция — не применять к БД стенда во время чужой верификации |
| 18 | nginx/, .github/, scripts/, tools/deploy.sh, main.py (чистка CSP), .mimosa | да; браузер в критериях не нужен |

Общие ресурсы — правила для всех сессий:

1. Пересборка `site-v2` + `stamp_assets --check` — один человек в конце своего пункта;
   при одновременном финише двух пунктов позже финишировавший пересобирает за обоих.
2. Стенд 8126 и браузер (16 ГБ RAM): census/скриншоты/замеры — строго по одному
   одновременно; параллельные сессии разводят окна верификации.
3. Миграции к БД стенда не применять, пока другой пункт верифицируется на стенде.
4. В план и журнал — только добавлять свои строки и галочки, не переписывать чужие.
5. Параллельные коммиты в одном репо: поймали `index.lock` — повторить команду.

Очереди после освобождения файлов пунктом 2: board.js → один из 4/5/8/11,
затем 3/7/13/16; prototype.html → 14 либо 9/15 (эти двое плюс 12 ждут ещё
management.js после пункта 10).

Статус держателей на 23.09 01:30: пункт 2 закрыт (`54c184f`); пункт 17 закрыт
(`5828f2d`, `16646a3`); пункт 6 закрыт чтением кода (`3fc2c80`, `7267607`);
воркер P0 (часть B) завершён (`cafc5a9`) — writeoff_groups_router.py,
schemas.py, services/payments.py, tools/v2-boot-template.js снова свободны;
пункт 16 разблокирован по routers/. Держат дальше: 3 и 4 — board.js/board.css/
prototype.html через worktrees `/private/tmp/crm-item3|4` (+ у 3 часть A
хотфикса P0 по спеке), 10 — management.js, select.css, 18 — инфра.
Часть C хотфикса и клиентская ветка флагов групп — к следующему держателю
board.js после слияния worktree'ов 3 и 4.

---

## Дефекты доски, найденные пунктом 6 (23.09) — очередь к держателю board.js

Источник: отчёт воркера пункта 6 (чтение кода), штамп в `V2-UI-AUDIT` §6 п.5.
Порядок = приоритет; пункт 1 очереди — хотфикс, обгоняет пункты 4/5/8/11.
Пакет для держателя board.js после слияния worktree'ов 3/4 (части C и D
спеки P0 + дефект 7, план верификации) собран в
`docs/audits/BOARD-PACKAGE-SPEC-2026-09-23.md`; прогон после фикса —
`tools/v2_queue_scenario.py` (CRM_USER/CRM_PASS, свободное браузерное окно).

1. **P0, живой на проде**: отмена групповой ТН, выписанной в текущей сессии,
   шлёт `DELETE /payments/transactions/{group_id}` (`shell-v2-board.js:2244`
   — `serverTxId = g.id`, ветка отмены `:1806-1843`) и удаляет ПОСТОРОННЮЮ
   транзакцию с этим номером. Хотфикс: серверный эндпоинт отмены группы
   (`writeoff_groups_router.py` + unwind в `services/payments.py`) и вызов
   его клиентом вместо `annul-tx`; до деплоя хотфикса — организационный
   запрет нажимать «Отменить» у групповых ТН. Серверная сторона готова
   (`cafc5a9`: эндпоинт annul + invoice_transaction_id); клиентская часть A —
   у сессии 3 по спеке, часть C — у следующего держателя board.js.
   **Клиент закрыт 23.09** (`88d31a2` в ветке `v2-item3-render-cost`, в main
   через `a7e504c`): `serverTxId` берётся из `invoice_transaction_id` (настоящий
   id документа группы), отмена выписанной группы идёт
   `POST /writeoffs/groups/{id}/annul` → `reloadData()`, `annul-tx` оставлен
   фолбэком только для документа группы без `written_off`,
   `DELETE /payments/transactions/*` из ветки отмены группы исчез. Стенд-прогон
   на копии прод-БД (8127): `invoice_transaction_id=426` ≠ id группы 12; отмена
   БЕЗ F5 и ПОСЛЕ F5 — ровно 1 `POST …/annul` 200 и **0** DELETE, документы
   197→198→197 (удалён ровно 426), оплаты 210→212 (остатки вернулись), карточки
   вышли из «Закрыто»; под `audit_manager` — 403, тост «доступна только
   администратору» и ни одной мутации. **Прод остаётся уязвим до деплоя обеих
   половин** (`tools/deploy.sh back routers/writeoff_groups_router.py
   services/payments.py schemas.py` + `tools/deploy.sh v2`); запрет нажимать
   «Отменить» у групповых ТН до этого момента в силе.
2. Отмена групповой ТН после перезагрузки — ложная кнопка: `annul-tx` по
   групповому документу (`card_id=NULL`) не снимает `written_off` с группы и
   не возвращает карточки из «Закрыто» (`services/payments.py:60-141`),
   а UI рапортует «остатки возвращены». **Закрыт 23.09** тем же клиентским
   фиксом (см. п.1): серверный annul разматывает группу по состоянию БД, а тост
   «остатки возвращены N карточкам» звучит только после 200 и успешного
   `reloadData()`, причём N берётся из ответа сервера; проверено сценарием
   «отмена после F5» на стенде 8127.
3. «Списано со склада» не делает `duplicate_as_document`
   (`shell-v2-board.js:1350-1364`; legacy `site/js/writeoffs.js:736-770`) —
   копия документа не появляется в «Документах».
4. Нет массового «убрать сделку из списания» (`DELETE /payments/cards/{id}/writeoff`,
   legacy `writeoffs.js:389-420`); покрыто поштучной отменой ТН + возвратом этапа.
5. Нет «прикрепить к группе / выйти / распустить» (серверные роуты есть:
   `writeoff_groups_router.py:103,128,232`).
6. Бэк, держателя нет: `tests/test_package_b_money.py::test_clients_list_overdue_deals`
   падает в окне 00:00–03:00 MSK — `routers/clients_router.py:47` считает дату
   в UTC, тест в локальной зоне; падает и на чистом HEAD (проверено воркером
   17 через git archive). **Исправлен 23.09 (`20ab333`)**: сравнение через
   `constants.business_today()` (Europe/Minsk), роутер и тест берут границу
   из одной точки; проверено прогоном внутри окна границы (26 passed).
7. Клиент, держатель board.js: после отмены ТН (одиночной) карточка до ручной
   перезагрузки выпадает из очереди «На списание» в «Списано»/историю:
   `doCancelLocal` инвалидирует `remaining_kop`/`issued_total`
   (`shell-v2-board.js:1852-1854`), а `cardRemaining` на undefined возвращает
   null и `hasRemaining(null)` = false (`:98-115`) — фильтры очередей считают
   карточку без остатка. Воспроизведено 3/3 на стенде 8126 (карточка 553,
   частичная выписка + отмена, 23.09): POST и DELETE 200 за 0.0 с, диалог
   закрывается за 0.2 с, но после отмены `remaining_kop`/`issued_total` = null
   и карточка в истории. Фикс: после annul наполнять денежные поля из ответа
   сервера или пересчитывать локально из docs (issued = Σ docs,
   remaining = amount − issued); семантику `hasRemaining(null)` не менять
   без ревизии всех фильтров очередей. **Исправлен 23.09 (`cbea561`)**: вместо
   инвалидации — `recalcCardMoney(card)` (issued = Σ docs, issued_total =
   issued/100, remaining_kop = amount − issued с отсечкой нулём как в
   `services/card_money.py:44`) в `doCancelLocal` и `doCancelGroupLocal`;
   вызов после `splice`, чтобы отменённая запись не попала в сумму. Формула
   спеки `amount*100 − issued` НЕ использована: `card.amount` в board.js уже
   хранится в копейках, умножение завысило бы остаток в сто раз; при неизвестном
   `amount` остаток остаётся `null`, семантика `hasRemaining(null)` не тронута.
   Проверено `tools/v2_queue_scenario.py` на стенде 8127 (копия прод-БД):
   ДО — «FAIL после отмены карточка снова в очереди «На списание» (дефект 7) —
   history», `remaining_kop=None`; ПОСЛЕ — «PASS … — pending»,
   `remaining_kop=1822186`, что совпало с ответом сервера по карточке 553 до
   копейки. Ограничение (следствие формулы спеки, не реализации): при складском
   списании без копии в «Накладных» локальный остаток завышен до ближайшего
   `reloadData` — пересекается с п.3–5. Симметричная инвалидация денег в
   `issueGroup` (путь ВЫПИСКИ группы) не правилась — в следующий пакет держателя
   board.js.
8. Клиент, держатель board.js (найден 23.09 верификацией пункта 10,
   предсуществующий): фокус проваливается в `body` после `kb:reloaded` —
   обработчик перезагрузки зовёт `openCard(fresh)`, содержимое дровера
   перестраивается (~244 мс после открытия, +2 мутации) и сфокусированная
   кнопка закрытия заменяется. Воспроизводится и на нетронутом пути клика по
   плитке (`activeElement=BODY`), поэтому к правкам 23.09 отношения не имеет;
   путь `KBBoard.open` после `ba0f65b` фокус возвращает и дефект не проявляет.
   Лечение: сохранять и возвращать фокус при перерисовке дровера (по `data-*`
   или роли последнего фокусного элемента) либо не перестраивать дровер целиком
   на `kb:reloaded`.
9. Клиент, держатель board.js (найден 23.09 при ревью P0, косметика): строка
   «Групповая ТН …» в списке «Накладные» дровера рисуется по `c.groupId` без
   проверки `writtenOff`, поэтому у разматанной группы (annul не снимает
   `writeoff_group_id` с карточек) и у группы в «Сборке» видна строка с пустым
   номером и кнопкой «Отменить». Клик теперь безопасен (сервер вернёт 400,
   данные перечитаются), но строка-пустышка осталась; рядом с п.5 реестра.
