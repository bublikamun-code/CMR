# Рабочий план v2 по пунктам (по одному пункту на сессию) — 22.09.2026

Порядок работы, согласованный с владельцем 22.09: **каждый пункт ниже выполняется
в отдельной новой сессии**; сессия начинает с чтения этого файла и
`docs/SESSION-BRIEF-<дата>.md`, делает ровно свой пункт, прогоняет его критерий
готовности, коммитит атомарно и помечает пункт `[x]` здесь. В конце — сведение
(пункт S): один прогон всех проверок и обновление документации.

Общие правила для любой сессии (из AGENTS.md, не повторять в пунктах):
- правки фронта v2 — только `server_snapshot/tools/mockups/` и шаблоны
  `server_snapshot/tools/v2-{api,boot}-template.js`; `site-v2/` руками не править;
  пересборка `cd server_snapshot && python3 tools/build_site_v2.py` +
  `python3 tools/stamp_assets.py --check` — одним человеком в конце пункта;
- CSP: без `onclick`/eval, только `data-*` + `addEventListener`;
- комментарии и тексты UI по-русски; идентификаторы по-английски;
- прод — только чтение; пуш и деплой — по явному слову владельца;
- порядок деплоя волны 23.09 (критично): `tools/deploy.sh` миграции не накатывает,
  а `Base.metadata.create_all` добавляет только таблицы, не колонки. После выкатки
  кода с `users.is_active`, но **до** рестарта PM2 обязателен
  `CRM_DATA_DIR=... python3 migrate.py` — иначе каждый аутентифицированный запрос
  падает в 500 (`no such column: users.is_active`);
- стенд: копия прод-БД в `/tmp/v2-audit-prod/`, порт 8126, вход
  `audit_admin` / `Audit-2026!` (как поднять — `docs/audits/V2-PLAN-RECON-2026-09-22.md`, §12);
- не трогать: архивный клон `/Users/yaroslav/cmr-svetvdome`, порт 8125 (чужой стенд),
  `docs/SESSION-BRIEF-*` (служебные, не коммитятся).

Источники фактов: `docs/audits/V2-PLAN-RECON-2026-09-22.md` (сверка плана с git),
`docs/audits/V2-UI-AUDIT-2026-09-22.md` (визуал/юзабилити/производительность),
расследование зависания комбобокса — `/tmp/supplier-freeze/` (отчёт приложен к брифу).

---

## Блок 0 — завершается в текущей сессии (не пункт для новых)

- [x] 0.1 Фикс бесконечной петли `MutationObserver` в дровере клиента
      (`shell-v2-insights.js`, метка «в работе» до вставки блока). Коммит `5eff3ef`.
      Подтверждено переписью: 1 запрос `/clients/{id}/balance` на весь обход
      (по F1 было 20 544), 0 `pageerror`.
- [x] 0.2 Перезагрузка данных без F5 (`applyInPlace`, `KBData.reload`, `kb:reloaded`),
      создание сделки с доски (`#kb-new-card` + форма-диалог + ветка `card-create`),
      идемпотентная загрузка модулей (`inject` с реестром, `showLogin` без дублей).
      Коммит `81a23f2` (+ `fca52d1` — шаг 6k аудита и автоподтверждение
      модального `#kb-confirm`, без него аудит вставал на 6e).
- [ ] 0.3 Фикс зависания на комбобоксе поставщика в «Закупке» (`.kb-sup-combo`,
      `initCombo`) — по отчёту расследования `/tmp/supplier-freeze/`.
- [ ] 0.4 Одна пересборка `site-v2` со всеми правками блока 0 + `stamp_assets --check`.
      Промежуточно: сборка со всеми правками 0.1–0.2 сделана (stamp: 0 расхождений),
      финальная — после 0.3; `site-v2` в git пока не закоммичен.
- [ ] 0.5 Верификация на стенде 8126: `v2_error_census` (цель 0 ошибок JS, кроме
      известных CSP-blob до пункта 1), `v2_functional_audit` (шаги 6a и 6k PASS),
      сценарии: клиент не виснет; создать сделку без F5; восстановить из корзины
      без F5; комбобокс поставщика не виснет (WebKit + Chromium).
      Промежуточно (после 0.1–0.2, до фикса 0.3): census — 0 `pageerror`,
      1 запрос `/balance`, 0 аномалий DOM, 0 дыр в KBData
      (`/tmp/v2-audit-prod/toolout/census-block0/`); functional_audit — 26 проверок,
      **6a и 6k PASS**, провалы только 7 (на стенде не заполнены настройки почты)
      и 10 (14 CSP-blob миниатюр). Осталось: сценарии корзины и комбобокса.
- [ ] 0.6 Атомарные коммиты: фикс петли; пакет reload+создание+boot; фикс комбобокса;
      пересборка site-v2. Сделаны первые два + инструментарий аудита
      (`5eff3ef`, `81a23f2`, `fca52d1`).
- [ ] 0.7 Бриф следующей сессии `docs/SESSION-BRIEF-<дата>.md` + коммит этого плана.
      Сам план закоммичен в `da97b25`.

## Пункты для новых сессий

### 1. [x] CSP: разрешить `blob:` для изображений
Правка: `server_snapshot/main.py:194` — `img-src 'self' data: https:` → добавить `blob:`.
Зачем: 14 миниатюр фото входящих накладных битые с загрузки страницы
(`V2-UI-AUDIT` F2; census: 14 ошибок CSP + 14 провалов `transport: csp`).
Критерий: `v2_error_census.py --base http://127.0.0.1:8126/v2/` → «Ошибки JS: 0»;
скриншот «Входящие накладные → НДС и файлы» с видимыми миниатюрами (на стенде фото
есть: 37 изображений скачаны в `/tmp/v2-audit-prod/uploads`).
Тест: добавить проверку заголовка CSP в `server_snapshot/tests/` (сейчас теста CSP нет).
Уточнение критерия (замер 22.09 на стенде, `/tmp/v2-audit-prod/toolout/census-block0/`):
после фикса `blob:` в переписи останутся 4 console-сообщения, к CSP не относящиеся —
1× `400 POST /email-parser/sync` («Настройки почты не заполнены» — на стенде почта
не настроена, не баг v2) и 3× «An invalid form control with name='' is not
focusable» в админке (скрытое required-поле в форме; отдельная мелочь, не пункт 1).
`pageerror` сейчас 0 — вот это и есть рабочая планка «Ошибки JS: 0».
Деплой — отдельное решение владельца.

### 2. [x] Производительность взаимодействий доски
Правки в `tools/mockups/shell-v2-board.js`: убрать полные `refresh()` из обработчиков
диалога (галочки закупки ~:1937, поля ~:1916, «ТН у нас» ~:1843) — точечно обновлять
плитку + `updateColumnHead()` + `renderQueue()`; рассылать `kb:documents`/`kb:groups`
только при реальном изменении документов/групп. В инлайн-скрипте прототипа
(`shell-v2-prototype.html:1258-1292`) собирать строки одним HTML вместо
`insertAdjacentHTML` в цикле.
Критерий: замер скриптом (образец `/tmp/v2-audit-prod/scripts/s3_perf.py`): одна
галочка → мутации DOM < 300 (сейчас 2092), long task < 30 мс (сейчас 60–69),
1 PATCH; перерисовка финансов не вызывается от галочки.

### 3. [x] Стоимость отрисовки: ленивые финансы + дешёвые плитки
Правки: не отрисовывать скрытый `#view-fin` до первого входа в раздел (сейчас
14 839 из 20 843 узлов DOM создаются сразу); `content-visibility:auto` +
`contain-intrinsic-size` на `.kb-card` (`shell-v2-board.css:70-77`); SVG-счётчики
плитки (`shell-v2-board.js:146-178`) → один `<symbol>` + `<use>`.
Критерий: DOM после входа < 8 000 узлов; SVG в доске < 100; матрица
`v2_design_matrix.py` — 0 горизонтальных скроллов и 0 наложений на всех ширинах/темах.
Итог 23.09: закрыт, но SVG-иконки плитки переведены не на `<symbol>`+`<use>`, а на
CSS-маски — `<use>` оставляет по элементу `<svg>` на экземпляр и критерий «< 100»
не закрывает в принципе. Подробности в журнале.

### 4. [x] Drag&drop: троттлинг и обратная связь
Правки в `shell-v2-board.js`: `placeDrop()` не чаще одного раза на кадр (флаг в rAF),
кэш `getBoundingClientRect()` плиток на время перетаскивания, убрать дублирующий вызов
из `scrollDrag()` (`:246-312`); после успешного переноса — тост (сейчас молча, F8).
Критерий: трейс перетаскивания без пачек layout-пересчётов; тост виден; статус
сохранён (`PATCH /kanban/cards/{id}/status` один).
Итог 23.09: закрыт в `7ff3a52`. Критерий трейсом не доказан — браузерное окно
не было занято замером; перемерить через `tools/v2_drag_perf.py` при следующем
подходе к доске (строка «не проверено» в журнале за 23.09).

### 5. [x] Поле почты на первой вкладке карточки (запрос владельца 22.09)
Добавить в «Обзор» дровера (`shell-v2-board.js`, секция полей сделки) поле
`sender_email`: показ текущего значения + правка с сохранением через существующий
`PATCH /cards/{id}` (поле уже в модели и схемах; в БД 741 карточка с `sender_email`).
Связать с вкладкой «История → Письма отправителя» (пункт 9): после сохранения почты
письма подтягиваются по новому значению.
Критерий: значение видно и редактируется; после сохранения уходит один PATCH;
скриншот до/после; census без новых ошибок.
Итог 23.09: закрыт в `7ff3a52`; одними правками дровера критерий не достигался —
`PATCH /cards/{id}` молча игнорировал `sender_email` (реестр дефектов п.11), бэк
починен в `68baf04`.

### 6. Списание в финансах НЕ создавать (решение владельца 22.09)
Не добавлять вкладку «Списание» в финансы и не переносить legacy-экран: списание
живёт очередями «На списание» / «Списано» на доске (они работают). Вычеркнуть
«Финансы: вкладка Списание» из списка паритета (`V2-UI-AUDIT` §6 п.5 — помечено
как не нужное). Проверить, что в финансах нет мёртвых кнопок/пунктов списания и что
очереди на доске покрывают сценарии legacy (выписка, отмена, групповая ТН).
Критерий: в финансах нет UI создания списания; сценарий «на списание → выписать ТН →
отменить» проходит на стенде без обращения к финансам; правка списка паритета
в `V2-UI-AUDIT` закоммичена.

### 7. [x] Поиск на доске: один инпут, один крестик (запрос владельца 22.09)
Сейчас при открытии поиска появляется вложенное «внутреннее окошко» с синим
выделением и два крестика (сброс внутри + закрытие снаружи). Сделать один инпут
с одним крестиком-сбросом и одним способом закрыть (Esc или тот же крестик);
пустой результат — подсказка «ничего не найдено» + кнопка «Сбросить фильтры»
(сейчас четыре плашки «Пусто» без подсказки, `V2-UI-AUDIT` D2).
Критерий: скриншот открытого поиска до/после; в DOM один `input` поиска и один
крестик; пустой поиск показывает подсказку и сброс; поиск по-прежнему фильтрует
(190 → N плиток).
Итог 23.09: закрыт в `7ff3a52`, скриншоты до/после на момент записи не сняты
(строка «не проверено» в журнале). Тот же паттерн «капсула + вложенное поле +
крестик внутри» остался в поиске финансов (`shell-v2-prototype.html`,
`.fin-search-field`/`fin-search-close`) — визуально D2 там не закрыт, долг
держателю прототипа.

### 8. [x] История карточки: письма отправителя
Вызывать `loadRelatedEmails()` при открытии вкладки «История» (сейчас вызывается
только из `linkEmailToCard()`, плейсхолдер «Загрузка…» вечный, запросов 0 — F3);
различать состояния «писем нет» и «ошибка загрузки» с кнопкой повтора.
Критерий: при открытии вкладки уходит `GET /email-parser/related/{card}`; через 2 с
плейсхолдера нет; скриншот обоих состояний (карточка с `sender_email` и без).
Итог 23.09: закрыт в `7ff3a52`. Чтение `GET /email-parser/related/{card_id}`
намеренно оставлено открытым для всех ролей — нужен этому пункту (границы гейтов —
блоком в пункте 17).

### 9. [x] Ролевая модель в UI
Под ролью manager сейчас виден весь раздел «Управление» с кнопками правки
справочников и пользователей (F5). Скрыть админ-карточки/кнопки по роли из
`/auth/me`; сверить с бэком: мутации под manager должны давать 403 (проверить и,
если бэк пропускает, — отдельным пунктом закрыть на сервере).
Критерий: под `audit_manager` нет кнопок «+ Новый пользователь/магазин/статус»,
«Изменить», «Синхронизировать»; попытка прямого POST/PATCH из консоли → 403;
под `audit_admin` всё на месте.
Итог 23.09: закрыт — бэк в `ec5fde1` (гейты + `users.is_active`), клиент в `353d028`
(механизм `data-admin-only` + `applyRoleVisibility`), досочные метки в `7ff3a52`
(7 кнопок). Границы гейтов и то, что сознательно оставлено открытым, — блоком в
пункте 17.

### 10. [x] Уведомления: без авто-прочтения, со ссылками
Убрать авто-`POST /notifications/read {ids:[]}` при открытии; добавить кнопку
«Прочитать все», ссылки/переход к объекту уведомления и удаление одного
(F9; legacy `site/js/notifications.js:84-104,145`).
Критерий: открытие колокольчика не меняет статусы; клик по уведомлению открывает
сделку/задачу; «Прочитать все» помечает; скриншоты.

### 11. [x] Мелочи дровера и текстов
Висячий пустой label «Условия оплаты» (F6); декодирование percent-имён вложений
+ tooltip на полное имя (F7); убрать «Демо-» формулировки на живых данных (F10).
Критерий: скриншоты «Обзор», «Закупка/вложения», «Накладные» без брака; census без
новых ошибок.
Итог 23.09: закрыт в `7ff3a52` (демо-тексты и «Демо-» формулировки — `353d028`,
9 мест, включая текст «Здесь демо просмотра, редактирование не реализовано» внутри
строк журнала на боевой базе). Имен вложений одного percent-декодера не хватило:
в dev-копии БД 25 из 92 имён приходят в MIME encoded-word (`=?windows-1251?B?…?=`) —
добавлен декодер RFC 2047, реестр п.20.

### 12. [x] Справочник статусов сделок на проде
Аддитивная миграция заполнения `deal_statuses` из фактических статусов карточек
(по образцу 0011 для магазинов): сейчас `/dictionaries/statuses` = `[]`, доска живёт
на клиентском хардкоде `STATUS_MAP` (`V2-PLAN-RECON` N1). Убрать зависимость доски
от хардкода после заполнения. Применение на проде — решение владельца (после `.backup`).
Критерий: на копии БД `/dictionaries/statuses` возвращает 6 статусов; админка v2
показывает их и даёт переименовать; доска рисует те же колонки.

**Статус 23.09: серверная половина закрыта** (`e77b86b` — миграция
`migrations/0013_fill_deal_statuses.py` + `tests/test_migration_0013_deal_statuses.py`).
Критерий по API на копии БД выполнен: 6 статусов в порядке `position` с цветами,
`PATCH /dictionaries/statuses/{id}` переименовывает (200). Клиентская половина
НЕ сделана, и **до неё миграцию нигде не применять — ни к проду, ни к общему
стенду 8126**: непустой `/dictionaries/statuses` переключает v2 с хардкода на
ветку словаря (`tools/v2-boot-template.js:124`), где «Закрыто» получает
`role='board'` — на доске появляется пятая колонка со 179 закрытыми сделками, а
её id `done` сталкивается с хардкодом «Списано» в списке этапов
(`tools/mockups/shell-v2-board.js:1289`) и с очередью списания, где
`stage === 'done'` означает «Списано» (`shell-v2-board.js:569`). Клиентской
половине заодно решить: переименование статуса не переписывает `cards.status`,
поэтому переименованная колонка оставит старые карточки без колонки.

Спецификация клиентской половины — `docs/audits/V2-ITEM12-CLIENT-SPEC-2026-09-23.md`:
принятые решения Р1–Р6 (роль «Закрыто» = `archive`, разведение id `closed` и
`done`, запрет переименования канона в UI, `canAssign` в drop и клавиатуре,
`is_active` в админке, отмена фолбэка «неизвестный статус → Новый запрос»),
работы по файлам A–E с держателями, критерий готовности 1–9 и таблица As-built.

**Статус 23.09 (вечер): пункт закрыт целиком — клиентская половина сделана**
(As-built в спеке; коммиты `8d24f13` — части A+B+C+E, `3c8473c` — часть D,
далее одна пересборка site-v2). Итог: доска живёт на серверном справочнике
(те же 4 колонки в обеих темах, «Закрыто» — архив/«Списано», не колонка);
очередь «Списано» сохраняет состав (192 = 177 закрытых + 15 дописанных);
в админке — полный список из API (включая неактивных), имя канона read-only
с подсказкой, переключатель `is_active` (снятие убирает колонку, карточки
остаются в достроенной колонке Р6, а не в «Новом запросе»); неканонический
статус создаётся колонкой, но не назначается ни селектом, ни drag&drop, ни
прямым путём (тост Р4), 0 4xx; переименование неканонического работает;
census 0/0 с планкой известных артефактов; перф-эталон пункта 2 совпал
(долгий таск «обратной» галочки воспроизводится и на старой сборке — средовой
шум, не регрессия); регрессия блока 0 OK; pytest 878. **Миграция 0013
разблокирована для релиза**: выкатывать одним пакетом — статика v2 + бэк с
0013, на проде `CRM_DATA_DIR=… python3 migrate.py` ДО рестарта PM2, по слову
владельца (до этого — запрет применять 0013 остаётся в силе). Находка вне
пункта: сервер валидирует `cards.status` по канону в ответе — неканонический
статус на карточке (только прямой вставкой в БД) роняет `GET /kanban/cards`
в 500; учтено в As-built спеки.

### 13. [x] Паритет-пакет 1: виды и фильтры доски
Режимы «Список» и «Архив» (закрытые сделки сейчас не видны нигде, кроме финансов),
фильтры по приоритету и типу оплаты («Безнал (БН)»), кнопка «Сбросить»
(`V2-UI-AUDIT` §6 п.1–3; legacy `site/js/kanban.js`, `site/index.html`).
Критерий: сравнение скриншотов с legacy (`/tmp/v2-audit-prod/shots/80-legacy-board.png`);
режим «Архив» показывает закрытые; фильтры сужают набор; сброс возвращает всё.
Этому держателю переданы сохранённые виды из пункта 15 (серверных роутов нет,
реализация в localStorage) — спецификация в блоке «Статус 23.09» пункта 15.

Итог 23.09: закрыт (код и пересборка — коммит с спекой
`docs/audits/V2-ITEM13-SPEC-2026-09-23.md`, исполнитель воркер `builder`,
ревью и верификация — главная сессия). Режимы «Список»/«Архив» — общая таблица
25 строк/страницу (просроченные наверх, новые сверху), «Архив» — живой фильтр
`stage==='done'` без снимка; фильтры приоритета и типа оплаты в тулбаре,
кнопка «Сбросить», подсказка «Ничего не найдено» учитывает новые фильтры;
сохранённые виды в localStorage по спецификации пункта 15 (ключ
`v2.savedViews.board.<userId|anon>`, состав `{id,name,q,store,statuses[],
assignee,mode}`, событие `kb:saved-view-applied`, тихая потеря
несуществующих фильтров, try/catch на `file://`). Осознанные отступления:
- **«Безнал (БН)» — клиентский фильтр по сырому `store_location`**
  (`storeRaw` в boot-маппинге): отдельного поля оплаты у карточки нет, в legacy
  «БН» — псевдо-магазин (`site/js/constants.js:12-16`); «Наличные» — магазин
  указан и не «БН», без магазина не попадает никуда (как в legacy). На копии
  прод-БД карточек с «БН» ноль, в справочнике магазинов «БН» отсутствует —
  опция фильтра остаётся для данных legacy-эпохи и сидов стенда.
- **Фильтр приоритета включает «Без приоритета» (0)** — в legacy его нет
  (`index.html:141-146`), но на проде все 391 активных карточек имеют priority 0,
  без этой опции фильтр всегда пуст.
- **Сортировка «Списка»: просроченные наверх** — по спеке; legacy фактически
  ставит их в конец (`kanban.js:1087-1092`, `aOverdue ? 1 : -1`).
- **Состав сохранённых видов аддитивно расширен `priority` и `pay`** — иначе вид
  не фиксировал бы новые фильтры; `statuses[]`/`assignee` хранятся как задел
  (`[]`/`null`), UI к ним по-прежнему нет.
- **Пересечение «Архива» с очередью «Списано» осознанное**: «Списано» —
  операционная очередь (`done` + полностью выписанные без смены статуса,
  `board.js:984-988`), «Архив» — реестр всех закрытых; отмена ТН возвращает
  карточку из архива при ближайшем `reloadData` (семантика живого фильтра
  проверена: мягкое удаление закрытой карточки уменьшает архив 177→176 без
  правок кода).
- Legacy-фильтры «Сумма от/до» и «Клиент» НЕ переносились — в формулировке
  пункта их нет (остаются паритет-долгом, держателя нет).
Верификация: `bash scripts/check.sh` exit 0 (pytest 878); перепись на стенде
8127 (`--dark`): 0 dom/дата-аномалий, консоль — ровно 8 известных артефактов
стенда; функциональный аудит 26 проверок / 1 средовой провал (почта); браузерные
сценарии **29/29 PASS** (`/tmp/item13-scenarios.json`): список 393/25-строк/
пагинация/открытие дровера, архив 177 все «Закрыто», приоритет и оплата сужают
(1 и 149 на доске), сброс возвращает, восстановление из
`#board?priority=3&pay=bn&mode=list`, вид сохраняется/применяется после F5/
удаляется, `kb:saved-view-applied` с detail, тихая потеря магазина по
`kb:dictionaries-changed` и битого вида, ширины 1280/1000/768 — 0 горизонтальных
переполнений (колонки прячутся медиазапросами); скриншоты обеих тем и диалога
видов в `gui-test-screenshots/2026-09-23-item13-*.png`, сверка с
`80-legacy-board.png` — режимы/приоритет/сброс на месте.

### 14. [x] Паритет-пакет 2: финансы и задачи
Вкладка «Контроль»/дебиторка в финансах; экспорт CSV документов (экспорт реестра
оплат уже есть и работает); отдельный раздел «Календарь»; фильтры задач
«Все / Просроченные»; карточка настроек «Внешний вид» (`V2-UI-AUDIT` §6 п.4,7–9,11).
Критерий: скриншоты новых экранов в обеих темах; экспорт скачивает файл; census чисто.

Итог 23.09: закрыт в `353d028` с двумя оговорками по объёму — осознанными решениями
владельца/главного агента:
- отдельный раздел «Календарь» НЕ добавлялся: календарь уже живёт в «Пульсе дня»,
  дубль был убран раньше намеренно; там же боевые данные вместо демо (реестр п.14);
- карточка настроек «Внешний вид» не потребовалась: тема переключается `#theme-btn`
  и запоминается в `kb-theme`;
- экспорт CSV документов — клиентская генерация, не серверный `GET /kanban/export/csv`:
  тот отдаёт сделки, а не документы (нет номера и даты ТН, номера счёта, флагов
  возврата и печати) и не уважает фильтры вкладки.
Скриншоты новых экранов в обеих темах на момент записи не сняты (строка
«не проверено» в журнале).

### 15. Паритет-пакет 3: настройки и админка
Workflows (11 роутов), кастомные объекты (9), сохранённые виды, вебхуки (6),
удаление/деактивация пользователя, авто-синк почты по таймеру
(`V2-PLAN-RECON` §4; legacy `site/js/settings.js`, `site/js/admin_page.js`).
Критерий: каждый экран открывается, CRUD проходит на стенде, census чисто;
права — только админ (связка с пунктом 9).

**Статус 23.09: закрыт частично — `[x]` не ставить.** Сделано (`ec5fde1`, `353d028`,
`68baf04`, `5ca4939`, `802dad9`): экраны и CRUD админки — пользователи (пилюля
«Активен/Отключён», деактивация `PATCH /auth/users/{id}` телом `{"is_active": bool}`,
удаление, защита «себя не тронуть» на двух уровнях), вебхуки (все 6 роутов, включая
тест с русским разбором SSRF-ответа и жест «Снять подпись» = `{"secret": null}`),
кастомные объекты (типы и поля, 6 роутов из 9 + добавленные сегодня
`PATCH /custom/objects/{id}` и `PATCH /custom/fields/{id}`), автоматизации (9 роутов
из 11), формы правки для всех четырёх.

Не сделано, осознанно:
- **сохранённые виды** — серверных роутов для них нет вообще (`models.SavedView`
  упомянут только в чистке при удалении пользователя), решено хранить в localStorage;
  задача передана держателю `board.js` вместе с пунктом 13. Спецификация: ключ
  `v2.savedViews.board.<userId>`, состав `{id, name, q, store, statuses[], assignee,
  mode}`, применять с записью адреса в формате, который доска уже парсит, событие
  `kb:saved-view-applied`, тихая потеря несуществующих фильтров по
  `kb:dictionaries-changed`;
- **авто-синк почты клиентским таймером** — не нужен: на сервере внешний cron
  (`server_snapshot/scripts/watchdog_crm.sh:46-58` → `POST /email-parser/sync-all`
  с `X-Cron-Token`, `cron_router` зарегистрирован в `main.py:96-99`). Клиентский
  таймер legacy был избыточным вторым путём; в карточке «Почта» вместо него русское
  пояснение;
- **создание триггера/шага сценария** — `TriggerCreate`/`StepCreate` принимают
  `config` произвольным `dict` без схемы, а роута запуска сценария на сервере нет
  (только журнал запусков); изобретать структуру, которую никто не потребляет,
  не стали;
- **записи кастомных объектов** (`/custom/objects/{id}/records`,
  `/custom/records/{id}`) на экран не выводились — это рабочие данные, а не
  конфигурация.

### 16. Сервер: пагинация и гигиена журналов (клиентская половина закрыта 23.09)
Серверные страницы для `/kanban/cards` и `/payments/transactions` (дефект B11:
474 КБ и 132 КБ одним куском) + точечная дозагрузка в клиенте; ротация
`record_versions`/`activity_log` (очистки нет вовсе; 1456/1638 строк и растут).
Критерий: первый экран доски грузит < 100 КБ; дозагрузка по скроллу работает;
миграция ротации аддитивна и идемпотентна.

### 17. Роли на бизнес-операциях и «Оригинал ТН возвращён»
V11: `require_role` на `DELETE /payments/transactions/{id}` и аналогичных
(решение владельца по матрице ролей); V7: либо колонка `originals_returned`
(миграция + схемы + API), либо легализовать маппинг на `is_invoice_doc`, устранив
конфликт с «ТН у нас» в финансах (`V2-PLAN-RECON` §6).
Критерий: тесты доступа в `server_snapshot/tests/` покрывают матрицу; конфликт
семантик устранён и задокументирован.

**Матрица ролей — дополнения 23.09 (решения владельца и главного агента):**
- **V17: роспуск группы списания — только админ.** `DELETE /writeoffs/groups/{id}`
  закрыт `require_role("admin","superadmin")` тем же механизмом, что соседний
  `/annul` (`1a87677`, 13 тестов). **Прикрепление и выход из группы остались
  открытыми любой роли намеренно** — это рабочая операция менеджера и склада;
  граница закреплена негативным тестом, чтобы следующий исполнитель не закрыл их
  «за компанию». Клиент отразил меткой `data-admin-only` на кнопке «Распустить»
  (`7ff3a52`).
- **Поставщики — операционная сущность, не админская.** Первая версия гейта на
  `POST/PATCH /suppliers` сломала четыре существующих теста
  `tests/test_data_integrity.py` (они прогоняют переименование поставщика под
  `manager`, проверяя синхронизацию снимков `supplier_name` в накладных) — гейт снят,
  тесты НЕ переписывались. Правило на будущее: если новый ролевой гейт роняет
  существующие тесты, оперирующие ручкой под не-админом, — это сигнал пересмотреть
  гейт, а не переводить тесты на admin-заголовки.
- **`POST /email-parser/sync` закрыт админом** (`ec5fde1`) — кнопка
  «Синхронизировать» по критерию пункта 9 скрывается от менеджера. Во v2 sync
  вызывается только этой кнопкой, поэтому консольного шума 403 не будет; клиентский
  автотаймер legacy у не-админа просто перестанет срабатывать (legacy заморожен).
  Автоимпорт не остановится: серверный cron живёт отдельно (`cron_router`,
  `X-Cron-Token`). Чтения не закрыты: `GET /email-parser/related/{card_id}` нужен
  пункту 8, `GET /email-parser/settings` нужен legacy.

### 18. Инфраструктура v2
Внести боевой nginx-конфиг в репо с location'ами `/v2` и `/legacy` (сейчас в репо их
нет вовсе); CI: сборка и выкатка v2 (сейчас `deploy-production` зовёт только
`tools/deploy.sh front`); гейт на свежесть `site-v2` в `scripts/check.sh`;
тест кэш-политики `/v2` (`main.py:214-236`); чистка устаревшего CSP
(`fonts.googleapis/gstatic`); usage-строка `deploy.sh:203`; мусор
`server_snapshot/js/.mimosa/` (`V2-PLAN-RECON` §8, N5–N7).
Критерий: `docker compose`-путь отдаёт `/v2/` корректно; CI-джоба v2 зелёная;
`scripts/check.sh` падает на рассинхроне mockups↔site-v2.

### S. Сведение (последняя сессия)
Прогнать всё разом: pytest оба профиля, `stamp_assets --check` (оба), `node --check`,
`v2_error_census` (0 ошибок), `v2_functional_audit` (все шаги PASS), матрица
(0 скроллов/наложений), сквозной обход ролей admin/manager, адаптив 768–1920,
обе темы. Обновить: чек-лист `V2-FIX-PLAN-2026-09-19.md`, `V2-UI-AUDIT`,
`docs/STATUS.md`, `docs/audits/INDEX.md`. Сверить с владельцем: деплой, пуш,
отзыв PAT, домен/HTTPS, офсайт-бэкап. Итоговый отчёт владельцу одним документом.

---

## Журнал выполнения (заполняется по мере закрытия пунктов)

| Дата | Пункт | Сессия/коммит | Статус |
| --- | --- | --- | --- |
| 22.09 | 0.1 | `5eff3ef` | закрыт, подтверждено census (1 запрос `/balance`, 0 `pageerror`) |
| 22.09 | 0.2 | `81a23f2` + `fca52d1` (аудит: шаг 6k, автоподтверждение `#kb-confirm`) | закрыт, 6a и 6k PASS на стенде 8126 |
| 22.09 | 0.3 | проверка на сборке `3c9d008`: карточка 1058 → «Закупка» → комбобокс поставщика (ввод текста + клик по подсказке) — страница отвечает, 1 `PATCH /checklists`, шквала `/balance` нет; A/B расследования (`/tmp/supplier-freeze/`, repro10–12): старая сборка зависает детерминированно 3/3 со шквалом `GET /clients/{id}/balance` (1961–7294 запросов), новая — 0 зажиганий за 12+ прогонов; механизм = петля дровера клиента из 0.1, код комбобокса не дефектен, записей-шквала в БД нет; правок не требуется, нужен деплой собранного | закрыт |
| 22.09 | 0.4–0.6 | `3c9d008` — пересборка `site-v2` закоммичена в main; верификация на стенде 8126: stamp 0 расхождений, census без новых ошибок, функциональный аудит 26 проверок / 2 провала-артефакта стенда, шаги 6a и 6k PASS, восстановление из корзины без F5 | закрыт |
| 23.09 | деплой блока 0 | `tools/deploy.sh v2` (статика, без рестарта); прод на штампе `4d896ae8e3` = локальному, маркеры фиксов на проде проверены curl'ом; `/` → 302 `/v2/`, `/legacy` → 200, кэш `immutable` | выполнен |
| 22.09 | 1 | `b361fd9` (main.py + tests/test_security_headers.py) | закрыт: `img-src 'self' data: blob: https:` (main.py:194), blob: только в img-src; перепись `census-item1`: js_errors 18→4 (остались 4 артефакта стенда — 400 `email-parser/sync` и 3 «invalid form control» в админке), net_failures 15→1 (14 провалов `transport: csp` ушли), 0 pageerror, 0 DOM/дата-аномалий; `s12_csp_blob_thumbs`: 14/14 blob-миниатюр загружены в обеих темах, 0 отказов CSP, скриншоты `incoming-thumbs-{light,dark}.png`; тест CSP 5 passed, негативная проверка (без blob:) падает; pytest 414 / 410+4skip; регрессия блока 0 OK. Деплой — решение владельца; 22.09 владелец выбрал «только v2»: back НЕ задеплоен, CSP прода всё ещё без `blob:` |
| 22.09 | деплой v2: фикс двойного меню комбобокса поставщика | `75a8682` + `tools/deploy.sh v2` (статика, без рестарта) | выполнен: прод на штампе `7a8311f788` = локальному, 0 вхождений `kb-suppliers-datalist` в bundle прода; до деплоя — перепись на стенде 8125: 0 pageerror, 0 аномалий, скриншоты обеих тем |
| 23.09 | 6 | воркер главной сессии: анализ + штамп в V2-UI-AUDIT §6 п.5 (`3fc2c80`) | закрыт чтением кода: UI создания списания в финансах v2 нет, мёртвых пунктов нет; таблица паритета 9 сценариев очередей в отчёте и штампе; стенд-сценарий отложен до свободного браузерного окна (8126, карточка-жертва для P0); найдено 5 дефектов доски — реестр разделом ниже, P0 во главе очереди board.js |
| 23.09 | 17 | воркер главной сессии `5828f2d` + гейт email-parser `16646a3` | закрыт: V11 — гейты admin на 5 деструктивных ручках + матрица 48 тестов, операционные действия менеджера/склада не тронуты (owner_id-скоупинг — отдельное решение владельца); V7 — без миграции, is_invoice_doc каноничен, PATCH 400 на не-документах; находка вне пункта: POST /email-parser/settings закрыт админом. pytest: 1 предсуществующее падение (timezone 00:00–03:00, реестр дефектов п.6). Отложено держателям: унификация подписи и скрытие удаления для не-админов (п.3), часть A P0 (сессия 3), части B/C + tnHere групп (воркер P0). **Поведение прода изменится при деплое бэка — нужно подтверждение владельца** |
| 23.09 | P0 часть B | воркер главной сессии `cafc5a9` | выполнен: POST /writeoffs/groups/{id}/annul (гейт admin, unwind зеркально одиночной отмене, идемпотентно 400), invoice_transaction_id в ответе issue-invoice, docTxId/tnHere/billHere групп в boot-шаблоне (проявится в пересборке пункта 3); 15 тестов + регрессия 72 passed, node --check OK; site-v2 не пересобирал. Часть C и клиентская ветка флагов — после слияния worktree'ов 3 и 4 |
| 23.09 | 6 стенд-сценарий | главная сессия, Playwright на 8126 (скриншоты `gui-test-screenshots/2026-09-23-*`) | частично: «выписать → отменить» проходит (POST/DELETE 200 за 0.0 с, диалог 0.2 с, жизненный цикл ТН чистый, 0 pageerror); «ТН у нас» переживает перезагрузку, обе темы сняты — замерный пробел пункта 2 по этому сценарию закрыт; FAIL: после отмены карточка падает в историю вместо «На списание» — дефект 7 реестра (инвалидация remaining_kop без пересчёта). Пункт 6 остаётся «закрыт чтением кода», сценарий перегнать после фикса дефекта 7 |
| 23.09 | 2 | код `54c184f`; верификация воркером `verifier` на HEAD `028afa4`, штамп сборки `V2_ASSET_VER=27a20e16db` | закрыт: повторный замер критерия на текущем main, два прогона дали идентичные числа — мутации `#kb-board` на одну галочку **4** (порог <300), longest task **0 мс** (порог <30), ровно **1** `PATCH /checklists/454` на взаимодействие, перерисовка финансов от галочки **0** (0 мутаций `#view-fin`, 0 событий `kb:documents`); итоговый флаг `"pass": true` в `/tmp/v2-audit-prod/perf-item2-recheck-{1,2}.json`. Провязка к коммиту есть: `site-v2` и `mockups` чистые относительно HEAD до и после прогонов, HEAD за время замеров не сдвинулся. Регрессия блока 0 (`/tmp/verify-0-block.py`) OK — clients-петля и комбобокс поставщика. Перепись `toolout/census-item2-recheck/`: 0 pageerror, js_errors 4 = известные артефакты стенда (400 `email-parser/sync` + 3 «invalid form control» в админке), 0 DOM/дата-аномалий, net_failures 1. **Не доказано замером:** сценарий «ТН у нас» (в карточке-жертве 1058 нет документов, `data-originals` отсутствует) и тёмная тема; порог «<300» считает только поддерево `#kb-board` (всплытие в диалоге, `counts.dialog.mut`=99, в вердикт не входит). Прежние артефакты `perf-item2-{before,after}.json` сняты за ~6 мин до `54c184f` и без штампа сборки — как доказательство не использовались. pytest не гоняли: бэкенд пунктом не тронут, в дереве были чужие незакоммиченные правки |
| 23.09 | 12 (серверная половина) | `e77b86b` — миграция `migrations/0013_fill_deal_statuses.py` + `tests/test_migration_0013_deal_statuses.py`; реализация воркером `builder`, ревью и контрольные прогоны в главной сессии | частично: `deal_statuses` наполняется 6 фактическими статусами карточек — `position` 0..5 и `color` именами существующих CSS-токенов (`faint/accent/warn/ok/danger/faint`), статус вне канона получает `position ≥ 50` и `color NULL`; аддитивно и идемпотентно, `cards` не меняются, созданные руками строки не перезаписываются, `tenant_id` не заполняется. Роутер не правили — GET/POST/PATCH `/dictionaries/statuses` уже были (`routers/dictionaries_router.py:55-86`). Проверено: `pytest -q` 488 passed (conftest накатывает все миграции в каждой сессии, 0013 в том числе); целевые 63 passed; на копии прод-БД `/tmp/item12` (`sqlite3 .backup` из `/tmp/v2-audit-prod`) — ровно 6 строк, повторный `migrate.py` «применено 0», прямой повторный `up()` дублей не дал; живой API на временном uvicorn:8127 — `GET /dictionaries/statuses` 6 объектов в порядке `position` с цветами, `PATCH /dictionaries/statuses/6` 200 + новое имя (имя откатили), стенд убит после проверки. Общий стенд 8126 и прод НЕ тронуты: в `/tmp/v2-audit-prod/crm_app.db` по-прежнему 0 строк и 11 миграций. **Миграцию не применять до клиентской половины** — см. пометку в пункте 12: непустой справочник переключает v2 на ветку словаря (`v2-boot-template.js:124`), добавляет пятую колонку «Закрыто» (179 сделок) и даёт коллизию id `done` с хардкодом «Списано» (`shell-v2-board.js:1289`, очередь `:569`). Не закрыто: админка v2 и доска (клиентская половина, файлы держат сессии пунктов 3/4/10), а также поведение при переименовании статуса — `cards.status` остаётся строкой |
| 23.09 | 3 | код `a7e504c`/`e5e7727` (ленивые финансы + `content-visibility`) и `9d4668d` (SVG-маски); исполнители — воркеры `general-purpose` и `builder` в worktree `/private/tmp/crm-item3`, `crm-integration` | закрыт: `#view-fin` не строится до первого входа — элементов в DOM после входа **6334** вместо 21 097 (критерий <8000), в скрытом разделе 111 узлов вместо 14 912; `kb:documents`/`kb:groups` до первого открытия копятся в `pending*` и проигрываются при сборке, итоговый `innerHTML #view-fin` побайтово равен прежнему (jsdom-паритет HEAD↔правка, 6 краевых сценариев PASS); `.kb-card` получил `content-visibility:auto` + `contain-intrinsic-size` 105/131px по замеру (средние высоты 123.4/135.4 минус паддинги и рамка 18/26px: свойство задаёт content-box, прежние оценочные 160/180 давали плейсхолдер 178/206px) — сжатие `scrollHeight` при первой прокрутке колонки из 168 плиток **−28.01% → −0.70%**, прыжки по скроллбару 2 из 4 (53px) → 0 из 4; SVG в доске **591 → 0**: иконки плиток переведены на CSS-маски (data-URL в `shell-v2-board.css`, `background-color: currentColor`), потому что `<symbol>`+`<use>` оставляет по `<svg>` на экземпляр и критерий не закрывает, а новые `.svg`-файлы сборщик не копирует; пиксельное сравнение 24 пар скриншотов (плитка/hover/чипы/доски 1280·1440·1920 × 2 плотности × 2 темы, scale 3) — 0 пикселей с расхождением >30/255, то же в WebKit; высоты плиток не изменились. Матрица дизайна 60/60 (0 скроллов, 0 наложений), перепись 0 pageerror. Побочно: миниатюры входящих накладных больше не грузятся до первого входа в финансы |
| 23.09 | P0 части A, C, D | `88d31a2` в ветке `v2-item3-render-cost`, в main через `a7e504c`; исполнитель `general-purpose` по спекам `P0-HOTFIX-GROUP-ANNUL-SPEC-2026-09-23.md` и `BOARD-PACKAGE-SPEC-2026-09-23.md` (разделы A, B) | закрыт клиент: A — `serverTxId` теперь `invoice_transaction_id` (был id группы — причина удаления посторонней транзакции), туда же `docTxId/tnHere/billHere`, чтобы флаги группы жили в сессии выписки; C — основной путь отмены `POST /writeoffs/groups/{id}/annul` (200 → `reloadData()` и тост с числом карточек ИЗ ОТВЕТА СЕРВЕРА; 403 → «доступна только администратору», данные не трогаем; 400/404 → сообщение + перечитывание; сеть/401 → сообщение без изменений), `annul-tx` оставлен фолбэком только для документа группы без `written_off`, `doCancelGroupLocal` — только для групп, которых нет на сервере; D — строка групповой ТН в «Документах» финансов собирается с `docTxId/tnHere/billHere` из `KBData.groups`, галочка «ТН у нас» уходит `PATCH /payments/transactions/{docTxId}` и переживает F5. Стенд 8127 (копия прод-БД): отмена БЕЗ F5 и ПОСЛЕ F5 — ровно 1 POST/200 и **0** `DELETE /payments/transactions/*`, документы 197→198→197 (удалён ровно id 426), оплаты 210→212 (остатки вернулись), карточки вышли из «Закрыто»; под `audit_manager` — 403 и ни одной мутации; часть D — PATCH 200 и галочка сохранена после F5. Деплой не выполнялся (решение владельца): до выкатки обеих половин запрет нажимать «Отменить» у групповых ТН в силе |
| 23.09 | дефект 7 реестра | `cbea561`; исполнитель воркер `builder` (`.qwen/agents`, `qwen3.8-flash`) | закрыт: `recalcCardMoney(card)` вместо инвалидации `remaining_kop`/`issued_total` в `doCancelLocal` и `doCancelGroupLocal`; расхождение со спекой осознанное — `card.amount` в board.js уже в копейках, поэтому `amount*100` завысило бы остаток в сто раз; при неизвестном `amount` остаток `null`, семантика `hasRemaining(null)` сохранена. `tools/v2_queue_scenario.py` на стенде 8127 (спека велит 8126, но там крутится код main без правок — отступление зафиксировано): ДО «FAIL после отмены карточка снова в очереди «На списание» (дефект 7) — history», `remaining_kop=None`; ПОСЛЕ «PASS … — pending», `remaining_kop=1822186` — совпало с сервером до копейки (карточка 553: total 57046.38, issued 38824.52, remaining 18221.86). Локальный путь отмены группы проверен синтетически (в копии БД групп нет). Побочно: тост об отмене печатает реальный остаток вместо «—». Не закрыто: симметричная инвалидация в `issueGroup` (путь выписки группы) |
| 23.09 | 10 | `1aed6a0` в ветке `v2-item10-notifications`, в main через `34558f9`; исполнитель `general-purpose` | закрыт: авто-`POST /notifications/read {ids:[]}` при открытии убран (3 открытия — 3 GET и 0 POST, бейдж и непрочитанные не меняются); «Прочитать все» рисуется только при непрочитанных и шлёт один POST; клик по уведомлению открывает объект синхронным кликом по рейке — дровер остаётся открытым и через 1.5 с (прежний путь через `location.hash` закрывал его отложенным `hashchange`), задача скроллится и получает фокус, клиент открывается дровером; удаление одного через существующий `DELETE /notifications/{id}` (`notifications_router.py:88`) с передачей фокуса на соседнюю корзину или колокольчик; уведомление без объекта — некликабельный `div` вне tab-порядка; Escape закрывает панель и возвращает фокус, `aria-expanded` переключается. Бэкенд не менялся; +6 тестов контрактов панели. Артефакты: `/tmp/v2-verify-int/toolout/b3*`. Находка вне пункта — дефект 8 реестра (фокус после `kb:reloaded`) |
| 23.09 | 18 | `c681eae` в ветке `v2-item18-infra`, в main через `eeeff45` + снятие автодеплоя в `e5e7727`; исполнитель `general-purpose` | частично: гейт свежести `site-v2` — `server_snapshot/tools/check_site_v2_fresh.py` (эталон собирается во временный каталог, сверка по sha1, коды 0/1/2, `--json`), подключён этапом в `scripts/check.sh` перед `stamp_assets --check` и в job `test` CI; при рассинхроне падает с русской подсказкой (обе ветки продемонстрированы) — он же доказал рассинхрон main после `cafc5a9` (boot-шаблон изменён без пересборки: расходятся `index.html` и `js/v2/boot.js`, `exit=1`), устранённый пересборкой `e5e7727`. Тесты кэш-политики `/v2` (`tests/test_v2_cache_headers.py`, 6): `no-cache` на HTML, `public, max-age=31536000, immutable` на css/js/fonts, контракт `V2_IMMUTABLE_PREFIXES`, непопадание политики на legacy, `/` → 302 `/v2/`. usage `deploy.sh` перечисляет все действия (`front/v2/back/restart/status/logs`). `nginx/snippets/v2-legacy-locations.conf` — реконструкция location'ов `/v2` и `/legacy`, помечена «требует сверки с боевым»; закрывает пробел, из-за которого `/v2/` в docker-пути молча отдаёт legacy `index.html` (`Dockerfile.frontend` копирует только `site/`), и дополняет `gzip_types` для `text/javascript` (в `nginx/http.conf:18` и `https.conf:55` его нет, а приложение отдаёт `.js` именно так). **Не сделано по решению владельца 23.09:** чистка CSP от `fonts.googleapis/gstatic` — сломала бы Manrope на замороженном legacy (локальных `@font-face` там нет, `site/index.html:10` тянет CSS с Google, CSP из `main.py` ставится и на `/legacy`, тесты это не ловят) — и автодеплой `deploy.sh v2` в `deploy-production`: выкатка основного фронта остаётся ручной. **Не проверено:** docker-путь (контейнеры не поднимали) и `nginx -t` (нет nginx на машине разработки), боевой конфиг не сверялся (SSH запрещён). Мусор `.mimosa` не удаляли — рекомендация: игнор скрытых путей в `collect_hashes()` `server_snapshot/tools/stamp_assets.py` (по образцу `IGNORED_PARTS` в новом гейте), на код выхода шум не влияет |
| 23.09 | 16 (серверная половина) | `d6bfd29` в ветке `v2-item16-server`, в main через `c1301c3` (слияние и ревизия в главной сессии); исполнитель `general-purpose` | частично: серверные страницы для `/kanban/cards` и `/payments/transactions` (дефект B11: 474 КБ и 132 КБ одним куском) — аддитивно и обратно совместимо: без `limit`/`offset` ответ прежний (плоский массив), с ними `{items,total,limit,offset}` + `X-Total-Count`; пороги взяты с единственного прецедента в репо (`GET /activity`: дефолт 50, потолок 200), валидация `Query(ge=/le=)` → 422, `total` одним `count(*)` по тому же фильтру, сортировка прежняя и тотальная (страницы не перекрываются), фильтры работают вместе со страницей; помощники в `db_utils.py`. +39 тестов (`tests/test_server_pagination.py`), pytest 544 passed; слияние с main без конфликтов (`payments_router.py` сошёлся с ролевыми гейтами пункта 17 и annul из `cafc5a9`). **Не сделано:** клиентская дозагрузка по скроллу (`board.js`/`api.js`) — без неё критерий «первый экран < 100 КБ» не закрывается; ротация `record_versions`/`activity_log` — по решению владельца 23.09 отложена до `.backup` боевой БД и отдельного окна (миграция безвозвратно удаляет строки журналов), лежит в ветке `v2-item16-server`: `git checkout v2-item16-server -- server_snapshot/migrations/0012_rotate_history_tables.py server_snapshot/tests/test_history_rotation.py`, перед применением **перенумеровать в 0015** (в main уже есть `0013_fill_deal_statuses.py`, а `0014_add_user_is_active.py` занят деактивацией пользователей из `ec5fde1`; номер 0012 пропущен)

**Статус 23.09 (вечер): клиентская половина закрыта — двухфазная загрузка**
(спека и итоги — `docs/audits/V2-ITEM16-CLIENT-SPEC-2026-09-23.md`). Разведка
показала, что честный infinite scroll молча занижает девять групп потребителей
`KBData.cards` (пульс, календарь, «Клиент 360», дебиторка, счётчики очередей и
колонок, discovery магазинов/статусов), а серверных агрегатов нет — сделана
двухфазная загрузка: фаза 1 `/kanban/cards?limit=80&offset=0` (**88 618 байт**
< 100 КБ, полный ответ был 496 КБ) → первая отрисовка; фаза 2 — автозапуск
после отрисовки + кик из скролла доски (троттлинг 300 мс), страницы
дописываются в конец массива (серверный порядок тотальный — shared order не
перемешивается), guard поколения данных гасит цикл при reload; по завершении —
один `kb:reloaded` и пересборка `KB_FIN_SOURCE` на полных карточках с
`kb:documents` (иначе реестр финансов перманентно терял привязку к карточкам
за первой страницей — дыра найдена на ревью, закрыта в главную сессию).
`/payments/transactions` (132 КБ) сознательно остался полным: grouped-режим
без честной SQL-пагинации, фин-раздел строится лениво один раз — урезание
вынесено в backend-долг. Проверено: сценарии 10/10 (`/tmp/item16_scenarios.py`)
— страницы 0/80/160/240/320, 391 уникальная карточка, счётчики доски/очередей/
списка/архива идентичны до-пагинационным (13/168/4/12, 2/192, 391, 177), кик
по скроллу уходит в сеть, фин-реестр 206/206 строк с правильными лейблами,
reload без дублей; census 0/0 (4 известных артефакта); s11 — **PASS** в обе
стороны (4/<300, 0 мс, 1 PATCH, fin 0 — эталон совпал, заодно подтверждая, что
провалы long task в пункте 12 были средовым шумом); Node-харнесс исполнителя
покрыл гонки (reload посреди фазы 2, сбой страницы, маленькая БД, плоский
ответ старого сервера). |
| 23.09 | вне пунктов: дровер и высоты плиток | `ba0f65b` | закрыт дефект, найденный верификацией пункта 10 и подтверждённый чтением кода: `KBBoard.open` писал `location.hash = '#board'`, отложенный `hashchange` → `render(true)` → `navigation.js:31-35` закрывал только что открытый `#kb-dialog`, из-за чего «Открыть сделку» с Пульта дня (`#day`) и из «Клиент 360» (`#clients`) захлопывало карточку сразу после открытия. Раздел переключается синхронным кликом по рейке, `queueMode`/`openCard` — после него, при отсутствующем пункте рейки оставлен фолбэк с открытием ПОСЛЕ `hashchange`; если уже на доске — клик не делается, поэтому путь уведомления не даёт двойного переключения (`railClicks=1`, `push=1`, back/forward адекватны). Попутно фокус возвращается в дровер после rAF оболочки (`render(true)` утаскивал его на `#shell-content`, который под модалкой inert). 37 браузерных проверок — 36 PASS, единственный FAIL — предсуществующий дефект 8. Там же: `contain-intrinsic-size` приведён к замеру и добавлен детерминированный тест коллизии id для P0 (`test_annul_does_not_touch_transaction_with_same_id`; мутационная проверка — 3 failed при откате реализации к поиску по `Transaction.id == group_id`) |
| 23.09 | сведение веток 3/10/18/16 | fast-forward main `d706234` → `9d4668d` (ветки `v2-item3-render-cost`, `v2-item10-notifications`, `v2-item18-infra`, `v2-item16-server`; worktree в `/private/tmp/crm-*`) | выполнено: потоки собраны в одну ветку, пересборка `site-v2` одна (`e5e7727`, правило одной пересборки), конфликтов при слиянии не было. Гейты в основном чекауте после fast-forward: `scripts/check.sh` **exit 0** (JS-синтаксис, data-handler 49/0, свежесть site-v2, штампы 37 ссылок/0 расхождений, pytest **544 passed**), профиль `CRM_TEST_SCHEMA=models` — 501 passed + 4 skipped; перепись ошибок на стенде 8127 — 0 pageerror и ровно 4 известных артефакта стенда; функциональный аудит 26 проверок / 1 провал-артефакт (пустые настройки почты); матрица дизайна 60/60. Дублирующий вариант серверного P0 (ветка `v2-p0-group-cancel`, `POST …/cancel-invoice`) в main НЕ вливался — вытеснен `cafc5a9`, оттуда взят только тест коллизии id. Деплой не выполнялся: прод на прежней сборке |
| 23.09 | 9 (бэк), 15 (деактивация), вне пунктов: мёртвый код | `ec5fde1` | бэк: ролевые гейты пункта 9 (`POST /email-parser/sync` под admin; системный ast-обход всех 79 мутирующих ручек новых дыр не нашёл), `users.is_active` (миграция `0014_add_user_is_active.py`, гейт входа ПОСЛЕ сверки пароля, чтобы не появился канал перечисления аккаунтов; `get_current_user` отклоняет токен отключённого) — реестр п.10: починка недостижимого тела `POST /email-parser/link/{card_id}` |
| 23.09 | 9-UI, 14, 15 (клиент) + реестр п.14–16, 23 | `353d028` | v2: механизм `data-admin-only` + `applyRoleVisibility` (идемпотентный, пересчёт по событию `v2:role`); вкладка «Контроль»/дебиторка на уже загруженных данных, без новых запросов, экспорт CSV документов (клиентская генерация), фильтры задач «Все / Просроченные»; экраны админки «пользователи / вебхуки / кастомные объекты / автоматизации», загрузка ленивая через `MGMT_LAZY`; календарь переведён с демо-данных на боевые (`KBData`: срок сделки, плановая дата оплаты, срок задачи; день, чип и «+N ещё» — реальные `<button>` с русскими `aria-label`, диалог «События дня», переход к объекту существующими мостами `KBBoard.open`/приём `gotoTarget`); дедупликация строк «Документы»; демо-ревизия (9 мест); `contactPhone()`; ленивая подписка `insights.js` на `kb:reloaded`; `createdAt`/`is_active` в boot-маппинге; `V2Api.userId()` (убрал лишний `GET /auth/me` при каждом открытии «Управления»); README мокапов приведён к коду |
| 23.09 | 5 (бэк), 15 (бэк), 9 (бэк) + реестр п.11–13, 22 | `68baf04` | бэк: `sender_email` в `update_card` (без него критерий пункта 5 был недостижим одним клиентом); null-семантика `*Update`-схем — восемь веток в задачах были на `is not None`, переведены на `model_fields_set` (снять срок/ответственного/привязку стало возможно; для not-null `title`/`status` — 400; пересчёт `card_title_snapshot`/`client_name_snapshot` и при очистке, и при смене привязки; ветка «срок снят» в уведомлениях стала достижимой), то же для вебхуков и сценариев, `{"secret": ""}` → 400 (снять подпись только явным `null`); 400 вместо 500 на несуществующем `assignee_id` (в `create_task` и `update_task`). Статический ast-гейт по всем парам (схема, поле): разобран 19 хендлеров / 18 схем / 116 пар, единственный баг этого класса во всём бэке — `sender_email` |
| 23.09 | 15 (кастомные объекты) + реестр п.21 | `5ca4939` | бэк: `PATCH /custom/objects/{id}` и `PATCH /custom/fields/{id}` (без них «изменить» означало «удали и создай заново», а удаление поля сносит его значения), 48 тестов. Смена типа поля разрешена только пока у поля нет значений: значения живут в шести типизированных столбцах `custom_field_values`, столбец выбирается по `field_type` при записи, а читается «первый непустой» — смена типа у непустого поля расщепила бы его без возможности починить |
| 23.09 | 17 (V17) | `1a87677` | бэк: роспуск группы списания `DELETE /writeoffs/groups/{id}` — только админ (решение владельца 23.09), 13 тестов, включая негативную защиту: прикрепление и выход под manager/warehouse остались рабочими. Граница и обоснование — блоком в пункте 17 |
| 23.09 | 15 (формы правки) | `802dad9` | v2: формы правки кастомных объектов, вебхуков и сценариев; в тело уходит только изменённое (сервер трактует отсутствие ключа как «не менять», а `null` как «очистить»); 400 про смену типа поля показывается русским текстом сервера, диалог при этом не закрывается и значения не теряются |
| 23.09 | 4, 5, 7, 8, 11 + дефекты реестра 3, 4, 5, 8, 9 + реестр п.17–20 | `7ff3a52` | v2: досочная волна целиком — троттлинг drag&drop и тост о переносе, поле `sender_email` в «Обзоре» дровера, один инпут и один крестик в поиске + подсказка при пустом результате, `loadRelatedEmails()` на вкладке «История» с различением «нет писем»/«ошибка», мелочи дровера и текстов; закрытие дефектов 3 (второй шаг «Списано со склада» → `duplicate_as_document`), 4 (массовое и поштучное «убрать из списания»), 5 (прикрепить/выйти/распустить), 8 (фокус в дровере), 9 (ложная строка групповой ТН); симметричный пересчёт денег в `issueGroup`, 7 меток `data-admin-only`, `clientId` в маппинге групп, приглушение отключённого ответственного + одна пересборка `site-v2` |
| 23.09 | верификация волны (коммиты `ec5fde1`…`7ff3a52`, HEAD `7ff3a52`) | — | `bash scripts/check.sh` — **exit 0**: версии статики (37 ссылок, расхождений 0), полный pytest **878 passed** (база на старте волны — 544, то есть +334 теста). Стенд: свой порт **8127** (8125 и 8126 заняты чужими сессиями), копия прод-БД в `/tmp/v2-wave-verify`, применена **только миграция 0014** (0013 по-прежнему заблокирована до клиентской половины пункта 12), `deal_statuses` пуст; для функционального аудита создан временный `v2_audit`/superadmin. `python3 tools/v2_error_census.py --base http://127.0.0.1:8127/v2/ --dark` — **0 pageerror, 0 dom_anomalies, 0 data_anomalies**; 96 шагов, 99 API-вызовов. Остаток — 8 консоль-записей, и это известные артефакты, **удвоенные прогоном в двух темах**: 3× «An invalid form control with name='' is not focusable.» и 1× `POST /email-parser/sync` → 400 «Настройки почты не заполнены» на каждую тему (в известном остатке прошлых прогонов было 4 артефакта на одну тему). Перепись кликала новые кнопки («Отключить», «Изменить», вкладку «Контроль», фильтры задач, навигацию календаря) — без ошибок; `POST /email-parser/sync` под админом дал 400, а не 403, то есть ролевой гейт админа не ломает. **Не проверено на момент записи:** функциональный аудит (идёт), браузерные критерии пунктов 4 и 7 (трейс перетаскивания без пачек layout через `tools/v2_drag_perf.py`, скриншоты поиска до/после), матрица дизайна в 4 точках, скриншоты новых экранов в обеих темах |
| 23.09 | 13 | `5686a2d` (код + спека `V2-ITEM13-SPEC-2026-09-23` + пересборка site-v2, штамп `c9ab4e0133`); исполнитель воркер `builder`, ревью и верификация — главная сессия | закрыт: все детали и отступления — блоком «Итог 23.09» в пункте 13. Гейты: `scripts/check.sh` exit 0 (pytest 878); перепись на стенде 8127 (`--dark`) — 0 аномалий, 8 известных консоль-артефактов; функциональный аудит 26/1 средовой; сценарии 29/29 (`/tmp/item13-scenarios.json`); ширины 1280/1000/768 без горскролла. Стенд 8127 — процесс сессии верификации волны на `/tmp/v2-wave-verify` переиспользован как есть (штамп сборки совпал, StaticFiles отдаёт свежую статику; на стенде для фильтров поставлены тестовые priority/«БН» карточкам 1066–1068 и мягко удалена одна закрытая) |
| 23.09 | 12 (клиентская половина) | `8d24f13` (A+B+C+E), `3c8473c` (D), пересборка site-v2 отдельным коммитом; исполнители — два воркера `builder` параллельно (файлы не пересекались), ревью и верификация — главная сессия | закрыт: As-built заполнен в спеке `V2-ITEM12-CLIENT-SPEC-2026-09-23.md` (классификация всех мест done/closed, числа по критериям 1–9). Ключевое: «Закрыто» = id `closed` (роль archive), псевдо-этап выписки остаётся `done`; Р4-тост в drop и смене этапа; Р6 — выключенный/неизвестный статус ведёт карточку в достроенную колонку, а не в «Новый запрос»; админка статусов — прямой GET (включая неактивных), Р3/Р5. Сценарии 15/15 (`/tmp/item12_scenarios.py` → `/tmp/item12-scenarios.json`); census 0/0, ровно 4 известных артефакта; регрессии пунктов 2 и блока 0 — OK. Стенд: свой `/tmp/v2-item12-stand` (копия прод-БД + 0013 + 0014 + `v2_audit`), порт 8127 (устаревший процесс волны убит). **Замечание:** перф-скрипт `s11_board_perf.py` игнорирует аргументы и жёстко бьёт на 8126 (`common.py:5`) — первый прогон случайно прошёл по чужому стенду старой сборки (галочка 454 и название 1058 туда-обратно, net zero); перемерено на копии с BASE=8127. `verify-0-block.py` — та же схема (BASE патчился в копии `/tmp/v2-item12-scripts/`) |
| 23.09 | 16 (клиентская половина) | код + пересборка site-v2 (штамп `ad336e1434`), исполнитель воркер `builder` с Node-харнессом гонок, ревью и верификация — главная сессия (правка ревью: пересборка KB_FIN_SOURCE + kb:documents после фазы 2) | закрыт (клиентская половина; ротация 0015 — за владельцем): фаза 1 limit=80 = 88 618 байт < 100 КБ (был 496 КБ), фаза 2 — автозапуск + кик по скроллу, страницы 0/80/160/240/320 → 391 уникальная карточка; все до-пагинационные числа доски воспроизведены (колонки 13/168/4/12, очереди 2/192, список 391, архив 177); фин-реестр после фазы 2 — 206/206 строк с лейблами карточек; census 0/0; s11 PASS в обе стороны. Сценарии 10/10 (`/tmp/item16_scenarios.py`); дизайн и запрет infinite scroll — в спеке `V2-ITEM16-CLIENT-SPEC-2026-09-23.md` |
| 24.09 | **деплой волны на прод** (слово владельца «ДЕЛАЙ ДЕПЛОЙ И ВСЕ ПРОВЕРКИ») | статика `tools/deploy.sh v2`; бэк — 23 файла (`8e3fd1f..HEAD`, без tests/tools/mockups) rsync по месту; `python3 migrate.py` с `.pm2.env` ДО рестарта; рестарт `tools/deploy.sh restart` | выполнен и проверен. **Порядок соблюдён**: бэкап `crm_app.db.backup-pre-deploy-2026-09-24` (ssh, до миграций) → копия файлов → миграции (0013+0014, «применено 2, всего 13»; `deal_statuses` 6 строк, `users.is_active` на месте) → рестарт PM2 (crm online, uvicorn 20008, старт без ошибок). **Проверки прода:** штамп `ad336e1434` = локальному; маркеры пунктов 12/13/16 в бандлах (CANON_ID/closed, Сохранённые виды, limit=80, ensureCardsLoaded, тост Р4); `/` → 302 `/v2/`, `/v2/` 200, `/legacy` 200 (рабочий адрес `https://87-232-64-12.nip.io`, по голому IP — парковка хостера); CSP `img-src` с `blob:` (пункт 1 наконец на проде); кэш: HTML no-cache, JS immutable. API: 6 статусов из справочника; `/kanban/cards?limit=1` → `{items,total}` + `X-Total-Count: 396`, полный ответ — плоский массив 508 КБ (совместимость); `POST /writeoffs/groups/999999/annul` → 404 (P0-роут жив); `/auth/users` все с `is_active`; md5 7 ключевых файлов = локальным. UI-смоук под временным суперадмином (создан по ssh, удалён после; вход 401 после удаления): доска на ЖИВОМ словаре — ровно 4 колонки 7/178/4/10 (сценарий, которого боялись при 0013), двухфазная загрузка 396 карточек, список 396 / архив 180, дровер открывается, консоль 0 ошибок. watchdog в cron (раз в 5 мин) и `.cron_token` на месте — прежние блокировочные пункты закрыты. **Инцидент при проверке:** хеш временного пользователя при первой вставке побился shell-подстановкой `$2b$…` в ssh-команде → 500 на его логине (`UnknownHashError`); боевые учётки не затронуты, хеш перезалил SQL-файлом через scp, логин проверен, пользователь удалён. **Осталось на владельце:** пуш (main впереди origin ~на 75 коммитов), ротация 0015 (после `.backup` — копия снята), домен/HTTPS (самоподписанный, рабочий адрес nip.io) || 24.09 | исправление плитки + финальные read-only проверки | текущий пакет без commit/push/deploy | закрыт: `bash scripts/check.sh` exit 0 (`897 passed`); design matrix на 8127 exit 0 (102 снимка, 0 page overflow, 0 наложений, оба тома/7 ширин); census на 8127 exit 0 (0 JS, 0 сети, 0 DOM-аномалий, 0 KBData, 73×200); единственный contrast-флаг `button.box` — false positive для прозрачного checkbox-текста; pointer-плитка и DOM accessibility tree подтверждены, Enter/Space в IAB не подтверждены |

---

## 11. 24.09 — As-built после исправления вложенной интерактивной семантики плитки

### Исправление

- В `.kb-card` сохранён drag-источник, но с него снята искусственная
  button-семантика (`role="button"`, `tabindex="0"`); открытие перенесено на
  нативную `.kb-card-open` с `aria-label` и `aria-haspopup="dialog"`.
- Кнопка удаления осталась отдельной кнопкой, а не вложенным интерактивным
  элементом внутри button. Click bridge различает открытие, свободную область
  контейнера и удаление; ручной Enter/Space-мост больше не применяется к
  нативной кнопке открытия.
- `.kb-card-open` получила локальный reset кнопки в `shell-v2-board.css`, чтобы
  сохранить визуальную геометрию плитки и не менять поведение drag&drop.
- `server_snapshot/site-v2/` пересобран штатным
  `server_snapshot/tools/build_site_v2.py`; ручных правок generated-артефактов
  не было. Freshness gate подтверждает совпадение 25 файлов с mockups.

### Проверки

- После reload accessibility tree показывает отдельные кнопки «Открыть сделку …»
  и «Удалить карточку …»; `role` и `tabindex` внешнего `.kb-card` равны `null`.
- Pointer-проверка на локальном стенде 8125: открытие кнопкой и свободной
  областью плитки открывает drawer; удаление открывает отдельный confirm и не
  открывает drawer. Confirm отменён, запись данных не выполнялась.
- Геометрия read-only: контейнер `263×133.92 px`, кнопка открытия
  `245×115.92 px`; `draggable="true"` сохранён.
- `bash scripts/check.sh` — exit 0: `897 passed`, JS syntax, data-handler,
  свежесть `site-v2` и версии статики зелёные.
- Последний валидный read-only census на 8127 (учётка `audit_admin`) — exit 0:
  0 ошибок JS, 0 сетевых сбоев, 0 DOM/дата-аномалий, 0 дыр `KBData`, 73/73
  API-вызовов с кодом 200; все разделы и `#admin` открылись. Повтор на 8125 с
  отклонённой учёткой дал 401 и не использовался как доказательство дефекта.
- `v2_design_matrix.py` на 8127 — exit 0, 102 снимка на ширинах
  1920/1440/1280/1024/768/430/375 в light/dark; 0 page overflow, 0 наложений.
  Единственный contrast-флаг в тёмной теме — `button.box` с прозрачным текстом
  и без текстового содержимого (`rgba(0,0,0,0)` на `rgb(31,34,36)`), то есть
  автоматический false positive для checkbox-кнопки, а не дефект читаемости.
  Сокращения длинных названий и контактов и малые touch-таргеты сохранены как
  известные ограничения/наблюдения, не новые blocking-дефекты.
- Отдельный keyboard runtime Enter/Space в IAB не подтверждён из-за backend
  quirks; DOM-семантика нативной кнопки и pointer-поведение подтверждены.
- Production, production-БД и legacy не затрагивались; commit, push и deploy
  не выполнялись.

## 12. 24.09 — классификация исторических F1–F10

- F1–F8 закрыты кодом и подтверждены соответствующими локальными runtime/
  census-сценариями; для F5, F6–F8 сохранены ограничения по отдельному
  manager/admin, свежему скриншоту и perf-трейсу.
- F9 закрыт кодом: открытие панели не помечает уведомления, «Прочитать все» —
  явное действие, переход к объекту и удаление одного элемента реализованы.
  Строгий повторный authenticated UI-прогон после последнего пакета не
  выполнялся.
- F10 закрыт кодом: demo-формулировки удалены из живых данных, `?demo=1`
  оставлен как законный fixture. Строгий authenticated runtime-прогон без
  demo не выполнялся.
- Открытыми остаются только ограничения доказательств, а не новые
  подтверждённые дефекты: design-matrix login timeout на 8125 и IAB
  keyboard-runtime, который нельзя объявлять проверенным.

## Параллелизация пунктов (добавлено 22.09)

Рабочий каталог один на все сессии → **один держатель файла одновременно**.
Держатели на момент добавления: `shell-v2-board.js` и `shell-v2-prototype.html` — пункт 2.
Пункт 2 закрыт 23.09 — его держательство снято. Фактически `board.js` и
`prototype.html` на 01:20 23.09 правят сессии пунктов 3 (worktree
`/private/tmp/crm-item3`, грязные `mockups/shell-v2-board.css` +
`shell-v2-prototype.html`) и 4 (`/private/tmp/crm-item4`, грязный
`mockups/shell-v2-board.js`) — перед любой правкой доски сверять
`git worktree list` и `git status` в чужих worktree.

| Пункт | Основные файлы | Параллельно с 2 |
| --- | --- | --- |
| 3 | board.js, board.css, prototype.html | нет (board.js + html) |
| 4 | board.js | нет |
| 5 | board.js | нет |
| 6 | V2-UI-AUDIT (список паритета), fin только чтение | да; сценарии на стенде — в окно, когда 2 не верифицируется |
| 7 | board.js, css поиска | нет |
| 8 | board.js | нет |
| 9 | management.js, prototype.html, routers/ | нет (html; management.js делит с 10 и 15) |
| 10 | management.js, select.css | да (пока не стартовали 9/15) |
| 11 | board.js | нет |
| 12 | migrations/, routers/, management.js, board.js | нет |
| 13 | board.js, board.css | нет |
| 14 | fin.js, prototype.html (новые экраны) | нет (html) |
| 15 | management.js, prototype.html, routers/ | нет (html) |
| 16 | routers/, board.js, api.js | нет (board.js) |
| 17 | routers/, migrations/ или маппинг, tests/, fin.js | да; верификация pytest; если V7 = колонка-миграция — не применять к БД стенда во время чужой верификации |
| 18 | nginx/, .github/, scripts/, tools/deploy.sh, main.py (чистка CSP), .mimosa | да; браузер в критериях не нужен |

Общие ресурсы — правила для всех сессий:

1. Пересборка `site-v2` + `stamp_assets --check` — один человек в конце своего пункта;
   при одновременном финише двух пунктов позже финишировавший пересобирает за обоих.
2. Стенд 8126 и браузер (16 ГБ RAM): census/скриншоты/замеры — строго по одному
   одновременно; параллельные сессии разводят окна верификации.
3. Миграции к БД стенда не применять, пока другой пункт верифицируется на стенде.
4. В план и журнал — только добавлять свои строки и галочки, не переписывать чужие.
5. Параллельные коммиты в одном репо: поймали `index.lock` — повторить команду.

Очереди после освобождения файлов пунктом 2: board.js → один из 4/5/8/11,
затем 3/7/13/16; prototype.html → 14 либо 9/15 (эти двое плюс 12 ждут ещё
management.js после пункта 10).

Статус держателей на 23.09 01:30: пункт 2 закрыт (`54c184f`); пункт 17 закрыт
(`5828f2d`, `16646a3`); пункт 6 закрыт чтением кода (`3fc2c80`, `7267607`);
воркер P0 (часть B) завершён (`cafc5a9`) — writeoff_groups_router.py,
schemas.py, services/payments.py, tools/v2-boot-template.js снова свободны;
пункт 16 разблокирован по routers/. Держат дальше: 3 и 4 — board.js/board.css/
prototype.html через worktrees `/private/tmp/crm-item3|4` (+ у 3 часть A
хотфикса P0 по спеке), 10 — management.js, select.css, 18 — инфра.
Часть C хотфикса и клиентская ветка флагов групп — к следующему держателю
board.js после слияния worktree'ов 3 и 4.

**Статус держателей на 23.09 после волны (`7ff3a52`):** `board.js` свободен — все
досочные пакеты закрыты (пункты 3, 4, 5, 7, 8, 11, дефекты реестра 1, 2, 3, 4, 5,
7, 8, 9). `management.js`/`prototype.html` тоже свободны: 9, 14 и клиентская часть
15 закончены. Очередь: **13** (виды и фильтры доски + сохранённые виды по
спецификации пункта 15) → **12** (клиентская половина справочника статусов по спеке
`docs/audits/V2-ITEM12-CLIENT-SPEC-2026-09-23.md`) → **16** (клиентская дозагрузка
страниц по скроллу; серверная половина уже в main: без `limit`/`offset` ответ
прежний, с ними `{items,total,limit,offset}` + `X-Total-Count`) → затем **S**
(сведение). Порядок 13 → 12 → 16 снимает конфликт: «Архив» из пункта 13 строится на
том же массиве карточек, который пункт 16 переводит на страницы. Пункт 12 — самый
«собирающий», трогает одновременно boot/board/insights/management/data, поэтому
идёт один, без параллельных дорожек; миграция 0013 до него заблокирована.

**Статус держателей на 24.09 после деплоя волны и ротации:** пункты 13, 12 и
клиентская половина 16 закрыты; волна v2 и бэкенда выложена на прод, миграции
0013/0014 применены до рестарта PM2, полный набор тестов — 897 passed.
Ротация `0015_rotate_history_tables.py` проверена на свежей копии боевой БД и
применена к прод-базе 24.09 после backup `crm_app.db.backup-pre-0015-20260924`:
удалено 94 старые версии, `activity_log` не изменён, `integrity_check` и
`foreign_key_check` чистые. Проверенный `main` запушен в GitHub коммитом
`ecdf724`.

Очередь **S** теперь только на сведение и окончательные решения по долгу:
финальная сверка паритета F, мелкие остатки доски/контрактов из раздела ниже,
домен/HTTPS, офсайт-копия бэкапов и отзыв старого GitHub PAT. Эти пункты не
блокируют текущий рабочий релиз.

Долг, переданный держателям: вынести общий `window.KBGoto = {toCard, toTask,
toClient}` вместо трёх копий приёма «клик по рейке + фокус в rAF» (`management.js`
`gotoTarget`, календарь в `insights.js`, «Пульт дня»); показать ответственного в
карточке «Клиент 360» (сейчас не выводится вовсе, поэтому помечать отключённого там
негде); в `shell-v2-prototype.html` тот же паттерн «капсула + вложенное поле +
крестик внутри» остался в поиске финансов (`.fin-search-field`/`fin-search-close`) —
визуально дефект D2 там не закрыт; `GET /kanban/cards?debt=true&sort=debt_desc` —
заявка к бэку, чтобы снять клиентский потолок 60 строк дебиторки (на копии прода
сейчас 6 должников, не критично); в `board.js` недостижимый `procurementFields()`
и его кнопки `[data-item-attach]`/`[data-item-file]` (файл оттуда на сервер не
загружается вовсе, в отличие от рабочего `data-inv-up`); у `mutate('card')` при
неуспехе нет отката значения поля — общий тост «Не сохранено в CRM»
(предсуществующее, не поле `sender_email` лично); отключённого пользователя
по-прежнему можно назначить ответственным (продуктовое решение не принято).

Ветки `v2-item3-render-cost`, `v2-item4-drag`, `v2-item10-notifications`,
`v2-item16-server`, `v2-item18-infra`, `v2-roles-audit` слиты или заменены;
worktree `/private/tmp/crm-item4` можно снять — его WIP перенесён в main в
`7ff3a52`. Запись для главного агента: **сам исполнитель его не снимает**.

---

## Дефекты доски, найденные пунктом 6 (23.09) — очередь к держателю board.js

Источник: отчёт воркера пункта 6 (чтение кода), штамп в `V2-UI-AUDIT` §6 п.5.
Порядок = приоритет; пункт 1 очереди — хотфикс, обгоняет пункты 4/5/8/11.
Пакет для держателя board.js после слияния worktree'ов 3/4 (части C и D
спеки P0 + дефект 7, план верификации) собран в
`docs/audits/BOARD-PACKAGE-SPEC-2026-09-23.md`; прогон после фикса —
`tools/v2_queue_scenario.py` (CRM_USER/CRM_PASS, свободное браузерное окно).

1. **P0, живой на проде**: отмена групповой ТН, выписанной в текущей сессии,
   шлёт `DELETE /payments/transactions/{group_id}` (`shell-v2-board.js:2244`
   — `serverTxId = g.id`, ветка отмены `:1806-1843`) и удаляет ПОСТОРОННЮЮ
   транзакцию с этим номером. Хотфикс: серверный эндпоинт отмены группы
   (`writeoff_groups_router.py` + unwind в `services/payments.py`) и вызов
   его клиентом вместо `annul-tx`; до деплоя хотфикса — организационный
   запрет нажимать «Отменить» у групповых ТН. Серверная сторона готова
   (`cafc5a9`: эндпоинт annul + invoice_transaction_id); клиентская часть A —
   у сессии 3 по спеке, часть C — у следующего держателя board.js.
   **Клиент закрыт 23.09** (`88d31a2` в ветке `v2-item3-render-cost`, в main
   через `a7e504c`): `serverTxId` берётся из `invoice_transaction_id` (настоящий
   id документа группы), отмена выписанной группы идёт
   `POST /writeoffs/groups/{id}/annul` → `reloadData()`, `annul-tx` оставлен
   фолбэком только для документа группы без `written_off`,
   `DELETE /payments/transactions/*` из ветки отмены группы исчез. Стенд-прогон
   на копии прод-БД (8127): `invoice_transaction_id=426` ≠ id группы 12; отмена
   БЕЗ F5 и ПОСЛЕ F5 — ровно 1 `POST …/annul` 200 и **0** DELETE, документы
   197→198→197 (удалён ровно 426), оплаты 210→212 (остатки вернулись), карточки
   вышли из «Закрыто»; под `audit_manager` — 403, тост «доступна только
   администратору» и ни одной мутации. **Прод остаётся уязвим до деплоя обеих
   половин** (`tools/deploy.sh back routers/writeoff_groups_router.py
   services/payments.py schemas.py` + `tools/deploy.sh v2`); запрет нажимать
   «Отменить» у групповых ТН до этого момента в силе.
2. Отмена групповой ТН после перезагрузки — ложная кнопка: `annul-tx` по
   групповому документу (`card_id=NULL`) не снимает `written_off` с группы и
   не возвращает карточки из «Закрыто» (`services/payments.py:60-141`),
   а UI рапортует «остатки возвращены». **Закрыт 23.09** тем же клиентским
   фиксом (см. п.1): серверный annul разматывает группу по состоянию БД, а тост
   «остатки возвращены N карточкам» звучит только после 200 и успешного
   `reloadData()`, причём N берётся из ответа сервера; проверено сценарием
   «отмена после F5» на стенде 8127.
3. «Списано со склада» не делает `duplicate_as_document`
   (`shell-v2-board.js:1350-1364`; legacy `site/js/writeoffs.js:736-770`) —
   копия документа не появляется в «Документах». **Закрыт 23.09 (`7ff3a52`)**:
   вторым шагом зовёт `POST /payments/transactions/{id}/duplicate_as_document`;
   отказ второго шага НЕ откатывает первый (флаг на сервере уже стоит) — вместо
   этого `c._docCopyFailed[txId]` + кнопка «Создать копию ТН» в строке накладной
   + тост с причиной; успех добавляет запись в `card.docs` и рассылает
   `kb:documents` через `syncCard`. **Оговорка по данным:** в штатном пути (ТН
   выписана через `issue-invoice`, который сам зовёт `_issue_document`) сервер по
   дедупу возвращает уже существующую копию, то есть вызов идемпотентен и реальную
   копию создаёт только для записей, заведённых в обход выписки, — на копии
   прод-БД одна из 198.
4. Нет массового «убрать сделку из списания» (`DELETE /payments/cards/{id}/writeoff`,
   legacy `writeoffs.js:389-420`); покрыто поштучной отменой ТН + возвратом этапа.
   **Закрыт 23.09 (`7ff3a52`)**: массовая кнопка в тулбаре очереди «На списание»
   по выборке `kb-q-pick` **и** поштучная в строке обеих очередей — выборка
   существует только в «На списание», поэтому полностью выписанная сделка из
   «Списано» иначе недостижима; запросы последовательные (поштучный учёт отказа);
   `groupPick` очищается только по обработанным; роут админский → `data-admin-only`.
5. Нет «прикрепить к группе / выйти / распустить» (серверные роуты есть:
   `writeoff_groups_router.py:103,128,232`). **Закрыт 23.09 (`7ff3a52`)**:
   прикрепить/выйти/распустить; кандидаты на прикрепление фильтруются теми же
   четырьмя проверками, что и сервер (незакрытость и серверный id группы, этап
   `assembly`/`writeoff`, тот же клиент, тот же магазин); выход различает два
   исхода (тело-группа или «Группа распущена» — сервер сам удаляет группу на
   последнем выходе).
6. Бэк, держателя нет: `tests/test_package_b_money.py::test_clients_list_overdue_deals`
   падает в окне 00:00–03:00 MSK — `routers/clients_router.py:47` считает дату
   в UTC, тест в локальной зоне; падает и на чистом HEAD (проверено воркером
   17 через git archive). **Исправлен 23.09 (`20ab333`)**: сравнение через
   `constants.business_today()` (Europe/Minsk), роутер и тест берут границу
   из одной точки; проверено прогоном внутри окна границы (26 passed).
7. Клиент, держатель board.js: после отмены ТН (одиночной) карточка до ручной
   перезагрузки выпадает из очереди «На списание» в «Списано»/историю:
   `doCancelLocal` инвалидирует `remaining_kop`/`issued_total`
   (`shell-v2-board.js:1852-1854`), а `cardRemaining` на undefined возвращает
   null и `hasRemaining(null)` = false (`:98-115`) — фильтры очередей считают
   карточку без остатка. Воспроизведено 3/3 на стенде 8126 (карточка 553,
   частичная выписка + отмена, 23.09): POST и DELETE 200 за 0.0 с, диалог
   закрывается за 0.2 с, но после отмены `remaining_kop`/`issued_total` = null
   и карточка в истории. Фикс: после annul наполнять денежные поля из ответа
   сервера или пересчитывать локально из docs (issued = Σ docs,
   remaining = amount − issued); семантику `hasRemaining(null)` не менять
   без ревизии всех фильтров очередей. **Исправлен 23.09 (`cbea561`)**: вместо
   инвалидации — `recalcCardMoney(card)` (issued = Σ docs, issued_total =
   issued/100, remaining_kop = amount − issued с отсечкой нулём как в
   `services/card_money.py:44`) в `doCancelLocal` и `doCancelGroupLocal`;
   вызов после `splice`, чтобы отменённая запись не попала в сумму. Формула
   спеки `amount*100 − issued` НЕ использована: `card.amount` в board.js уже
   хранится в копейках, умножение завысило бы остаток в сто раз; при неизвестном
   `amount` остаток остаётся `null`, семантика `hasRemaining(null)` не тронута.
   Проверено `tools/v2_queue_scenario.py` на стенде 8127 (копия прод-БД):
   ДО — «FAIL после отмены карточка снова в очереди «На списание» (дефект 7) —
   history», `remaining_kop=None`; ПОСЛЕ — «PASS … — pending»,
   `remaining_kop=1822186`, что совпало с ответом сервера по карточке 553 до
   копейки. Ограничение (следствие формулы спеки, не реализации): при складском
   списании без копии в «Накладных» локальный остаток завышен до ближайшего
   `reloadData` — пересекается с п.3–5. Симметричная инвалидация денег в
   `issueGroup` (путь ВЫПИСКИ группы) не правилась — в следующий пакет держателя
   board.js. **Сделано 23.09 (`7ff3a52`)**: симметричный пересчёт денег добавлен
   и в путь выписки группы.
8. Клиент, держатель board.js (найден 23.09 верификацией пункта 10,
   предсуществующий): фокус проваливается в `body` после `kb:reloaded` —
   обработчик перезагрузки зовёт `openCard(fresh)`, содержимое дровера
   перестраивается (~244 мс после открытия, +2 мутации) и сфокусированная
   кнопка закрытия заменяется. Воспроизводится и на нетронутом пути клика по
   плитке (`activeElement=BODY`), поэтому к правкам 23.09 отношения не имеет;
   путь `KBBoard.open` после `ba0f65b` фокус возвращает и дефект не проявляет.
   Лечение: сохранять и возвращать фокус при перерисовке дровера (по `data-*`
   или роли последнего фокусного элемента) либо не перестраивать дровер целиком
   на `kb:reloaded`. **Закрыт 23.09 (`7ff3a52`)**: адрес сфокусированного
   элемента снимается первой строкой `openCard` и возвращается последней —
   единственная точка сборки дровера, ~15 вызовов заплатками не обвешаны;
   разрешение id → путь → `data-*`, финальная сверка фактом
   `document.activeElement === el`, при неудаче возврат на `.kb-detail-close`,
   а не в `body`; тот же провал закрыт по пути `KBConfirm`; путь `KBBoard.open`
   из `ba0f65b` не сломан (в его rAF ранний выход, когда фокус уже внутри дровера).
9. Клиент, держатель board.js (найден 23.09 при ревью P0, косметика): строка
   «Групповая ТН …» в списке «Накладные» дровера рисуется по `c.groupId` без
   проверки `writtenOff`, поэтому у разматанной группы (annul не снимает
   `writeoff_group_id` с карточек) и у группы в «Сборке» видна строка с пустым
   номером и кнопкой «Отменить». Клик теперь безопасен (сервер вернёт 400,
   данные перечитаются), но строка-пустышка осталась; рядом с п.5 реестра.
   **Закрыт 23.09 (`7ff3a52`)**: строка групповой ТН рисуется только при серверном
   `writtenOff`; вместо пустышки — осмысленная строка «Группа «…» · карточек: N ·
   групповая ТН ещё не выписана». Попутно убран второй такой же обман в «Истории»:
   счётчик «Выписанные накладные» больше не приписывает единицу за невыписанную
   группу, сумма покрытия показывает «—» вместо 0,00 (сервер разбивки не отдаёт,
   `covers[].amount === null`); для осиротевшей записи-документа оставлен доступ
   «Удалить документ группы» (штатный `annul-tx`, под админом).

**Дефекты, найденные и закрытые 23.09 при закрытии пунктов** (не входили в исходную
очередь пункта 6; номера продолжены, чтобы не перенумеровывать очередь выше — все
уже исправлены, приоритета не несут):

10. Бэк: `POST /email-parser/link/{card_id}` не делал ничего — всё тело после
    `raise HTTPException(404)` было втянуто дедентом в ветку `if not src or not dst`,
    то есть недостижимо; ручка отвечала `null` с кодом 200, а оба клиента (v2
    `board.js` и legacy `card_modal.js:2207`) рапортовали успех. Починено дедентом
    в `ec5fde1`, 6 регрессионных тестов (доказанно падают на старом коде — 3 failed).
    Системный ast-обход 29 файлов бэка других случаев недостижимого кода не нашёл;
    сканер проверен на себе: на версии из HEAD находит ровно одно место — этот баг.
11. Бэк: `PATCH /cards/{id}` молча игнорировал `sender_email` — поле есть в схеме и
    модели, но в `card_details_router.py` не встречалось ни разу: 200 без изменений,
    критерий пункта 5 был недостижим одним клиентом. Починено в `68baf04`.
12. Бэк: `TaskUpdate` — `null` не очищал поле: восемь веток гейтились `is not None`,
    а legacy-форма шлёт `null` именно как «очистили», то есть снять срок,
    ответственного или привязку у задачи было невозможно. Переведено на
    `model_fields_set` в `68baf04`; для not-null колонок (`title`, `status`) — 400;
    пересчёт снимков `card_title_snapshot`/`client_name_snapshot` и при очистке, и
    при смене привязки; ветка «срок снят» в уведомлениях стала достижимой.
13. Бэк: `assignee_id` давал 500 вместо 400 при несуществующем id (FK) — и в
    `create_task`, и в `update_task`. `68baf04`.
14. v2: календарь рисовал демо-данные на боевой базе — события только под гейтом
    `cal.y === 2026 && cal.m === 8`, на других месяцах пустая сетка и подпись
    «Демо-события есть только в сентябре 2026», ни день ни событие не кликабельны,
    `title` кнопки «Сегодня» обещал возврат к демонстрационному месяцу. Переведён на
    боевые данные из `KBData` (срок сделки, плановая дата оплаты, срок задачи),
    день/чип/«+N ещё» стали реальными `<button>` с русскими `aria-label`, добавлен
    диалог «События дня», переход к объекту существующими мостами (`KBBoard.open`,
    приём `gotoTarget`). `353d028`. Это ответ на вопрос брифа «не остались ли в v2
    демо-данные вне `?demo=1`» — остались, почищено девять мест, включая текст
    «Здесь демо просмотра, редактирование не реализовано» внутри строк журнала на
    боевой базе.
15. v2: одна ТН показывалась дважды в таблице «Документы» — строкой реестра из
    `KB_FIN_SOURCE` и строкой с доски из `card.docs`; счётчик «Видимых документов»
    был завышен вдвое (198 строк реестра при 194 различных `docTxId`). Оставлена
    строка реестра (она несёт магазин, счёт, `print`-селект и серверные
    `is_invoice_doc`/`is_bill_doc`), итог 197 строк = 197 документов, повторный
    проход идемпотентен. `353d028`. **Внимание для владельца:** видимое число
    «Всё у нас» уменьшится примерно до 133 с прежних задвоенных 160+, потому что у
    27 документов `is_invoice_doc=1, is_bill_doc=0`, и «полноту» рисовала строка
    с доски.
16. v2: `contactPhone()` была определена дважды, второй IIFE вызывал её из своей
    области → `ReferenceError` при пустом телефоне; не стреляло только потому, что
    кнопка «Позвонить» без телефона `disabled`. `353d028`.
17. v2, доска: `issueGroup` падал с `ReferenceError` на отказе — `catch` писал
    `submitted = false`, а `submitted` является локальной переменной `openGroupIssue`;
    ошибка глушилась, форма висела. `7ff3a52`.
18. v2, доска: Enter/Space на галочке выборки открывал дровер вместо действия —
    обработчик `keydown` на `[data-card][role=button]` звал `preventDefault()`, не
    глядя на вложенный контрол. Добавлен guard (`7ff3a52`).
19. v2, доска: локальный снимок группы после выписки писал `store` id-значением и
    дату в ISO — финансы печатали id и ISO до F5. `7ff3a52`.
20. v2: имена вложений в MIME encoded-word (`=?windows-1251?B?…?=`,
    `=?koi8-r?B?…?=`) не декодировались — в dev-копии БД 25 из 92 имён. Добавлен
    декодер RFC 2047: сначала MIME, и только если он ничего не нашёл — percent
    (иначе декодировали бы уже декодированное); сверен с эталонным
    `email.header.decode_header` 25/25, любой отказ возвращает исходную строку.
    `7ff3a52`.
21. Бэк: не было `PATCH /custom/objects/{id}` и правки поля — «изменить» означало
    «удали и создай заново», а удаление поля сносит его значения. Добавлены
    `PATCH /custom/objects/{id}` и `PATCH /custom/fields/{id}`; смена типа поля
    разрешена только пока у поля нет значений (значения живут в шести типизированных
    столбцах `custom_field_values`, столбец выбирается по `field_type` при записи,
    а читается «первый непустой» — смена типа у непустого поля расщепила бы его без
    возможности починить). `5ca4939`.
22. Бэк: `{"secret": ""}` у вебхука проходил с 200 и молча лишал получателя проверки
    подписи — теперь пустая строка и пробелы дают 400, снять подпись можно только
    явным `null`. `68baf04`.
23. v2: `insights.js` не был подписан на `kb:reloaded` — после `KBData.reload()`
    пульт, задачи и клиенты оставались на старом снимке. Подписка сделана ленивой
    (перерисовка только видимых разделов, наблюдатель за `hidden`/`class`), «Пульт»
    и календарь не дублируются — их обновляет `kb:documents` от `board.js`.
    `353d028`.

## 24.09 — плавность v2: первый пакет и переносы в списке

Проверен и собран локально на стенде 8127 с копией БД. В текущий пакет вошли
точечное обновление панели накладных после `loadWarehouseFlags()` без полного
`openCard()`, устранение двойного render при применении сохранённого вида и
дедупликация `GET /clients/{id}/balance` в Client 360. Дополнительно исправлены
переносы в списке сделок: `.col-pay` и `.col-store` получили `white-space: nowrap`
для ширины от 901 px, адаптивное скрытие колонок на узких экранах сохранено.
Изменения вносятся только в `server_snapshot/tools/mockups/`, `site-v2` пересобран
штатным сборщиком; прод, БД и legacy не затрагивались.

Проверки локального gate: `bash scripts/check.sh` — exit 0, `897 passed`, JS syntax OK,
свежесть `site-v2` OK, версии статики OK; census на стенде — exit 0, 104 API-вызова,
0 аномалий `undefined/NaN`, KBData загрузился, карточки и разделы открылись в обеих
темах. Две записи `400` census относятся к существующему безопасному клику
«Синхронизировать сейчас» при незаполненных почтовых настройках. Функциональный
аудит: 24/26; два известных провала состояния копии БД — рассинхронизация `stage` у 177
из 391 карточек и пустые настройки почты (`Синхронизаций ещё не было`). Оба не
относятся к текущим правкам. Визуальная проверка списка на 1280 px подтвердила
однострочные ячейки и высоту строк около 32 px; на 800 px обе колонки скрываются.
Коммит, push и деплой не выполнялись.

## 24.09 — пакет 4: loading-пути и единый финансовый поиск

Проверен фактический порядок обновления в корзине, уведомлениях, workflow и custom
fields. Эти loading-пути уже ограничены изменяемой областью (`kb-queue-body`,
`v2-notif-panel`, строки detail/fields), а начальный сброс полей при смене типа
нужен для исключения устаревших данных; дополнительная перерисовка не внесена.

Подтверждённый визуальный долг D2 исправлен: все три финансовых поиска переведены
с вложенной «капсулы» на плоский контрол с одним `input[type=search]` и одним
крестиком. Обработчик `wireFinSearch()` переведён на прямые потомки, умеет очищать
непустой запрос без закрытия, закрывать пустой поиск крестиком и Escape, ставит
`inert` в закрытом состоянии и возвращает фокус на лупу. Нативный WebKit cancel
скрыт, поэтому второго крестика нет.

Проверки после пересборки: `bash scripts/check.sh` — exit 0 (`897 passed`, JS
syntax OK, свежесть `site-v2` OK, версии статики OK); `git diff --check` — OK.
Стенд 8127: в светлой и тёмной темах проверены открытие, плоская геометрия,
фильтрация, очистка, закрытие пустым крестиком и Escape для реестра оплат;
структурная проверка подтвердила отсутствие старых вложенных классов у всех трёх
поисков. Прямой `KeyboardEvent` Escape дал `defaultPrevented=true`, закрытие и
возврат фокуса. Ограничение браузерного стенда: Playwright `press('Escape')` в IAB
не доставил событие, поэтому для подтверждения обработчика использована прямая
доставка DOM-события. Повторный census с локальной учёткой `audit_admin` был
запущен, но завис внутри диагностического сценария более чем на 40 минут без вывода
и остановлен; это не результат ошибки приложения. Ранее завершённый census этой
кампании остаётся зелёным, а текущие изменения не трогают API. Коммит, push и
деплой не выполнялись.

## 24.09 — пакет 5: ленивые сетевые загрузки вкладок карточки

Следующий минимальный шаг после пакета 4 реализован в `shell-v2-board.js`: полная
DOM-ленивость вкладок отложена, но eager-вызовы сетевых загрузчиков убраны из
общего `openCard()`. `loadCardHistory(c)` теперь запускается при выборе `history`,
`loadWarehouseFlags(c)` — при выборе `invoices`; `loadRelatedEmails(c, false)`
сохранён в той же ветке истории. Кеши `historyLoaded` и `_invFlags` не менялись.

`loadWarehouseFlags()` после ответа точечно добавляет отсутствующие чекбоксы в
существующие строки накладных через `updateWarehouseFlagControls()`, вместо полного
`openCard()`. Сохранены обработчики списания, повторной копии ТН и rollback; активная
вкладка, фокус и прокрутка дровера не перестраиваются.

Проверки после штатной пересборки `python3 tools/build_site_v2.py`:
`bash scripts/check.sh` — exit 0 (`897 passed`, JS syntax OK, свежесть `site-v2` OK,
версии статики OK), `git diff --check` — OK, `node --check` для source и generated
`shell-v2-board.js` — OK.

На демо-стенде 8127 открытие карточки K-104 на «Обзоре» не вызвало ни одного
запроса; переключение на «Историю» и «Накладные» переключило панели без запросов.
В demo-режиме загрузчики штатно завершаются раньше API из-за отсутствия CRM-токена
и/или данных документа, поэтому реальные GET-ветки подтверждены структурно по
исходнику, а не измерением authenticated API. Предыдущий census этой кампании
остаётся зелёным; текущий пакет не меняет API-контракты. Коммит, push и деплой не
выполнялись.

Дополнительная авторизованная проверка 24.09 на локальном стенде 8127 с копией БД,
узким read-only сценарием на карточке 1063 с одной накладной: на «Обзоре» не было
GET истории и складских флагов; переход на «Историю» дал один GET
`/activity?card_id=1063&limit=100`; переход на «Накладные» дал один GET
`/payments/cards/1063/invoices`, после чего появился чекбокс `data-wh`. Повторные
переходы туда-обратно после кеша не дали запросов, `pageerror=0`, активная вкладка
invoices сохранилась. У дровера не было отдельного scroll-контейнера, поэтому
`scrollTop` отдельно не применим.

Read-only census без demo на том же стенде с локальной sandbox-учёткой: light —
exit 0, 73 API, все 200, 0 JS, 0 network, 0 `undefined/NaN`, 0 KBData; dark —
exit 0, 80 API, все 200, те же нули. В обеих темах видны все 7 разделов.

Диагностический census без `--read-only` не имеет общего deadline: при 401
авторизации он продолжает ждать login overlay/KBData, затем может выполнять длинный
обход контролов (до 40 на представление, `click_soft` до 5 секунд на клик). Поэтому
запуск был ограничен 240 секунд и остановлен; это не новый pageerror приложения.
Census считать зелёным можно только для `--read-only`, а обычный diagnostic pass
пока не классифицирован.

## 24.09 — финальная проверка пакета плавности перед релизом

Фактически подтверждены `_historyLoading` и `_invFlagsLoading`: быстрые переходы по
вкладкам карточки не создают дублирующие GET, пустой успешный ответ истории
кешируется, ошибка не кешируется и retry возможен. Ответы обновляют DOM только у
активной карточки. В Client 360 подтверждены общий кеш/in-flight баланса, защита от
stale-ответа старого клиента и переключение вкладок без полной пересборки drawer.
Проверка `kb:reloaded` выполнена корректно через `document.dispatchEvent(...)`:
после события баланс был запрошен повторно, `pageerror=0`; отправка в `window` была
ошибочной для слушателя `document` и дефектом приложения не считается.

Финальный `bash scripts/check.sh` — exit 0: 897 passed, JS syntax, свежесть
`site-v2`, версии статики и `git diff --check` зелёные. Read-only census на стенде
8127 с локальной копией БД прошёл в обеих темах: light — 74 API/200, dark —
81 API/200; 0 JS, 0 network, 0 DOM-аномалий, 0 KBData, все 7 разделов видны.
Первый повтор с неверной локальной учёткой дал 401 и не учитывается как проверка
приложения. Legacy, backend-контракты и production-БД не менялись.

## 24.09 — S: полный v2-аудит и As-built после последней правки

Проведён полный проход по v2 на локальной копии БД и sandbox-авторизации. В текущем
дереве подтверждены и оставлены следующие исправления: единый плоский финансовый
поиск, канонические dialog shells, primary/ghost semantics, dark accent tokens,
нативные finance date/open buttons, keyboard bridge, combo/listbox ARIA, bounded
financial details и responsive table geometry. Последним подтверждённым дефектом был
конфликт общего `#view-fin button[aria-pressed="true"]` с тёмной финвкладкой; общий
селектор теперь исключает `[data-fin-tab]`. После штатной пересборки браузер подтвердил
`rgb(142, 138, 245)` фон, `rgb(20, 22, 26)` текст и отсутствие box-shadow.

Проверки текущей As-built:

- `bash scripts/check.sh` — exit 0, `897 passed`, JS syntax, data-handler heuristic,
  свежесть `site-v2` и версии статики зелёные;
- `v2_design_matrix.py` — 102 снимка на 1920/1440/1280/1024/768/430/375 в обеих
  темах, 0 page overflow, 0 overlaps; длинные названия/контакты отмечены как
  ожидаемое truncation, прозрачный текст checkbox-кнопки — false-positive contrast;
- browser matrix на 8125 — семь маршрутов, light/dark, responsive 375–1440, focus,
  Escape, finance search, date editor, finance open, combo и dark pressed-state PASS;
- функциональный аудит на локальной копии 8127 после исправления самого сценария:
  26 проверок, 25 PASS; единственный FAIL — настройки почты в fixture
  («Синхронизаций ещё не было»), это ограничение данных стенда;
- read-only census на 8127 — exit 0: 0 JS-ошибок, 0 сетевых сбоев, 0 DOM-аномалий,
  0 дыр `KBData`, 73 API-вызова, все 73×200;
- `git diff --check` и `py_compile tools/v2_functional_audit.py` — OK.

Диагностика устранила два ложных отказа аудита: сверка статуса «Закрыто» теперь
использует канонический `closed`, а после API-мутации сценарий перезагружает
`KBData` перед проверкой финансовой строки. Production, production-БД и legacy не
использовались; коммит, push и деплой не выполнялись. Пункт 0.3 (историческое
расследование зависания supplier combo) не переоткрывался: в текущем проходе он
не воспроизводился, а combo-поведение проверено отдельно.

## 24.09 — отдельный read-only аудит backend

Проведён аудит backend без изменения кода, миграций, конфигурации, production и
рабочей production-БД. Полный доказательный отчёт — `docs/audits/BACKEND-AUDIT-2026-09-24.md`.

As-built этого прохода:

- `cd server_snapshot && ./.venv/bin/python -m pytest -q` — exit 0, `897 passed`;
- `CRM_TEST_SCHEMA=models ./.venv/bin/python -m pytest -q` — exit 0,
  `893 passed, 4 skipped` (ожидаемые profile-specific skips);
- disposable production fixture + migrations: `PRAGMA integrity_check=ok`,
  `PRAGMA foreign_key_check=0`, 31 таблица/76 индексов;
- подтверждены bearer после logout (`200` после `POST /auth/logout`), legacy JWT
  без `pv` после смены пароля (`200`), soft-deleted card direct GET
  (`GET /kanban/cards/{id}` → `200`), а также конкурентный перевыпуск накладной
  (2 успешных `issue_invoice`, 2 invoice rows, ledger `[40, 60, 60]` при сумме
  сделки 100);
- числовой harness подтвердил acceptance `NaN`, `Infinity` и отрицательного
  transaction amount; production headers повторно подтвердили CSP с
  `unsafe-inline` и отсутствие HSTS;
- `git diff --check` — exit 0; untracked session brief не включён.

Изменения кода, исправления, commit, push и deploy в рамках аудита не выполнялись.
Следующий шаг требует отдельного решения владельца: выбрать P0/P1 пакет из
backend-отчёта и реализовать его с отдельными конкурентными/security проверками.

## 24.09 — первая волна исправлений backend

По итогам read-only аудита локально закрыты BA-01, BA-02 и BA-03. Объём волны
не включал production, миграцию на production, изменение v2/legacy-фронта, commit,
push или deploy. Полный As-built — `docs/audits/BACKEND-AUDIT-2026-09-24.md`.

Выполнено:

- **BA-01:** добавлены модель и миграция `0016_revoked_auth_tokens.py`; logout
  независимо отзывает bearer и cookie, проверка отзыва выполняется до sliding
  renewal, legacy JWT без `jti` использует SHA-256 fallback, сырой JWT в БД не
  сохраняется;
- **BA-02:** `add_invoice` и `issue_invoice` сериализованы no-op UPDATE,
  резервирующим SQLite writer до чтения остатка и удерживаемым до commit;
- **BA-03:** `GET /kanban/cards/{card_id}` скрывает soft-deleted card; trash,
  restore, soft-delete и permanent-delete не менялись;
- добавлены отдельные auth-session/migration-тесты, четыре конкурентных
  payment HTTP-теста и полный card lifecycle-тест.

Фактическая финальная проверка:

- targeted production DDL — `88 passed`;
- targeted ORM — `87 passed, 1 skipped` (ожидаемый profile-specific skip);
- полный suite production DDL — `910 passed in 669.87s`, exit 0;
- полный suite ORM — `906 passed, 4 skipped in 675.12s`, exit 0;
- `git diff --check` — exit 0; legacy `site/`, generated `site-v2/`, production и
  untracked session brief не входят в изменения волны.

BA-04 (legacy JWT без `pv`) и остальные находки backend-аудита не менялись.
Перед релизом исправлений требуется отдельное решение владельца на backup,
production migration 0016, backend deploy и post-deploy проверки.
