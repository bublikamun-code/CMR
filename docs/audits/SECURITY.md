# Состояние безопасности CRM — обновлено 31.08.2026

Документ заменяет аудит от 07.08.2026: большинство его находок устарело
(исправлено или неактуально). Ниже — проверенное состояние на 31.08.2026.
Метод: код-ревью + активные тесты на локальном стенде + пассивные проверки
прода (GET). Полный отчёт с сырыми результатами — в сессии ZCode от 31.08.

## ✅ Проверено — работает

| Проверка | Результат |
|---|---|
| Неавторизованный доступ (7 ключевых эндпоинтов API) | 401 везде |
| Эскалация ролей (manager → admin-эндпоинты, create-tenant, самоповышение) | 403 |
| Поддельный JWT | 401 |
| Удалённый пользователь / смена пароля | Токен умирает мгновенно (перезачитывание БД + claim `pv`) |
| Rate limit логина 30/мин | 429 после ~28; обход через X-Forwarded-For закрыт (`forwarded_allow_ips=127.0.0.1`) |
| SQL-инъекции | SQLAlchemy параметризует; проба `' OR 1=1--` без аномалий |
| XSS | Фронтенд экранирует (проверено живым payload в названии карточки) |
| Path traversal при скачивании файлов | basename + realpath-гейт |
| Секреты/БД через веб (`.secret_key`, `.db`, бэкапы, `email_settings.json`) | 404, не отдаются |
| Секреты в git | Никогда не коммитились (`.gitignore` + пустая история) |
| Хранение паролей | bcrypt `$2b$12`, уникальная соль, plaintext нигде |
| `/email-parser/sync-all` | Закрыт cron-токеном (`X-Cron-Token`, compare_digest) |
| Загрузка файлов | Белый список расширений + magic bytes (с 31.08), файлы вне webroot, раздаются с авторизацией |
| Бэкапы | Ежедневно 03:01, ротация 37 копий, лог «OK», секреты 600 |
| Watchdog | Рестарт только при падении процесса |

## ⚠️ Открытые риски (по убыванию)

| # | Риск | Суть | Оценка |
|---|---|---|---|
| 1 | Токены в localStorage, 24 ч, нет CSP-истории | XSS-экспозиция: найденный XSS украл бы токен. Экранирование работает, CSP добавлен 31.08 (осторожный, `unsafe-inline` для скриптов — легаси-фронт). Следующий шаг: сокращение жизни токена / HttpOnly-cookie | Средний |
| 2 | Rate limit только на логине | Остальные эндпоинты без лимитов; поллинг каждые 15 с усугубляет DoS-поверхность | Низкий-средний |
| 3 | Нет пагинации на списках | Канбан отдаёт все карточки целиком; при росте на порядки — деградация | Низкий |
| 4 | Webhook SSRF-редиректы, длина строк без лимитов | Из старого аудита, не проверялись повторно | Низкий |

## 🔴 Инциденты/потери данных (к истории)

- **Вложения id 156, 157, 162 и подобные (август 2026)** — файлы физически
  отсутствуют в `uploads/` (старая схема санитизации имён + перенос хостинга).
  Восстановлению кодом не подлежат — только перезагрузка вручную.
  Записи в БД показывают «Файл не найден на сервере».
- **31.08.2026, split-brain воркеров**: `pm2 restart --update-env` из пустого
  ssh-шелла выкинул `CRM_DATA_DIR` — новый воркер открыл пустую базу в корне.
  Закрыто: `.pm2.env` на сервере + его подхват в `server.py` и в `deploy.sh`.

## 🖥 Транспорт

- TLS на `*.nip.io` — самоподписанный сертификат; редирект HTTP→HTTPS и HSTS
  не включены (включение покажет браузерное предупреждение). План: боевой домен
  + Let's Encrypt (домен `cmr-svetvdome.online` сейчас не резолвится — DNS).
- С 31.08 приложение отдаёт `Referrer-Policy`, `Permissions-Policy` и
  осторожный `Content-Security-Policy` (см. `main.py` middleware).

## Как перепроверить

Активные тесты из сессии 31.08: неавторизованные GET к API (ожидаем 401),
manager против админ-эндпоинтов (403), 31 попытка логина (429), payload XSS
в названии карточки (экранируется), exe-под-видом-jpg при загрузке (400).
Прод-пассив: заголовки ответа, недоступность `.secret_key`/`.db`/бэкапов по HTTP.
