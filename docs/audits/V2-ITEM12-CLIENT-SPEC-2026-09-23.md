# Пункт 12, клиентская половина: доска и админка на серверном справочнике статусов (спецификация, 23.09)

Документ для исполнителя — сессии, которая возьмёт клиентскую половину пункта 12
(`docs/audits/V2-WORKPLAN-2026-09-22.md`, пункт 12). Серверная половина уже
готова: миграция `server_snapshot/migrations/0013_fill_deal_statuses.py` +
`tests/test_migration_0013_deal_statuses.py`, коммит `e77b86b`. Все ссылки ниже
сняты с `main` @ `9a0b454` (23.09 02:08); перед правкой сверить строки — дерево
меняется несколькими сессиями.

## 0. Главное ограничение: релиз только вместе с миграцией

Непустой `/dictionaries/statuses` переключает v2 с хардкода `STATUS_MAP` на ветку
словаря (`server_snapshot/tools/v2-boot-template.js:124`). Пока клиентская
половина не сделана, применение миграции 0013 ломает доску: «Закрыто» получает
`role='board'` (`v2-boot-template.js:134`) → появляется пятая колонка со 179
закрытыми сделками, а её id `done` дублирует псевдо-этап «Списано»
(`tools/mockups/shell-v2-board.js:1289`). Поэтому:

- **не применять 0013 ни к проду, ни к общему стенду 8126** (`/tmp/v2-audit-prod`)
  до того, как этот документ исполнен и пересобран `site-v2`;
- деплоить одним релизом: статика v2 (`tools/deploy.sh v2`) + бэкенд с миграцией
  (`tools/deploy.sh back`), на проде — после `.backup` и слова владельца;
- для разработки поднимать собственный стенд на своём порту со своей копией БД
  (`sqlite3 /tmp/v2-audit-prod/crm_app.db ".backup /tmp/item12-client/crm_app.db"`,
  `CRM_DATA_DIR=...`), порты 8125 и 8126 не занимать.

Правки — только в источниках: `server_snapshot/tools/v2-boot-template.js`,
`server_snapshot/tools/mockups/shell-v2-*.{js,css}`; `site-v2/` не править
руками, пересборка `cd server_snapshot && python3 tools/build_site_v2.py`.

## 1. Решения, которые уже приняты (исполнителю не перепроектировать)

**Р1. Роль статуса остаётся выводом клиента из имени, но «Закрыто» — не колонка.**
Серверной колонки `role` нет (`models.py:300-305`, `schemas.py:868-871` отдают
только `id, name, position, color, is_active`). Ввести `role` в схему — отдельная
задача и решение владельца (миграция 0014 + админка). Здесь: в
`v2-boot-template.js:134` роль выводится как
`name === 'На списание' ? 'writeoff' : name === 'Закрыто' ? 'archive' : 'board'`.
Колонки доски по-прежнему четыре, «Архив» — это пункт 13, а не 12.

**Р2. Развести два смысла id `done`.** Сегодня `done` означает одновременно
(а) канонический статус «Закрыто» (`CANON_ID`, `v2-boot-template.js:121`) и
(б) производный псевдо-этап «полностью списано» (`board.js:1693-1694` пишет
`stage='done'` после полной выписки; очередь `board.js:569`). Из-за (а)+(б)
179 закрытых сделок уже сейчас попадают в очередь «Списано». Решение:
каноническому «Закрыто» присвоить id **`closed`**, а `done` оставить
исключительно за псевдо-этапом выписки. Поведение сохранить: фильтр очереди
`board.js:569` дополнить `closed`, чтобы закрытые сделки остались в «Списано»
так же, как сейчас (отдельный вопрос «правильно ли это» — в пункт 13/архив).

**Р3. Переименование канонического статуса запрещено в UI.** Карточка хранит
имя строкой, контракт с бэком — имя (`boot:234`, `stageName` → PATCH), а сервер
валидирует статус по `schemas.CARD_STATUSES` (`schemas.py:237-238`,
`CardUpdateStatus` `:326-333`, роут `routers/kanban_router.py:147-148`) и отдаёт
422 на неканоническое имя (`tests/test_statuses_kanban.py:82-97`). Переименование
«Сборка» → «Сборка заказов» сегодня молча уронило бы 19 карточек в
«Новый запрос» (фолбэк `boot:234`) и сделало бы статус неназначаемым. Поэтому:
в админке (`tools/mockups/shell-v2-management.js:98-126`) имя канонического
статуса — только чтение (цвет и позиция — можно), с подсказкой почему.
Переименование с пропагацией в `cards.status` — отдельный пункт, требует
решения владельца (меняет прод-данные).

**Р4. `canAssign` проверять везде, где статус можно назначить.** Сейчас флаг
ставится только в ветке словаря (`boot:139`) и потребляется ровно в одном месте —
фильтре option'ов селекта «Этап» (`board.js:1297`). Обход: drag&drop
(drop-зоны `.kb-col` `board.js:275-276`, обработка `:348`) и клавиатурная смена
(`:1947`) флаг не смотрят → пользователь получит 422 от сервера. Добавить
проверку `canAssign !== false` в оба места с внятным тостом («Статус не из
справочника карточек: сервер его не примет»), а не тихим отказом.

**Р5. `is_active` вывести в админку.** Клиент уже фильтрует неактивные
(`boot:126`), а управлять флагом нечем: в форме статуса полей только
`name/color/position` (`management.js:21`), `is_active` приходит в
`schemas.DealStatusUpdate` (`schemas.py:861-865`). Добавить переключатель;
снятие флага убирает колонку.

**Р6. Убрать фолбэк «неизвестный статус → Новый запрос».** `boot:234`:
`stage: stageOf[String(c.status||'')] || 'new'`. При отключённом или
переименованном статусе карточки молча переезжают в первую колонку. Третья ветка
`boot:198-206` уже умеет достраивать колонку для неизвестного статуса
(`position: 100+len`), но из-за фолбэка `stage` у карточки к тому моменту равен
`'new'` — появляется пустая колонка, а карточки висят не там. Фолбэк заменить на
тот же slug, что используется в `:198-206`, чтобы карточка попадала в свою
достроенную колонку.

## 2. Работы по файлам и держатели

| Часть | Файл | Держатель на 23.09 | Что делать |
| --- | --- | --- | --- |
| A | `tools/v2-boot-template.js` | свободен | Р1 (роль `archive` для «Закрыто»), Р2 (`CANON_ID['Закрыто'] = 'closed'`, `stageOf`), Р6 (фолбэк), удалить мёртвую `STAGE_OF_STATUS` (`:57-60`, не читается), привести `writeoffStatus()` (`:394-396`, без сортировки) к виду демо-дубля (`data.js:160-163`, с сортировкой) |
| B | `tools/mockups/shell-v2-board.js` | пункты 3 и 4 (worktree `crm-item3`, `crm-item4`) — ждать слияния | Р2 (все места про статус → `closed`, про выписку → оставить `done`), Р4 (drop `:275-276`/`:348`, клавиатура `:1947`), убрать дубль `{id:'done',name:'Списано'}` в селекте этапов `:1289` |
| C | `tools/mockups/shell-v2-insights.js` | свободен | те же id: `:56, :177, :337, :551` |
| D | `tools/mockups/shell-v2-management.js` | пункт 10 (worktree `crm-item10`) | Р3 (имя канонического — read-only + подсказка), Р5 (`is_active`), роль показывать честно (`:16`, `:61-66`) |
| E | `tools/mockups/shell-v2-data.js` | свободен | синхронизировать демо-дубли `boardStatuses()` `:156-158`, `writeoffStatus()` `:160-163`, `statusColor` `:152-155` с boot-версией, чтобы `?demo=1` не расходился с продом |

Части A+B+C+E должны лечь одним набором: boot-шаблон без board.js сломает
доску, board.js без boot-шаблона — тоже. D можно отдельным коммитом.

**Полный список мест с `done`/«Списано»** (найдено разведкой, для части B):
boot `:59, :121, :144, :151`; `board.js` `:569, :684, :758, :785, :798, :882,
:896, :1288, :1289, :1297, :1303, :1693, :1694, :1698, :1820, :1855, :1932,
:1947`; `insights.js` `:56, :177, :337, :551`. Правило классификации: если код
говорит о статусе сделки (колонка, бейдж этапа, селект, имя для сервера) — это
`closed`; если о факте полной выписки (очередь «Списано», readonly после
выписки, откат из выписки, `stage='done'` после полного списания) — это `done`.
Каждое место исполнитель классифицирует сам по контексту и приводит
классификацию в разделе As-built — список мест может измениться до старта.
Не относятся к статусам: `board.js:917` («Списано со склада»),
`prototype.html:814, 1110, 1378` (колонка реестра `posted`).

## 3. Критерий готовности (проверяемый)

Стенд: своя копия БД с применённой 0013, порт не 8125/8126.

1. Доска рисует **те же четыре колонки** («Новый запрос», «В работе»,
   «Ждет оплаты», «Сборка») и очередь «На списание»; «Закрыто» колонкой НЕ стало.
   Скриншоты обеих тем до правки (на хардкоде, справочник пуст) и после (со
   справочником) совпадают по набору и порядку колонок.
2. Очередь «Списано» содержит тот же набор id карточек, что и до правки
   (сравнить списки, а не «на глаз») — поведение Р2 сохранено.
3. Админка v2 → «Статусы сделок»: 6 строк из справочника, цвет и позиция
   редактируются, имя канонического — read-only с подсказкой, `is_active`
   переключается; снятие флага убирает колонку, а карточки этого статуса
   оказываются в достроенной колонке (Р6), а не в «Новом запросе».
4. Новый неканонический статус (например «Тестовый») создаётся из админки,
   появляется колонкой, но назначить его карточке нельзя ни селектом, ни
   перетаскиванием, ни клавиатурой (Р4); 422 от сервера не возникает.
5. Переименование неканонического статуса работает и не теряет карточки;
   `PATCH /dictionaries/statuses/{id}` → 200.
6. Перепись `python3 tools/v2_error_census.py --base <свой стенд>/v2/`:
   0 pageerror, 0 DOM/дата-аномалий, js_errors не выше планки известных
   артефактов стенда (4: один `400 POST /email-parser/sync` + три
   «invalid form control» в админке).
7. Регрессия производительности пункта 2 не ухудшилась:
   `python3 /tmp/v2-audit-prod/scripts/s11_board_perf.py --out <свой файл>` —
   мутации `#kb-board` на галочку < 300, longest task < 30 мс, 1 PATCH,
   0 мутаций `#view-fin` (эталон — `perf-item2-recheck-{1,2}.json`: 4 / 0 / 1 / 0).
8. Регрессия блока 0: `python3 /tmp/verify-0-block.py` (если стенд на обычном
   порту; иначе прогнать его сценарий вручную — клиенты-петля и комбобокс
   поставщика).
9. `node --check` на каждом правленом JS; пересборка
   `cd server_snapshot && python3 tools/build_site_v2.py`; pytest только если
   тронут бэкенд (по этой спеке он не трогается).

## 4. Что в пункт НЕ входит

- Серверная колонка `role` в `deal_statuses` (миграция 0014 + схема + админка) —
  отдельное решение владельца; сейчас роль выводится клиентом из имени (Р1).
- Пропагация переименования статуса в `cards.status` — меняет прод-данные.
- Режим «Архив»/«Список» для закрытых сделок — пункт 13.
- Разбор того, правильно ли закрытые сделки висеть в очереди «Списано» —
  поведение намеренно сохранено (Р2), вопрос к пункту 13.

## 5. As-built (заполняет исполнитель)

| Дата | Коммит | Что сделано по частям A–E | Классификация мест `done`/`closed` | Проверки 1–9 (числа) | Что осталось |
| --- | --- | --- | --- | --- | --- |
| 23.09 | A+B+C+E — `8d24f13` (исполнитель воркер `builder`, ревью — главная сессия; якоря после пункта 13 — `V2-ITEM12-EXEC-NOTES-2026-09-23.md`); D — `3c8473c` (отдельный воркер); пересборка site-v2 — коммит «пересборка» | A: CANON_ID['Закрыто']='closed', роль archive из имени (Р1), stageOf['Закрыто']='closed' сохранён для выключенного статуса (архив/«Списано» не теряют карточки; колонкой «Закрыто» не становится — ветка достроения исключает по имени), фолбэк пустого словаря тоже 'closed', STAGE_OF_STATUS удалён, Р6 slug-фолбэк, writeoffStatus с сортировкой. B: архив→closed, LIST_STAGE_FALLBACK done→«Списано»/closed→«Закрыто», очередь «Списано»+closed, групповая выписка→closed, псевдо-опция closed в селекте «Этап» (закрытые readonly), «Внести оплату» скрыта у закрытых, Р4-тост в drop и смене этапа; правка ревью — бейдж «Закрыто» по-русски в фолбэке (stageName не находит статус в пустом словаре). C: insights active=не done/не closed, «Списано» пульса=done+closed, календарь скипает оба. D: раздел статусов — прямой GET (все строки, включая неактивных), имя канона readonly+подсказка (Р3), is_active чекбокс (Р5, POST создаёт активным — снятый флаг доводится PATCH), роль из имени, «Отключён»-пилюля, синхронизация KBData.statuses «на месте» (приём mergeUsers). E: .slice() в демо-writeoffStatus | остались `done` (псевдо-этап/readonly выписки): board.js :1514, :1602, :1629/:1642, :1838/:1853, :2477 (бейдж), :2482/:2490, :2904, :2930–2935, :3035, :3065/:3193, :3279; insights :566 (статус задачи, не сделки). → `closed`: board.js :305 (групповая выписка), :1240 (очередь), :1314 (fallback), :1323 (архив п.13), :2482/:2490 (псевдо-опция), :2497 (кнопка оплаты), :3302–3309 (guard); insights :71, :216, :376, :616 | 1) 4 колонки в обеих темах, порядок канона, без «Закрыто» (скриншоты `2026-09-23-item12-board-{light,dark}.png`; «до» — скриншоты пункта 13, снятые на хардкоде). 2) «Списано» 192 = 177 закрытых + 15 дописанных — совпало с ожиданием по API и эталоном волны. 3) админка: 6 строк; PATCH канона 200 с телом без имени; имя readonly+подсказка; «Ждет оплаты» выкл → колонка ушла, 4/4 карточек в достроенной колонке (последней, position 100+), не в «Новом запросе»; вкл → вернулась. 4) «Тестовый»: POST 200, пятая колонка пустая; в селекте «Этап» отсутствует; drag → тост Р4, карточка на месте; 0 4xx. 5) rename → «Тестовый 2»: PATCH 200, колонка переименовалась. 6) census: 0 pageerror, 0 DOM/дата-аномалий, 4 известных артефакта. 7) s11 на 8127: мутации 4 (<300), PATCH 1, финансы 0, kb:documents 0 — эталон совпал; long task «обратной» галочки 247 мс воспроизводится и на старой сборке 8126 без пунктов 12/13 (129/224 мс) — средовой шум ноутбука, не регрессия пакета. 8) клиенты-петля OK, комбобокс поставщика OK. 9) node --check ×5, пересборка одна, pytest 878 (check.sh exit 0) | серверная колонка `role` (Р1 — решение владельца); пропагация переименования в cards.status; деплой одним релизом: статика v2 + бэк + миграция 0013 (порядок: migrate.py ДО рестарта PM2), по слову владельца. **Находка вне пункта:** сервер валидирует `cards.status` по канону в CardResponse — карточка с неканоническим статусом в БД роняет `GET /kanban/cards` в 500 (ResponseValidationError); прямые вставки в БД в тестах недопустимы, Р4 — единственная защита клиента |
