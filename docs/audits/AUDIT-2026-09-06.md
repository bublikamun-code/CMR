# Полный аудит CRM — 06.09.2026

Охват: бекенд (безопасность, надёжность, структура — все 18 роутеров, ядро,
скрипты, конфиги деплоя), фронтенд (все 5 страниц, 25 JS-модулей, CSS),
юзабилити и дизайн. Метод: статический анализ кода с точечными чтениями,
программные прогоны (py_compile, node --check, штампы статики, скрипты
эвристики XSS), сверка с аудитами 04.08 и 04.09.

Продолжение линии аудитов: `AUDIT-2026-09-04.md` (его находки сверены —
что исправлено, что осталось). Всё, что помечено «ИСПРАВЛЕНО 06.09»,
поправлено в этом же проходе и проверено синтаксисом/штампами.

---

## 0. Резюме

Состояние проекта **существенно лучше, чем на момент аудита 04.08**:
httpOnly-cookie внедрена, секреты в git не попадают, SSRF-защита вебхуков
двухэтапная, загрузки файлов с magic-bytes и лимитом, глобальный хендлер
ошибок без утечек, канбан чист от N+1, XSS-дисциплина (escapeHtml)
соблюдается по всему фронтенду.

Найдено и **исправлено сразу — 3 глобальные ошибки**:

1. 🔴 **Порча финансовых данных**: правка суммы сделки перезаписывала суммы
   ВСЕХ её транзакций, включая уже выписанные накладные — ИСПРАВЛЕНО 06.09.
2. 🟠 **Последний IDOR**: `GET /files/{filename}` отдавал любой файл из
   uploads любому авторизованному без проверки владельца (фронтенд им уже
   не пользовался) — эндпоинт и его мёртвый клиент удалены — ИСПРАВЛЕНО 06.09.
3. 🟠 **Зависание почтового синка**: IMAP-connect без таймаута и `logout`
   только на успехе — ИСПРАВЛЕНО 06.09.

Главные открытые риски (не код, а процессы/инфраструктура):

- 🔴 **Сайт живёт по HTTP без TLS** (риск №1 ещё с 04.09, ждёт A-запись домена).
- 🔴 **`site/css/liquid-glass.css` не закоммичен** — режим «стекло» работает
  на проде, но его нет в git; потеря файла = потеря фичи. То же касается
  незакоммиченных правок `kanban.js`, `features.js`, `card_modal.js`,
  `style.css` и др. (деплой идёт по FTP мимо git).
- 🟠 **Тестов нет**: CI «Run tests» выполняет smoke-импорт `main` (10 строк,
  без единого ассерта) — регрессии ловятся только руками.

---

## 1. Бекенд — безопасность

### Подтверждённые сильные стороны

| Механизм | Где | Оценка |
|---|---|---|
| JWT HS256, exp 24ч, jti, claim `pv` (отзыв токенов при смене пароля) | auth.py:54-60, 91-97 | образцово |
| httpOnly-cookie `crm_token` (SameSite=Lax; Secure — по схеме) + переходный Authorization | auth_router.py:34-42 | соответствует плану P2-1 |
| Лимит логина 10/мин, warning в лог при неудаче | auth_router.py:18-24 | ОК |
| Cron-токен через `compare_digest` (timing-safe) | auth.py:121-130 | ОК |
| SSRF-защита вебхуков: блок-лист хостов + проверка всех IP через getaddrinfo + повторная проверка перед запросом + запрет редиректов | webhooks_router.py:26-81 | образцово |
| Загрузки: magic bytes, белый список расширений, лимит 25 МБ стримингом, realpath-containment | card_details_router.py:244-277, 289-311 | ОК |
| Path traversal: basename + realpath-префикс везде | card_details_router.py:468-471 и др. | ОК |
| CORS: без wildcard, credentials=True только с конкретными origin | main.py:56-73 | ОК |
| Глобальный хендлер 500 без утечки деталей наружу, стек — в лог | main.py:48-51 | ОК |
| CSP + nosniff + DENY + Referrer/Permissions-Policy на двух уровнях (app + nginx) | main.py:147-164, nginx/snippets/ | ОК |
| Секреты: `.secret_key`/`.cron_token`/`.env*`/`email_settings.json` в git НЕ трекаются (проверено `git ls-files` и историей `--diff-filter=A` — их там никогда не было) | .gitignore | ОК |
| Пароль почты не отдаётся клиенту (маска `********`), шифруется отдельным от JWT ключом | email_parser_router.py:246-259, 44-85 | ОК |
| Пароли bcrypt, min 8, смена с инвалидацией всех токенов | schemas.py:14-19, auth_router.py:64-75 | ОК |

### Находки

| № | Серьёзность | Находка | Где | Статус |
|---|---|---|---|---|
| С1 | 🔴→✅ | `GET /files/{filename}` — отдача любого файла из uploads любому авторизованному, без проверки владельца/тенанта. Фронтенд не использует его с 31.08 (скачивание по id) | card_details_router.py:466-481 | **ИСПРАВЛЕНО 06.09** (эндпоинт удалён) |
| С2 | ⚠️ | Прод без TLS: токен-кука и данные ходят открытым текстом. Скрипт `setup_ssl.sh` готов, ждёт DNS | инфра | открыт (блокер: A-запись) |
| С3 | ⚠️ | Rate limit только на `/auth/login`. Смена пароля, загрузки, `/email-parser/sync` (auth_required) — без лимитов. При плечe httpOnly-cookie + auth это понижает остроту, но брутфорс старого пароля на `/auth/me/password` не затруднён | limiter_config.py, auth_router.py:64 | бэклог P2 |
| С4 | ℹ️ | `secure=(request.url.scheme == "https")` — за nginx-прокси схема может определиться как http, если uvicorn не видит `X-Forwarded-Proto` (проверить `--proxy-headers`/`forwarded-allow-ips` в server.py при переезде на HTTPS). Cookie без Secure на https-продакшене будет слабостью | auth_router.py:39 | учесть при P0-HTTPS |
| С5 | ℹ️ | CSP: `unsafe-inline` в script-src (26 инлайн-onclick в index.html + инлайн-скрипты тем). Остаточный риск при XSS | main.py:156 | бэклог P2 (перенос на addEventListener) |
| С6 | ℹ️ | `/docs`, `/redoc`, `/openapi.json` публичны — раскрывают карту API (данных не раскрывают) | main.py:39, nginx app-locations | по желанию: `docs_url=None` |
| С7 | ℹ️ | Утечка текста исключения почтового сервера клиенту в detail | email_parser_router.py:485, 502 | низкий, можно обобщить |
| С8 | ℹ️ | `update_user` позволяет админу назначить роль `superadmin`? Нет — проверка есть (auth_router.py:139-140). Но admin может сменить пароль ДРУГОГО admin'а (проверки роли цели нет) — в одной компании приемлемо | auth_router.py:131-146 | принятый компромисс |

Вывод по секции: после фикса С1 известных критических дыр в коде бекенда нет.
Остаточные риски — инфраструктурные (TLS) и «гигиенические» (CSP, лимиты).

---

## 2. Бекенд — надёжность и структура

### Сильные стороны

- SQLite настроен образцово: WAL, busy_timeout=10000, synchronous=NORMAL,
  foreign_keys=ON с предварительной инвентаризацией сирот (database.py:26-38).
- Бэкапы через SQLite backup API + integrity_check + retention; тест
  восстановления проводился 04.09; офсайт-копии `tools/pull_backups.sh`.
- Deferred-close сессий с подробным разбором DetachedInstanceError
  (database.py:44-113) — зрелое решение.
- Каскадная гигиена: все hard-delete вручную отвязывают детей и удаляют
  файлы (kanban_router.py:223-243; clients_router.py:145-147).
- Идемпотентный `trigger_from_card`, пересчёт статуса сделки после удалений,
  admin-only `repair-writeoffs` с dry_run (payments_router.py:52-61, 341-356, 795-868).
- Retention-джобы для уведомлений и версий (notifications_router.py:200-223).
- CI: gitleaks + Bandit + Trivy + проверка образа на отсутствие секретов.

### Находки

| № | Серьёзность | Находка | Где |
|---|---|---|---|
| Н1 | 🔴→✅ | **Правка суммы сделки затирала суммы всех транзакций** (включая выписанные накладные) — расхождение реестра оплат с документами | card_details_router.py:358-361 → **ИСПРАВЛЕНО 06.09** (пересчитывается только запись-остаток) |
| Н2 | 🟠 | **Тестов нет**: `test_routes.py` = 10 строк импорта без ассертов; CI подаёт это как «Run tests» | test_routes.py, build-deploy.yml:78-80 |
| Н3 | 🟠→✅ | IMAP: TCP-connect без таймаута (settimeout уже после конструктора) + `mail.logout()` только на успешном пути → зависания и утечка соединений | email_parser_router.py:296-297, 466 → **ИСПРАВЛЕНО 06.09** (timeout=15 в конструкторе с фолбэком; logout в finally) |
| Н4 | 🟠 | PATCH /cards копирует `total_amount` в транзакции; тот же паттерн для `store_location` переписывает склад у уже выписанных накладных — если это сознательное правило «склад сделки единый», ок; если нет — надо решить | card_details_router.py:362-365 | 
| Н5 | 🟠 | Нет миграций схемы: alembic в requirements, но каталога нет; эволюция — create_all + ручные правки. Риск рассинхрона при следующем изменении моделей | requirements.txt:10 |
| Н6 | 🟠 | Денежные суммы — float в схемах/арифметике (в БД Numeric(12,2)), сравнения с допуском 0.01. На текущих объёмах работает | schemas.py:113, 296…; payments_router.py:143, 465-470 |
| Н7 | 🟠 | Смешение aware/naive дат: модели пишут tz-aware UTC, tasks/notifications — naive `utcnow()`. Строковое сравнение в SQLite даёт неверный порядок/фильтры | tasks_router.py:156, 204; notifications_router.py:77, 129 |
| Н8 | 🟡 | `IntegrityError` не обрабатывается нигде (grep = 0); rollback в роутерах — 1 место. Любое нарушение констрейнта — непрозрачный 500 | все роутеры |
| Н9 | 🟡 | Гонка идемпотентности `trigger_from_card`: проверка-потом-вставка без unique-констрейнта — двойной клик в конкурентных запросах создаст дубль | payments_router.py:56-61 |
| Н10 | 🟡 | Эвристика удаления «двойников» по сумме ±0.01 — две накладные на одну сумму = удалится не та | payments_router.py:305-309 |
| Н11 | 🟡 | Списочные эндпоинты без пагинации: kanban `/cards`, `/trash`, `/clients`, `/suppliers`, `/tags`, `/writeoffs/pending`, `/writeoffs/groups`. (В payments/tasks/notifications/activity лимиты есть) | перечисленные роутеры |
| Н12 | 🟡 | N+1: `/writeoffs/pending` и `/clients/{id}/cards` отдают CardResponse без selectinload; `list_records` кастомных объектов — запрос в цикле | writeoffs_router.py:37-44; clients_router.py:51-58; custom_objects_router.py:181-184 |
| Н13 | 🟡 | 45 копий superadmin-бойлерплейта `if role == "superadmin" … session = db` в 7 роутерах + 68 `try/finally tdb.close()`. Ошибка в одной копии = дыра изоляции. Просится один dependency | db_utils.py + роутеры |
| Н14 | 🟡 | Tenant-движки: пул 50+50 (собственный комментарий говорит «избыточно»), гонка `_tenant_engines` без lock | database.py:124-153 |
| Н15 | 🟡 | Медленный учёт: 11 из 17 роутеров без единого log-вызова; бизнес-ошибки видны только через глобальный 500 | kanban, payments, clients… |
| Н16 | 🟡 | 14× `except Exception: pass` вокруг side-channel'ов (webhook/notify) — намеренно, но молча | kanban_router.py:172, card_details_router.py:409… |
| Н17 | ⚪ | Мёртвое/сломанное: `pm2_restart.js`, `restart_trigger.sh`, битый `restart_crm.sh` (пути `/Users/aroslav/...`), `routers/.secret_key` (не читается кодом), `tenants/managerd.db`, устаревшие `scripts/{backup,deploy,rollback}.sh` со старыми путями хостинга | server_snapshot/ |
| Н18 | ⚪ | `tenants.db_path` пишется и никогда не читается; создание тенанта пишет slug-путь, а открывается файл по числовому id | auth_router.py:160, models_tenant.py:9 |
| Н19 | ⚪ | Валидационные пробелы: `CardUpdateStatus.status` — любая строка; `TransactionUpdate.amount` без `gt=0`; часть payload'ов инлайн в роутерах мимо schemas.py | schemas.py:256-257, 296 |
| Н20 | ⚪ | Workflow-движок не существует: CRUD триггеров/шагов/прогонов без исполнителя («В разработке», workflows_router.py:18-21) — полу-мёртвая фича; её под-эндпоинты без tenant-фильтра (admin-only, поэтому низко) | workflows_router.py:117-201 |
| Н21 | ⚪ | Вебхуки: поток-демон на событие без пула/ретраев; файл на диск пишется до коммита БД (сироты при сбое) | webhooks_router.py:270-288; card_details_router.py:300-311 |
| Н22 | ⚪ | Лимит тела: nginx `client_max_body_size 50M` есть в обоих conf — ОК; gzip в nginx не настроен (отдача 250КБ CSS без сжатия — браузер распакует, но трафик ×4) | nginx/*.conf |
| Н23 | ⚪ | CI деплоит только фронт (`tools/deploy.sh front`); бекенд на прод — вручную `deploy.sh back`. Расхождение «CI зелёный, но бекенд старый» | build-deploy.yml:162-177 |
| Н24 | ⚪ | Лог watchdog растёт без ротации | scripts/watchdog_crm.sh:9-13 |

---

## 3. Фронтенд — код

### Сильные стороны

- **XSS-дисциплина**: escapeHtml/escAttr применяются во всех 19 файлах с
  innerHTML; эвристический скан интерполяций без экранирования нашёл только
  вставки в `textContent`, диалоги и тосты (безопасно). `eval/new
  Function/document.write` — 0.
- **api.js** — зрелый клиент: 401-хук с защитой от петли, таймауты (30 с,
  ×4 на файлы), человеческие сообщения на AbortError/TypeError, разбор
  422-массивов FastAPI, индикатор сетевой активности, content-type проверка
  (HTML-ошибка прокси не «падает» в JSON.parse) — api.js:93-237.
- **realtime.js**: polling 15/30 с, пауза на скрытой вкладке/модалках/драге/
  несохранённом вводе с лимитом 3 мин и событием `realtime:stale`.
- **Двойные отправки закрыты** в критичных местах: card_modal.js:1837
  (накладная), clients.js:265, writeoffs.js:812.
- **Карточка сделки**: несохранённые изменения → диалог, Escape-обработчики,
  спиннер/статус сохранения (card_modal.js:117-128, 186-232).
- Константы магазинов/статусов централизованы в constants.js (захардкоженных
  «Матусевича/Богдановича» вне его — нет).
- Синтаксис: py_compile и node --check чисты по всем файлам; штампы `?v=`
  синхронны (52 ссылки, 0 расхождений).

### Находки

| № | Серьёзность | Находка | Где |
|---|---|---|---|
| Ф1 | 🟠 | **Страницы-сироты**: `settings.html`, `workflows.html`, `custom_objects.html` не связаны навигацией с index.html (на них нет ни одной ссылки) — дубли функционала вкладок index. Пользователь попадает только вручную по URL | site/*.html |
| Ф2 | 🟠 | **liquid-glass.css не в git** (+ незакоммиченные правки 10 файлов) — работа прод-режима зависит от файлов, которых нет в истории | git status |
| Ф3 | 🟡 | CSS-долг: **1181 `!important`** в style.css (на 04.08 было 1022 — растёт), **15 разных media-брейкпоинтов** (включая дубли max-width 900/768/640), 128 vars. Каждый новый фикс требует всё более специфичных селекторов | css/style.css |
| Ф4 | 🟡 | 24 инпута с id без связанного label/aria-label в index.html (из 49); 26 инлайн-onclick | index.html |
| Ф5 | 🟡 | Файл-фикстура `site/_fx4.html` в web-корне (после `deploy.sh front` уедет на прод) | site/_fx4.html |
| Ф6 | ⚪ | `getToken()` читает `crm_token` из cookie — путь мёртв (кука httpOnly, JS её не видит); остаток переходного периода | api.js:42-45 |
| Ф7 | ⚪ | Роль для показа ссылки «Админ-панель» берётся из localStorage (`crm_role`) — косметика; сервер роли проверяет | auth.js:22-25 |
| Ф8 | ⚪ | `.bak`-копии (style.css ×5, ui-polish.js ×1) в рабочей папке site/ — в git не трекаются, но рискуют уехать деплоем | site/css/, site/js/ |
| Ф9 | ⚪ | Общий вес фронтенда ~1.1 МБ без сжатия (CSS 250 КБ + JS ~600 КБ + Chart.js 205 КБ); без gzip nginx и без бандлера | site/ |

---

## 4. Фронтенд — юзабилити и дизайн

### Сильные стороны

- Единая навигация: одинаковый сайдбар на всех страницах, active-state,
  aria-label на каждом пункте, кнопка «Админ-панель» показывается по роли.
- Empty states продуманы: «Клиенты не найдены» с кнопкой «Добавить первого»,
  «Задач нет», «Архив пуст»/«Ничего не найдено» в корзине, спиннеры загрузки.
- Удаления — через `confirmDialog` во всех модулях (9 файлов).
- Ошибки везде доводятся тостами (aria-live регионы ещё с 08.08).
- Таблицы с `overflow-x: auto` — не сжимаются до вертикального текста.
- Темы: светлая/тёмная + glass с фолбэками `@supports not backdrop-filter`
  и `prefers-reduced-transparency`; чекбоксы исключены из стеклянного правила
  (фикс «белой галочки» 05.09).
- Тач: ⋯-меню карточки для touch вместо hover-действий, горизонтальный скролл
  канбана на ≤900px.

### Находки

| № | Приоритет | Находка |
|---|---|---|
| У1 | 🟠 | Дублирование страниц (Ф1) путает: настройки существуют в двух местах (вкладка index и /settings.html) — данные те же, но外观 разный; воркфлоу-страница описывает фичу, которой нет (Н20). Либо убрать страницы, либо связать навигацию |
| У2 | 🟡 | 24 поля без подписей — при заполнении форм скринридер и «плавающие» placeholder'ы не называют поле (WCAG 1.3.1/3.3.2) |
| У3 | 🟡 | Клавиатурная альтернатива drag-and-drop канбана по-прежнему отсутствует (WCAG 2.1.1) — перенос карточки мышью-only |
| У4 | 🟡 | 15 брейкпоинтов без системы: одинаковые элементы ломаются на разных ширинах; стоит свести к 3-4 токенам |
| У5 | ⚪ | Форматирование сумм/дат едино (formatMoneyBYN, tabular-nums) — нареканий нет; локализация BYN/₽ консистентна |
| У6 | ⚪ | Первичный онбординг: пустой канбан не предлагает «создать первую сделку» (в клиентах — предлагает; в канбане нет аналогичного empty state с CTA) |

---

## 5. Исправлено 06.09 (этот проход)

1. **card_details_router.py — PATCH /cards/{card_id}**: изменение суммы
   сделки больше не трогает выписанные накладные и списания. Пересчитывается
   только запись-остаток (новый остаток = сумма сделки − сумма выписанного);
   если остаток исчезает — запись удаляется (как в issue_invoice), если
   остатка не было, а сумма выросла — создаётся. Случай «без накладных»
   ведёт себя как раньше.
2. **card_details_router.py**: удалён `GET /files/{filename}` (IDOR: любой
   авторизованный качал любой файл uploads; фронтенд с 31.08 качает по id).
   В card_modal.js удалена его мёртвая клиентская обёртка `downloadFile`
   (используемая всеми `sniffExt` и `downloadById` сохранены).
3. **email_parser_router.py — sync**: таймаут 15 с на TCP-connect (с фолбэком
   для Python < 3.9), `mail.logout()` перенесён в finally — соединение
   закрывается и при ошибке посреди выборки.
4. Штампы статики переставлены (`card_modal.js?v=ef542b41e1`), py_compile и
   node --check — чисто.

---

## 6. Бэклог по приоритетам

**P0 (сейчас)**
1. Закоммитить liquid-glass.css и незакоммиченные правки (риск потери работы).
2. TLS: A-запись домена → `scripts/setup_ssl.sh` → редирект → добавить https
   в CRM_CORS_ORIGINS (риск №1 с 04.09, не закрывается кодом).

**P1 (неделя)**
3. Реальные тесты: pytest + TestClient на 5-10 критичных сценариев (логин,
   изоляция, issue-invoice, новый пересчёт остатка из фикса №1) — заменить
   smoke «Run tests» в CI.
4. Мини-раннер миграций (план P2-3 из 04.09) до следующего изменения моделей.
5. Убрать `_fx4.html` из site/ (перенести в tools/ или удалить) и .bak-файлы
   из site/ перед деплоем.
6. Удалить мёртвые файлы бекенда (Н17): pm2_restart.js, restart_trigger.sh,
   битый restart_crm.sh, routers/.secret_key, tenants/managerd.db.

**P2 (месяц)**
7. Rate limit на /auth/me/password и загрузки (С3).
8. CSP без unsafe-inline: перенос 26 onclick в addEventListener (С5 = Ф4).
9. Пагинация/ленивые списки на kanban /cards и /trash (Н11).
10. Унификация superadmin-бойлерплейта в один dependency (Н13) — снижает
    риск будущей дыры изоляции.
11. Единый формат дат в БД (Н7) — разовая конвертация naive→aware.
12. Индексы на tenant_id-колонки (если вернётся мульти-аренда).

**Решения владельца**
13. Страницы-сироты settings/workflows/custom_objects: убрать или связать (У1).
14. Workflow-фича: строить движок или вырезать CRUD (Н20+У1).
15. Поведение «склад сделки → переписывать склад накладных» (Н4).
16. gzip в nginx (Н22) — одна директива, спросить хостера/добавить в conf.

---

*Метод и полнота: проверены все 18 роутеров (эндпоинт-лист сверен с кодом),
ядро (auth/database/db_utils/models/schemas/versioning/notify), конфиги
(nginx ×2 + 3 snippets, 3 compose, 2 Dockerfile, CI), все 5 HTML-страниц,
выборочно-полностью 25 JS-модулей, метрики CSS. Живой прогон в браузере в
этот проход не входил (см. AUDIT-2026-09-04 §4 — свежие живые прогоны UI).*
