# План: v2 вместо старого фронта — фиксы, паритет, переключение

Дата: 19.09.2026, ночь. Статус этапа жить в этом файле (обновлять чек-лист).
Репозиторий-источник правды: `~/Documents/crm-svetvdome`
(бэкенд `server_snapshot/`, старый фронт `site/`, прототип `server_snapshot/site-v2/`
— генерируется из `server_snapshot/tools/mockups/` сборщиком
`server_snapshot/tools/build_site_v2.py`, шаблоны `tools/v2-{api,boot}-template.js`).

## Жёсткие ограничения (решения владельца 19.09)

1. **Данные под запретом.** Никаких destructive-операций над БД: карточки,
   клиенты, суммы, оплаты, накладные, вложения должны остаться. Разрешены только
   аддитивные миграции и, отдельно согласованная, гигиена FK-сирот.
2. **Прод:** только чтение (curl, SELECT, `sqlite3 .backup`) и деплой кода
   через `tools/deploy.sh`. Тесты с мутациями — на локальной копии БД.
3. Порядок: **сначала корневые фиксы v2 → верификация → потом переключение**
   `/` → v2, старый фронт на `/legacy`.
4. Правки фронта — в `tools/mockups/` (и в шаблонах api/boot), затем
   `python3 tools/build_site_v2.py` и `python3 tools/stamp_assets.py`.
   `site-v2/` руками не править. Пересборку делает ОДИН человек (главный агент),
   чтобы параллельные воркеры не перетирали сборку.

## Проверенное состояние (факты, не оценки)

- `site-v2` локально == прод (md5 7/7 ключевых файлов), бэкенд прод ==
  рабочее дерево (29/29 md5). Стенд на копии прод-БД воспроизводит прод честно.
- Стенд: `cd server_snapshot && CRM_DATA_DIR=/Users/yaroslav/cmr-svetvdome/tmp/prod-live
  CRM_UPLOADS_DIR=.../uploads ./.venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8125`
  → API и UI на `http://127.0.0.1:8125/` и `/v2/`. Копия БД снята 19.09 03:14
  через `sqlite3 .backup`, `integrity_check: ok`.
- `pytest`: **382 passed** (финальный прогон 19.09 вечером после P0-2/V6/V10; профиль prod).
- Функциональный аудит v2 на копии прод-БД: **25 проверок, 23 PASS, 2 FAIL,
  оба — артефакты песочницы**: настройки почты (локальный `email_settings.json`
  пустой; на проде заполнен) и 404 фото накладных (в локальную копию uploads
  файлы не переносились). Зелёное: 367 карточек по колонкам (0 расхождений),
  очереди 2/2 и 185/185, сводка 553, выписка, галочки реестра, CSV, ТН/Печать,
  все режимы оплаты, аванс в кассу, закрытие из баланса, импорт почты,
  колокольчик, все разделы.
- **Вечерняя верификация 19.09 (коммит `3623fa5`)**: P0-2 — создание
  (`POST /auth/users`, OK id=9) и правка (`PATCH /auth/users/{id}` →
  «Пользователь обновлён») через `user-save` на стенде; V6 — фикс доставлен в
  сборку, контракт `/email-parser/sync` подтверждён curl'ом
  (`{detail: Настройки почты не заполнены}` — текст сервера дойдёт до тоста);
  V10 — фантомной строки «—» больше нет (7→8 строк без прочерка); тестовый
  пользователь со стенда удалён. Перепись ошибок на стенде: 0 DOM-аномалий,
  0 аномалий данных; сетевые провалы — только отсутствующие файлы uploads.
  Целевые проверки: `/tmp/verify_p02b.log` (сценарий `/tmp/verify_p02b.py`).
- **Вывод:** v2 на прод-данных функционально состоятелен. Дальше — корневые
  дефекты, которые ломаются на краевых данных и при сетевых сбоях, паритет
  и переключение.

## Реестр дефектов

Классы: `client` (v2), `server` (бэкенд), `contract` (рассогласование),
`data` (БД), `process` (репо/деплой).

### A. Контракты клиент ↔ сервер (ломаются на реальных данных)

| # | Дефект | Клиент | Сервер | Фикс |
| --- | --- | --- | --- | --- |
| A1 | Статусы сделок: при пустом справочнике сервер отдаёт хардкод `['Новый','В работе','Закрыто']`, а PATCH валидирует по БД → выбор «В работе» даёт 400 | `boot.js:30-33` | `dictionaries_router.py:26` | убрать fallback (server) |
| A2 | `doc_type`: клиент шлёт «Накладная»/«Счёт», сервер хранит ТН/ТТН/УПД и молча подменяет → фильтр UI никогда не совпадает | `board.js:3371`, `boot.js:503` | `nakladnye_router.py:281-302,466-467` | валидировать `doc_type` по `constants.TTN_TYPES` (422), убрать тихую подмену |
| A3 | Оплата ≥ суммы сделки перезаписывает `total_amount := paid_amount` — сумма сделки теряется при переплате | — | `card_details_router.py:456` | не переписывать `total_amount`; статус «Оплачен» оставить |
| A4 | Дубль накладной: сервер отдаёт 201 ничего не создав, клиент показывает «Сохранено», после reload запись исчезает | `boot.js:158` | `nakladnye_router.py:448-450` | 409 + `existing_id`, клиент — честный тост «уже есть» |
| A5 | Авансы расходятся: сервер кладёт в реестр (`is_advances` не учитывает `payment_kind`), клиент прячет по `kind==='advance'` | `board.js:3302,3347`, `fin.js:497` | `payments_router.py:292-320` | `is_advances` = `purpose` ИЛИ `payment_kind=='advance'` (server — источник) |
| A6 | Пустой комментарий: клиент шлёт `note:''` при смене статуса, сервер создаёт запись «Комментарий» с пустым текстом | `board.js:2262` | `card_details_router.py:584-585` | не создавать при пустом `note` (server) + не слать пустой (client) |
| A7 | `client_id: null` затирает клиента карточки при любом сохранении | `boot.js:484` | `card_details_router.py:414-416` | клиент не шлёт неизменённое; сервер не принимает явный null без флага |
| A8 | Отрицательная сумма сделки → 500 вместо внятной ошибки | `boot.js:379` | `card_details_router.py:495` | 400 с текстом |
| A9 | Чек-лист задачи клиент угадывает по тексту заголовка | `board.js:3139-3140,230-231` | `tasks_router.py:18-60` | отдавать items в `TaskOut` |
| A10 | Вложения: клиент шлёт multipart в `/files/attach/{id}`, у сервера там JSON → 405; имя поля тоже расходится | `board.js:2329-2334`, `boot.js:407-412` | `files_router.py:136-144,56` | добавить multipart-эндпоинт под контракт клиента |
| A11 | Баланс клиента: клиент сам считает 11 `clientPayments`; `/clients` не отдаёт `cash_balance`/`overdue_deals`, `/clients/{id}/balance` нет | `board.js:2559,2649`, `boot.js:280-281` | `clients_router.py`, `schemas.py:88-100` | поля в список клиентов + эндпоинт баланса |
| A12 | `remaining`/`writeoff_status` считаются на клиенте клоном серверной логики | `board.js:391-394,241-255` | `services/writeoffs.py:11-29,32-69`, `schemas.py:34-35` | сервер отдаёт `remaining` и `writeoff_status` в карточках; клиент только отображает |
| A13 | `payment_status` «Частично» против канона «Частично оплачен» | `board.js:491` | `kanban_router.py:475-476`, `models.py:242` | клиент шлёт канон |
| A14 | N+1 в списке клиентов (6 запросов на клиента × 400) | — | `clients_router.py:46-65` | агрегирующие запросы |
| A15 | Восстановление из корзины не инвалидирует кэш → «призрак» карточки | `board.js:464-465` | — | инвалидация `_allCards` после restore |

### B. Надёжность клиента (то, что «ломается без видимых причин»)

| # | Дефект | Где | Фикс |
| --- | --- | --- | --- |
| B1 | 7 тихих `catch {}` — сбой выглядит как успех | `board.js:3129,3309,3362,3442`; `fin.js:486`; `insights.js:817`; `management.js:62` | тост + признак ошибки, без молчания |
| B2 | Провал запроса трактуется как «данных нет» и кешируется пустота | `insights.js:143-146` | не кешировать при ошибке, состояние ошибки в UI |
| B3 | Локальное время вместо UTC: «сегодня» разъезжается вечером при UTC+3 | `insights.js:898`; `board.js:3146,3426` | единый `todayUTC()` из слоя нормализации (`boot.js:531-542`) |
| B4 | Закрытый диалог оставляет форму в DOM с пустым `required` → `invalid form control is not focusable`, форма молча не отправляется | `management.js` (диалог `#mgmt-dialog`, поле `#mgmt-username`) | чистить содержимое диалога при закрытии; валидация с видимым сообщением |
| B5 | Нет защиты от двойного сабмита POST-созданий → дубли при медленной сети | `boot.js:150-162` | in-flight блокировка по ключу операции |
| B6 | Токен и роль кешируются из ответа логина без проверки | `boot.js:121,141` | всегда `/auth/me` при старте |
| B7 | Оптимистичные мутации без `await`/отката (галочки, статусы) | `board.js:3016,3089,3141` | await + откат + тост |
| B8 | Округление денег двумя способами: `Math.round(x*100)` против `cents()` | `board.js:1538,2375,3100,4022`; `boot.js:509-510` | один хелпер `cents()` |
| B9 | Гонка перезагрузок: `loaded` защищает только первую загрузку, 18 мест зовут `all()` повторно — параллельные вызовы перетирают `all` | `boot.js:170-193` + 18 call-sites | коалесинг/очередь перезагрузок |
| B10 | Полный `innerHTML`-перерендер теряет фокус/скролл/открытые меню | `board.js:1386,2304`; `insights.js:1009,1367`; `fin.js:286` | точечное обновление для известных мест |
| B11 | Нет пагинации: `/kanban/cards` 420 КБ и `/payments/transactions` 131 КБ грузятся целиком, доска рисует 200–400 карточек | `boot.js:195-211`; `board.js:1193-1234` | серверные страницы + точечная дозагрузка (отдельный этап) |
| B12 | `restore` у задачи дёргает несуществующий обработчик `data-handler="restore"` | `board.js:3131-3133`; `management.js:186-189` | один источник действий |
| B13 | Хардкод `payment_status==='Оплачен'` как признак полной оплаты (при «Не оплачен» + 100% выписки статус не меняется) | `board.js:294` | признак по серверному остатку, не по строке |
| B14 | Демо-режим достраивается из реальных данных → в `?demo=1` попадают настоящие клиенты | `boot.js:779-787` | демо полностью автономно |

### C. Паритет со старым фронтом (без этого замена неприемлема)

Деньги и документы (обязательно до переключения): печать ТН/ТТН (`site/js/documents.js:537-609`,
роут `/payments/documents/{id}/print`), экспорт реестра в Excel
(`payments.js:1143-1193`), загрузка и скачивание файла сделки (`card_modal.js:1322-1339`).

Управление (обязательно): workflows/автоматизации (`workflows.js`, 36 эндпоинтов),
кастомные объекты (`custom_objects.js`, 23), сохранённые виды (`saved_views.js`, 6),
админка (`admin.html` + `js/admin.js`: пользователи, справочники статусов/магазинов,
почта, вебхуки, журнал версий, диагностика тенанта).

Желательно до переключения: уведомления с badge и прочтением (`notifications.js`,
в v2 только `list_unread`), почта (`email_parser.js` — синк, импорт писем в сделку),
входящие накладные (`nakladnye.bundle.js` — в v2 только список в карточке).

Мелочи: `POST /notifications/sync-overdue` никто не зовёт (просрочки не обновляются),
`/email-parser/sync-status` из HANDOFF не существует (роутера нет — править документацию).

### D. Данные и процесс

| # | Факт | Действие |
| --- | --- | --- |
| D1 | 205 нарушений FK на проде: пользователь id=7 удалён, остались 144 `record_versions` + 58 `activity_log`; ещё 3 `activity_log` → удалённые карточки | подготовить аддитивную миграцию (обнуление сирот), применять на проде только с согласия владельца и после `.backup` |
| D2 | `site/index.html` в коммитах 2b2918f/dfefbc3/0f390b4/6ed87d2 пересобран без `stamp_assets.py` — кэш-бастеры устарели | прогнать `stamp_assets.py`, проверить `--check` |
| D3 | GitHub PAT в открытом виде в `origin` URL активного репо | отозвать токен, перевести remote на SSH (владелец) |
| D4 | Второй клон `/Users/yaroslav/cmr-svetvdome` отстал на сутки, тот же GitHub-remote | решение принято: не трогать; источник правды — Documents |

## Рабочие пакеты (непересекающиеся файлы)

- **Пакет A (server)**: A1, A2, A4 → `routers/dictionaries_router.py`,
  `routers/nakladnye_router.py`, `constants.py`, тесты. `schemas.py` НЕ трогать.
- **Пакет B (server, деньги и карточка)**: A3, A5, A6(server), A7(server), A8,
  A9, A11, A12, A14 → `routers/card_details_router.py`, `routers/payments_router.py`,
  `routers/tasks_router.py`, `routers/clients_router.py`, `routers/kanban_router.py`,
  `schemas.py`, `services/`, тесты. Владеет `schemas.py` единолично.
- **Пакет C (client, слой данных)**: A6(client), A7(client), B2, B5, B6, B8, B9 →
  `tools/v2-api-template.js`, `tools/v2-boot-template.js`.
- **Пакет D (client, доска/карточка)**: A12(потребление), A13, A15, B1(board), B3(board),
  B7, B10(board), B12, B13 → `tools/mockups/shell-v2-board.js`.
- **Пакет E (client, разделы)**: B1(insights/fin/management), B3(insights), B4, B14 →
  `tools/mockups/shell-v2-{insights,fin,management,data}.js`.
- **Пакет F (паритет)**: печать, Excel, файл сделки, уведомления, почта, админка —
  после A–E, отдельное планирование.
- **Пакет G (переключение)**: `/` → v2, старый фронт → `/legacy`, `stamp_assets.py`,
  деплой, откат. После верификации A–E и приёмки владельца.

Порядок: A + B + C + E параллельно → пересборка → верификация на стенде →
D (board) → верификация → F → G.

## Контракт для пакета D/B (фиксировать здесь, чтобы работать параллельно)

Сервер в ответе карточки (`/kanban/cards`, `/cards/{id}`) дополнительно отдаёт:

- `remaining: float` — остаток к выписке в рублях (считает сервер по
  `services/writeoffs.py`), и `remaining_kop: int` — те же в копейках;
- `writeoff_status: str` — `not_written | partially_written | written`,
  правило `services/writeoffs.py:11-29`;
- `issued_total: float` — сумма непустых документов по сделке.

Клиент обязан: не считать остаток и статус самостоятельно, брать поля сервера;
при отсутствии поля (старый кэш) — показать «—», а не додумывать.

## Протокол верификации каждого пакета

1. `cd server_snapshot && ./.venv/bin/python -m pytest -q` (344+ passed) и
   `CRM_TEST_SCHEMA=models ./.venv/bin/python -m pytest -q`.
2. Для фронта: `python3 tools/build_site_v2.py` → `python3 tools/stamp_assets.py --check`.
3. Стенд на копии прод-БД (порт 8125) →
   `tools/v2_functional_audit.py` (V2_BASE=стенд) — не хуже 24/25,
   `tools/v2_error_census.py --base http://127.0.0.1:8125/v2/` — 0 ошибок JS,
   0 сетевых провалов, 0 `undefined/NaN`.
4. Сверка чисел UI ↔ БД по деньгам (остатки, оплачено, выписано, касса) на копии.
5. Только затем `tools/deploy.sh` и контрольный прогон read-only по проду.

## Чек-лист этапа

- [x] Копия прод-БД снята, стенд поднят, прод и локаль сверены по md5
- [x] Аудит 25 проверок на прод-данных (24 PASS, 1 артефакт стенда)
- [x] Перепись ошибок на проде read-only (чисто)
- [x] Незакоммиченный бэкенд закоммичен (0009 + реестр без корзины)
- [x] Инструменты проверки закоммичены (`v2_error_census.py`, аудит параметризован)
- [x] Дизайн-аудит v2: 100% и 75% (1920/1440/1280/1024) + тёмная тема
- [x] Дизайн-фиксы: 1024 рабочий, контраст-токены, контролы ≥32px, суммы не режутся
      (проверено матрицей и визуально; «80+ белых поверхностей» из первого аудита
      не подтвердились на текущем коде — артефакт замера/старого билда)
- [x] Пакет A (server: справочник статусов, doc_type, дубль накладной)
- [x] Пакет B (server: деньги, remaining/writeoff_status, клиенты, задачи, вложения)
- [x] P0-фиксы слоя данных (главный агент) — проверены на копии прод-БД
- [x] Пакет C (client: слой данных) — коалесинг, роль из /auth/me, cents/todayUTC
- [x] Пакет E (client: разделы) — 0 тихих catch, диалог очищается, UTC-«сегодня»
- [x] Пакет D (client: доска/карточка) — серверные деньги, null-safety, await+откат
- [x] Пересборка site-v2 + stamp_assets (коммит 6243a62)
- [x] Деплой-путь для v2: tools/deploy.sh v2 (пересборка + rsync --delete)
- [x] Деплой проверенного на прод (19.09 вечер): `deploy.sh v2` + `deploy.sh back main.py`
      — site-v2 и main.py на проде побайтово = локальной сборке (md5 совпали),
      pm2 `crm` перезапущен (`Application startup complete`, ошибок нет).
      `nakladnye-bot` рестарта НЕ потребовал: `telegram_nakladnye_bot.py`
      на проде идентичен репозиторию (md5 2bf207c4). Корень пока отдаёт legacy
      (`CRM_FRONTEND` в `.pm2.env` не задан) — переключение впереди (Пакет G).
- [~] Верификация после деплоя: стенд — полная (pytest 382 passed, аудит 23/25
      при 2 артефактах песочницы, перепись 0 ошибок JS / 0 аномалий DOM и данных);
      прод — на уровне деплоя (`/`, `/legacy`, `/legacy/admin`, `/v2/` → 200,
      md5 сборки). Полная read-only перепись по проду ждёт креды владельца.
- [ ] Пакет F (паритет: печать, Excel, файлы, уведомления, почта, админка) —
      частично закрыт: P0-2 (user-save), V6 (синк), V10 плюс правки F1–F3
      (вложения/почта в карточке, фото/Excel входящих) в коммите `3623fa5`.
- [x] V1–V5 (P1, деньги/списания/баланс) — закрыты 19.09 вечером, коммиты
      `0e069e6` (V1+V2), `19984fa` (V3), `dfe3e11` (V4), `df0cab4` (V5).
      Все фиксы серверные, фронт не менялся — пересборка site-v2 не нужна.
      Два старых теста, фиксировавших дефектное поведение (hard-delete
      оставлял транзакции; execute закрывал из любого статуса), переписаны
      под новые контракты. pytest: 404 passed (prod) / 400 passed + 4
      skipped (models). На прод НЕ деплоилось — за владельцем.
- [x] Пакет G (переключение `/` → v2) — выполнен 19.09 по решению владельца:
      дефолт `CRM_FRONTEND` зафиксирован в КОДЕ как "v2" (потеря/сброс
      `.pm2.env` больше не откатывает корень молча; осознанный откат —
      `CRM_FRONTEND=legacy` в `.pm2.env` + `pm2 restart crm`), `/admin`
      в v2-режиме ведёт в SPA, старый фронт заморожен на `/legacy`
      (+ `/legacy/admin`, маркер `site/ARCHIVED.md`). Контракт
      переключателя покрыт `tests/test_frontend_switch.py`. Известный
      паритет-разрыв: воркфлоу/автоматизации в v2 нет — до порта раздела
      доступны через `/legacy`.
- [x] Плавность UI v2 (исследование krayin/frappe/elevenlabs 19.09, «чтобы
      ничего не дёргалось») — 7 коммитов 19.09 поздно вечером: тема без
      вспышки (`00ec177`), V9 закрытие дровера при смене раздела
      (`e58bc66`), debounce поиска доски/клиентов/поставщиков 150 мс
      (`e7ec197`), движение — тосты/диалоги/поповеры/выход дровера + токены
      `--dur-*` (`379be2c`), оптимистичный drop без полной перерисовки
      (`12876fa` + фикс `2bdbf82` — PATCH уходил в никуда), пресс-фидбек
      `:active` + глобальный reduced-motion + конвенции движения в
      mockups/README (`b996446`). Верификация: pytest 409 passed, перепись
      v2_error_census на копии прод-БД — 0 pageerror, 0 DOM/дата-аномалий
      (404 фото и 400 почты — известные артефакты песочницы; 3 консольных
      предупреждения — до-существующее поведение пустой формы смены пароля
      в админке), сквозной тест drop в демо (локальный перенос, шапки
      колонок, без полной перерисовки, 0 ошибок), скриншоты обеих тем
      (доска/дровер/дропдаун/тост). Отложено: SWR-кэш списков (деньги —
      нужен отдельный подход к свежести), массовые действия, скелетоны.
- [x] D1: миграция гигиены FK — **уже применена** 19.09 06:46 UTC
      (`schema_migrations`: 0010 + 0011); на проде `foreign_key_check` = 0 строк,
      `integrity_check` = ok. Дополнительных destructive-операций не потребовалось.
- [~] D3: `origin` переведён на `git@github.com:bublikamun-code/CMR.git`, PAT
      из `.git/config` обоих клонов убран, `git ls-remote` по SSH работает;
      отзыв самого токена в GitHub — за владельцем (только веб-интерфейс).
      Смена пароля ManagerY отложена владельцем: сначала перепись под текущими
      кредами, потом смена.

## Дополнение из аудита прода (AUDIT-2026-09-19-prod-v2.md, 10:11)

Сверка находок аудита с этим планом и кодом. Часть уже закрыта коммитами
после написания плана — зафиксировано, чтобы не чинить дважды.

### Уже закрыто (проверено по коду 19.09, вечер)

- **P0-1 (404 групповой накладной)** — пути в boot-шаблоне и доске
  переведены на `/writeoffs/groups/...`; сборка site-v2 содержит фикс.
- **«Нет id» у свежей ТН (P1)** — `services/payments.py` отдаёт `invoice_id`,
  доска читает `saved.invoice_id` в `doc.txId`.
- **Групповая выписка мутирует память до ответа** — `issueGroup` правит
  кэш только в `.then`, при отказе — ошибка в форме, кнопка разблокируется.
- **Смена пароля молча теряла данные** — теперь logout + очистка токена +
  `location.reload()`.
- **«Загружается…» в дровере клиента (hoisting)** — в `renderDrawer()`
  запрос баланса стоит уже после присвоения `client`.
- **FK-сироты 205 шт.** — миграция 0010 (применение на проде — за владельцем,
  см. D1).

### Не закрыто — добавить в работу

**P0-2 (пользователи в v2-админке молча не сохраняются) — ИСПРАВЛЕН 19.09 вечером.**
В boot-шаблон добавлен обработчик `user-save` (POST для нового с паролем ≥8,
PATCH `/auth/users/{id}` для правки: role + опциональный пароль); из диалога
убрано несуществующее на сервере поле `full_name`; клиент валидирует пароль
только при создании. Требуется пересборка site-v2 и проверка с экрана.

| # | Дефект (аудит) | Где | Фикс | Приоритет |
| --- | --- | --- | --- | --- |
| V1 | ✅ исправлено 19.09 (вечер, `0e069e6`): «Закрыто из баланса» не проверяет баланс → минус; журнал не пишется | `card_details_router.py:496-497` | проверка баланса + 400; запись в журнал | P1 |
| V2 | ✅ исправлено 19.09 (вечер, `0e069e6`): «Не оплачен» не стирает дату отсрочки: сервер игнорирует `None` | `card_details_router.py:520` | принимать явный `None` (по образцу clear_client) | P1 |
| V3 | ✅ исправлено 19.09 (вечер, `19984fa`): после «удалить навсегда» транзакции/документы остаются в реестре (FK SET NULL) + группа не пересчитывается | `models.py:198`, `payments_router.py:190` | каскадная очистка/пересчёт при hard-delete | P1 |
| V4 | ✅ исправлено 19.09 (вечер, `dfe3e11`): правка суммы ТН не пересчитывает остаток и статус карточки, без проверки дубля номера | `payments_router.py` (PATCH transactions) | пересчёт + 409 на дубль | P1 |
| V5 | ✅ исправлено 19.09 (вечер, `df0cab4`): очередь списания показывает удалённые карточки; `execute` закрывает из любого статуса без флагов и записи в ленту | `writeoffs_router.py:34` | фильтр `is_deleted`, валидация статуса | P1 |
| V6 | ✅ исправлено 19.09: кнопка «Синхронизировать сейчас» всегда «не прошла»: клиент читает `res.results[0]`/`total_count`, сервер отдаёт `{success, count, cards}` | `management.js:203`, `email_parser_router.py:536` | клиент по фактическому контракту | P1 |
| V7 | «Оригинал ТН возвращён» не сохраняется: поля нет ни в модели, ни в API (клиент мапит его на чужой `is_invoice_doc`) | сервер + `board.js:1605` | миграция колонки + приём в API + PATCH | P2 |
| V8 | Файл счёта после загрузки не появляется в строке до F5 | `board.js` (procurement) | перерисовать строку из ответа | P3 |
| V9 | ✅ исправлено 19.09 поздно вечером (`e58bc66`): дровер карточки не закрывается при смене раздела | `navigation.js render()` | render() закрывает открытый #kb-dialog до переключения видов | P3 |
| V10 | ✅ исправлено 19.09: у не-админа фантомная строка «—» в списке пользователей (`us-none` — служебный псевдопользователь) | `management.js` | не рендерить `us-none` в списке | P3 |
| V11 | Роли manager/warehouse/documents не разграничивают бизнес-операции (любой удаляет платежи) | сервер | осознанное упрощение — зафиксировать, решение за владельцем | note |

**Инфраструктура (не код, владельцу):**

- **Домена cmr-svetvdome.online не существует** (NXDOMAIN), работа по HTTP
  с nip.io — главный инфра-риск: зарегистрировать домен, Let's Encrypt, https.
- **Бэкапы отличные, но на том же сервере** — офсайт-копия .enc наружу.
- 13 FK-нарушений `activity_log → cards` — источник редких 500-х: чистка
  (входит в 0010/D1) + каскадная очистка лога при hard-delete (= V3).
- Убедиться, что легаси-`email_settings.json` рядом с кодом не вернётся
  при следующем деплое (шторм 1637 ошибок 18–19.09).
- nginx: `client_max_body_size` ≥25 МБ, `unsafe-inline` в CSP (при ужесточении
  сломается `V2_ASSET_VER` из index.html) — проверить на уровне панели.


## P0: что найдено и уже исправлено (ночь 19.09, главный агент)

Все пять пунктов воспроизведены на копии прод-БД и проверены после правки
(`/Users/yaroslav/cmr-svetvdome/tmp/verify_p0_fixes.py`, стенд 127.0.0.1:8125):

1. **Демо-данные вместо боевых при любом сбое.** `boot.js` грузил 10 разделов
   одним `Promise.all`, и ошибка любого из них вела в `bootDemo()`: менеджер
   видел выдуманные сделки «К-101…К-160» и работал с ними как с настоящими.
   Сейчас — `Promise.allSettled`, данные не применяются частично, показывается
   экран ошибки с перечислением непоставших разделов и кнопкой «Повторить».
   Проверено: 500 на `/nakladnye` → 0 карточек, текст «не загрузили разделы:
   входящие накладные (код 500)».
2. **Запись при чтении.** Открытие карточки вызывало `updatePayment()` из
   отрисовки → `PATCH /cards/{id}` c `payment_terms`/`payment_due_date` и новую
   строку `record_versions` на каждый просмотр. Факт до правки: versions 23→24
   при открытии карточки 1005. Разделено на `paintPayment()` (отрисовка) и
   `updatePayment()` (только input/change). После правки: 0 не-GET запросов
   при открытии, версии не растут.
3. **Входящие накладные всегда пусты.** `buildFinSource(cards, transactions,
   documents)` — 3 параметра, вызов с 5 аргументами: `/nakladnye` вычитывался
   и выбрасывался. После правки: 14 строк в UI = 14 в API.
4. **Флаги закупки и «Оригинал ТН возвращён» слетали после F5.**
   `ordered/received` и `originalsReturned` были захардкожены `false`, хотя
   сервер их отдаёт (миграция 0009, `is_invoice_doc`). Также не читалась сумма
   пункта закупки. После правки: ordered=true и amount=113,6 восстанавливаются,
   `originalsReturned=[true]` восстанавливается.
5. **Подстановка чужих значений вместо пустых.** Магазин: при пустом
   `store_location` подставлялся `stores[0].id` (на проде справочник пуст →
   674 карточки получили бы чужой магазин, и сохранение записало бы его).
   Клиент: при отсутствии клиента подставлялось название сделки → сохранение
   создавало клиента с именем сделки (994 карточки без клиента). Сейчас и там
   и там пустое значение, ничего не выдумывается.
6. **Группы списания не грузились** (`KBData.groups = []`), поэтому групповая ТН
   жила только до перезагрузки, а отмена группы была недостижима. Добавлена
   загрузка `GET /writeoffs/groups/` и маппинг в форму, которую ждёт доска.
   На проде групп сейчас 0 — проверка на данных невозможна, только контракт.

## Фактическое состояние прод-данных (снято 19.09 03:14, копия)

- Карточки: 367 активных, 1360 всего; `payment_terms`: NULL 1023, full 3, deferred 1;
  `payment_due_date` заполнена у 1 карточки (id 1005).
- `store_location`: NULL 674, «Матусевича» 258, «Богдановича» 95;
  справочник `store_locations` — **0 строк**.
- Клиент не указан у 994 карточек (336 из них активные).
- `client_payments`: 29 строк; таблицы `payment_allocations` нет (план 2.7 не внедрён).
- Входящие накладные: 14; `invoice_date` — все ISO или пусто (некорректных форматов нет).
- **205 нарушений FK**: пользователь id=7 удалён, остались 144 `record_versions`
  и 58 `activity_log`; ещё 3 `activity_log` ссылаются на удалённые карточки.
- **6 аномалий денег** (найдены пакетом B): `paid_amount` карточки не равен сумме
  её непустых `client_payments` (например card 27: 1000 против 1010; card 52:
  6430.98 против 6756.51; card 1022: 0 против 1000). Ни один эндпоинт
  `paid_amount` не перезаписывает — расхождение историческое. Нужна отдельная
  сверка с владельцем; данные НЕ правились.

