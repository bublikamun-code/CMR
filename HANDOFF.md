# Промт для продолжения в новой сессии

Вставить целиком в новую сессию:

````
Ты продолжаешь работу над CRM «Свет в доме» (светотехническая компания, Минск).
Локальный репозиторий: /Users/yaroslav/cmr-svetvdome — это источник правды.
Ветка main, remote: приватный git@github.com:bublikamun-code/CMR.git (пуш по SSH).

ПРОД
- SSH: ssh -i ~/.ssh/crm_svetvdome_deploy -o IdentitiesOnly=yes h212005@87.232.64.12
- Корень сайта: /var/www/h212005/data/www/cmr-svetvdome.online
- БД: /var/www/h212005/data/crm_data/crm_app.db + tenants/*.db (WAL)
- Приложение: pm2-процесс «crm», порт 20008. Рестарт ТОЛЬКО `bash scripts/deploy.sh`
  (бэкап + миграции + pm2 restart + health). scripts/restart_crm.sh устарел —
  запускает процесс вне pm2, не использовать.
- URL: http://87-232-64-12.nip.io/

ЖЁСТКИЕ ПРАВИЛА
- Все правки: локально → коммит → пуш → scp на сервер → (рестарт через deploy.sh,
  если трогал backend). Никогда не редактировать файлы напрямую на сервере.
- Ничего не удалять/перезаписывать на сервере без явного согласия пользователя.
- Перед изменениями — бэкап (серверные backups/ + ежедневный cron scripts/backup_crm.sh,
  покрывает основную БД, тенант-БД, uploads, секреты).
- Секреты в чат не печатать.

ИНСТРУМЕНТ ВИЗУАЛЬНОЙ ПРОВЕРКИ
tools/visual-check/ (playwright-core + системный Chrome): walk.mjs, audit-matrix.mjs,
probe.mjs; тестовый логин в .env (права 600); скриншоты в shots/.
Пользователь работает в браузере с масштабом 75% → проверять ширины
1920 (≈75% на мониторе 1440), 1440, 1280, 1024 + тёмную тему.
Кэш-бастер: ?v=<первые 10 символов md5 файла> — обновлять во всех пяти страницах
(index/admin/settings/workflows/custom_objects.html) при правке css/js.

УЖЕ СДЕЛАНО (не переделывать)
Базовая синхронизация локалки с продом и приватный репозиторий; дизайн-раунды по
референсам ~/Downloads/DESIGN.md и DESIGN-2.md (кнопки-капсулы, радиусы 10/16,
светлые шапки таблиц, табличные цифры, тональные вторичные); фиксы обрезаний в
таблицах, теней канбана, шапок колонок, сайдбара, панели уведомлений; компактное
списание с видимым крестиком; безопасность (/files/ только своего тенанта, настройки
почты только админам); целостность фронта (Esc-отмена, двойной сабмит, откаты при
ошибках, живая кнопка «Повторить»); тенант-БД в ежедневном бэкапе.

ТЕКУЩАЯ ЗАДАЧА (прервана на полуслове)
1. Дописать в конец css/style.css именованным блоком (конвенция проекта):
   - .card-meta канбан-карточки: display:flex; flex-wrap:wrap; align-items:center;
     gap:6px; margin-top:8px; у .store-badge внутри margin-top:0, padding:3px 10px,
     font-size:11px; у .card-manager margin-top:0 и margin-left:auto — бейджи
     («магазин», менеджер, «не заполнена») должны стоять ровной строкой, а не вразнобой.
   - Жёлтые заливки убрать (пользователю не нравятся и на карточке доски, и
     «Осталось оплатить» в модалке): .badge-warning и .incomplete-badge →
     background:transparent; border:1px solid rgba(180,83,9,.35);
     color:var(--warning-color); в тёмной теме border rgba(245,158,11,.4),
     color #fbbf24.
   Затем: новый ?v= для style.css, коммит, пуш, scp css+html, контрольный скриншот.
2. Списание (Финансы → Списание): пользователь говорит «в карточке всё ещё
   скрываются блоки». Его скриншот повредился; на 1920/1440 я не воспроизвёл —
   все блоки видны. Запроси у пользователя свежий скриншот или ищи клиппинг на
   его реальной ширине (75% на мониторе 1920 = 2560 CSS px).
3. Ждёт решения пользователя (трогает прод-данные/схему — без согласия не делать):
   дедупликация record_versions + уникальный индекс (дубли уже есть: cards 277/339/379);
   FK задач на карточки основной БД (у тенантов 500 при привязке); foreign_keys=ON
   в тенант-БД; вынос IMAP-синка почты в фон.

Стиль: по-русски, кратко, по делу. Памяти проекта доверяй, но проверяй кодом.
````
