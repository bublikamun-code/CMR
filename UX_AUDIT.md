# UX-аудит (Фаза 5.1) — дефекты юзабилити с замерами

Дата: **2026-09-12**. Пара к документу: `DESIGN_AUDIT.md`. План: `PLAN_MODERNIZATION.md`,
раздел «Фаза 5» (строки 544–756). Правило владельца (2026-09-11): речь не о косметике, а о
минимизации визуального шума и снижении стоимости сценариев. Рабочий диапазон — десктоп
**1280–1920 CSS-px**; владелец работает при масштабе браузера **75 %** (его вьюпорт
на 1440-физ. = 1920 CSS-px), коллеги — при 100 %.

Каждый дефект ниже — с числом-доказательством, ссылкой на код (`файл:строка`) и скриншотом.
Скриншоты — в `tmp/ux-audit/shots/` (вне git, регенерируются инструментами, см. Методику).

**Приоритеты:** **P0** — сломано: теряются данные или функция недостижима; **P1** — дорогие
сценарии и тяжёлый шум; **P2** — косметика с замером.

## Методика

- Стенд: локальный сервер `http://127.0.0.1:8801` на **копии боевой БД** (~922 карточки,
  ~357 транзакций, 183 платежа, 14 накладных). Read-only: сценарии доводились до открытой
  формы, финальные сабмиты не выполнялись.
- Скриншоты: 8 разделов × 4 ширины (1280 / 1440 / 1600 / 1920 CSS-px) + тёмная серия +
  модалки + админка = **44 экрана, горизонтального скролла и ошибок консоли нет ни на одном**
  (`tmp/ux-audit/shots/report.json`).
- Динамический замер шума: distinct computed-значения font-size / цветов текста / фонов /
  радиусов / теней / границ + счёт текстов <13px и интерактивных элементов, viewport 1440×900.
- Замер стоимости задач: 8 сценариев, считаются клики (обёртка `page.click`, включая опции
  селектов) и поля, которые нужно прочитать для решения.
- Инструменты (все в `tools/visual-check/`, playwright-core + системный Chrome):
  - `audit-shots.mjs` — матрица скриншотов и `report.json`;
  - `ux-noise-metrics.mjs` — динамические метрики шума (`tmp/ux-audit/noise-metrics.{md,json}`);
  - `task-cost.mjs` — сценарии (`tmp/ux-audit/task-cost.md`);
  - `verify-s1-fix.mjs` — повторный замер сценария S1 после фикса.
- Статический разбор `js/card_modal.js` — `tmp/ux-audit/card-modal-analysis.md`.
- Все исходные замеры: `tmp/ux-audit/`. Сырые PNG в `tmp/ux-audit/shots/` в git не входят.

## Сводная таблица дефектов

| # | Дефект | Замер | Экран/файл | Приоритет | План |
|---|---|---|---|---|---|
| D1 | `localDateISO is not defined` — форма выписки накладной из карточки не рендерилась | 1 клик до формы, формы нет; JS-ошибка | `js/card_modal.js:1831` | **P0 — исправлен** (коммит `36158ae`, ветка `fix/invoice-draft-localdateiso`) | вне плана, инцидент |
| D2 | Приёмка накладной = один чекбокс, необратимый PATCH каскадом без подтверждения | 1 чекбокс → до 4 полей за раз | `frontend/nakladnye.js:401,423` | **P0 — исправлен** (коммит `712875a`) | 5.5.5 |
| D3 | Дебиторка «Контроль» написана и не подключена ни к одной странице | 0 точек входа; `loadControlBoard` undefined | `js/control.js` | **P0 — исправлен** (коммит `9997fd7`) | 5.2 |
| D4 | Нет фильтра по статусу оплаты на канбане | видно 47 карточек из 922; «В работе 15 из 129» | `js/kanban.js` | **P1 — исправлен** (коммит `e2d8ec3`) | 5.3 |
| D5 | Модалка сделки: 41 действие без иерархии | 61 distinct-значение, 54 % текста <13px, 5–9 fetch на открытие | `js/card_modal.js` | **P1** | 5.2, 5.3.4, 5.6.3 |
| D6 | Канбан — второй по шуму экран | 50 distinct; 57,4 % текста <13px (516 шт.); 5-я колонка обрезана на 1280 | `js/kanban.js`, `css/style.css` | **P1** | 5.2, 5.4 |
| D7 | Реестр оплат: итоги футера не пересчитываются при поиске; 752 интерактивных элемента | «химкорпласт» → 1 строка из 183, итог остался 183 шт. / 404 946,86 BYN | `js/payments.js:57,321` | **P1 — итоги исправлены** (коммит `8e49c4b`); 752 элемента — вне среза | 5.3.3, 5.5.4 |
| D8 | `style.css` — монолит: шум зашит в 487,5 КБ | 13 962 строки; 1 156 строк с `!important`; 68 % `font-size` ≤13px; 406 цветов; 13 радиусов | `css/style.css` | **P1** | 5.6.2 |
| D9 | Брейкпоинтов под реальный диапазон 1280–1920 нет | из 22 `@media` — 16 max-width (все ≤900px) и 1 `min-width: 901px`; при масштабе 75 %: 11–12px = 8,25–9 физ. px | `css/style.css` | **P1** | 5.4 |
| D10 | Защита от потери правок покрывает 5 полей из всех редактируемых | черновик заметки теряется молча; летящие PATCH чек-листа/оплаты не трекаются | `js/card_modal.js:194` | **P1 — исправлен** (коммит `d9074ac`) | 5.5.5 |
| D11 | Пустой `GET /writeoffs/groups/` на каждое открытие модалки; перерисовка = полный рефетч | 5–9 запросов на открытие; 9 мест «переоткрыть ради галочки» | `js/card_modal.js:1984` | **P1 — кэш исправлен** (коммит `de4813b`); точечные ререндеры — план 5.3.4 | 5.3.4, 5.6 |
| D12 | Таблицы с `min-width !important`: на 1280 колонка «Примечание» уходит за край | `#payments-table` 1100px, `#documents-table` 1000px | `css/style.css:776` | **P1 — исправлен** (коммит `1957a1f`) | 5.4.5 |
| D13 | Числовые колонки накладных переносят цифры по разрядам на всех ширинах | «823,0 2 BYN», «137, 17 BYN» на 1440 | `frontend/nakladnye.js` | **P1 — исправлен** (коммит `40b291f`) | 5.2 |
| D14 | Пилюли фильтров накладных обрезаны | ширины 146 / 116 / 98 px: «Все мага…», «Все т…», «Все ста…» | `frontend/nakladnye.js` | **P2 — исправлен** (коммит `40b291f`) | 5.4, 5.5 |
| D15 | Кнопки плиток списаний видны только по hover | кнопки плитки: «Выписать накладную», «Прикрепить сделку» | `css/style.css:1760,5561` | **P2 — исправлен** (коммит `11d5b17`) | 5.3.2, 5.7 |
| D16 | Пустые колонки задач не скрываются | пустая колонка = заглушка «—» вместо скрытия | `js/tasks.js:200` | **P2 — исправлен** (коммит `11d5b17`) | 5.3 |
| D17 | Мёртвый код в модалке сделки | ветка `.btn-delete-attachment` никогда не сработает; двойной кэш поставщиков; readonly-поле в первом экране | `js/card_modal.js:645` | **P2 — частично** (коммит `de4813b`); readonly-поле — с переработкой D5 | 5.6.2 |

**Итого: 17 дефектов — P0: 3 (все исправлены), P1: 10 (исправлено 8: D4, D7-итоги, D10, D11-кэш, D12, D13; открыты D5, D6), P2: 4 (исправлено 3: D14, D15, D16; D17 частично).**
Срез 12–13.09 (`feat/phase5-ux`, коммиты 40b291f…e2d8ec3): каждый через гейт check.sh, эталоны обновлены осознанно.

---

## P0 — сломано

### D1. Форма выписки накладной из карточки не рендерилась (`localDateISO is not defined`)

- **Что:** сценарий S1 «найти неоплаченную сделку и выписать накладную» упирался в пустую
  секцию: черновик `false`, полей `0`. Причина — вызов неопределённой функции `localDateISO`
  при рендере формы. Найдено аудитом, **исправлено** (функция добавлена в
  `js/card_modal.js:36-39`, YYYY-MM-DD из локальных компонент — без UTC-сдвига даты).
- **Замер:** 1 клик до модалки + скролл до секции «Накладные» → формы нет; pageerror
  `localDateISO is not defined`. После фикса (`verify-s1-fix.mjs`, карточка «химкорпласт»
  id=897): форма на месте — поля №/дата/сумма (дефолт «238.80» = остаток), дата
  `2026-09-12`, pageerror нет.
- **Скрины:** `tmp/ux-audit/shots/task-s1-invoice-form.png` (до, секция пустая),
  `tmp/ux-audit/shots/task-s1-fixed.png` (после, форма с полями).
- **Остаток риска:** второй вызов (`js/card_modal.js:2088`, дата общей накладной группы)
  на живых данных не проверен — в копии БД нет ни одной writeoff-группы (см. D11/S3).
  Проверить при первой появившейся группе.
- **Статус:** коммит `36158ae`, ветка `fix/invoice-draft-localdateiso` → будет в main.
  Пункт плана: вне 5.2–5.6 — регрессионный инцидент; подтверждает необходимость гейта 5.7.

### D2. Приёмка накладной — один чекбокс, необратимый PATCH каскадом, без подтверждения

- **Что:** сценарий S2. Приёмка, проверка и оплата — это чекбоксы прямо в строке таблицы
  (`cb-arrived`, `cb-verified`, `cb-paid`). `onchange` немедленно шлёт PATCH. Каскад
  разносит один клик по нескольким полям: «оплачена» ⇒ `is_arrived + is_verified + status`,
  «пришла» ⇒ `is_verified + status`; снятие галочки каскадно снимает остальные. Одно
  случайное нажатие переключает до 4 полей, отмены/подтверждения нет.
- **Где:** `frontend/nakladnye.js:401` (обработчик), `frontend/nakladnye.js:423` (`_updateCb`,
  каскадные `updateData`).
- **Замер:** приёмка = 1 клик по чекбоксу «Пришла»; таблица 14 строк × 14 колонок; соседние
  цели клика — 3 чекбокса и кнопки в одной строке.
- **Скрин:** `tmp/ux-audit/shots/task-s2-nakladnaya-arrive.png`.
- **Исправлено (2026-09-12, коммит `712875a`, ветка `feat/phase5-ux`):** если PATCH
  меняет не только нажатый флаг (есть каскад), перед запросом открывается
  `confirmDialog` с явным перечислением последствий («Пришла» — отметить,
  «Проверена» — отметить, статус — «Оплачена»); «Отмена» возвращает чекбокс и не
  шлёт запрос. Простые изменения без каскада (снять «Оплачена», отметить
  «Проверена») остались в один клик. Функциональное покрытие:
  `tools/visual-check/control-check.mjs` (диалог, отмена без PATCH, простой флаг).
- **План:** 5.5.5 (деструктивные/значимые действия требуют подтверждения).

### D3. Дебиторка («Контроль») написана и недостижима

- **Что:** `js/control.js` (дебиторка и очередь на списание, написана 08.09) не подключена
  ни к `index.html`, ни к `admin.html`: `grep control.js` по обоим — пусто,
  `window.loadControlBoard` — undefined, `#page-control` в DOM отсутствует. Во вкладках
  Финансов вкладки «Контроль» нет. Фича написана — и мертва.
- **Замер:** сценарий S6 — 2 клика до Финансов → вкладки нет; 0 точек входа в коде.
- **Скрин:** `tmp/ux-audit/shots/task-s6-finance-tabs-no-control.png` (вкладки: Реестр оплат,
  Списание, Документы, Накладные).
- **Исправлено (2026-09-12, коммит `9997fd7`, ветка `feat/phase5-ux`):** вкладка
  «Контроль» первой во вкладках Финансов; `js/control.js` подключен, панель
  `#page-control` оформлена как bento-сводка (3 плитки: Дебиторка / Долг старше
  30 дней / Очередь на списание — план 5.2 «bento только для сводок») над двумя
  обычными таблицами. Лоадер зарегистрирован в `features.js` (переключение
  вкладок) и `page-freshness.js` (обновление при возврате в раздел). Покрытие:
  `tools/visual-check/control-check.mjs` — открытие, плитки, таблицы, клик по
  долгу открывает карточку сделки; 14/14 OK, 0 ошибок консоли.
- **План:** 5.2 — Zero-Based Redesign прямо ставит вопрос про раскладку Финансов и
  «подключить `js/control.js` и оформить как bento-сводку».

---

## P1 — дорогие сценарии и тяжёлый шум

### D4. На канбане нет фильтра по статусу оплаты

- **Что:** фильтры доски — магазин, сумма от/до, клиент, приоритет (видно на скрине).
  Фильтра по оплате («не оплачено», «частично») нет, хотя бейдж оплаты — первый смысловой
  сигнал карточки. Поиск `#kanban-search` ищет только по названию сделки/компании.
- **Замер:** в копии БД 922 карточки, на доске видно **47**; счётчики колонок: «Новый
  запрос 16», «**В работе 15 из 129**», «Ждет оплаты 2», «Сборка 14», «114 старых».
  Найти «неоплаченную сделку» (сценарий-пример из плана, 5.1.3) без фильтра нельзя —
  только прокликивать колонки.
- **Скрин:** `tmp/ux-audit/shots/1280-kanban.png`.
- **План:** 5.3 (информационная архитектура от задач, а не текущей раскладки).

### D5. Модалка сделки — самый шумный экран системы, 41 действие без иерархии

- **Что:** `js/card_modal.js` — 2 357 строк (на момент статического разбора 2 349, до
  фикса D1). `renderModalContent` — `js/card_modal.js:396-1055`, **~660 строк (~28 % файла)**:
  рендер + контроллер + API-слой в одной функции (6 fetch и ~15 обработчиков внутри).
  Вкладок нет — всё рендерится сразу, условные секции прячутся после fetch. **41 действие**
  на одну модалку: оплата, закупка у поставщиков, накладные, групповое списание,
  email-связывание, теги, вложения, чек-лист, лента.
- **Замер (динамика шума, 1440×900):** distinct-значений **61** (font-size 10, цвета 17,
  фоны 15, радиусы 5, тени 5, границы 7) — максимум по системе; текста <13px — **530 из 981
  (54 %)**; интерактивных элементов — **281**; видимых элементов — 1698. На открытие —
  **5–9 HTTP-запросов** (карточка, клиенты, активность, теги, группы списаний; +накладные,
  письмо, поставщики по условиям). Тёплый кэш не спасает: см. D11.
- **Скрин:** `tmp/ux-audit/shots/modal-deal.png` (слева — 8 постоянных полей + чек-лист +
  вложения + кнопка-этап; справа — лента; email отправителя readonly занимает строку
  первого экрана).
- **План:** 5.2 «Карточка сделки» (разбор на вкладки, редкое за раскрытием), 5.3.4
  (группировка/прогрессирующее раскрытие), 5.6.3 (компонентный слой). Предложение по
  иерархии вкладок — `tmp/ux-audit/card-modal-analysis.md`, раздел 9.

### D6. Канбан — второй по шуму экран; на 1280 обрезана 5-я колонка

- **Что:** карточки несут бейджи оплаты, пилюли «Без склада»/«Без ответственного», бейдж
  «не заполнена», счётчики «Заказано 0/2 Пришло 0/1» — по 5–7 смысловых меток на карточку.
- **Замер:** distinct-значений **50** (font-size 8, цвета 15, фоны 11, радиусы 5, тени 4,
  границы 5); текста <13px — **516 из 899 (57,4 %)**, из них 11px — 273, 12px — 243;
  интерактивных — 264; видимых элементов — 1564.
- **Раскладка:** на **1280** видно 4 полных колонки и обрезанную 5-ю — доска живёт во
  внутреннем горизонтальном скролле; горизонтального скролла страницы нет, но край
  колонки «Сборка» уходит за вьюпорт.
- **Скрины:** `tmp/ux-audit/shots/1280-kanban.png`, `tmp/ux-audit/shots/1440-kanban.png`.
- **План:** 5.2 «Канбан» (тихие карточки, цвет только у стадии, tabular-nums у сумм),
  5.4 (брейкпоинты под 1280–1920).

### D7. Реестр оплат: итоги футера не пересчитываются при поиске; 752 интерактивных элемента

- **Что:** поиск `#payments-search` прячет строки (`bindTableSearch`), а футер
  «Итого: N шт. / X BYN» считается один раз по полному массиву (`updatePaymentsTotals`) —
  после поиска показывает итог по всем строкам, а не по найденным. Человек ищет платёж и
  получает чужую сумму.
- **Где:** `js/payments.js:57` (поиск-скрытие), `js/payments.js:321-326` (итоги).
- **Замер:** запрос «химкорпласт» → **1 строка из 183**, итог до и после поиска одинаковый:
  **183 шт. / 404 946,86 BYN**. Экран Финансы целиком: **752 интерактивных элемента**
  (максимум системы), текста <13px — 366 из 3355 (10,9 %), distinct — 48.
- **Скрин:** `tmp/ux-audit/shots/task-s8-payment-found.png`.
- **План:** 5.3.3 (плотность и сканируемость), 5.5.4 (согласованная обратная связь —
  числа должны соответствовать тому, что видно).

### D8. `style.css` — монолит, шум зашит в коде

- **Замер (статика, сверено с планом 5.6.2):** **487 503 байта (487,5 КБ)**, **13 962 строки**;
  `!important` — **1 182 строки** по grep (1 156 в коде без комментариев, 1 299 вхождений);
  объявлений `font-size` — **466**, различных значений — **17**; **68 % объявлений ≤13px**
  (13px×105, 12px×102, 11px×84, 10px×13, плюс var-токены xs/sm); различных цветовых
  литералов — **406**; различных `border-radius` — **13**. `css/liquid-glass.css` —
  ещё 16,5 КБ, 63 `!important`, 86 цветов.
- **Следствие:** любая правка масштаба — сотни точечных изменений; специфичность
  забита `!important`, новые стили вынуждены добавлять ещё `!important`.
- **План:** 5.6.2 (разбор на доменные файлы, чистка `!important` под скриншот-регрессией),
  5.6.1 (токены вместо 17 значений).

### D9. Брейкпоинтов под рабочий диапазон 1280–1920 нет; мелкий шрифт системный

- **Что (брейкпоинты):** из **22 `@media`** в `style.css` — **16 с `max-width` (все ≤900px:
  900/768/720/700/640/600/560/480/400), 1 `min-width: 901px`**, 5 без брейкпоинта по ширине
  (hover / prefers-reduced-motion). В диапазоне 1024–1920 нет ни одного правила —
  раскладка «как получится» (прямое проявление: D6, D12, D14).
- **Что (шрифт):** на экранах текст <13px: канбан **57,4 %**, модалка сделки **54 %**,
  календарь **32,7 %** (в т.ч. 10px — 38 элементов), дашборд **22,7 %**, задачи **15,9 %**.
  При масштабе владельца 75 % это **8,25–9,75 физического px** — ниже читаемости.
- **Скрины:** `tmp/ux-audit/shots/1280-kanban.png` (мелкие пилюли и счётчики),
  `tmp/ux-audit/shots/modal-deal.png` (мелкая лента и метаданные).
- **План:** 5.4.1 (брейкпоинты 1280/1440/1600/1920), 5.4.2 (минимум 13/14px), 5.4.3
  (шкала токенами, не 17 хардкодных значений).

### D10. Защита от потери правок покрывает 5 полей из всех редактируемых

- **Что:** `CardSaveTracker` трекает PATCH только через `saveCardField` (название, сумма,
  дата, магазин, клиент). Прямые `apiFetch` — заметка, все PATCH чек-листа (оплата/приход/
  сумма/примечание/привязка/удаление), оплата, накладные — **не трекаются**: закрытие
  модалки во время летящего PATCH ничего не спросит. Набранная, но не отправленная заметка
  в `#input-description` при закрытии теряется молча (а Esc в фокусе textarea блокирует
  глобальный Esc-обработчик — можно решить, что заметка сохранилась). `failed`-слот
  один: несколько упавших полей схлопываются в одну ошибку.
- **Где:** `js/card_modal.js:194` (`unsavedChangesDialog`), архитектура —
  `js/card_modal.js:63-301`; приоритет слушателей обеспечен порядком подключения
  `index.html:964` (card_modal.js) vs `index.html:978` (ui-polish.js).
- **Замер:** редактируемых групп полей в модалке — ≥10 (см. перечень 41 действия, D5);
  трекаемых — 5.
- **План:** 5.5.5 («несохранённые изменения блокируют закрытие» — довести до всех PATCH).

### D11. Пустой запрос групп списаний на каждое открытие модалки; перерисовка = полный рефетч

- **Что:** `renderCardGroupBlock` всегда делает `Promise.all([GET /kanban/cards,
  GET /writeoffs/groups/])` и только потом решает, что секция не нужна (`section.hidden`).
  Для сделок «Новый запрос»/«В работе»/«Ждет оплаты» (большинство открытий) результат
  выбрасывается. Отдельно: любое действие с файлом/чек-листом завершается полным
  `openCardModal(card.id)` — рефетч карточки и всех сопутствующих запросов ради одной
  галочки.
- **Где:** `js/card_modal.js:1984` (fetch групп), 8 мест «переоткрытия той же карточки
  как перерисовки» (`js/card_modal.js:614,812,908,921,937,971,1467,1474`) + открытие
  соседней карточки из related-баннера (`js/card_modal.js:2207`).
- **Замер:** 5–9 запросов на открытие (см. D5); каждая галочка чек-листа = повторные 5–9.
- **План:** 5.3.4 (ленивая загрузка секций по раскрытию), 5.6 (модуль секций с
  `rerender(section)` вместо полного переоткрытия).

### D12. `min-width !important` у таблиц — колонки за краем на 1280

- **Что:** `#payments-table { min-width: 1100px !important }`,
  `#documents-table { min-width: 1000px !important }` — таблицы не влезают в контентную
  область с сайдбаром и давят соседние колонки.
- **Замер (скрин 1280):** в «Реестре оплат» колонки после «Магазин» сжаты в треть экрана,
  колонка **«Примечание» уходит за правый край**; содержимое ячеек схлопывается.
  На 1440/1600/1920 — терпимо, но фиксированные ширины (140px — 9 мест, 900px — 6)
  не дают таблицам использовать ширину.
- **Где:** `css/style.css:776-777` (+ повтор `css/style.css:3888`, `css/style.css:13930`).
- **Скрины:** `tmp/ux-audit/shots/1280-finance.png`, `tmp/ux-audit/shots/1440-finance.png`.
- **План:** 5.4.5 (приоритет колонок вместо `min-width !important`, липкая первая колонка).

### D13. Числовые колонки накладных переносят цифры по разрядам на всех ширинах

- **Что:** денежные колонки «Сумма с НДС» и «НДС» узкие, цифры переносятся посередине
  разряда. Суммы не сканируются ни на одной ширине — а это деньги.
- **Замер:** на 1440: «823,0 2 BYN», «137, 17 BYN», «2 293, 44 BYN»; дата рвётся
  («10.09.2 026»). Подтверждено на всех 4 ширинах серии.
- **Скрин:** `tmp/ux-audit/shots/finance-nakladnye-tab.png`.
- **План:** 5.2 «Табличные цифры и моноширина для идентификаторов»
  (`font-variant-numeric: tabular-nums` + правое выравнивание сумм, моноширинные
  серия+номер, ширина колонок под контент).

---

## P2 — косметика с замером

### D14. Пилюли фильтров накладных обрезаны

- **Замер:** ширины селектов фильтров на вкладке «Накладные» — **146 / 116 / 98 px**;
  подписи читаются как «Все мага…», «Все т…», «Все ста…» (магазин / тип / статус).
  Человек не понимает, какой фильтр выбран, без раскрытия.
- **Скрин:** `tmp/ux-audit/shots/finance-nakladnye-tab.png`.
- **План:** 5.4 (фиксированные ширины → приоритет контента), 5.5.2 (единая шкала контролов).

### D15. Кнопки плиток списаний видны только по hover

- **Что:** действия плитки списания («Выписать накладную», «⧉ Прикрепить сделку») и
  кнопка удаления появляются только при наведении (opacity 0 → hover). Тот же паттерн у
  карточек задач. Не открывается с клавиатуры, не видно на тачпаде без наведения,
  новый пользователь не узнает о существовании действий.
- **Где:** паттерн подтверждён в CSS — `css/style.css:1760`
  (`.writeoff-card:hover .btn-delete-writeoff`), `css/style.css:5561-5562`
  (`.task-card-actions { opacity: 0 } :hover { opacity: 1 }`).
- **Замер:** сценарий S3 — кнопки одиночной плитки получены только в hover-состоянии;
  групповых плиток в данных нет (0 из 0), проверить групповой hover невозможно.
- **Скрин:** `tmp/ux-audit/shots/task-s3-writeoff-single.png`.
- **План:** 5.3.2 (убрать конкурирующие акценты, один источник акцента на уровень),
  5.7 (чек-лист доступности: видимость целей клика).

### D16. Пустые колонки задач не скрываются

- **Что:** на доске задач пустая колонка остаётся на месте с заглушкой «—» вместо того,
  чтобы скрыться и отдать ширину непустым.
- **Где:** `js/tasks.js:200-212` (колонки управляются фильтром статуса, пустым ставится
  `.task-col-empty` = «—»).
- **Замер:** в копии БД на доске 1 карточка — пустые колонки занимают ширину экрана
  (видно на серии 1280–1920).
- **Скрин:** `tmp/ux-audit/shots/task-s4-tasks-overdue.png`.
- **План:** 5.3 (пустые состояния и раскладка от данных).

### D17. Мёртвый и дублирующий код модалки сделки

- **Что:** (1) ветка `.btn-delete-attachment` в обработчике никогда не сработает — элементы
  с этим классом не рендерит никто (вложения рисуются с `file-chip__delete`), в `index.html`
  класса нет; (2) двойной кэш поставщиков: `_suppliersCache`/`loadSuppliersList`
  параллельно общему `CRM_STORE`, у которого уже есть инвалидация; (3) readonly-поле
  «Email отправителя» (`js/card_modal.js:426`) занимает строку первого экрана, но не
  маппится ни на какой PATCH.
- **Где:** `js/card_modal.js:645`, `js/card_modal.js:2321` (кэш поставщиков).
- **Замер:** перекрёстный grep `js/` + `index.html` по каждому пункту; полностью
  неиспользуемых функций в файле нет — мусор точечный.
- **План:** 5.6.2 (чистка вместе с разбором `style.css`).

---

## Базовая линия для 5.7 «Как проверяем, что стало лучше»

Стартовые числа — сравнивать после переработки повторным прогоном `ux-noise-metrics.mjs`,
`task-cost.mjs` и `audit-shots.mjs` на том же стенде (копия боевой БД).

**Перемер после D2+D3 (12.09 вечер)**: deal-modal 59, kanban 50, finance 48.
**Перемер после ночного среза D4–D17 (13.09, стенд 8801)**: deal-modal 61,
kanban 51 (+1 — строка чипов оплаты D4 добавила один размер шрифта; плата за
новую capability), finance 48. Сценарии: S8 «Найти платёж» — футер теперь
пересчитывается при поиске (D7); S2 — каскад подтверждается (D2); S6 —
вкладка «Контроль» жива (D3); канбан получил фильтр оплаты (D4). Строки
таблицы ниже — исходная базовая линия ДО 5.2.

**Шум (distinct-значений на экран, viewport 1440×900):**

| Экран | Сумма distinct | font-size | цвета | фоны | радиусы | тени | границы | Текст <13px (шт / %) | Интерактивных |
|---|---|---|---|---|---|---|---|---|---|
| deal-modal | **61** | 10 | 17 | 15 | 5 | 5 | 7 | 530 / 981 (54,0 %) | 281 |
| kanban | **50** | 8 | 15 | 11 | 5 | 4 | 5 | 516 / 899 (57,4 %) | 264 |
| finance | **48** | 7 | 15 | 12 | 6 | 2 | 4 | 366 / 3355 (10,9 %) | 752 |
| tasks | 47 | 9 | 14 | 11 | 4 | 2 | 5 | 11 / 69 (15,9 %) | 23 |
| calendar | 44 | 7 | 13 | 14 | 4 | 1 | 3 | 52 / 159 (32,7 %) | 16 |
| dashboard | 42 | 9 | 12 | 9 | 5 | 3 | 2 | 47 / 207 (22,7 %) | 15 |
| clients | 41 | 8 | 12 | 9 | 6 | 2 | 2 | 3 / 166 (1,8 %) | 71 |
| suppliers | 40 | 8 | 11 | 8 | 7 | 2 | 2 | 2 / 222 (0,9 %) | 105 |
| settings | 33 | 6 | 11 | 7 | 3 | 2 | 2 | 0 / 49 (0 %) | 22 |

Цель по направлению 5.2/5.3: сумма distinct на топ-экранах снижается (font-size — к шкале
не более 5–6 ступеней, цвет — к семантическим токенам). Сырец: `tmp/ux-audit/noise-metrics.json`.

**Стоимость сценариев (клики, до финального сабмита):**

| Сценарий | Клики | Примечание-бaseline |
|---|---|---|
| S1. Неоплаченная сделка → выписка накладной | **1** (+скролл до секции, 3 поля) | после фикса D1; было: формы нет |
| S2. Принять накладную от магазина | 4 | чекбокс-приёмка без подтверждения (D2) |
| S3. Провести списание по группе | 1 | групповых плиток в данных нет — перепроверить после появления групп |
| S4. Найти просроченную задачу | 2 | чип «Просроченные» работает |
| S5. Найти сделку по названию клиента | 3 | поиск по доске работает |
| S6. Дебиторка («Контроль») | 2 → тупик | вкладки нет (D3); цель — сделать сценарий проходимым |
| S7. Добавить оплату по сделке | 5 | карточка → бейдж → «Частично» → сумма |
| S8. Найти платёж в реестре | 3 | итоги футера врут при поиске (D7) |

**Статика CSS:** 487 503 байт / 13 962 строки / `!important` 1 182 строк (1 156 в коде) /
22 `@media` (16 max-width ≤900px, 1 min-width 901px) / font-size: 466 объявлений, 17
значений, 68 % ≤13px / 406 цветов / 13 радиусов.

**Скриншот-регрессия (audit-shots.mjs):** 44 экрана, `hscroll: false` везде,
`errors: []` — эти asserts из 5.4/5.7 должны остаться зелёными; добавить asserts:
минимальный font-size ≥13px, интерактивные элементы ≥32 CSS-px.

## Что НЕ является проблемой (честно)

- **Горизонтального скролла страницы нет** ни на одном из 44 экранов на всех ширинах
  (1280–1920), включая тёмную серию: `hscrollPx: 0` везде в `report.json`. Внутренние
  скроллы отдельных блоков (канбан на 1280, таблицы финансов) — это D6/D12.
- **Шрифтовых семейств всего 2** на всех экранах (Manrope + Times у отдельных элементов) —
  типографическая база консистентна; проблема в размерах (D9), не в семействах.
- **Ошибок консоли нет** на всех 44 экранах (`report.json: "errors": []`). Единственная
  JS-ошибка найдена сценариями (D1) — исправлена. Единственный «шум» консоли —
  штатные 401 до логина и `favicon.ico` 404.
- **Тёмная тема рендерится** на всех 8 разделах (серия `dark-*.png`) — это база,
  а не набор сломанных переопределений; приводить к токенам (5.6.4), не чинить с нуля.
- **Спокойные экраны есть:** «Клиенты» (1,8 % мелкого текста), «Поставщики» (0,9 %),
  «Настройки» (0 %, 6 размеров шрифта) — образец для шкалы из 5.6.1; переработка
  начинается с канбана/модалки/финансов, а не с них.
- **Чип «Просроченные» в задачах и поиск по доске работают** (S4/S5 проходимы за 2–3 клика)
  — образец приемлемой стоимости сценария.
